# 22. 구조 과제 자율 진행 (2026-09-05 00:11 ~ 09-06 06:30)

**이 파일이 재개 지점이다.** 세션이 끊기면 여기부터 읽고 이어간다.

## 사용자 지시 (2026-09-05 00:11)

> 조사 결과 나오면 1번부터 진행해. 내일 오전 06:30까지 승인없이 계속 진행해.
> 대신 작업한 내용을 06:30에 보고서로 작성해줘.
> 작업은 서로 영향이 없는 한 잘게 잘라서 커밋해서 작업단위 롤백이 가능하도록.
> 중간에 세션한도에 걸리면 한도재개시점까지 기다렸다가 자동으로 재개해.

앞서 받은 작업 원칙(2026-09-04)도 그대로 유효하다:

> 작업을 최대한 잘게 쪼개서 작업하되, 매 커밋마다 관련 기능 전수테스트를 수행하자.
> 문제가 있으면 원복하고 원인해결 후 다음 과제 진행.

## 착수 승인된 것 (2026-09-05)

| # | 과제 | 상태 |
|---|------|------|
| 1 | `verify_build.py`(D-F1 92건)를 CI 에서 돌게 | **대기** |
| 2 | CI 를 게이트로 — 배포를 테스트에 묶는다 | 대기 |
| 3 | `merchants.html` 분리 3단계 (허브 결합 해소) | 대기 |

순서 근거: 1·2 는 인프라라 위험이 낮고 **3을 지켜 준다.** 가장 위험한 3을 가드레일 뒤에 둔다.

## 스스로 지킬 것

- **한 발견/한 변경 = 한 커밋.** `git revert <해시>` 하나로 그것만 되돌릴 수 있어야 한다.
- 커밋 전 **관련 전수 테스트**. 통과하지 못하면 커밋하지 않고 원인부터 해결한다.
- **이동 커밋과 수정 커밋을 섞지 않는다.**
- 검증하지 못한 것은 "검증하지 못했다"고 적는다. 통과로 눙치지 않는다.
- 되돌리기 어려운 결정(특히 2번 CI 게이트)은 **빠져나가는 길**을 함께 만든다.
- 조사·검증 에이전트의 보고를 그대로 옮기지 않는다 — **중요한 수치는 직접 확인**한다.

## 기준선 (시작 시점)

```
커밋      12f72da
백엔드    JUnit 201
도구/배치 314 (tools-ci 자동)
프론트    정적 91 · 렌더 101 (렌더는 로컬 수동)
D-F1      verify_build.py 92 (CI 밖 — 1번 과제)
merchants.html 1,910줄
서버      앱·배치 클론 4ad196b, 앱 컨테이너 healthy
```

## 진행 기록

각 단계를 마칠 때마다 여기에 한 줄씩 추가한다(커밋 해시·검증 결과).

| 시각 | 단계 | 커밋 | 검증 |
|------|------|------|------|
| 00:11 | 착수 · 조사 워크플로 대기 | — | — |
| 00:4x | **1-a** 원본 번들을 저장소에 (재빌드·검증의 전제) | `e309355` | 신선 얕은 클론에서 D-F1 92 통과 · 재빌드 바이트 동일 · 작업트리 무변경 |
| 00:5x | **1-b** D-F1 + 재빌드 비교를 tools-ci 에 | (다음) | 변조 2종 실측 — 둘 다 D-F1 은 못 잡고 재빌드 비교가 잡는다 |


## 1번 과제 — verify_build.py CI 편입 (기록)

### 조사가 드러낸 것: 문제가 생각보다 컸다

막는 것은 `verify_build.py` 하나가 아니었다. **`build_index.py`(18·82행)도 같은 원본을 읽는다.**
즉 원본이 없으면 검증만 막히는 게 아니라 **`index.html` 을 재생성할 수 없다.**

그리고 더 중요한 것 — **92건 토큰 검사는 "index.html 이 build_index.py 산출물임"을 지키지 못한다.**
목록에 적어 둔 토큰이 있나 없나를 볼 뿐이라, 아무도 적지 않은 자리에서 소스와 산출물이 갈리면 조용히 통과한다.
**직접 변조 실험으로 확인했다**:

| 변조 | D-F1 92건 | 재빌드 바이트 비교 |
|---|---|---|
| 빌더의 출력 문구만 고치고 재빌드 누락 | 통과(못 잡음) | **EXIT=1 (잡음)** |
| 데이터 내용만 고치고 재빌드 누락(개수 불변) | 통과(못 잡음) | **EXIT=1 (잡음)** |

2026-08-18 파비콘 사고가 잡힌 것은 **사고 뒤에 (h) 검사를 새로 만들어 넣었기 때문**이지
검사가 원래 그 층을 덮어서가 아니다. 다음 사고는 아직 아무도 적지 않은 토큰에서 난다.

### 방안 선택 — 조사 권고를 따르지 않았다

조사는 "원본에서 뽑은 픽스처 + `index.html` 로부터 재구성"(+40 KB)을 권했다. **택하지 않았다.**

| | 조사 권고 (d) | 채택 (a) |
|---|---|---|
| 저장소 증가 | +40 KB | **+2,540 KB**(직접 측정) |
| 새 코드 | 스크립트 2개 | **0줄** |
| 순환 의존 | 있음 — manifest 를 `index.html` 에서 가져온다 | 없음 |
| `manifest 라인 불변` 검사 | **자기 자신 비교(항등식)** | 유효 |

두 가지가 결정했다.

**①순환과 그것을 지키는 게이트.** (d)는 sha256 게이트가 반드시 필요한데, 그 게이트는 나중에 누가
"어차피 같은 값"이라며 정리할 수 있다. ADR-21 때 `ProbeEndpoint` 주석을 믿고 정리했다가 robots 감시가
**허용 방향으로 조용히 망가진** 것과 같은 유형이다. 게다가 (d)에서는 검사 하나가 항등식이 된다.

**②검증기를 고치는 것 자체가 이 파일의 사고 이력이다.** 2026-08-12~18 에 라인 인덱스가 밀려
`verify_build.py` 가 **6일간 죽어 있었다.** (a)는 검증기를 한 줄도 안 고친다.

비용은 감당할 만하다 — 2.5 MB 는 한 번 넣으면 다시 바뀌지 않는 불변 파일이고, 이 저장소는
이미 **실수로 커밋된 빌드 산출물 54 MB**(jar 43.9 · terraform provider 9.8)를 지고 있다.

### 검증 수치를 직접 재서 조사를 정정했다

적대적 검증이 "(a)는 +8 KB — git 이 `index.html` 에 델타 압축한다"고 주장했다. **재현되지 않았다.**
`--no-hardlinks` 클론 + `gc --aggressive` 전후로 재니 **+2,540 KB** 이고,
블롭은 `3,093,802 B`로 **델타 없이** 저장된다(`deltabase=0000…`). 그래도 결론은 (a)다 —
검증의 수치는 틀렸지만 (d)의 순환 지적은 옳았고, 그 둘을 함께 놓으면 (a)가 남는다.

### CI 편입

`tools-ci` 에 두 스텝을 더했다 — D-F1 92건, 그리고 **재빌드 후 `git diff --exit-code`**.
토큰 검사와 재빌드 비교는 **어느 하나가 다른 하나를 대체하지 않는다**(재빌드 비교는 `build_index.py`
자체가 틀리게 고쳐지면 통과한다). 둘을 함께 돌려야 의미가 있다.

트리거에 `data/offline_categories.json` 을 더했다. `*.html` 이 이미 있어 '데이터를 고치고 재빌드까지 한'
커밋은 어차피 걸리지만, **데이터만 고치고 재빌드를 잊은** 커밋은 이 줄이 없으면 안 걸린다 —
그것이 정확히 D-F1 이 잡으라고 있는 시나리오다(위 변조 B).

낡은 라벨도 정정했다 — 프론트 정적 `66건 → 91건`, 잡 이름 `380건 → 497건`.
