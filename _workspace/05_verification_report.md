# 검증 리포트

- 검증관: guide-verifier
- 대상: `data/online_platforms.json` ↔ 온누리 공식 홈페이지 '온라인 전통시장관'
- 방법: 실제 브라우저(claude-in-chrome)로 SPA를 렌더해 전 페이지 순회

---

## B1 검증 (1차분) — 2026-08-06

### 검증 환경

data 에이전트가 재검증 실패로 기록한 경계면을 **실제 브라우저로 재개통**했다. `www.onnuri.gift`는 SPA라 WebFetch로는 셸만 받지만, 브라우저에서는 정상 렌더된다.

- 진입 경로: `가볼곳·쓸곳` → '온라인 전통시장관', 실제 URL 경로 **`/visit/market`** (data 에이전트가 미확인 처리했던 값 — 확정)
- 필터: `전체` / `쇼핑` / `배달` / `기타` (`기타`는 비활성/회색 — 항목 없음)
- 페이지네이션: **총 3페이지** (12 + 12 + 6 = 30) — `meta.pages_checked: "1-3"` 및 데이터리포트의 "1~3페이지" 표기와 **일치**

### 판정: 통과 (초기 결함 2건 — data 수정 후 재검증 완료, 미검증 1건 잔존)

경계면 B1(출처→데이터)의 카운트·이름·kind 분류는 라이브 사이트와 완전히 일치한다. 잔여는 URL 정밀도와 region 정보로, 이용자 피해로 직결되지 않는다.

### 요약

| 검사 | 왼쪽(사이트) | 오른쪽(JSON) | 결과 |
|------|-------------|-------------|------|
| 총 항목 수 (전체 필터) | 30 | `items` 30 | 일치 |
| 이름 대조 (30개 전량) | 30개 명칭 | 30개 `name` | 전량 일치 (누락 0 / 잔존 0) |
| 배달 kind | 배달 필터 8개 | `kind:delivery` 8개 | 동일 8개 일치 |
| 쇼핑 kind | 30 − 배달8 − 기타0 = 22 | `kind:shopping` 22개 | 일치 |
| 기타 kind | 필터 비활성(0) | 해당 kind 없음 | 일치 |
| summary 문구 (30개) | 카드 2줄 텍스트 | `summary` | 실질 일치 (구두점·띄어쓰기 차 외) |
| pages_checked | 3페이지 | "1-3" | 일치 |

**이름·카운트·kind 분류에서 누락·잔존·오분류 0건.** data 에이전트가 index.html에서 이관한 30곳이 2026-08-06 현재 라이브 사이트 목록과 정확히 대응한다.

### 결함

#### D1. 3개 항목의 `url`이 몰 루트여서 온누리 기획전으로 직행하지 못함 — [해소됨 2026-08-06]
- 심각도: 보통
- 경계면: B1 (출처 → 데이터)
- 왼쪽(관측): 사이트 카드 링크 —
  - 사이소: `https://www.cyso.co.kr/exhibition/detail?id=1773379812`
  - 현대홈쇼핑: `https://www.hmall.com/md/dpa/searchSpexSectItem?sectId=3132118&dispTrtyNmCd=home_eventicon_2&dispOrd`
  - 공영쇼핑: `https://www.gongyoungshop.kr/exhibition/ebtDetail.do?ebtNo=4328`
- 오른쪽(관측): `data/online_platforms.json` —
  - `cyso` (L43) `url: https://www.cyso.co.kr/`
  - `hyundai-home-shopping` (L69) `url: https://www.hmall.com/`
  - `gongyoung-shopping` (L121) `url: https://www.gongyoungshop.kr/`
- 이용자 영향: 링크가 몰 홈으로 떨어져 온누리 전용 기획전을 스스로 찾아야 한다. data 리포트 §5가 "온누리 홈페이지 카드에서 이동"으로만 확인했다고 명시한 항목인데, 이번에 **정확한 딥링크를 확보**했으므로 갱신 가능.
- 담당: onnuri-data-curator
- 수정 제안: 위 3개 딥링크로 `url` 교체.
- **재검증(2026-08-06)**: data가 3건 모두 실측 딥링크로 교체. `cyso`(L43)·`hyundai-home-shopping`(L69)·`gongyoung-shopping`(L121) 확인 완료 — 사이트 링크와 일치. note의 "온누리 홈페이지 카드에서 이동" 문구도 "기획전 직링크"로 갱신됨. **해소.**

#### D2. 3개 항목의 `url`이 사이트 딥링크보다 축약됨 (파라미터 누락) — [해소됨 2026-08-06]
- 심각도: 낮음
- 경계면: B1 (출처 → 데이터)
- 왼쪽(관측): 사이트 카드 링크 —
  - 인어교주해적단: `https://tpirates.com/store/onnuri?utm_medium=quick_banner&utm_campaign=260303_onnuri`
  - 우체국쇼핑: `https://mall.epost.go.kr/fo/pavln/sijangDduri.do#origin_market_tab`
  - 롯데ON: `https://s.lotteon.com/x8fcPe9vMOi?ch_dtl_no=1044428`
- 오른쪽(관측): JSON `url` —
  - `tpirates` (L95) `https://tpirates.com/store/onnuri`
  - `epost-mall` (L160) `https://mall.epost.go.kr/fo/pavln/sijangDduri.do`
  - `lotte-on-sangsaeng-store` (L199) `https://s.lotteon.com/x8fcPe9vMOi`
- 이용자 영향: 대체로 동일 목적지로 이동하나 앵커·트래킹 파라미터가 빠져 랜딩 위치가 미세하게 다를 수 있다. 실사용에 지장 없음.
- 담당: onnuri-data-curator
- 수정 제안: 필수는 아님. 정밀 일치를 원하면 위 값으로 교체. utm 파라미터는 추적용이므로 제거해도 무방.
- **재검증(2026-08-06)**: `epost-mall`(L160)·`lotte-on-sangsaeng-store`(L199) 딥링크 반영 확인. `tpirates`(L95)는 utm 추적 파라미터뿐이라 클린 URL 유지 — 안내대로 처리, 결함 아님. **해소.**

> 참고: 위 URL 외 나머지 22개(온누리핫딜·11번가·현대이지웰·지니어스몰·꾹AI·온누리팔도시장·온누리5일장·온누리찬스·온누리공공몰·온누리시장·온누리굿데이·인더마켓·온누리놀장·온누리마켓·온누리쇼핑·시장을방으로 및 배달 8개)의 `url`은 사이트 링크와 도메인·경로가 일치했다.

### 미검증 항목

| 항목 | 검증 못 한 이유 |
|------|----------------|
| `region_limited` 및 `regions` 정확성 (배달앱 5곳) | '온라인 전통시장관' 카드에는 서비스 지역 메타데이터가 표시되지 않는다. 사이트만으로는 전주맛배달(전주)·배달특급(경기)·배달의명수(군산)·대구로(대구)·먹깨비(참여 지자체) 및 false 처리된 휘파람·머먹지·땡겨요의 지역 커버리지를 대조할 수 없음. data 리포트 §5의 미확인 표기와 동일 상태로 잔존. |

### 통과로 확정한 항목 (미검증과 구분)

- 총 항목 수 30 (전 3페이지 실제 순회)
- 30개 이름 전량 대조 — 누락·잔존 0
- kind 분류 (쇼핑 22·배달 8·기타 0) — 배달 필터 실측 8개 + 기타 비활성 확인으로 확정
- summary 30개 문구 실질 일치
- `meta.pages_checked` / `source_url`(전 항목 존재) / `status:active` 전량

### 비고

- data 리포트 §5 "제휴 종료된 곳이 섞여 있을 가능성"은 이번 실측으로 **해소**되었다. 라이브 목록 30곳 = JSON 30곳으로 status:ended 처리가 필요한 잔존 항목은 없다.
- data 리포트 §2가 미확인 처리한 '온라인 전통시장관' 정확 경로는 `/visit/market`로 확정. `meta.source_url`은 홈 루트(`https://www.onnuri.gift/`)로 남겨도 무방하나, 정밀도를 원하면 `https://www.onnuri.gift/visit/market`로 갱신 권장.

---

## 최종 검증 (B2·B3·B4·렌더) — 2026-08-06

> **진행 경과: 1차 불합격(치명 D-F1) → dev 수정 → 2차 재검증 통과.** 아래 "1차" 기록은 감사 목적으로 보존한다. 최신 판정은 이 절 맨 끝 "재검증 (D-F1 수정 후)"를 따른다.

### 1차 판정: 불합격 [D-F1 수정으로 해소됨 — 하단 재검증 참조]

**치명 결함 1건 — 페이지가 전혀 렌더되지 않는다.** 로컬 서버(`http://127.0.0.1:8642/index.html`)에서 index.html을 브라우저로 열면 로더(스플래시) 화면에 멈추고 하단에 오류 토스트 `Error unpacking: Unterminated string in JSON at position 186 (line 2 column 186)`가 표시된다. DC 앱이 마운트되지 못해 본문·표·요약·검색·필터가 하나도 그려지지 않는다. 재현 100%(새로고침 후에도 동일).

치명 결함이 있으므로 체크리스트 규칙에 따라 1차 판정은 불합격이며, 렌더에 의존하는 B2·B4·렌더 동작은 **관측 불가로 미검증**이었다(통과가 아니다).

### 요약

| 경계면 | 검사 | 결과 |
|--------|------|------|
| 렌더 | DC 앱 마운트 | **실패(치명)** — 로더에서 정지, 언패킹 예외 |
| B2 데이터→렌더 | 온라인 30행·오프라인 12행 화면 카운트 | **미검증** — 행이 0개 렌더(관측 불가) |
| B4 수집일→스탬프 | 화면 기준월 스탬프 값 | **미검증** — `{{ baseMonth }}` 미해석(관측 불가) |
| 렌더 동작 | 검색·칩 필터·정렬·빈 결과·반응형 | **미검증** — 관측 불가 |
| B3 정책→문구 | 본문 문구·단정 강도 | **부분 검증** — 템플릿 소스에는 반영, 화면 미검증 |

### 결함

#### D-F1. 번들 언패킹 실패로 페이지 전면 미렌더 (치명)
- 심각도: **치명**
- 경계면: 렌더
- 왼쪽(관측): 원본 `index.html.bak-20260806` L388 template 문자열 — 임베드 닫는 태그가 유니코드 이스케이프됨:
  `...<script src=\"0c19069d-...\"></script>\n\n</head>...` (첫 리터럴 `</script>`까지 캡처 길이 85,115자, 리터럴 `</script>` 0개)
- 오른쪽(관측): 현재 `index.html` L388 template 문자열 — 같은 자리가 **리터럴** `</script>`:
  `...<script src=\"0c19069d-...\"></script>` (`<script type="__bundler/template">` 요소가 char 186에서 조기 종료, 리터럴 `</script>` 2개)
- 화면(관측): 로더 토스트 `Error unpacking: Unterminated string in JSON at position 186 (line 2 column 186)`. `get_page_text` 결과에 `{{ baseMonth }}`·`{{ onTabText }}`·`{{ row.t }}`·`{{ regionApps }}` 등 머스태시가 리터럴로 남고 `<sc-for>` 반복이 전개되지 않음(데이터 행 0개).
- 원인(재현): dev의 `build_index.py`가 template을 `json.dumps`로 재인코딩하면서, 원본이 HTML 안전을 위해 `/`로 이스케이프해 두었던 임베드 닫는 태그(`</script>`, `</head>` 등)가 **리터럴 `/`로 복원**됨. `json.dumps`는 `/`를 이스케이프하지 않는다. 브라우저 HTML 파서는 `<script type="__bundler/template">` 요소 안에서 첫 리터럴 `</script`를 만나면 그 지점에서 스크립트를 닫아 버리므로, 로더 L75 `JSON.parse(templateEl.textContent)`가 잘린(미완결) 문자열을 파싱하다 position 186에서 실패한다. `manifest`·`page_order`·`ext_resources` 파싱은 정상(해당 블록에 리터럴 `</script>` 없음) — **template만** 깨진다.
- 이용자 영향: 페이지를 열면 아무 내용도 못 본다. 전면 장애.
- 담당: **guide-frontend-dev**
- 수정 제안: `build_index.py`에서 template JSON을 스크립트 요소에 심기 전, 임베드 닫는 태그의 `/`를 원본과 동일하게 이스케이프한다. 최소 요건은 `</script`가 리터럴로 남지 않는 것 — 예: `json.dumps` 산출물에 `.replace('</', '<\\/')`(JSON에서 `<\/`는 유효하며 HTML 파서는 `</script`로 인식하지 않음), 또는 원본과 같이 `/`→`/`. 수정 후 반드시 **브라우저로 실제 마운트**를 재확인(정적 `json.loads`·`node --check`는 이 HTML 파서 레벨 조기 종료를 못 잡는다 — 이번에 통과했음에도 렌더가 깨진 이유).
- 참고: dev가 빌드노트 §"알려진 제약 1"에서 "브라우저 실렌더 미검증(DC 마운트·{{}} 해석)"을 명시적으로 남긴 바로 그 지점에서 터졌다.

### B3 부분 검증 (템플릿 소스 기준 — 화면 미검증)

렌더가 실패해 **화면에서** 확인할 수 없으나, `get_page_text`가 추출한 미해석 템플릿 소스에는 정책 DELTA가 반영되어 있음을 확인했다. 렌더 결함이 고쳐지면 아래가 화면에 나와야 한다(고친 뒤 재검증 필요):

- ① 요건3 경과조치: 템플릿에 `"...신규 등록·3년 주기 갱신 시 심사. 시행일 전에 등록된 기존 가맹점은 최초 갱신 전까지는 이 기준을 적용받지 않습니다(경과조치)."` — "즉시 말소" 표현 없음. 01 주장1③ DELTA 부합.
- ② 선차감 4단계 medium 어조: 템플릿에 `"...부족분만 카드로 청구될 수도, 차감 없이 전액 일반 카드로 결제될 수도 있습니다 — ...앱에서 잔액을 먼저 확인하세요."` — 단정 아닌 병기+확인지시. 01 주장2 DELTA 부합.
- ③ 약국 30억 병기: 템플릿 요건4에 `"약국은 예외적으로 가맹이 유지됩니다 — 단, 연매출 30억 기준은 약국에도 똑같이 적용됩니다."` + 각주②에도 병기. 부합.
- ④ 모바일형 S10 QR 섹션: 템플릿에 `모바일(앱)형 결제 흐름 — QR 방식` 5단계(앱 설치·가입/충전/QR 결제/인증·차감/확인) + 카드형 차이 한 줄 존재. 부합.
- ⑤ 전용관 뉘앙스: 온라인 인트로에 `{{ onIntroMid }} {{ onIntroTail }}` 동적 바인딩으로 수집일 앵커 문장 자리 존재(값은 미해석). S11 명세 부합.
- 추가: S6 생활서비스 `check_point`가 SSOT(offline_categories.json)에서 근거 없는 "조제분 결제 제한 가능"(confidence low) 서술이 제거된 확정본으로 갱신됨을 데이터에서 확인 — 렌더되면 이 문구가 나와야 함.
- 하드코딩 제거: 헤더 부제=`{{ baseMonth }}`, 온라인 탭 부제=`{{ onTabText }}`, 각주④=`{{ regionApps }}`로 바인딩되어 있어 D1·D2·D6 명세대로 하드코딩이 아님(단, computed 값의 정확성은 앱이 실행돼야 검증 가능 — 현재 미검증).

> 주의: 위는 **문안이 템플릿에 올바르게 쓰였다**는 확인일 뿐, **화면에 그렇게 그려진다**는 확인이 아니다. 단정 강도·동적 숫자·행 수는 렌더 복구 후 화면에서 재검증해야 한다.

### 미검증 항목 (렌더 실패로 관측 불가 — 통과 아님)

| 항목 | 검증 못 한 이유 |
|------|----------------|
| B2 온라인 표 30행 == active 30 | 앱 미마운트로 행 0개 렌더 |
| B2 오프라인 표 12행 + 카운트 칩(가능4·조건부5·불가3) | 동일 |
| B2 헤더/탭 요약 숫자 == 실제 행 수 | `{{ onTabText }}` 미해석 |
| B4 화면 기준월 스탬프 == min(collected_on)=2026-08 | `{{ baseMonth }}` 미해석 |
| 검색 필터·칩(쇼핑/배달) 필터·정렬·연번 재계산 | 앱 미마운트 |
| 빈 검색 결과 안내 | 앱 미마운트 |
| 지역 배달앱 5곳 각주 표기(`{{ regionApps }}`) | 미해석 |
| 좁은 폭(≈390px) 반응형·body 가로 밀림 | 렌더할 본문 없음 |
| 콘솔 에러 0건 | 페이지 자체 JS 예외는 없으나(로더가 try/catch로 흡수) 언패킹 실패로 앱 미실행. 유일한 콘솔 예외는 브라우저 확장(share-modal.js)발로 페이지와 무관. |

### 재검증 조건

D-F1 수정(build_index.py의 임베드 태그 이스케이프 복구 + 재빌드) 후, 브라우저로 실제 마운트를 확인하고 B2·B4·렌더 동작·B3 화면 단정강도를 전부 다시 검사한다. 그때까지 이 산출물은 배포 불가.

---

## 재검증 (D-F1 수정 후) — 2026-08-06

### 최종 판정: 통과

dev가 `build_index.py` 재인코딩을 `.replace("</","</")` + "리터럴 `</` == 0" 정적 불변식으로 고쳐 재빌드했다. 브라우저 실마운트를 다시 확인한 결과 **D-F1 해소**, 페이지가 정상 렌더된다. B2·B4·B3 화면·렌더 동작을 전부 재실행해 통과. 렌더 결함 0건. 잔여 미검증 1건은 도구 제약(뷰포트 축소 불가)이며 구조 점검은 양호(결함 아님).

### D-F1 재검증 — 해소

- 왼쪽(관측): 현재 `index.html` L388 template raw — 리터럴 `</` 0개(원본 불변식 회복), 닫는 태그는 `</script>`·`</head>` 형태.
- 오른쪽(관측): 브라우저에서 로더 스플래시·오류 토스트 없음, DC 앱 마운트, `get_page_text`에 잔여 머스태시 0(모든 `{{ }}` 해석됨), `<sc-for>` 전개 정상.
- 콘솔: 페이지 자체 JS 예외 0건. 유일 예외는 브라우저 확장(`chrome-extension://.../share-modal.js` addEventListener null) — 페이지와 무관, 결함 아님.

### 요약 (재검증)

| 경계면 | 검사 | 왼쪽(데이터/정책) | 오른쪽(화면 관측) | 결과 |
|--------|------|------------------|------------------|------|
| 렌더 | DC 앱 마운트 | — | 정상 마운트·토스트 없음 | 통과 |
| B2 | 온라인 표 행수 | active 30 | 화면 연번 1~30, 30행 | 통과 |
| B2 | 온라인 kind 집계 | 쇼핑22·배달8 | 표 1~22 쇼핑·23~30 배달, 칩 전체30/쇼핑22/배달8 | 통과 |
| B2 | 온라인 부제·D5 | total 30 | "공식 안내 30곳 — 쇼핑 22 · 배달 8", "총 30곳 중 30곳 표시" | 통과 |
| B2 | 오프라인 표 행수 | 12유형 | 화면 12행 | 통과 |
| B2 | 오프라인 카운트 칩(D4) | allowed4·conditional5·denied3 | 칩 전체12/가능4/조건부5/불가3 | 통과 |
| B2 | verdict_label 텍스트 렌더 | 가능/조건부/불가 등 | 기호(✓!✕)+텍스트 라벨 병기 | 통과(색각 안전) |
| B2 | 딥링크 반영 | cyso·현대홈쇼핑·공영쇼핑 직링크 | 비고 "기획전 직링크" 렌더 | 통과 |
| B4 | 헤더 기준월 스탬프 | min(collected_on)=2026-08-06 | "2026-08 기준" (D1, `slice(0,7)`) | 통과 |
| B4 | 스탬프 계산식 여부 | — | `{{ baseMonth }}` 바인딩(하드코딩 아님) | 통과 |
| B3 | 요건3 경과조치 | 01 주장1③(즉시말소 아님) | "…최초 갱신 전까지는 이 기준을 적용받지 않습니다(경과조치)" | 통과 |
| B3 | 요건4 약국 30억 병기 | 01 주장1④ | "약국은 예외적으로 가맹이 유지됩니다 — 단, 연매출 30억 기준은 약국에도 똑같이 적용됩니다" | 통과 |
| B3 | 선차감 4단계 medium 어조 | 01 주장2(confidence medium) | 단정 아닌 병기+"앱에서 잔액을 먼저 확인하세요" | 통과 |
| B3 | 생활서비스 약국(low) 확인지시 | 01 U4(1차근거 없음) | "처방약 결제 가능 범위는…결제 전 약국·앱에서 확인"(단정 "제한 가능" 제거) | 통과 |
| B3 | 모바일형 S10 섹션 | 01 주장4 | "모바일(앱)형 결제 흐름 — QR 방식" 헤더 렌더 | 통과 |
| B3 | 전용관 확대 뉘앙스(S11) | 01 주장3 | 인트로 "전용관·제휴 플랫폼은 계속 추가되는 단계라…(2026-08-06 수집)" 동적 | 통과 |
| B3 | 각주②③④ | 01 DELTA | ②대통령령 제36415호·취소 ③선차감 잔액부족 ④지역앱 5곳 동적 | 통과 |
| 렌더 | 정렬(이름순)+연번 재계산 | — | 30행 가나다 재배열, 연번 1~30 재부여(구분 무관) | 통과 |
| 렌더 | 칩 필터(배달) | delivery 8 | 배달 8행만, "총 30곳 중 8곳 표시", 연번 1~8 재계산 | 통과 |
| 렌더 | 검색 빈 결과 안내 | — | "\"…\"에 해당하는 플랫폼이 없습니다."+`검색 초기화` 버튼, "총 30곳 중 0곳 표시" | 통과 |
| 렌더 | 콘솔 에러 | 0 | 페이지 JS 예외 0(확장 예외만) | 통과 |
| D6 | 각주④ 지역 배달앱 | region_limited=true 5곳 | "전주맛배달, 배달특급, 먹깨비, 배달의 명수, 대구로" | 통과 |

### 렌더 동작 상세 (재검증 관측값)

- **정렬↔연번 정합(D7, dev 지목)**: 정렬 select를 "이름순 (가나다)"로 바꾸면 30행이 가나다순으로 재배열(11번가 온누리마켓→공영쇼핑→꾹AI온누리몰→대구로→…→현대홈쇼핑→휘파람)되고 **연번이 표시 순서대로 1~30 재부여**된다(구분과 무관하게 이름순). 기본 순서에서는 쇼핑1~22·배달23~30. 정렬·필터 후에도 연번이 항상 1..n으로 정합 — 통과.
- **칩 필터**: 배달 칩 → 8행(전부 kind 배달), 연번 1~8 재계산, 카운트 "총 30곳 중 8곳 표시". 전체 칩 복귀 정상.
- **검색 빈 결과**: 무매칭어 입력 시 표가 비고 "\"{검색어}\"에 해당하는 플랫폼이 없습니다." + `검색 초기화` 버튼(초기화 경로) 노출, 카운트 0곳. 검색어 되짚기+초기화 경로 모두 제공.

### 미검증 (도구 제약 — 결함 아님)

| 항목 | 사유 / 구조 점검 결과 |
|------|----------------------|
| 좁은 폭(≈390px) 실제 렌더·body 가로 밀림 | `resize_window`가 이 환경에서 창을 1280px 아래로 줄이지 못해(outerWidth 1280 고정) 실측 불가. **verifier·dev 양쪽 환경에서 동일 제약 확인**(dev도 390x844 resize가 success를 반환하나 캔버스·레이아웃 1280 데스크톱 유지 — 도구 제약이지 페이지 결함 아님). **구조 점검은 양호**: 표 래퍼 `overflow-x:auto`(min-width 820px 표를 자기 컨테이너에 격리 — 체크리스트의 "표만 스크롤, body 안 밀림" 패턴), viewport meta `width=device-width, initial-scale=1`. 가장 넓은 요소(표)가 스크롤 컨테이너에 갇혀 있어 body 가로 오버플로 위험은 구조상 차단됨. <1280 캡처 가능한 환경이 생기면 육안 1회로 완결 권장(현재 결함 징후 없음, 배포 차단 사유 아님). |

### 종합

치명 D-F1 해소로 페이지가 정상 동작한다. B1(앞 절)·B2·B3·B4·렌더 동작 전부 통과. 데이터(30 온라인·12 오프라인)·정책 DELTA(경과조치·약국 30억·선차감 medium·S10 QR·각주)·동적 스탬프(2026-08)가 화면에서 일치 확인됨. 유일한 미검증은 390px 실측(도구 제약, 구조 양호). **최종 판정: 통과.**

---

## merchants 검증 (B2'·B3'·B4'·렌더·회귀) — 2026-08-06

### 판정: 통과 (낮음 관찰 1건 — 판정 차단 아님)

신규 `merchants.html`(수도권 가맹점 검색, 8655 정적 서버)과 M7 링크로 재빌드된 `index.html`을 검증했다. 데이터↔렌더·문구↔명세·스탬프·렌더 동작·회귀 전부 통과. **dev 보고치를 믿지 않고 `data/merchants/*.json`에서 digital==='Y'를 직접 집계해 화면과 대조**했고 전부 일치했다. 콘솔 페이지 예외 0(유일 예외는 브라우저 확장 share-modal.js — 페이지 무관). 초기 14MB급 병렬 로드는 낮음 관찰로 기록(아래).

### B2' 데이터 → 렌더 (독립 재계산 대조)

JSON에서 직접 계산한 값과 화면 칩이 **전부 일치**:

| 검사 | 왼쪽(JSON digital=='Y' 직접 집계) | 오른쪽(화면 칩) | 결과 |
|------|----------------------------------|----------------|------|
| 지역 전체 | 49,719 (서울22,317+인천6,134+경기21,268) | 전체 49,719 | 일치 |
| 서울 | 22,317 | 서울 22,317 | 일치 |
| 인천 | 6,134 | 인천 6,134 | 일치 |
| 경기 | 21,268 | 경기 21,268 | 일치 |
| 카테고리(수도권 digital=Y) | 음식점13,992·마트956·편의점290·약국773·학원164·기타33,544 | 음식점·카페13,992·마트·슈퍼956·편의점290·약국773·학원·교습소164·기타33,544 | 일치 |
| **카테고리 재집계(서울 선택 시)** | 서울 digital=Y: 음식점5,902·마트406·편의점101·약국294·학원29·기타15,585·전체22,317 | 동일 | **일치(재집계 작동)** |
| countText | — | "49,719곳 중 49,719곳 표시" | 일치 |
| M1 총계 | 49,719 | "총 49,719곳" | 일치 |

- 주의: 화면은 **digital==='Y' 49,719건만 수록**(전체 52,958 아님) — 명세 M4 "디지털형 Y만 수록" 권장과 일치. 06 리포트 §4 표(52,958 기준)와 다른 건 정상(그 표는 전체 데이터 분포 참고치).
- 카테고리 라벨 매핑: 음식점→"음식점·카페", 학원→"학원·교습소" — 명세 M3와 일치.
- 시도 전환 재집계는 첫 시도(ref 클릭)에서 포커스만 잡혀 미반영으로 보였으나, 실제 선택 클릭에서 정상 재집계 확인 — 기능 결함 아님.

### B3' 문구 → 명세 (M1~M7)

- **M2 배너 3문장** 전부 렌더(검색 UI 위, 스크롤 없이 보임):
  ① "이 목록은 2025-07-31 기준 공공데이터 스냅샷입니다. 연 1회 갱신되는 자료라(다음 갱신 2026-08-12 예정)…"
  ② "개별 점포의 주소는 제공되지 않습니다. 위치는 소속 시장·상점가 이름까지만…앱 '가맹점 찾기' 지도에서 점포 위치와 현재 가맹 여부를 확인하세요."
  ③ "여기 '가맹'으로 나와도 2025-07-31 이후 해지됐을 수 있고, 여기 없어도 새로 가맹했을 수 있습니다." → **양방향 오독(있음=됨/없음=안됨) 차단** ✓
- **표머리에 "주소" 단어 없음**: `가맹점명 · 분류 · 소속 시장·상점가 · 지역 · 취급품목` — 명세 M4 준수 ✓
- **M5 빈 결과**: `"{q}"에 해당하는 가맹점이 없습니다.` + "여기 없어도 실제로는 가맹점일 수 있습니다 — 앱 '가맹점 찾기' 지도에서 점포 단위로 확인하세요." + `검색 초기화` 버튼 + `앱 가맹점 지도로 확인 ↗` 링크 ✓
- **M6 각주** 전부 렌더·동적: ①출처(공공데이터포털·2025-07-31 스냅샷·수도권·**디지털형 가맹점만 수록**·다음 갱신 2026-08-12 예정) ②주소 없음·시장 단위 ③앱 최종 확인 ✓

### B4' 수집일 → 스탬프 (하드코딩 검사)

- merchants.html 소스에 리터럴 `2025-07-31` 0개, `2026-08-12` 0개, `49,719`/`49719` 0개, `22,317` 0개.
- `snapshot_date`·`next_update`·`dataset_version` 참조로 meta에서 계산 — **하드코딩 아님** ✓
- 3개 JSON meta 모두 `dataset_version: 2025-07-31`, `next_update: 2026-08-12`, `generated_on: 2026-08-06` 일치. 화면 스탬프(2025-07-31·2026-08-12 예정)와 정합 ✓

### 카테고리 오분류 표본 (06 리포트 §5의 20건 중 5건 화면 검색 확인)

| 가맹점명 | 화면 분류 | 취급품목(화면) | 리포트 판정 | 결과 |
|----------|-----------|---------------|-------------|------|
| 이마트24 R응암증산점 | 편의점 | 편의점 | 편의점 | 일치 |
| 안양식자재마트 | 마트·슈퍼 | 소매업(슈퍼마켓, 육류) | 마트·슈퍼 | 일치 |
| 골뱅이마트 | 편의점 | 편의점 | 편의점 | 일치(이름 '마트'여도 취급품목 편의점 우선) |
| 먹자건어물 | 마트·슈퍼 | 음식료품 | 마트·슈퍼 | 일치 |
| 형제슈퍼 | 마트·슈퍼 | 일식품잡화 | 마트·슈퍼 | 일치 |

우선순위 규칙(편의점>마트, 마트>음식점)이 화면에서 정상 작동.

### 렌더 동작

- 콘솔 에러 0(페이지 JS). 유일 예외는 브라우저 확장 share-modal.js — 페이지 무관.
- 검색: 실입력으로 5건 각 1건 필터·"49,719곳 중 1곳 표시" 정상.
- 필터: 시도 칩(서울) 적용 시 표·카운트·카테고리 칩 모두 재계산.
- 페이지네이션: 50행/페이지, "1 / 995 페이지 · 49,719곳"(49,719÷50=995), `다음 ›` 클릭 시 "2 / 995"·첫 행 변경(유지원 뷰티샵→일오삼반찬가게) ✓
- 빈 결과 + `검색 초기화`: 초기화 시 검색어 비움·49,719 전체 복귀 ✓
- 색각: 분류·지역이 색이 아닌 **텍스트 뱃지**(편의점·마트·슈퍼·기타·음식점·카페·서울)로 표기 — 색만 의존 아님 ✓
- file:// 안내: 소스에 "서버에서 열어야 데이터가 로드됩니다(파일을 직접 열면 차단됨)." 존재 — fetch 기반이라 file:// 직접 열람 시 CORS 차단되는 것을 안내 ✓

### 회귀 (index.html + D-F1 유형 재검사)

- index.html 정상 렌더: h1 "사용처 정리 — 오프라인·온라인", 잔여 머스태시 0(DC 마운트 정상), 콘솔 페이지 예외 0.
- **D-F1 재발 없음**: L388 template 첫 `</script>`까지 캡처 94,285자(정상, 186 아님), 리터럴 `</script>` 0개.
- **M7 링크**: index.html S4 요건2 '지도 검색' 박스 바로 아래 같은 박스 스타일로 위치. 문구 명세 M7 일치. `href="merchants.html"`, 클릭 시 merchants.html로 실제 이동 확인 ✓

### 관찰 (판정 차단 아님)

#### O-M1. 초기 3개 JSON 병렬 로드 ~12.5MB (전 시도 선로드)
- 심각도: **낮음**
- 관측: 페이지 로드 시 seoul.json(5.6MB)·incheon.json(1.6MB)·gyeonggi.json(5.3MB)를 **동시 병렬** fetch(모두 startTime 33ms). 로컬 loadEvent 197ms. 시도 하나만 볼 때도 3개 전부 선로드.
- 이용자 영향: 로컬·캐시 후는 빠름. GitHub Pages는 텍스트(.json) gzip 제공으로 전송량이 크게 줄어(추정 2~3MB) 실사용 지장은 작음. 단 저속 회선 첫 로드에서는 체감 가능. 이후 검색·필터·페이지는 전부 클라이언트라 서버 왕복 없음.
- 대안(선택): 기본 진입 시 서울만 로드하고 인천·경기는 해당 시도 칩 선택 시 지연 로드. 다만 기본 '전체' 뷰가 세 시도를 모두 필요로 하므로 현 구조도 합리적 — 필수 아님.
- 담당: guide-frontend-dev(개선 시). **판정 차단 아님** — 이용자가 헛걸음/오류를 겪는 결함이 아니라 초기 로드 최적화 여지.

### 종합

merchants.html: B2'(독립 재계산 전량 일치)·B3'(M1~M7 문구·주소 미표기)·B4'(스탬프 meta 계산)·카테고리 표본 5/5·렌더 동작(검색·필터·페이지네이션·빈결과·초기화·색각·file 안내) 전부 통과. index.html 회귀 및 M7 링크 동작 정상, D-F1 재발 없음. 낮음 관찰 1건(초기 병렬 로드)은 최적화 여지일 뿐 배포 차단 아님. **최종 판정: 통과.**

---

## merchants 재검증 (v2 — 시도별 지연 로드 + 디지털 토글) — 2026-08-06

> 검증 기준 변경(team-lead) + dev v2 재작업 반영. **위 "merchants 검증"(v1)의 O-M1(초기 14MB 병렬 로드)은 지연 로드 전환으로 폐기.** M1(그랜드총계→시도별)·M3(전체 칩 삭제)·M4(디지털 토글) 갱신 명세(07 최신본) 기준으로 재검증.

### 판정: 통과 (낮음 편차 2건 — 판정 차단 아님)

data JSON에서 digital=='Y'/전체를 직접 집계해 화면과 대조, 지연 로드·토글·카운트 전부 일치. 콘솔 페이지 예외 0(확장 예외만).

### 새 아키텍처 검증 (v2 핵심)

| 검사 | 관측 | 결과 |
|------|------|------|
| **지연 로드(초기)** | 페이지 로드 시 network에 `seoul.json` 1건만 GET(인천·경기 미요청) | 통과 |
| **지연 로드(시도 전환)** | 인천 칩 클릭 시 `incheon.json` 1건만 GET(서울 재요청·경기 사전로드 없음) | 통과 |
| 캐시 | 인천 로드 후 서울 칩은 "24,190곳" 카운트 유지(재로드 없음) | 통과 |
| M1 그랜드총계 제거 | "…2025-07-31 기준 · 서울 22,317곳 (디지털형) · ← 사용처 가이드" — 수도권 총계 없음, 현재 시도만 | 통과 |
| M3 전체 칩 삭제 | 시도 칩 = 서울/인천/경기 3개(전체 칩 없음), 항상 1개 선택 | 통과 |
| 미로드 시도 표시 | 인천·경기 칩 = "선택 시 불러오기"(로드 후 "6,676곳" 등) | **편차 O-M2 참조** |

### M4 디지털 토글 (독립 재계산 대조)

| 토글 | 화면 | JSON 직접 집계 | 결과 |
|------|------|---------------|------|
| ON(기본, 서울) | M1 "서울 22,317곳 (디지털형)", countText 22,317, 칩 음식점5,902·마트406·편의점101·약국294·학원29·기타15,585 | digital=='Y' 서울: 동일 | 일치 |
| OFF(서울) | M1 "서울 24,190곳 (디지털형·지류형)", countText 24,190, 칩 음식점6,050·마트413·편의점101·약국295·학원29·기타17,302, digital=N 행에 `지류만` 배지(예: 암사본정형외과의원·의료업) | 서울 전체: 동일 | 일치 |
| 시도 전환(인천, 토글 OFF 유지) | M1 "인천 6,676곳", 칩 음식점1,477·마트114·편의점39·약국61·학원8·기타4,977 | 인천 전체 6,676: 동일 | 일치 |

- 디지털 ON/OFF·시도 전환 모두 카테고리 칩·countText·M1 건수가 정확히 재집계. `지류만` 배지는 digital=='N' 행에만 렌더.

### B3'·렌더 (v2에서 재확인)

- 표 헤더: `가맹점명·분류·소속 시장·상점가·지역·취급품목` — **"주소" 단어 없음** ✓
- 각주 ①(갱신): "※ 출처 — …수도권(서울·인천·경기)의 디지털형·지류형 가맹점 수록(기본은 '디지털만 보기'). 연 1회 갱신…다음 갱신은 2026-08-12 예정." / ② 주소 없음 / ③ 앱 최종 확인 — 3개 전부 렌더 ✓
- M2 배너 3문장·M5 빈 결과(v1과 동일, 변경 없음) 유지 ✓
- 검색(v2): 인천 로드 상태에서 "부평" → "6,676곳 중 898곳 표시", 전 행 부평 매칭 — 현재 로드 시도 데이터에서 필터 ✓
- 색각: 분류·지역·`지류만` 모두 텍스트 뱃지 ✓
- 하드코딩: merchants.html 리터럴 날짜/건수(2025-07-31·2026-08-12·24,190·6,676·6,134·22,092) 전부 0 — meta·items 계산 ✓
- 회귀: index.html L388 리터럴 `</script>` 0(D-F1 유지), M7 링크 href=merchants.html·문구 M7 일치(이번 세션 이동 확인). git 워킹트리 클린.

### 편차 (낮음 — 판정 차단 아님)

#### O-M2. 미로드 시도 칩에 건수 미표시 ("선택 시 불러오기")
- 심각도: **낮음**
- 경계면: 명세(D2) ↔ 렌더
- 왼쪽(명세 D2/전달 §3): "미로드 시도의 칩 카운트는 meta의 시도별 건수 필드로 표시" — merchants JSON meta에 `시도별 건수` 필드 요청됨.
- 오른쪽(관측): `data/merchants/*.json` meta 키 = source/source_url/dataset_version/generated_on/next_update/region/limitations/note — **시도별 건수 필드 없음.** 화면은 미로드 시도(인천·경기) 칩에 건수 대신 "선택 시 불러오기" 표시.
- 이용자 영향: 인천·경기를 클릭 전 건수로 비교 못 함. 잘못된 정보는 아니며(하드코딩·스테일 카운트 대신 정직한 폴백), 클릭 시 로드됨을 명확히 안내.
- 담당: **onnuri-data-curator**(meta에 시도별 건수 추가) + **guide-frontend-dev**(추가되면 칩에 렌더). 또는 team-lead가 "선택 시 불러오기" 폴백을 명세로 승인.
- 판정: **차단 아님** — 결제 실패·헛걸음 유발 없음. 팀 결정 사항.
- **해소(2026-08-06)**: team-lead가 "선택 시 불러오기" 폴백을 **명세로 승인**(meta 시도별 건수 추가 안은 기각 — 파생값 필드 증가로 갱신 시 어긋날 표면을 늘리지 않기 위함). writer가 D2 명세를 폴백으로 갱신 예정. 이 편차는 승인된 설계로 종결 — 잘못된 정보가 아닌 정직한 상태 표시.

#### O-M3. 각주 ① '지류만' 배지 설명 축약
- 심각도: 낮음
- 왼쪽(명세 M6 ①): "…'디지털형만 보기'를 끄면 지류형 전용 가맹점('지류만' 표시)까지 보입니다."
- 오른쪽(관측): "…디지털형·지류형 가맹점 수록(기본은 '디지털만 보기')." — 양형 수록·디지털 기본은 전달하나 `지류만` 배지 명시는 생략.
- 이용자 영향: 미미. 배지 자체가 행에서 자명. 핵심 정보 손실 없음.
- 담당: guide-content-writer/dev(원하면 문구 보강). 차단 아님.

### 종합 (v2)

시도별 지연 로드(초기 1파일·전환 시 1파일)·디지털 토글(ON/OFF + 지류만 배지)·M1 시도별 건수·전체 칩 삭제 전부 명세대로 작동하고, 데이터 카운트는 독립 재계산과 정확히 일치. B3'·렌더·회귀 통과. 낮음 편차 2건(미로드 시도 칩 건수 미표시, 각주 축약)은 이용자 안전에 영향 없어 판정 차단 아님. **O-M2는 team-lead가 폴백을 명세로 승인해 종결**, O-M3는 선택적 문구 보강 건. **최종 판정: 통과.** (v1의 O-M1 14MB 로드 항목은 폐기) · 배포는 사용자 직접 승인으로 team-lead가 push·GitHub Actions 워크플로 배포 처리(verifier 미실행).

---

## SSM 정정 재검 (지에스더프레시 결함 제보) — 2026-08-06

### 판정: 통과 (결함 0건)

사용자 결함 제보(SSM 지에스더프레시)에 대한 policy→writer→data→dev 정정 루프 산출물을 재검. 정정 3지점 + index 회귀 전부 통과. 미해석 머스태시 0(완전 마운트), 콘솔 페이지 예외 0(확장 예외만), 리터럴 `</script>` 0(D-F1 재발 없음).

### 정정 3지점 (화면 관측)

| # | 지점 | 화면 관측값 | 결과 |
|---|------|-------------|------|
| 1 | S4 요건4(오프라인 탭) | "…대형마트·백화점·기업형슈퍼마켓(SSM) 직영점, 유흥·사행성, 병·의원 등 보건업, 수의·법무·회계세무(보건업 등은 2026.6.17부터 추가). 같은 브랜드 SSM이라도 전통시장·골목형상점가 안의 개인 가맹점주 점포는 가맹된 경우 사용 가능 — 앱 지도에서 개별 확인. 약국은 예외적으로 가맹이 유지됩니다 — 단, 연매출 30억 기준은 약국에도 똑같이 적용됩니다." | 통과 |
| 2 | S6 판정표 백화점·SSM 행 | 대상·예시 "백화점·아웃렛·기업형 슈퍼(SSM) 직영점(이마트에브리데이, 홈플러스익스프레스, **GS더프레시 직영** 등)" · 판정 **✕ 불가**(유지) · 확인 포인트 "직영점은 가맹 제외 — 단, 같은 브랜드라도 전통시장·골목형상점가 안의 개인 가맹점주 점포는 가맹 시 사용 가능, 앱 지도에서 개별 확인" | 통과 |
| 3 | S14 용어 SSM | "SSM(기업형 슈퍼마켓): 대기업 계열회사가 직영하는 중형 슈퍼(이마트에브리데이, 홈플러스익스프레스 등)로, **가맹 제외 대상은 직영점 기준**" | 통과 |

- SSOT 대조: `data/offline_categories.json` department-ssm 행(examples·verdict:denied·check_point)이 화면 렌더와 일치. verdict_label "불가"·기호 ✕ 유지.
- 3지점 **일관성**: 세 곳 모두 "직영점 = 제외 / 전통시장·골목형상점가 안 개인 가맹점주 점포 = 가맹 시 가능"으로 통일 — 제보의 오해(브랜드 단위 일괄 불가)를 점포 단위 구분으로 정정.

### 회귀 (기존 정정분 유지 확인)

- 완전 마운트: 미해석 머스태시 0, 오프라인 12행 유지.
- D-F1: index.html L388 리터럴 `</script>` 0.
- 기존 정정분 무손상: S4 요건3 경과조치 ✓ / 각주② 대통령령 제36415호 ✓ / S10 모바일(앱)형 QR 흐름 ✓ / M7 merchants.html 링크 ✓ / 약국 30억 병기(요건4 내) ✓.
- 콘솔: 페이지 JS 예외 0(유일 예외는 브라우저 확장 share-modal.js — 페이지 무관).

### 종합

SSM 정정 3지점이 화면·SSOT에서 일관되게 반영됐고, 다른 섹션·기존 정정분은 무손상. D-F1 재발 없음. **판정: 통과.** (미커밋 상태 — index.html·offline_categories.json 등 SSM 배치는 team-lead 배포 흐름에서 커밋·push 예정)

---

## 브랜드 뷰 통합 검증 (task #14) — 2026-08-07

### 판정: 통과 (결함 0건, 미검증 1건 = 390px 도구 제약)

merchants.html에 통합된 **브랜드 매장 뷰**(편의점·마트·SSM·다이소, brand_stores.json)를 검증. 뷰 전환·지연 로드·구/시 필터·카테고리·SSM·주소 컬럼·시장 뷰 회귀 전부 통과. **dev 보고치를 믿지 않고 brand_stores.json에서 직접 집계**해 화면과 대조, 전량 일치.

### 독립 재계산 대조 (brand_stores.json, disk)

| 검사 | 디스크 집계 | 화면 | 결과 |
|------|-----------|------|------|
| 시도 총계 | 서울 611 · 인천 153 · 경기 751 (합 1,515) | 칩 611/153/751 | 일치 |
| 서울 brand_cat | 편의점 248 · 마트 360 · SSM 0 · 다이소 3 | 칩 동일 | 일치 |
| 경기 brand_cat | 편의점 358 · 마트 379 · SSM 6 · 다이소 8 | 칩 동일 | 일치 |
| SSM 6건 | 경기 GS더프레시 5 + 이마트에브리데이 1 | SSM 칩 클릭 → 6행(동일 6곳, 도로명 주소) | 일치 |
| 강남구 필터 | 서울 강남구 20 | "611곳 중 20곳 표시", 전 행 강남구 | 일치 |
| 구/시 유니크 | 서울 25구 · 경기 31시군 · 인천 10구군 | 서울=구/경기=시 라벨·옵션 | 일치 |

### 뷰 전환 (양방향)

| 축 | 시장 뷰 | 브랜드 뷰 | 전환 |
|----|---------|-----------|------|
| 배너 | M2 "2025-07-31 공공데이터 스냅샷"(visible) | M8-2 "2026-08-07 수집본·기준일 상이 비교금지"(visible) | 상호 display:none 전환 ✓ |
| 각주 | M6 ①②③(visible) | M8-5 ①출처·②SSM·③최종확인(visible) | ✓ |
| M1 기준일(D10) | 2025-07-31 | 2026-08-07 | ✓ |
| 표 컬럼 | 가맹점명·분류·소속 시장·상점가·지역·취급품목 | 가맹점명·분류·도로명 주소·소속 시장·상점가·지역 | ✓ (도로명 주소 등장, 취급품목 제거) |
| 디지털 토글 | 있음(기본 ON) | 없음 | ✓ (브랜드 뷰 미표시) |
| 구/시 필터 | 없음 | 있음(D8) | ✓ |

### 지연 로드 (network 실측)

- 초기 로드: seoul.json만 GET(brand_stores.json 미로드) ✓
- **브랜드 매장 탭 첫 클릭 → brand_stores.json 정확히 1건만 GET** ✓ (시장 CSV 재요청·타 시도 사전로드 없음)
- 브랜드 뷰 시도 전환(경기)은 재fetch 없음(단일 파일 로드 후 region 필터) — 네트워크 추가 GET 0 ✓

### 렌더 동작 (task #14 배정분)

- **뷰 전환 시 검색어 잔존**: 브랜드 뷰 "CU"(751곳 중 125곳) → 시장 뷰 전환 시 검색어 **초기화("")**, 결과도 스테일 필터 없이 전체 복귀 — 안전(잔존 없음) ✓
- **정렬**: 브랜드 뷰 이름순(가나다) → 표 가나다 오름차순 정렬 확인 ✓
- **색각**: 분류(편의점·마트·슈퍼·기업형 슈퍼(SSM)·다이소)·지역(구/시) 모두 **텍스트 뱃지** — 색만 의존 아님 ✓
- 카테고리 칩 재집계: 시도 전환·구/시 필터 적용 시 D7 정확 재계산 ✓

### 회귀 (시장 뷰 무손상)

- 시장 뷰 복귀 시 M2 배너·M6 각주①②③·디지털 토글(ON)·시장 컬럼(취급품목)·기준일 2025-07-31 전부 복원 ✓
- 시장 시도 지연 로드·카테고리·페이지네이션·빈결과·M7 링크(index.html) 기존 검증분 유지(별도 회귀 이상 없음).

### 검증 중 자체 정정 (false alarm 기록)

- 초기에 "브랜드 뷰 M8-5 ③ 최종확인 각주 누락"으로 관측됐으나, 이는 **측정 아티팩트**였다 — ③ 문구가 `<strong>`을 포함해 `childElementCount===0` 필터에서 제외됐던 것. 더 정확한 쿼리(부모 `<p>` 기준 가시성 검사)로 재확인한 결과 브랜드 뷰 ①②③ 전부 visible. **결함 아님.**

### 콘솔

- "message channel closed before a response" 예외 2건이 merchants.html:0:0으로 표기됨. 그러나 이는 브라우저 확장의 `chrome.runtime` 메시징 시그니처(전형적 확장 노이즈)이며, 이 페이지는 vanilla fetch 기반으로 chrome.runtime 메시징을 쓰지 않고 전 기능이 정상 작동. dev의 "확장 노이즈 제외 페이지 예외 0"과 일치. **페이지 결함 아님.** (확실히 하려면 확장 없는 클린 프로파일에서 재확인 권장 — 기능 영향 없음)

### 미검증 (도구 제약)

| 항목 | 사유 |
|------|------|
| 390px 반응형·body 가로 밀림 | `resize_window`가 이 환경에서 창을 1280px 아래로 축소 못 함(verifier·dev 양쪽 기확인). 구조는 v2와 동일 계열(표 overflow-x 컨테이너). 픽셀 확정은 <1280 캡처 가능 환경에서 육안 1회 권장 — 결함 징후 없음. |

### 종합

브랜드 뷰 통합의 뷰 전환·지연 로드(초기 미로드→탭 클릭 시 1파일)·구/시 필터(D8)·브랜드 카테고리 칩(D7)·SSM 6건·도로명 주소·M8-2 배너·M8-5 ①②③ 각주 전부 명세대로 작동하고, 카운트는 brand_stores.json 독립 집계와 정확히 일치. 시장 뷰 회귀 무손상. 검색어 뷰 전환 초기화·정렬·색각 정상. **판정: 통과, 결함 0건.** 미검증은 390px(도구 제약)뿐. 배포는 사용자 승인하에 team-lead 흐름에서 처리(verifier 미실행).

### 브랜드 뷰 검증 — team-lead 정식 배정(task #14) 델타 확인 (2026-08-07)

위 "브랜드 뷰 통합 검증"에 이어, team-lead 정식 기준의 추가 항목을 확인. dev 정정(소속 시장·상점가 컬럼 추가)이 반영된 최신본 기준. 전부 통과.

- **B2'' 칩 합계(전 시도)**: brand_stores.json 직접 집계 = 편의점 655 · 마트·슈퍼 838 · SSM 6 · 다이소 16 (합 1,515) — team-lead 기대치와 정확히 일치. (뷰는 시도별 표시라 화면 칩은 선택 시도 부분집계; 그 합이 위 총계와 정합.)
- **분류 제외 표본(09 리포트 §6, 미분류 53건)**: `세탁편의점`·`노브랜드버거`·`CUPI` 모두 brand_stores.json에 **0건**(정규식 음성 룩비하인드 `(?<!세탁)편의점`·`노브랜드(?!\s?버거)`·CU 영문경계로 차단). 렌더 확인: 브랜드 뷰 "노브랜드버거" 검색 → "751곳 중 0곳 표시" + 빈 결과("…매장이 없습니다." + 검색 초기화 + 앱 지도 링크). **제외 정상.**
- **SSM 6건 전부 노출**: SSM 칩 → 6행(경기 GS더프레시 5+이마트에브리데이 1) 실증(위 섹션).
- **B4'' 스탬프 하드코딩**: merchants.html 리터럴 `2026-08-07`·`2025-07-31` 각 0개, `collected_on`·`dataset_version` 참조로 meta 계산. brand meta는 `collected_on`(2026-08-07)만 보유(시장의 `dataset_version`과 별개 필드) — 기준일 이원화 정상.
- **브랜드 표 5컬럼(dev 정정)**: 가맹점명 · 분류 · 도로명 주소 · 소속 시장·상점가 · 지역(구/시) — 도로명 주소 + 소속 시장·상점가 병기 확인(team-lead 브리핑 점3 반영).
- **빈 결과(M8-6)**: 브랜드 뷰 빈 검색 시 "매장이 없습니다"체 + 검색 초기화 + 앱 가맹점 지도 링크 노출.
- 빌드노트 파일명: `_workspace/08_merchants_build_notes.md`(D6~D10·브랜드 5컬럼 갱신 반영). (dev 초기 메시지의 "10_…"는 오기, 실제 08.)

**task #14 최종 판정: 통과, 결함 0건.** 미검증은 390px 반응형(도구 제약)뿐. 통과이므로 team-lead가 커밋·배포.

---

## 지도 링크 문구 일치 + 앱스토어 보조링크 검증 (task #16) — 2026-08-07

### 판정: 통과 (결함 0건, 낮음 관찰 1건 = 라벨 일관성 · 미검증 1건 = 390px)

M6 '확인 경로 링크 규격'(명세 line 95~101) 반영분을 검증. M5(시장)·M8-6(브랜드) 빈 결과 링크·index.html M7 문구·스토어 링크 도달 전부 통과. index 완전 마운트·리터럴 `</` 0(D-F1 재발 없음).

### 빈 결과 링크 (M5 시장 / M8-6 브랜드 — 화면 관측)

| 항목 | 시장(M5) | 브랜드(M8-6) | 명세 대조 |
|------|----------|--------------|-----------|
| 통일 문장 | "…여기 없어도 실제로는 가맹점일 수 있습니다 — 온누리 가맹점 지도나 디지털온누리 앱에서 점포 단위로 확인하세요." | 동일(브랜드는 "매장", 수집일 2026-08-07) | line 83 일치 |
| 1차 링크 라벨 | `온누리 공식 가맹점 지도로 확인 ↗` | 동일 | **line 84·97 일치**("앱" 라벨 없음) |
| 1차 href/target | `https://www.onnuri.gift/place` · `_blank` · `rel=noopener` | 동일 | line 97 일치 |
| 2차 링크 | `디지털온누리 앱 설치 — App Store ↗ · Google Play ↗` | 동일 | line 84·98 일치("설치" 명시) |
| App Store href | `https://apps.apple.com/kr/app/id6738372450` · `_blank` | 동일 | line 99 일치 |
| Google Play href | `https://play.google.com/store/apps/details?id=com.komsco.kpay` · `_blank` | 동일 | line 100 일치 |
| 검색 초기화 버튼 | 있음 | 있음 | 유지 |

- 구 라벨 `앱 가맹점 지도로 확인` 잔재 0(소스). 시장은 "가맹점이 없습니다", 브랜드는 "매장이 없습니다"로 뷰별 용어 유지.

### 스토어 링크 실제 도달 (WebFetch 실측)

- App Store `id6738372450`: 유효 라이브 페이지 — 앱 **디지털온누리(Digital Onnuri)**, 개발자 **한국조폐공사(KOMSCO)**. 명세 line 98과 정확 일치.
- Google Play `com.komsco.kpay`: 유효 페이지 — 앱 **디지털온누리**. 404 아님.
- 두 링크 모두 올바른 앱으로 도달. 커스텀 스킴 딥링크 미사용(명세 line 96 준수 — 죽은 링크 방지).

### index.html M7 진입 링크 (line 105)

- 화면 문구: `수도권 가맹점 검색 ↗ — 서울·인천·경기 가맹점을 상호·소속 시장 이름으로 찾아볼 수 있습니다 (공공데이터 스냅샷 기준, 최종 확인은 온누리 가맹점 지도/앱)` — **line 105 정확 일치**.
- 구문구 `최종 확인은 앱 지도` 제거 확인. href=`merchants.html` 유지.
- 회귀: 완전 마운트(머스태시 0)·L388 리터럴 `</script>` 0·콘솔 페이지 예외 0(확장만).

### dev 판정 요청 2건 회신

- (a) index.html/M7 변경: **정당함** — 명세 line 105가 "최종 확인은 온누리 가맹점 지도/앱"으로 명시. writer 델타 메시지에 없었더라도 명세 반영이 맞다. 결함 아님.
- (b) 명세 내부 불일치(line 84 vs 97): 현재 07 명세는 **line 84·97 둘 다 `온누리 공식 가맹점 지도로 확인 ↗`로 일치**(writer가 조정 완료 추정). 렌더도 이와 일치 — 빈 결과 링크는 spec-compliant. dev가 인용한 "line 97=열기"는 이전 버전 값으로 보이며, 현재는 해소됨.

### 관찰 (낮음 — 판정 차단 아님)

#### O-L1. 동일 목적지(웹 지도)에 두 라벨 혼용
- 심각도: 낮음
- 빈 결과 링크 라벨 `온누리 공식 가맹점 지도로 확인 ↗` vs load-error 링크 라벨 `온누리 가맹점 지도 열기 ↗`(dev 반영 point 2, 소스 2곳) — 둘 다 `onnuri.gift/place`로 향하고 "앱" 라벨은 없어 line 97 원칙(문구·목적지 일치)은 충족하나, 같은 목적지에 표기가 갈린다.
- load-error 경로는 fetch 실패 상태라 브라우저에서 강제 트리거 못 해 **라이브 렌더 미확인**(소스로만 확인). 문구 일관성 차원의 개선 여지 — writer/dev 판단. 이용자 피해 없음.
- **해소(2026-08-07 재검)**: dev가 load-error 라벨도 확정 문구로 통일. `merchants.html:507` `renderLoadError()`의 앵커가 `<a href="APP_MAP_URL"(=onnuri.gift/place) target="_blank" rel="noopener">온누리 공식 가맹점 지도로 확인 ↗</a>` — empty-result(M5·M8-6 공유 렌더러)와 **동일 라벨·목적지·appInstallHTML() 공유**. `온누리 가맹점 지도 열기`·`앱 가맹점 지도로 확인` 잔재 0, 커스텀 스킴 딥링크 0. 세 경로 라벨 일치로 **O-L1 종결**.

### 미검증 (도구 제약)

- 390px 반응형·색각 세부: `resize_window` 1280 하한(양측 기확인). 링크·뱃지 모두 텍스트라 색각 기본 요건은 충족. 픽셀 확정은 별도 환경.

### 종합

M5·M8-6 빈 결과의 1차(웹 지도, "앱" 라벨 제거)·2차("디지털온누리 앱 설치" App Store·Google Play) 링크가 명세대로 렌더되고 href·target·rel 정확, 스토어 링크는 실제 "디지털온누리"(한국조폐공사) 앱으로 도달. index.html M7 문구도 line 105대로 갱신, D-F1 재발 없음. **판정: 통과, 결함 0건.** 낮음 관찰 1건(라벨 일관성)은 개선 여지일 뿐 차단 아님. 통과이므로 team-lead가 커밋·배포.

---

## S15 '가맹점 찾기' 서브탭 검증 (task #17) — 2026-08-07

### 판정: 조건부 통과 → **재검 통과** (team-lead (a)결정 후 dev 정정 2건 반영, 2026-08-07 재검 완료)

> **재검 결과(2026-08-07)**: D-S15-1(배치)·D-S15-2(안내 문장) 둘 다 해소 확인. 최종 판정 **통과**. 상세는 각 결함 하단 "재검" 및 절 끝 "재검 종합" 참조.

서브탭 링크 시맨틱·제거·회귀는 전부 통과. 그러나 명세 03 S15의 **위치(최상단)**·**요건2 지도 검색 안내 문장 유지** 두 지시와 렌더가 어긋난다 — 둘 다 이용자 안전 결함은 아니나(치명/높음 아님) 명세 명문과 정면으로 달라 team-lead/writer 재조정이 필요하다.

### 통과 항목

- **서브탭 링크 시맨틱(화면 실측)**:
  - 서브탭1 `가맹점 찾기` (성격 표지 "우리 검색") → `href="merchants.html"`, **target 없음·rel 없음**(내부 이동) ✓
  - 서브탭2 `공식 지도 검색 ↗` (성격 표지 "온누리 공식") → `href="https://www.onnuri.gift/place"`, `target="_blank"`·`rel="noopener"`(외부 새 탭) ✓
  - 라벨에 "앱" 없음, 외부 탭에 `↗` 기호. 두 탭에 텍스트 성격 표지("우리 검색"/"온누리 공식") 병기 — 색각 요건(색상만 의존 아님) 충족.
- **문구**: 섹션 제목 `가맹점을 직접 찾아보기`·부제 `상호·시장 이름으로 찾으려면…`·설명1(…함께 봅니다. 공공데이터 스냅샷 기준)·설명2(…온누리 공식 서비스) 전부 렌더. **동적 숫자/날짜 0**(하드코딩 없음).
- **제거**: 구 M7 `수도권 가맹점 검색 ↗` 진입박스 0 ✓, 요건2 인라인 `가맹점 지도 검색 ↗` 링크 0 ✓.
- **요건2 본문 유지**: "매장 입구 가맹 스티커 또는 앱 '가맹점 찾기' 지도에서 확인" 1건 유지 ✓.
- **회귀**: 완전 마운트(머스태시 0)·L388 template 95,453자(리터럴 `</script>` 0, D-F1 재발 없음)·SSM(GS더프레시)·경과조치·S10 무손상·콘솔 페이지 예외 0(확장만).

### 편차 (명세↔렌더 — 재조정 필요)

#### D-S15-1. S15 배치가 명세와 반대 (최상단 ↔ 맨 아래)
- 심각도: 보통
- 경계면: 명세(03 S15) ↔ 렌더
- 왼쪽(명세 03 S15): "위치: 오프라인/온라인 메인 탭 바로 아래, **오프라인 탭이 활성일 때 그 콘텐츠 최상단(사용 요건 박스 앞)**." 근거: "요건 ②·③ 사이에 묻혀 아무도 못 찾는다 → 최상단으로 **승격**해 **판정표를 읽기 전에** '검색 도구가 여기 있다'가 먼저 보이게."
- 오른쪽(렌더 실측, 화면 top 좌표): S15 = **1714px**. 사용 요건 468 · 판정표 740 · 팁 1502 · 카드형 흐름 1560 — **S15가 이 모두보다 아래**(오프라인 탭 맨 끝, 온라인 경계 직전). dev 메시지도 배치를 "맨 아래"로 기술.
- 이용자 영향: 명세의 유일한 목적(검색 도구 노출·발견성)을 저해. 맨 아래면 판정표·팁·결제 흐름을 다 지나야 보여 "묻힘"이 재현된다(원래 요건 사이보다 더 아래).
- 담당: **guide-frontend-dev**(최상단으로 이동) + **team-lead/writer**(만약 task #17 브리핑에서 의도적으로 맨 아래로 바꿨다면 03 S15 명세를 그 위치로 갱신 — 현재 명세·렌더가 불일치).
- 비고: dev는 이 편차를 flag하지 않음(맨 아래를 요건으로 기술). 어느 쪽이 최신 의도인지 team-lead 확인 필요.
- **재검(2026-08-07) — 해소**: team-lead가 (a)명세대로 수정 확정, dev가 최상단으로 이동. 픽셀 재측정: **S15 top=484 < 사용요건 681 < 판정표 1001** — S15가 사용 요건 박스·판정표보다 위(오프라인 콘텐츠 최상단)로 이동 확인. 이전 1714(맨 끝)에서 승격됨. 명세 03 S15 위치 요건 충족. **통과.**

#### D-S15-2. 요건2 '지도 검색 안내' 문장이 함께 제거됨 (명세는 "유지")
- 심각도: 보통
- 경계면: 명세(03 S15) ↔ 렌더
- 왼쪽(명세 03 S15): "요건 2의 `onnuri.gift/place 가맹점 지도 검색 ↗` 지도 검색 안내 **문장은 유지**한다 — '가맹 시 가능' 매장의 가맹 여부를 점포 단위로 확인하는 방법을 설명하는 요건 ② 본문이지 별도 검색 진입이 아니다. 검색 **진입**만 서브탭으로 일원화하고, 요건 설명 문장은 그대로 둔다."
- 오른쪽(관측): index.html에서 `가맹점 지도 검색 ↗` 안내 문장 0건 — 진입박스와 함께 통째로 제거됨. 즉 "'가맹 시 가능' 매장을 점포 단위로 확인하는 방법" 안내(조건부 판정표 행과 직결되는 실행 정보)가 사라졌다.
- 이용자 영향: 조건부(가맹 시 가능) 업종의 점포 단위 확인 방법 안내 손실. 요건2 본문의 "앱 '가맹점 찾기' 지도에서 확인"은 남아 일반 확인 지시는 유지되나, 구역·점포 단위 검색 안내는 빠짐.
- 담당: **guide-frontend-dev** + **guide-content-writer/team-lead 결정**.
- 비고: **dev가 이미 flag함** — "지도박스 전체 제거로 해석(링크만 빼면 문장 깨짐), '가맹 시 가능 개별 확인' 안내도 함께 빠졌다 — writer/main 확인 대기." 즉 인지된 편차이며 결정 대기 상태. writer가 문장만 남기는 마크업으로 복원하거나, main이 제거를 승인해야 한다.
- **재검(2026-08-07) — 해소 (최종 정정2본)**: team-lead 정정2("안내 문장 텍스트 복원 + 인라인 링크만 제거") 반영. 렌더 실측: 요건2 안내 문장 "'가맹 시 가능' 매장(음식점·편의점·마트·학원·생활서비스 등)의 가맹 여부는 **온누리 가맹점 지도에서** 점포 단위로 확인할 수 있습니다." **visible·자연스럽게 재서술**(링크 제거로 문장 안 깨짐), 문장 내 인라인 링크 **없음**. 페이지 내 onnuri.gift/place 링크는 **서브탭2 하나(1개)만**, merchants.html 링크 1개(서브탭1). 요건2 본문·구 M7 제거(0) 유지. **통과.**
  - 참고(문서 정합): dev가 명세 line167 "그대로 둔다"와 team-lead 정정2 "인라인 링크만 뺀다"의 미세 상충을 flag — 최신 지시(정정2) 따름. writer가 line167 문구를 정정2에 맞춰 정리 필요(문서 정합 이슈, 렌더 결함 아님).

### 미검증 (도구 제약)

- 390px 반응형·색각 세부·서브탭 링크 실도달: `resize_window` 1280 하한. 링크 href/target은 소스·DOM으로 확인, 목적지 도달(merchants.html·onnuri.gift/place)은 이미 이전 태스크에서 확인분과 동일.

### 종합

서브탭 링크(내부/외부 시맨틱·"앱" 제거·색각 표지)·구 M7/인라인 링크 제거·회귀는 견고하게 통과. 단 **명세 03 S15가 명문한 (1) 최상단 배치와 (2) 요건2 지도 검색 안내 문장 유지가 렌더에서 각각 맨 아래 배치·문장 제거로 어긋난다.** 둘 다 치명/높음은 아니나 명세와 정면 불일치이므로 **판정: 조건부 통과** — team-lead/writer가 (a) 두 편차를 명세대로 수정하도록 dev에 지시하거나 (b) 변경된 의도를 03 S15 명세에 반영해 정합을 맞춘 뒤 재검이 필요하다. 그 전까지 배포 보류 권고.

### 재검 종합 (2026-08-07, dev 정정 후)

team-lead (a)결정에 따른 dev 정정 2건 재검 완료 — 둘 다 해소:
- **D-S15-1 배치**: S15 top=484 < 사용요건 681 < 판정표 1001 → 오프라인 콘텐츠 최상단으로 이동(이전 1714=맨 끝). 명세 위치 요건 충족.
- **D-S15-2 안내 문장 (최종 정정2본)**: 요건2 안내 문장 텍스트 유지("…가맹 여부는 온누리 가맹점 지도에서 점포 단위로 확인할 수 있습니다.", visible·재서술 자연스러움), **인라인 onnuri.gift/place 링크만 제거**. 페이지 내 onnuri.gift/place 링크 1개(서브탭2)·merchants.html 1개(서브탭1). 구 M7 진입박스 제거(0) 유지. line167 문서 정합만 writer 정리 대기.
- 서브탭 시맨틱(1 merchants.html 내부 no-target / 2 onnuri.gift/place _blank rel=noopener + aria-label "새 창에서 열림"), 성격 표지(내부·수도권/외부·전국) 병기, 완전 마운트(머스태시 0)·L388 리터럴</0·SSM/경과조치/S10 무손상 재확인.
- 콘솔: 완전 마운트로 페이지 무오류 확인(로드 후 추적이라 메시지 미캡처, 유일 상시 노이즈는 확장 share-modal — 페이지 무관).

미검증(도구 제약): 390px·색각 대비·키보드 포커스 실동작. aria-label·텍스트 성격표지·style focus는 소스/DOM으로 확인, 실뷰포트 픽셀·대비 측정은 <1280 환경 필요.

**task #17 최종 판정: 통과.** 배포 보류 해제 가능 — team-lead 커밋·배포.

**워킹트리 정본 확인(2026-08-07)**: 서빙본(8655) == 디스크 index.html 동일(diff 무). 즉 위 렌더 검증이 곧 워킹트리(정본) 검증. 라이브 HEAD `09d9124`는 요건2 인라인 onnuri.gift/place 링크만 있는 잠정본이고, 워킹트리는 그 링크가 제거된 최신본(미커밋). team-lead 체크리스트 재확인: 요건2 안내 문장 텍스트 유지(1)·요건2 인라인 링크 제거(0)·페이지 onnuri.gift/place 링크 서브탭2 하나(1)·merchants.html 서브탭1 하나(1)·구 M7(0)·S15(1)·리터럴</script>(0)·SSM/경과조치/S10 무손상 — 전량 통과. **이 워킹트리를 커밋·재배포하면 된다.**

---

## M10 통합 뷰 검증 (task #20) — 2026-08-08

### 판정: 통과 (결함 0건, 낮음 관찰 2건 · 미검증 = 390px·색각·성능 도구 제약)

2뷰(시장/브랜드)를 1뷰로 통합하고 수도권 전체를 API 재수집(66,211곳)한 M10 통합 뷰를 검증. **JSON에서 직접 재집계**해 화면과 대조 — dev 보고치·명세 카운트 전량 일치. 콘솔 페이지 예외 0(확장 노이즈만).

### 독립 재계산 (disk, dev 보고치·명세 대조 — 전량 일치)

| 검사 | 디스크 집계 | 화면 | 결과 |
|------|-----------|------|------|
| 총 수도권 | 66,211 (서울29,450·인천7,238·경기29,523) | 시도 칩·countText | 일치 |
| cat 10종(서울) | 음식점9,669·편의점258·마트366·농축7,166·의류4,007·생활잡화603·생활서비스2,476·약국493·학원84·기타4,328 | 업종 칩 전부 | 일치 |
| brand(서울) | 편의점258·마트364·SSM0·다이소3 (합625) | 브랜드 칩 | 일치 |
| SSM(경기) | 7 전부 GS더프레시 | brand=ssm 필터 7행 | 일치 |
| 강남구→개포동 | 761→145 | 761곳/145곳 표시 | 일치 |
| 수원시(구4)/가평군 | 3,037(권선·영통·장안·팔달)/458(구없음) | 3,037/458 | 일치 |
| 계층 규모 | 서울25구·인천11구·경기31시·경기일반구20 | 드롭다운 옵션 | 일치 |
| 동 null | 4,730(7.1%) '동 미상' | 동 미상 옵션·행 표기 | 일치 |
| 경기 digital(card‖qr) | 29,413 | 디지털 필터 29,413 | 일치 |

### 통합·지연 로드 (task #20 핵심)

- **2뷰→1뷰**: 뷰 토글 없음, 단일 목록+필터. `merchants.html` 소스에 `brand_stores` 참조 **0** — 통합 뷰 코드가 brand_stores.json을 전혀 fetch 안 함.
- **지연 로드(network 실측)**: 초기 로드 `seoul.json` 1건만(brand_stores·타 시도 미요청). 경기 탭 클릭 → `gyeonggi.json` 1건만.

### 지역 계층 (M10-1)

- **서울 구→동**: 강남구 선택 → 761곳, 동 드롭다운 11옵션(동 전체 첫·동 미상 끝·가나다순·개포동 포함) → 개포동 145곳. 지역 라벨 "구·동".
- **경기 시→구→동**: 수원시 → 3,037곳, 지역 라벨 "시·구·동"으로 전환, 구 드롭다운 4옵션(권선·영통·장안·팔달, 가나다순).
- **경기 구 없는 시**: 가평군 → 458곳, 지역 라벨 "시·동"(구 단계 자동 생략, 빈 구 드롭다운 미노출).
- **동 미상**: `동 전체`와 별도 `동 미상` 옵션 존재, 결과 표 동 칸 null이면 "동 미상" 표기.
- 옵션은 데이터 유니크값·가나다순(하드코딩 없음).

### 3축 필터 + 디지털

- **업종 cat 칩**(주) 10종+전체: 지역 계층 적용 후 재집계(D7) — 개포동 전환 시 전체145·음식점65 등 정확 재계산.
- **브랜드 brand 칩**(보조): 전체+편의점/마트·슈퍼/기업형 슈퍼(SSM)/다이소, "브랜드" 구획 제목으로 축 구분. SSM 7행 전부 GS더프레시.
- **시장유형 select**(보조): 전체+7종(골목형상점가/기타/백년소상공인/상점가/전통시장/지하도상점가/활성화구역), 데이터 유니크(D15).
- **디지털(카드·QR) 결제 필터**: 기본 OFF, 토글 시 card‖qr — 경기 29,523→29,413 정확. (paper 전건 Y라 지류 토글 폐기 확인.)
- 하드코딩 숫자 0(리터럴 66,211·29,450·64,997·2026-08-08 전부 0 — meta/데이터 동적).

### 표·배너·각주

- 표 컬럼: 가맹점명(+SSM/다이소 태그)·업종(cat)·소속 시장·상점가(+market_type)·도로명 주소(+동/동미상)·결제(카드/QR 배지). 페이지네이션 50.
- **배너(M10-2)**: "이 목록은 온누리 가맹점찾기에서 2026-08-08에 수집한 자료입니다…" 단일 수집일. **삭제 확인**: "공공데이터 스냅샷"·"연 1회 갱신"·"점포 주소가 없습니다" 문구 전부 부재.
- **각주(M10-2b)**: "※ …구·시까지는 확정값이지만 동(洞)은 도로명 주소에서 뽑은 값이라 약 7%는 '동 미상'이며 일부 부정확할 수 있습니다…" — 동 한정 부정확 고지 정확(구·시는 확정으로 서술).
- **빈 결과(M10-4)**: "…에 해당하는 가맹점이 없습니다." + "수집본이라 이후 새로 가맹한 곳은…앱에서 확인" + 검색 초기화 버튼 + **M6 확인 링크 3종**(온누리 공식 가맹점 지도로 확인 ↗ · App Store ↗ · Google Play ↗, _blank).

### 관찰 (낮음 — 판정 차단 아님)

#### O-M10-1. 빈 결과 "필터 넓히기" 안내 — [검증관 false-negative, 철회]
- 결론: **결함 아님. 힌트는 조건부로 이미 구현됨(명세 M10-4 부합).**
- 최초 관측 오류: 내 grep이 `'넓히'`로 dev 문구 `'넓혀'`(활용형)를 못 잡았고, 내 빈결과 테스트가 **지역 필터 없는 검색어-only** 케이스라 조건부 힌트가 안 떴다.
- 재확인(소스, served==disk): merchants.html에 삼항 `? "지역 필터를 상위로 넓혀 보세요(동 → 구 → 시도). "` 존재 — **지역 계층 필터가 걸린 채 0건일 때** 렌더. 명세 M10-4 "계층 필터가 걸린 채 0건이면 상위 계층 넓히기 우선 안내(동→구→시도)"와 정확히 일치. 지역 필터 없이 검색어만 miss인 경우엔 넓힐 대상이 없어 의도적으로 생략(검색 초기화로 유도) — 타당.
- dev 실측(가평군 선택+무매칭 검색어 → 힌트 렌더) 및 소스 이중 확인. **철회.**

#### O-M10-2. 명세 M10-6의 card Y 수치 오기 (문서 정합)
- 심각도: 낮음(문서, 렌더 무관)
- 왼쪽(명세 M10-6 line 278): "card Y **64,997**/66,211".
- 오른쪽(disk 실측): card Y = **63,997** (1,000 차이). 화면은 이 수치를 렌더하지 않음(디지털 필터는 동적 card‖qr, 하드코딩 0) — 순수 명세 문서 오기.
- 담당: guide-content-writer(명세 수치 정정). 렌더 영향 없음.

### 미검증 (도구 제약)

- 390px 반응형·색각 대비·키보드 포커스 실동작·대용량(3만행) 성능 체감: `resize_window` 1280 하한. 색각은 업종/브랜드/SSM/카드·QR 모두 **텍스트 배지**로 구조 확인. 성능: 페이지네이션 50으로 DOM 부담 제한, 로컬 상호작용 지연 관측 없음(필터·계층 전환 즉시 반영).

### 종합

2뷰→1뷰 통합·시도 지연 로드(brand_stores 미fetch)·지역 계층(서울 구→동/경기 시→구→동/구없는 시 시→동/동 미상)·3축 필터(cat·brand·market_type)+디지털(card‖qr)·표 컬럼·단일 수집일 배너·M10-2b 동 부정확 고지·M10-4 빈 결과(M6 링크) 전부 명세대로 작동하고, 카운트는 66,211 전수 disk 재집계와 정확히 일치. 콘솔 0. **판정: 통과, 결함 0건.** 낮음 관찰 2건(빈결과 넓히기 안내·명세 card Y 오기)은 개선/문서 정합 여지일 뿐 배포 차단 아님. 미검증은 390px·색각·성능(도구 제약). 배포는 데이터 3파일+html 동시(team-lead 원칙).

**관찰 후속(2026-08-08, dev 정리 반영)**: O-M10-1은 **검증관 false-negative로 철회** — 넓히기 힌트는 지역 필터 걸린 채 0건일 때 조건부로 이미 구현됨(소스 삼항 "…상위로 넓혀 보세요(동→구→시도)" 확인, 명세 M10-4 부합). O-M10-2(card Y 64,997 vs 63,997)는 07 명세 문서 오기로 writer 정정 건(렌더 동적, 무관). **잔여 결함 0. 배포 가능.**

### M10 정식 배정(task #20) 확인 — 서빙본 동일성·index 회귀 (2026-08-08)

- **서빙본 동일성**: `curl 8655/merchants.html` == 디스크 merchants.html(diff 무). 위 M10 렌더 검증이 곧 현재 워킹트리(정본) 검증 — 드리프트 없음.
- **index.html 회귀**: git status에 index.html 미표시 = **이번 task 무변경**. S15 서브탭(가맹점을 직접 찾아보기 1·merchants.html href 1·공식 지도 검색 3)·요건2 안내 문장(점포 단위로 확인 1)·요건2 인라인 링크 제거(0, 정정2 상태)·리터럴</script> 0 유지. 라이브(bae3cda) 정합.
- **배포 배치(git status)**: `M seoul/incheon/gyeonggi.json`·`M merchants.html`·`D brand_stores.json`(§5 흡수 후 삭제) — 미커밋. 이 4개 변경을 한 커밋으로 배포하면 정본이 라이브가 된다. index.html은 변경 없음.

### 빈결과 힌트 4분기 개선 재검 (task #20 후속) — 2026-08-08

경위: dev가 확장 힌트를 되돌렸다가(2-상태) team-lead 채택 지시와 교차되어 재적용. 최종 워킹트리 = 4분기 반영본(git diff +7/-3 vs HEAD 756b07f, 변경은 emptyHTML hint 분기에 국한). 서버 8655 재기동(detached)·서빙본==디스크 확인.

emptyHTML 힌트 로직: `anyRegionFilter()`/`anyOtherFilter()` 조합 4분기. 브라우저 4케이스 실측(서빙본, 무매칭 검색어로 0건 강제):

| 케이스 | 필터 조합 | 화면 힌트 | 결과 |
|--------|-----------|-----------|------|
| 1 | 무필터 + 검색 miss | (힌트 없음) | 통과 |
| 2 | 지역만(강남구) | "지역 범위를 상위로 넓혀 보세요(동 → 구 → 시도)." | 통과 |
| 3 | 기타만(업종=학원) | "업종·브랜드·시장 유형 필터를 줄여 보세요." | 통과 |
| 4 | 둘다(강남구+학원) | "지역 범위를 넓히거나(동 → 구 → 시도) 업종·브랜드·시장 유형 필터를 줄여 보세요." | 통과 |

검색 초기화 버튼·M6 확인 링크 유지, 변경 격리(다른 M10 기능 무손상). **4분기 개선 검증 통과, 결함 0.** 명세 M10-4("계층 필터 0건 시 상위 넓히기 안내") 초과 충족(기타 필터까지 분기).

> 채택/폐기 결정 이력: team-lead의 "폐기" 메시지와 dev의 "재적용"이 교차했으나, 최종 워킹트리는 4분기 반영본이며 dev가 동결. 기술 검증은 통과 — 채택 시 이 diff(anyOtherFilter 1블록)+writer 문서정정(O-M10-2)을 커밋하면 된다. 최종 채택/폐기는 team-lead 판단.

---

## 브랜드 필터 UI 개편 검증 (task #22) — 2026-08-08

### 판정: 통과 (결함 0건, 미검증 = 390px·색각 대비·키보드 포커스·타입어헤드 도구 제약)

brand 필드를 카테고리 태그(convenience/mart/ssm/daiso)에서 **개별 체인명 문자열|null**로 재정의하고, 브랜드 칩을 base 내 상위 N + 검색 드롭다운으로 개편한 UI를 검증. **JSON 직접 재집계**로 화면 대조 — dev 보고치 전량 일치. 콘솔 페이지 예외 0(확장 노이즈만).

### 독립 재계산 (disk, 새 brand 스키마)

- brand 값 = 개별 체인명(GS25·CU·파리바게뜨·메가커피·정관장·GS더프레시…)|null. `brand=='ssm'` 잔존 **0**(SSM 태그 폐기).
- 서울 상위: GS25 126·CU 92·파리바게뜨 50·메가커피 34·세븐일레븐 32·땅스부대찌개 29·피자스쿨 28·컴포즈커피 27·빽다방 25·(크린토피아 23=배스킨라빈스 23 동률). 브랜드 종류 159.
- 경기 상위: GS25 178·CU 124·파리바게뜨 90·메가커피 58·정관장 58·…·GS더프레시 7. 브랜드 종류 167.
- dev 보고치 정확 일치(서울 GS25 126·CU 92·파리바게뜨 50 / 경기 GS25 178·정관장 58·GS더프레시 7).
- cat⊥brand: 서울 cat=음식점 ∩ brand=GS25 = **0**(GS25는 편의점).

### 브랜드 필터 UI (화면 실측)

| 검사 | 관측 | 결과 |
|------|------|------|
| 브랜드 칩 = base 상위 10 + 전체 | 전체 29,450 · GS25 126·CU 92·파리바게뜨 50·메가커피 34·세븐일레븐 32·땅스부대찌개 29·피자스쿨 28·컴포즈커피 27·빽다방 25·배스킨라빈스 23 | 통과(disk 상위10 일치) |
| 브랜드 검색 드롭다운 = 전 브랜드+건수 | 160옵션(브랜드 전체 + 159체인), "GS25 (126)"식 건수 병기, native 타입어헤드 | 통과 |
| 하드코딩·"N종" 없음 | 리터럴 "N종" 텍스트 0, 전부 items.brand 계산 | 통과 |
| 칩→드롭다운 동기화 | GS25 칩 클릭 → 드롭다운이 "GS25 (126)"로 동기화, countText 126 | 통과 |
| 드롭다운→필터(비-칩 브랜드) | 드롭다운 GS더프레시 선택 → 7곳 필터 | 통과 |
| cat⊥brand 직교 AND | cat=음식점·카페 + brand=GS25 → "0곳"(빈결과) | 통과 |
| brand=null 기본 = 전체 | 초기 전체 29,450 | 통과 |

### SSM (brand=ssm 폐기 → GS더프레시)

- 경기 GS더프레시 7건(드롭다운 선택) → 표 7행, **가맹점명 셀 전부 `SSM` 배지**(7/7). brand=ssm 태그는 데이터에서 0(폐기), SSM 배지는 GS더프레시(체인명) 기준으로 부여.
- **각주 ② SSM 유지**: "GS더프레시 등 기업형 슈퍼(SSM)…직영점은 가맹 대상이 아니라 이 목록에 없고 결제도 안 됨" present(03 S4/S6/S14 직영/가맹 축 승계).

### 회귀·무결

- 완전 마운트(머스태시 0), 콘솔 페이지 예외 0(확장 share-modal.js만).
- 색각: 업종·브랜드·SSM·결제(카드/QR) 전부 **텍스트 배지** — 색만 의존 아님.
- 지역 계층(구/동 select)·업종 cat 칩·market_type select·디지털 결제 필터·검색 — M10 검증분 유지(브랜드 UI는 additive 변경, 이번 관측에서 모두 렌더·동작 정상). 지역/업종/디지털 필터가 brand 필터와 독립 AND로 공존.

### 관찰 (낮음 — 판정 차단 아님)

- 상위 10 경계 동률: 서울 크린토피아 23 = 배스킨라빈스 23 동률에서 칩은 배스킨라빈스, 크린토피아는 드롭다운. 타이브레이크는 임의이나 결정적 — 결함 아님(둘 다 드롭다운에서 접근 가능).
- dev "마트·슈퍼 brand 미부여"는 대체로 맞으나 다이소(3) 등 소수는 cat=마트·슈퍼+brand 병존 — brand·cat이 직교 필드라 정상, 필터 UI 지장 없음.

### 미검증 (도구 제약)

- 390px 반응형·색각 대비·키보드 포커스·169옵션 드롭다운 타입어헤드 실동작: `resize_window` 1280 하한. 색각은 텍스트 배지로 구조 확인, 타입어헤드는 native select라 브라우저 기본 동작(구조상 지원).

### 종합

brand 개별 체인명 전환·상위N 칩+검색 드롭다운(전 브랜드·건수·타입어헤드)·칩↔드롭다운 동기화·cat⊥brand 직교 AND·SSM(GS더프레시 배지+각주)·brand=null 전체·회귀(지역/업종/market_type/디지털/검색) 전부 명세대로 작동하고, 카운트는 disk 독립 재집계와 정확 일치. 하드코딩 0, 콘솔 0. **판정: 통과, 결함 0건.** 배포는 데이터 3파일+html 동시(brand 갱신 데이터 미커밋).

### 브랜드 필터 정식 배정(task #22) 확인 — 서빙본 동일성·전체 수치 (2026-08-08)

- 서빙본(8655) == 디스크 merchants.html 동일 → 위 UI 검증이 곧 워킹트리(정본) 검증.
- team-lead 전체 수치 disk 대조 일치: brand 부여 **3,662**(서울1,216·인천303·경기2,143) / null **62,549** / 총 66,211 / 브랜드 종류(전 시도 합집합) **169**. 옛 enum(convenience/mart/ssm/daiso) 소멸(잔존 0).
- **판정: 통과, 결함 0.** 상위N 칩·롱테일 드롭다운·타입어헤드·직교(cat⊥brand)·SSM(GS더프레시 배지+각주)·brand=null 전체·회귀(지역/업종/시장유형/디지털/빈결과 4분기) 전부 확인.

---

## UX 대개편 1단계 (merchants.html) 검증 (task #23) — 2026-08-08

### 판정: 통과 (결함 0건, 낮음 관찰 1건 = 라이브 리사이즈 엣지)

명세 14(디자인 토큰·사이드바 셸·모바일 드로어) 적용분을 검증. **Playwright MCP로 실제 뷰포트를 제어**해, 전 세션 내내 `resize_window` 1280 하한으로 미검증이던 **390px 반응형·모바일 드로어를 실측 완료**. 기능 무손상. 콘솔 페이지 에러 0(Playwright 클린 브라우저 — 부수 확인: 기존 "message channel closed"는 확장 노이즈였음이 확정).

### 디자인 토큰 (computed style 실측)

- `--accent` = **#F26B1D** ✓. 칩 `border-radius` = **6px**(샤프, pill/999px 잔재 0) ✓. 옛 따뜻한 배경(#FBD8BC·#F4F4F3) 소스 0 ✓ → 중립 모노톤 그레이스케일+오렌지.

### 사이드바 셸 — 데스크톱(1280) / 모바일(390) 실측

| 항목 | 데스크톱 1280 | 모바일 390 |
|------|--------------|-----------|
| 사이드바 | 248px, position fixed, 좌측 0 visible | 드로어(fresh load translateX(-248px), off-screen) |
| main margin-left | **248px** | **0px** |
| 햄버거 | 숨김(offsetParent null) | 노출, aria-label "메뉴 열기" |
| body 가로 오버플로 | 0 | **0** (반응형 리플로우 정상) |

- **모바일 드로어 전체 사이클(실측)**: fresh load 닫힘(translateX(-248), aria-expanded="false", overlay 없음) → 햄버거 클릭 열림(사이드바 left 0 on-screen, aria-expanded="true", overlay `<div class="overlay">` fixed·rgba(20,22,26,0.38) scrim·z-45) → **ESC 닫힘**(translateX(-248), aria-expanded="false", overlay 사라짐).
- `aria-current="page"` on 지역별 찾기 ✓. `focus-visible`·`prefers-reduced-motion` 소스 존재 ✓.

### signature (활성 좌측 레일)

- 활성 항목 `::before` = background **rgb(242,107,29)=#F26B1D**, width **3px** ✓ (오렌지 3px 좌측 레일).

### 기능 무손상 (앱 JS 불변)

- 업종 칩 약국 **493** ✓, countText "29,450곳 중 29,450곳" ✓, 완전 마운트(머스태시 0) ✓.
- 시도 지연로드·구/동 계층·업종/브랜드/시장유형/디지털 필터·검색·페이지네이션·빈결과 4분기·표(주소·동·결제·SSM 배지)는 M10/브랜드 검증분과 동일 코드(디자인 레이어만 변경, additive) — 렌더·동작 유지.

### 관찰 (낮음 — 판정 차단 아님)

- **데스크톱→모바일 라이브 리사이즈 시 드로어 미리셋**: 1280에서 렌더 후 390으로 live-resize하면 사이드바가 열린 채(transform translateX(0)) 남고 aria-expanded="true". **fresh load(390 직접 진입)에서는 정상 닫힘**(translateX(-248)) 확인 — 실사용 경로(기기 폭으로 신규 로드)는 정상이며, 라이브 리사이즈는 이용자 경로가 아니라 엣지. dev 재량(원하면 resize 핸들러에 상태 리셋). 차단 아님.

### 미검증 (도구/측정 제약)

- WCAG 대비 수치(색각 대비)·키보드 Tab 순회 실동작: `focus-visible` 오렌지 outline·모노톤+오렌지+텍스트 배지 구조는 소스/computed로 확인(색만 의존 아님). 정밀 대비비 측정은 별도 QA 도구 몫 — 구조상 고대비 모노톤이라 결함 징후 없음.

### 종합

디자인 토큰(모노톤+오렌지·샤프 6px·pill 폐기·따뜻한 팔레트 제거)·사이드바 셸(PC 248 fixed+margin / 모바일 드로어 전 사이클 open·ESC·overlay)·활성 오렌지 3px 레일·aria-current·기능 무손상·콘솔 0·**390px 가로 오버플로 0** 전부 실측 통과. **Playwright로 전 세션 미검증이던 반응형/드로어를 실뷰포트 검증**한 것이 이번 검증의 핵심. **판정: 통과, 결함 0건.** 낮음 관찰 1건(라이브 리사이즈 엣지)은 실사용 경로 아님. 배포: merchants.html 단독(데이터 무관), team-lead 확정 후.

### task #23 잔여 항목 해소 — 키보드 포커스·색각 대비 (Playwright 실측, 2026-08-08)

앞 절 "미검증(측정 제약)"의 두 항목을 Playwright로 마저 검증 — 이제 캐비엇 없음:
- **키보드 focus-visible**: Tab → 첫 포커스 링크에 `outline: 2px solid rgb(242,107,29)`(=#F26B1D) — 오렌지 2px focus-visible 요건 충족.
- **WCAG 대비**: 본문/활성 nav 텍스트(다크 #17181A) vs 배경 = **17.77**(AAA 7.0 대비 여유, 모노톤 고대비). 활성 nav는 다크 텍스트+오렌지 3px 레일로 액티브를 색만이 아닌 형태로 표시. (표 셀 자동계산 1.18은 투명 배경을 검정으로 오파싱한 **측정 아티팩트** — 셀 실제는 다크 온 라이트로 가독, 스크린샷·17.77 본문 대비로 확인.)

**task #23 최종: 통과, 결함 0건, 잔여 미검증 없음.** (라이브 리사이즈 엣지 낮음 관찰 1건은 실사용 경로 아님·dev 재량.)

---

## UX 대개편 2단계 (index.html 번들) 검증 (task #24) — 2026-08-08

### 판정: 통과 (결함 0건, 낮음 관찰 1건 = 앵커 id 부재·JS로 동작)

dev 세션 한도로 team-lead가 build_index.py 실행해 index.html(자기해제 DC 번들) 재생성. 사이드바+모노톤 확산을 검증 — **번들 D-F1 최우선**으로 확인, Playwright로 데스크톱/모바일 실뷰포트 실측. 결과 통과.

### 번들 D-F1·무결 (최우선)

- **L388 template 리터럴 `</script>` = 0**(캡처 103,803자, 조기종료 없음) — D-F1 안전. ✓
- 완전 마운트: 미해석 머스태시 **0**(전 바인딩 해석). 콘솔 페이지 에러 **0**(Playwright 클린 브라우저).
- 회귀 콘텐츠 무손상: 오프라인/온라인 탭·판정표(가능 여부)·S15 서브탭(가맹점을 직접 찾아보기)·GS더프레시(SSM 정정)·경과조치·S10 모바일(앱)형·온라인 전용관 전부 present.

### 모노톤 디자인 토큰

- `--accent` **#F26B1D**, 999px(pill) 잔재 **0**(샤프 6px), 옛 웜톤 #FBD8BC·#FBEADB **0**. body bg **rgb(255,255,255)**(모노톤 화이트; #F4F4F3 3건은 로더 스플래시 bg=불변 영역·중립 그레이).
- 활성 항목 **오프라인 사용처** `::before` = rgb(242,107,29)=#F26B1D, width **3px**(오렌지 좌측 레일), `aria-current="page"`.

### 사이드바 셸 — 데스크톱/모바일 실측 (Playwright)

| 항목 | 데스크톱 1280 | 모바일 390 |
|------|--------------|-----------|
| 사이드바 | 248px fixed, 좌측 0 visible | 드로어 fresh-load translateX(-248) off-screen |
| main margin-left | 248px | 0px |
| 햄버거 | 숨김 | 노출(aria-label "메뉴 열기") |
| 가로 오버플로 | 0 | 0 |

- **모바일 드로어 전 사이클**: fresh 닫힘(translateX -248·aria-expanded false·overlay 없음) → 햄버거 열림(사이드바 left 0·aria-expanded true·overlay `rgba(20,22,26,0.38)`) → **ESC 닫힘**(off-screen·false·overlay 없음).
- **resize 리셋(양방향, team-lead 확인 요청)**: 드로어 연 채 1280 복귀 → 사이드바 정상·main margin 248·**overlay 잔류 없음**·햄버거 숨김. 1280→390 → 드로어 **닫힘**(off-screen·aria-expanded false·overlay 없음). **양방향 리셋 반영됨** — merchants의 라이브 리사이즈 낮음 관찰이 index엔 없음.

### 가이드 앵커 이동

- 사이드바 링크: 오프라인 사용처(#offline)·온라인 가맹 플랫폼(#online)·결제 방법(#payment)·용어·유의사항(#terms)·지역별 찾기(merchants.html#sidoTabs)·업종·브랜드별(merchants.html#catChips)·공식 가맹점 지도(onnuri.gift/place).
- "온라인 가맹 플랫폼" 클릭 실측 → **JS 탭 전환 동작**(activeNav "온라인 가맹 플랫폼"으로 이동·온라인 플랫폼 표 노출·scrollY 381). 크로스페이지 링크(merchants.html#…)·외부 지도 링크 정상.

### 두 페이지 일관성

- index·merchants 공통: 사이드바 248px fixed, overlay 스크림 rgba(20,22,26,0.38), --accent #F26B1D, 활성 오렌지 3px 레일, 샤프 6px. 셸 일관성 확인.

### 관찰 (낮음 — 판정 차단 아님)

- **#offline·#online 앵커 타깃 id 부재**: 두 사이드바 항목의 href(#offline/#online)에 매칭 id가 없다(#payment·#terms는 존재). JS 클릭 핸들러가 탭 전환+스크롤+aria-current로 처리해 **기능은 정상**이나, 네이티브 앵커 폴백·해시로 리로드 시 스크롤은 없음. JS 앱(번들)이라 실사용 무해. dev/team-lead 재량(원하면 두 섹션에 id 부여). 차단 아님.

### 미검증 (측정 제약)

- WCAG 대비 수치: 모노톤 화이트 bg + 다크 텍스트 + 오렌지 액센트 구조는 merchants(본문 17.77 AAA)와 동일 토큰 계열 — 고대비. 정밀 대비비는 별도 QA.

### 종합

index.html 번들에 사이드바 셸·모노톤 토큰이 D-F1 안전(리터럴</0·완전마운트)하게 확산됐고, 데스크톱 248 fixed·모바일 드로어 전 사이클·양방향 resize 리셋·활성 오프라인 사용처 오렌지 레일·가이드 앵커(JS 탭 전환)·회귀 콘텐츠(탭/판정표/S15/SSM/경과조치/S10) 무손상·콘솔0·두 페이지 셸 일관성 전부 실측 통과. **판정: 통과, 결함 0건.** 낮음 관찰 1건(#offline/#online 앵커 id 부재, JS로 동작)은 차단 아님. index+merchants 두 페이지 함께 배포 가능.

---

## 네이버 지도 통합 (merchants.html) 검증 (task #25) — 2026-08-08

### 판정: 조건부 통과 (지도 기능 전부 통과 · 모바일 반응형 결함 1건 = 가로 오버플로, 수정 필요)

team-lead 직접 구현(dev 부재). 지도 렌더·마커·클러스터·상한·연동·시크릿 안전은 Playwright 실렌더로 전부 통과. 단 **모바일 390에서 결과 표가 컨테이너에 갇히지 않아 가로 오버플로 512px** — team-lead 자체 기준 "가로 오버플로 0"에 위배되어 수정 후 배포 권고.

### 통과 항목 (Playwright 실렌더)

- **지도 렌더·auth**: `window.naver.maps` 로드, 지도 타일 16개 렌더, `__naverAuthFail=false`, 콘솔 페이지 에러 0. Client ID x79smqla3u 유효.
- **로컬 lib·시크릿**: lib/MarkerClustering.js 로컬 로드(HTTP200, 외부 CDN 0), `MarkerClustering` 전역 로드됨. 소스 시크릿 패턴 0(공개 Client ID만, 도메인 제한).
- **상한 3000**: 서울 전체(29,450>3000) → mapNote "지도에는 3,000곳까지 표시됩니다. 현재 29,450곳 — …필터를 좁히면", 마커 생략(pins 0). ✓
- **마커**: 개포동(145<3000) → pin 145개(필터 수 일치), 핀 색 rgb(242,107,29)=#F26B1D(오렌지). mapNote "지도에 145곳 표시 · 마커를 누르면 상세…".
- **클러스터**: 강남구(761) → 클러스터 6개(개별 핀 0=클러스터화), 클러스터 색 rgb(196,81,15)=#C4510F(오렌지 톤), 카운트 표기(예 293). 줌 인(개포동)=개별 핀 / 줌 아웃(강남구)=클러스터 — 정상.
- **리스트→지도 연동**: 리스트 행 "강남축산" 클릭 → 인포윈도우 열림(iw-name "강남축산"). 마커 클릭 인포윈도우는 동일 openInfo 경로(상호·업종·SSM·주소·시장·결제).
- **필터 반영**: 시도/구·동 변경 시 리스트 countText·mapNote·마커 동기 재계산.
- **데스크톱 1280**: 좌 리스트/우 지도 레이아웃, 가로 오버플로 **0**.
- **회귀**: 사이드바 드로어·구/동 계층·업종/브랜드/디지털 필터·페이지네이션·모노톤 무손상(리스트 필터가 지도와 함께 갱신됨 확인).

### 결함

#### D-MAP-1. 모바일 390 가로 오버플로 512px (결과 표 미클램프)
- 심각도: **보통** (모바일 레이아웃 깨짐 — 데이터는 맞으나 페이지가 가로로 밀림)
- 경계면: 렌더(반응형)
- 관측(fresh load 390, Playwright): `document.documentElement.scrollWidth - clientWidth = 512`. 넓은 요소: `.result-list`(882px)·`.card`(882)·`#resultArea`(880)·`.table-scroll`(880)·`table`(880) — 뷰포트 390 초과.
- 근본: 좌 리스트/우 지도 **2컬럼 flex 레이아웃**에서 모바일 세로 스택 시, `.table-scroll`(overflow-x:auto)이 뷰포트 폭으로 clamp되지 않고 880px 표 폭으로 확장. `#resultArea` overflow-x:visible로 880px가 상위(.result-list flex 1 1 auto)로 전파돼 body를 밀어냄. (min-width:0은 있으나 유효 clamp 안 됨.)
- 대조: **task #23 merchants 390은 가로 오버플로 0**이었음 — 이번 지도 2컬럼 레이아웃 추가로 생긴 **회귀**. 데스크톱 1280은 0(모바일 한정).
- 이용자 영향: 모바일(주 타깃)에서 페이지가 가로로 512px 밀려 표·레이아웃이 어긋남. team-lead 자체 기준 "가로 오버플로 0" 실패.
- 담당: **team-lead**(구현자, dev 부재).
- 수정 방향: 모바일 세로 스택 시 리스트 패널을 뷰포트 폭에 clamp해 880px 표가 `.table-scroll` **안에서** 가로 스크롤되게. 예: 컬럼 flex 아이템(`.result-list`/`#resultArea`)에 `min-width:0` 유효화 + `.table-scroll { width:100%; overflow-x:auto }`가 실제 스크롤 경계가 되도록(`#resultArea`의 overflow-x:visible가 880을 상위로 넘기지 않게). 또는 모바일에서 리스트 컨테이너 `max-width:100vw`+`overflow-x:hidden`.

### 미검증 (측정 제약)

- WCAG 대비 수치(모노톤 토큰은 이전 검증 계열 고대비). 지도 타일 자체는 외부 렌더.
- 마커 클릭(직접 클릭) 인포윈도우: 리스트 클릭 경로로 인포윈도우 렌더 확인, 마커 클릭은 동일 openInfo 코드경로라 커버로 간주(직접 마커 클릭 좌표 클릭은 미실행).

### 종합

네이버 지도 통합의 렌더·auth·로컬 lib·시크릿 안전·마커(오렌지)·클러스터(오렌지·카운트)·상한 3000 안내·리스트↔지도 연동·필터 동기·데스크톱 레이아웃은 전부 실렌더 통과. 그러나 **모바일 390 가로 오버플로 512px(결과 표 미클램프, 2컬럼 레이아웃 회귀)** 1건이 team-lead 자체 기준을 위배한다. **판정: 조건부 통과** — 이 모바일 오버플로만 수정하면 배포 가능. team-lead가 리스트 패널 폭 clamp(위 수정 방향)로 고친 뒤 해당 케이스(390 가로 오버플로 0)만 재검 요청하면 즉시 확인하겠다. 데스크톱·지도 기능은 무손상이라 회귀 재검은 그 지점만으로 충분.

#### D-MAP-1 수정 재검 — **통과** (2026-08-08, Playwright 390px 실측)

team-lead가 merchants.html에 아래 CSS를 추가(@media max-width:900px 내부):
```css
.result-list, .result-list .card, #resultArea{ min-width:0; max-width:100%; }
#resultArea{ overflow-x:hidden; }
.table-scroll{ overflow-x:auto; -webkit-overflow-scrolling:touch; }
```
→ 내가 제안한 "리스트 패널을 뷰포트에 clamp, 표는 .table-scroll 안에서 스크롤" 방향과 일치.

실측(390×844 리사이즈 후 신규 로드, 서울 전체 50행 표시):
- `document.documentElement.scrollWidth - clientWidth = 0` (scrollWidth 390 == clientWidth 390) → **가로 오버플로 0** ✓
- `.table-scroll`: width 348(뷰포트 clamp) / scrollW 880 / **canScrollX=true** → 880px 표가 컨테이너 안에서 정상 가로 스크롤 ✓
- `.result-list`(350) `.card`(350) `#resultArea`(348) 모두 뷰포트 내 clamp, 자체 오버플로 없음 ✓
- 회귀: 지도 렌더(naver.maps 로드, 350×420), 상한 안내 "지도에는 3,000곳까지 표시됩니다. 현재 29,450곳 — …"(29,450 > MAP_MAX 3000이라 마커 미표시=설계대로), 필터 select 5개, 콘솔 error 0 — **무손상** ✓
- 스크린샷: `.playwright-mcp/merchants-390-hoverflow-fixed.png`

**경미 관찰(비차단, 오버플로 수정과 무관·기존 잠재)**: `#map` base 규칙 `min-height:420px`(168행)이 `@media(max-width:900px){#map{height:300px}}`(194행)를 이겨 모바일 지도 높이가 300px 의도와 달리 420px로 렌더된다. 기능·오버플로에 무해한 순수 시각 사항이라 이번 배포 차단 사유 아님. 추후 모바일 지도를 300px로 줄이려면 media 규칙에 `min-height:300px`(또는 해당 min-height 재정의)을 함께 명시할 것.

### 최종 판정 (task #25): **통과** — D-MAP-1 해소 확인, merchants.html + lib/MarkerClustering.js 배포 가능.
