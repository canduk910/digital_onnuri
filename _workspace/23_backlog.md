# 23. 남은 과제 목록 (2026-09-05 2차 갱신)

**이 파일이 남은 과제의 정본이다.** 종전에는 목록이 대화 안에만 있어 세션이 끊기면 사라졌다.

만드는 방법: 저장소를 여섯 갈래(`_workspace` 문서 · ADR · 코드 주석 · 변경 이력 · 운영 문서 ·
데이터 신선도)로 훑어 후보를 모으고, 각 후보가 **현재 트리에서 정말 열려 있는지**를 파일·커밋으로
확인했다. 후보 121건 중 중복·설계상 그런 것·이미 닫힌 것을 걷어내고 남긴 것이 아래다.

각 항목의 `확인` 칸은 **내가 직접 재본 것인지**를 밝힌다. 이 저장소의 원칙대로,
확인하지 않은 것을 확인한 것처럼 적지 않는다.

---

## 완료된 것 (2026-09-05 밤)

승인받은 구조 3건과 그 과정에서 나온 것들은 전부 닫혔다. 근거는 `22_structural_run.md`.

- 배포 게이트(1·2번 과제) · merchants 분리 3-a~3-e(3번 과제, 1,934 → 1,552줄)
- 배치 점검에서 나온 판정 결함 4건 — 공공몰 없음-문구 · 이지웰 점검 페이지 오판 ·
  robots 를 HTML 로 파싱 · 배치 스캐너의 같은 함정 (원칙은 **ADR-22**)
- 컬럼 폭 손잡이가 절반만 잡히던 기존 결함
- 사용자 제보 — 실시간 조회 펜딩 화면에서 몰 6곳(롯데ON 포함)이 빠지던 것

**판정 조임(`ea38f5b`)은 유지로 확정됐다.** 되돌릴 조건도 두지 않는다 —
그 8곳의 문구가 바뀌면 할 일은 조임을 푸는 것이 아니라 **문구를 고치는 것**이다.

---

## 처리 현황 (2026-09-05 갱신)

| 묶음 | 상태 |
|---|---|
| **A** 지금 고치면 되는 것 7건 | **전부 완료** — `06a574c`(bootstrap 브랜치) · `d690c6a`(run.sh 정본·회귀값·env) · `7b1babd`(README·CI 라벨) · `6413da8`(죽은 regionApps) |
| **B** 데이터 3건 | **전부 닫힘** — B1·B2 완료 — 전수 재채록 반영(브랜드 +61·카테고리 +27, `cfc2b5f`) · 델타 노이즈 분리(C4 와 함께). **B3** 는 2026-09-06 사용자 결정으로 `partial` 유지(F-5 참조) |
| **C** 사람이 결정할 것 4건 | **전부 결정·반영** — C1 실측 보고서(`67552fc`) · C2 지도 범위가 지역 대체(`cdc2c76`) · C3 게이트 4종(`2d3d3a6`·`2b0294c`) · C4 채록 리듬(`2b0294c`) |
| **D** 알고 있는 한계 6건 | 그대로 — 닫지 않고 명시만 한다 |
| **E** 문서↔코드 어긋남 4건 | **전부 완료** — E1 은 C2 로 코드를 ADR 쪽으로 되돌려 해소, E2·E3·E4 는 `9866379` |

### 새로 열린 것 — **F 절로 옮겼다**

A·B·C·E 를 닫은 뒤, **그 작업들이 정말 끝났는지**를 네 갈래(오늘 커밋의 이웃 · 새로 만든 도구의
경계 · 같은 사실을 적은 다른 사본 · 오늘 만든 테스트의 사정권)로 다시 훑었다.
후보에서 확인까지 남은 것이 **17건**이고 전부 **F 절**에 있다.

그중 넷은 위험해서 훑는 중에 바로 고쳤다(아래 표). 나머지 13건 + 이전 목록에서 넘어온 4건이 열려 있다.

| 훑기가 잡아 즉시 고친 것 | 실제 | 커밋 |
|---|---|---|
| 판정 조임 대상을 **6곳**이라 적음 | 코드로 세니 **8곳**. 그 숫자가 네 문서에 복제돼 있었다 | `973059b` |
| 주석 5곳이 **없어진 안전망을 있다고** 말함 | 조임이 정확히 그 폴백을 없앴는데 `ProbeTargets` 를 안 열었다 | `973059b` |
| **C2 가 만든 회귀** — 착지가 지역을 버림 | 지도 범위 모드에서 챗 착지 시 결과 50행이 전부 다른 시도(재현) | `2c71a9a` |
| 챗봇 코퍼스가 **낡은 데이터에 오늘 날짜** | 품목 청크 44건 중 35건이 2주 전 관찰인데 오늘 스탬프 | `07cc788` |

---

## A. 지금 고치면 되는 것

전부 직접 재서 확인했다. 하나같이 작지만 **A1·A2 는 사고 때 드러나는 종류**라 위에 뒀다.

### A1. `DEPLOY.md` 의 `run.sh` 정본이 실제 서버와 다르다 — 그대로 복사하면 배치가 죽는다
`확인: 실측(서버 run.sh 30줄 대조)` · `크기: 작다`

문서의 "정본" 블록에 **없는 것**이 다섯이다. 서버에는 2026-08-23 · 09-01 에 하나씩 들어갔는데
문서는 8월 22일 상태에 멈춰 있다.

| 없는 줄 | 없으면 |
|---|---|
| `export PATH=/opt/node20/bin:$PATH` | 시스템 node v12 를 집어 **단계 D 스킵** |
| `export APP_BASE_URL` · `APP_ADMIN_KEY` | **단계 E(카나리아) 스킵** |
| `export OPENAI_API_KEY` (문서는 대신 `--skip-rag`) | **단계 C 스킵** |
| `export SURVEY_OUT_DIR` | 리포트가 기본 위치로 흩어진다 |
| `git checkout -- data/merchants …` | 산출물이 남은 순간 `pull` 실패 → `set -e` 로 **배치 전체 사망** |

마지막 줄은 2026-09-01 에 실제로 겪은 사고다(변경 이력에 기록됨). 문서만 보고 서버를 다시
세우면 **여섯 단계 중 셋이 조용히 안 돌고, 언젠가 전부 죽는다.**
고치는 법: 서버의 실제 `run.sh` 를 비밀값만 가려 문서에 그대로 옮긴다.

### A2. `bootstrap.sh` 가 폐기된 브랜치를 클론한다 — 새 서버 구축이 즉사
`확인: 실측(bootstrap.sh:5,10,32)` · `크기: 작다`

```
BRANCH="${BRANCH:-feat/backend-scaffold}"
git clone -b "$BRANCH" "$REPO_URL" "$WORKDIR"
```

`feat/backend-scaffold` 는 **2026-08-12 단일 브랜치 통합 때 폐기**됐다(ADR 및 변경 이력).
5행의 안내 `curl` 주소도 같은 브랜치를 가리킨다. 재해 복구·서버 이전 때만 쓰는 파일이라
평소에는 아무도 실행하지 않아 **정확히 필요한 순간에 실패한다.** `main` 으로 바꾸면 끝난다.

### A3. 배포 직후 회귀 기준값이 낡았다
`확인: 라이브 실측` · `크기: 작다`

| DEPLOY.md | 실제(2026-09-05) |
|---|---|
| 적재 `66,211` 건 | **79,800** |
| 개포동 `145` | 145 ✓ |
| 팔달구 `1241` | **1259** |

배포 직후 대조가 목적인 값인데 두 개가 틀리면, 다음 사람은 **정상인데 실패로 읽거나
아예 안 보게 된다.** 가맹점은 매일 배치가 갱신하므로 "그날의 값"을 적는 방식으로 바꾸거나,
변동이 적은 개포동 같은 값만 남기는 편이 낫다.

### A4. `.env.example` 에 관리자 키 항목이 없다
`확인: 실측(grep 0건)` · `크기: 작다`

`APP_ADMIN_KEY`(배치 단계 E)·`APP_ADMIN_PASSWORD`(제보 관리자 페이지)가 예시에 없다.
값은 당연히 비워 두되 **키 이름은 있어야** 새 서버에서 빠뜨리지 않는다.

### A5. README 곳 수가 낡았다
`확인: 실측` · `크기: 작다`

온라인 `30곳` → **31곳**, 가맹점 `78,000여 곳` → **79,800**.

### A6. 죽은 `const regionApps` 정의
`확인: 실측(build_index.py 3곳 · index.html 1곳)` · `크기: 작다`

2026-08-19 에 "미해결(판단 필요)"로 남긴 둘 중 하나다(다른 하나인 디자인 토큰 이원화는
그날 닫혔다). 쓰이지 않는 정의라 지우면 된다.

### A7. CI 스텝 이름의 건수가 낡았다
`확인: 실측` · `크기: 작다`

`robots 감시 (71건)` → 실제 75, 프론트 정적 `91건` → 실제 158.
아이러니하게도 `pages.yml` 스스로가 *"건수를 잡·스텝 이름에 적지 않는다 — tools-ci 가
'66건'이라 적어 두고 실제는 91건이 됐다"* 고 적어 둔 자리에서 또 났다.
**갱신이 아니라 제거가 맞다.**

---

## B. 데이터가 낡고 있다

가맹점은 배치가 매일 갱신해 낡지 않는다. **손으로 채록하는 온라인 카탈로그**가 낡는 쪽이다.

### B1. 온라인 취급품목 채록이 13~26일 낡았다
`확인: 실측` · `크기: 보통`

| 채록일 | 곳 수 |
|---|---|
| 2026-08-21 | 19 |
| 2026-08-23 | 2 |
| 2026-08-10 | 1 (시장을 방으로 — `partial`) |

화면은 `취급품목 실측 2026-08-23 기준 · 19곳은 2026-08-21 · 1곳은 2026-08-10 확인분` 으로
**정직하게 밝히고 있다.** 거짓말은 아니지만, 재채록 시점이다.

### B2. 단계 D 델타가 14회차 쌓였는데 아무도 반영하지 않았다
`확인: 서버 리포트 14개 집계` · `크기: 보통`

2026-08-23 ~ 09-05, 22곳 전부 한 바퀴 이상 돌았고 **누적 새 브랜드 209 · 새 카테고리 123**
후보가 서버에만 쌓여 있다. 저장소 카탈로그는 2026-08-27 이후 무변경이다.

```
온누리 찬스   118 브랜드      온누리핫딜  20      우체국쇼핑 13 + 13카테고리
온누리5일장    11             인더마켓     9      현대홈쇼핑  7 + 9카테고리
```

**탐지만 하는 것은 ADR-16 의 의도된 설계다**(자동 반영은 사이소 딥링크 오염 사고로 기각).
문제는 그 다음이 없다는 것 — **읽고 반영하는 절차가 없어** 리포트가 쌓이기만 한다.

게다가 노이즈가 크다. 09-05 우체국쇼핑 델타의 "새 브랜드" 13개는
`축소/확대 버튼` · `최근 본 상품` · `TOP` · `Previous` · `↓` 처럼 **UI 텍스트**다.
리포트에 그것을 구분하는 표시가 없어(`suspect` 가 델타에 실리지 않는다) 사람이 통째로
무시하게 된다 — 그러면 감시 장치가 이름만 남는다.

### B3. 시장을 방으로(oligopalgo) — **2026-09-06 결정: 그대로 둔다 (닫음)**
`확인: 실측` · `크기: 크다`

배달 주소 선택이 선행돼야 카탈로그가 열리는 구조라 비로그인 채록이 안 된다.
실시간 조회에서도 비대상(`EX_SCOPE_FIRST`)이고 색인 층에도 없다 — **세 층 모두 비어 있다.**
여는 길은 C 항목의 헤드리스 사이드카와 사실상 같은 과제다.

---

## C. 사람이 결정할 것

내가 정할 수 없는 것들이다. 재료는 다 모아 뒀다.

### C1. 허브(`state`·`SNAP`·`refresh`)를 해체할 것인가
merchants 는 1,552줄에서 멈췄다. 남은 후보의 결합도는 `22_structural_run.md` 에 실측표로 있다.
허브를 정말 풀려면 **상태 저장소 + 구독**으로 바꾸는 설계 변경이 되고, 그건 페이지 동작을
바꾸는 일이라 사람의 결정이 필요하다. 22 문서에 "ADR 감"이라 적어 놓고 ADR 은 만들지 않았다.

### C2. 지도 범위가 시도를 대체하게 할 것인가
2026-09-04 에 "아침 판단으로 남긴다(아래 제안 참조)"고 적었는데 **가리킨 「제안」 절이
문서에 없다.** 현재 동작은 `region AND bounds` 이고, 화면이 `지도 범위 · 서울 N곳` 으로
기준을 밝혀 거짓은 아니다. 다만 "보이는 것을 검색한다"는 멘탈 모델과는 어긋나 있다.

### C3. 배포 게이트를 어디까지 넓힐 것인가
2026-09-05 에 Pages 에 게이트를 붙였지만 **거기 걸린 것은 정적 계약 158건 하나**다.

| 레인 | 게이트 |
|---|---|
| Pages(프론트) | 정적 계약 158건 ✓ |
| backend-ci(앱 배포) | `gradlew test` 만 — 정적·D-F1 은 안 지난다 |
| tools-ci(배치·채록) | **게이트 아님**(사후 알림, 문서에 명시) |
| 서버 야간 배치 | 게이트 없음 — 00:30 에 스스로 `git pull` |

렌더 113 · 스모크 · 실동작 45경로 · 스타일 대조는 **어느 워크플로에도 없다**(브라우저가 필요해서).
지도 절은 원격에서 어차피 스킵되므로 *지도 제외 렌더 + 스모크만* 넣는 절충이 가능하다.
배치 레인은 저장소 밖 `run.sh` 를 고쳐야 해 성격이 다르다.

### C4. 채록 델타를 읽는 리듬을 만들 것인가
B2 와 짝이다. 주 1회 사람이 리포트를 보는 절차를 두거나, 노이즈를 줄여
(브랜드 후보에 `suspect` 표시를 델타에 싣는 것부터) 읽을 만하게 만들거나.

---

## D. 알고 있는 한계 — 닫지 않고 명시만 한다

고칠 것이 아니라 **잊지 않기 위해** 적는다. 전부 코드 주석이나 ADR 에 이미 적혀 있다.

| 한계 | 어디 |
|---|---|
| 같은 호스트 안의 점검 페이지는 출처 가드로 안 잡힌다 | ADR-22 · `ProbeFetcher.java:206` |
| 온누리마켓은 토큰 0 이 유일한 확정 수단이라 위 한계에 열려 있다 | ADR-22(의도된 예외, 테스트로 고정) |
| 롯데ON 은 '없다'를 못 말해 필터 파손 탐지가 `present` 한 건뿐 | ADR-19 4차 |
| 가맹점 동(洞) 값 **5,111곳 / 79,800곳 = 6.4%** 가 미상 | 도로명 주소 파싱 한계 |
| 라이브 지도 마커 클릭을 자동 테스트가 못 잡는다 | 원격 base 에서 지도 절 스킵 |
| 다리 버튼이 여러 대분류에 걸친 품목을 한쪽만 넘긴다 | ADR-20(현 46규칙에 손실 사례 0) |

---

## E. 문서가 코드와 어긋난 것

**E1 은 위험 방향이 나쁘다** — 다음 사람이 문서를 정본으로 믿고 코드를 "고치면" 동작이 바뀐다.

### E1. ADR-7 이 코드와 정면으로 어긋난다
`확인: 실측` · `크기: 작다`

```
ADR-7  : bounds 활성 시 시도 무관 전 지역 검색 — API는 region 파라미터 생략
실제    : regionParams() { var p = { region: state.sido }; … if (LIST_BY_MAP) p = assign(p, b); }
```

2026-08-12 "뷰포트 자동 지도" 커밋이 `bounds` 를 **추가 AND** 로 바꾸며 대체 의미를 폐기했고
그 사실은 변경 이력에 있다. 그런데 **ADR 은 안 고쳤다.** ADR-4 는 `폐기`, ADR-11 은 `부분 개정`
표기를 갖고 있는데 ADR-7 만 옛 상태다. 개정 표기 한 줄이면 닫힌다.

### E2. ADR-10 이 "판단 필요"에 멈춰 있다
`확인: 실측` · `크기: 작다`

`재평가 시점 도래(2026-09-04)` · `유예를 계속할지 판단이 필요하다` 로 끝난다.
실제로는 1~3단계가 끝나 2,638 → 1,552줄(−41%)이 됐다. "유예 종료 — 남은 것은 허브"로
상태를 갱신하면 닫힌다(C1 과 함께 처리하면 자연스럽다).

### E3. ADR-12 가 "비스트리밍"이라 적혀 있다
`확인: 실측` · `크기: 작다`

`비스트리밍 수신 후 청크 SSE(OpenAI 스트림 파싱 복잡도 회피)` 로 적혀 있으나,
2026-08-11 에 실스트리밍(`chatStream`)으로 바뀌었다.

### E4. `19_online_probe.md` 1·2절 표가 대체됐는데 남아 있다
`확인: 훑기가 지목 — 미확인` · `크기: 작다`

1절 실현성 표의 "불가/차단"으로 적힌 곳들이 그 뒤 ADR-19 로 대부분 편입됐고,
2절 robots 표(8곳)는 ADR-21 재설계로 대체됐다.

---

## 이 목록을 만들며 확인한 것

- **훑기 후보 121건 중 실제로 손댈 것은 20건 남짓**이었다. 나머지는 중복이거나,
  설계상 그런 것(ADR-16 탐지 전용 · tools-ci 사후 알림 · 배치 하루 시차)이거나,
  이미 닫힌 것이거나, 초기 구축용 체크리스트 템플릿이었다.
- 자동 훑기는 **"열려 있다고 적혀 있다"와 "지금도 안 돼 있다"를 잘 구분하지 못한다.**
  후보를 모으는 데는 값졌지만 판정은 사람 몫이었다 — 이 목록의 `확인` 칸이 그래서 있다.

---

## F. 오늘 작업이 남긴 끊긴 실 (2026-09-05 2차 훑기)

**패턴이 하나로 모인다: 같은 사실이 두 곳에 있는데 한 곳만 고쳤다.**
조임 대상 명단(코드 ↔ 문서 넷) · 안전망 주석(`ProbeJudge` ↔ `ProbeTargets`) ·
게이트 서술(tools-ci 머리 ↔ 꼬리) · 회귀값(DEPLOY.md ↔ 스킬) ·
적재 예시(같은 파일 열 줄 차이) — 전부 같은 모양이다.

그래서 조임 대상 명단만 **코드가 세게** 만들었다(명단이 바뀌면 테스트가 먼저 깨져 문서를 함께
고치게 된다). 나머지는 아직 사람 규율에 걸려 있다.

### F-1. 이용자 화면에 나가는 것

#### F1. `regionTotal` 수명 불일치 — "N곳 중 M곳"에서 **M > N**
`확인: 화면 재현` · `크기: 보통` · `원인 코드는 2026-08-12 부터`

```
진입 직후 :  95곳 중    95곳 표시
축소 후   :  95곳 중 2,163곳 표시
```

`regionTotal` 은 `scope="all"` 에서만 갱신되는데 지도 범위 모드의 지도 이동은 `"facet"` 이다
(`merchants.html:1009` 가 `facet` 의 유일한 호출자).

**재현하려면 서버 연결 모드여야 한다.** `updateBoundsBtn` 이 `b.hidden = (MODE !== "api")` 로
버튼을 감추므로(`merchants.html:969`), 백엔드 없이 로컬 파일만 열면 '이 지도 범위로 목록 보기'
버튼 자체가 없어 이 결함에 닿을 수 없다. 2026-09-05 회차 리포트가 세 번 시도하고 재현하지
못한 이유가 이것이다 — 결함이 없어서가 아니라 그 경로에 들어갈 수 없어서였다.

**그리고 그 코드에 달린 주석이 지금은 거짓이다** — `apiQuery`(`:564`)가 "지역 총계는 **지역 축이
안 바뀌었으니** 유지한다"고 적는데, C2 로 bounds 가 지역을 대체하게 되면서 그 유일한 호출 경로가
정확히 지역 축이 바뀌는 경우가 됐다. C1 보고서는 "원인은 C2 가 아니라 scope 표를 손으로 관리해 온
것"이라 적었는데 **절반만 맞다** — 결함은 2026-08-12 부터 있었지만 **주석을 거짓으로 만든 것은 C2**
이고 그 커밋이 이 자리를 열지 않았다. 다음 사람은 주석을 믿고 `wantRegionTotal` 을 후보로 안 본다.

### F-2. 오늘 만든 도구가 반쯤 됐다

C4(채록 리듬)의 **마지막 칸이 비어 있다.** 리듬을 만들었는데 그 리듬을 따라가면 막힌다.

#### F2. 주간 다이제스트와 반영 도구가 맞물리지 않는다
`확인: 실측` · `크기: 보통`

다이제스트는 **7회차를 합쳐** 보여 주고(`survey_nightly.js:194-232`), 반영 도구는 **회차 파일
하나**만 먹는다(`apply_survey_delta.js:23-24`). 다이제스트를 그대로 먹이면 `TypeError` 로 죽고,
일주일치를 반영하려면 델타 7개를 각각 돌려야 하며 그때마다 모든 몰이 **실행 당일 날짜**로 찍힌다
(실제 관측일이 아니라). `DEPLOY.md:330-332` 의 절차는 **도구가 생기기 전 문장**을 그대로 가리킨다.

#### F3. 반영 도구가 `config.js` `dataVersion` 을 올리지 않는다
`확인: 실측(config 참조 0건)` · `크기: 작다`

파일명이 고정이라 `?v=` 만이 캐시를 깨는 유일한 수단이다. **이 저장소가 이미 겪은 사고다** —
2026-08-21 에 데이터만 고치고 빼먹어 기존 방문자가 옛 카탈로그를 계속 받았고 별도 커밋(`64261b8`)
으로 수습했다. 손으로 하던 두 단계 중 **하나만 코드로 옮긴 셈**이라, 도구를 쓸수록 남은 한 단계를
잊기 쉬워진다. 로컬에서는 최신으로 보이고 재방문자 화면에서만 옛 태그가 남는다.

#### F4. `isDeepLink()` 가 온누리 전용몰 2곳을 오분류한다
`확인: 실측` · `크기: 보통` · `지금 발현 중`

경로 깊이만 보는 휴리스틱이라(`survey_nightly.js:88-96`) **인어교주해적단·온누리팔도시장**이
'기획전 딥링크'로 잡힌다. 그 둘은 **틀린 사유로 갱신에서 제외**되고, 도구가 자동으로 거르므로
사람이 개입하지 않는 한 **영원히 2026-08-21 에 묶인다** — 오늘 22곳 전수 재채록을 돌렸는데도
반영되지 않았다. 2026-09-03 에 명세로 박아 둔 유형이다:
**"틀린 사유를 대는 것은 안 대는 것보다 나쁘다."**

#### F5. 반영 도구의 소분류 규칙이 **한 방향으로만** 구현됐다
`확인: 실측(온누리마켓)` · `크기: 보통`

소분류가 새로 붙어도 **기존 부모 단독 id 를 걷어내지 않는다**(`apply_survey_delta.js:48`).
2026-08-23 에 손으로 치웠던 상태로 되돌아왔고, 이번에는 그 판단을 **코드로 남겼는데도** 되돌아왔다.
증상은 카드 태그 — `CAP=8` 로 접는데 정보량 0 인 부모 칩이 그 칸을 먹어
`가전·디지털` 과 `가전·디지털(생활·주방가전)` 이 나란히 뜬다. 앞으로 어떤 몰이든 같은 순간에 재발한다.

#### F6. 반영 도구가 `survey_status:"partial"` 을 보지 않는다
`확인: 실측` · `크기: 보통`

보호가 `thin`(본문 1500자)이라는 **대리 지표**에 우연히 걸려 있다. 상대 사이트가 배너·공지만 늘려
1500자를 넘기는 날 — 우리가 통제하지 못하는 변경이다 — 조건이 뒤집혀 `surveyed_on` 이 오늘로
올라가고 `behind` 에서 빠진다. **"이 몰은 확인 못 했다"는 유일한 고지가 화면에서 사라진다**
(태그는 2026-08-10 그대로인 채 스탬프만 최신).

#### F7. `apply_survey_delta.js` 와 `weeklyDigest` 에 테스트가 하나도 없다
`확인: 실측` · `크기: 보통`

둘 다 **경계에서만 드러나는** 코드다. 다이제스트는 `files.length < 7`·`fresh.length < 7`·
`lastDigest` 문자열 비교로 발화 시점을 정하는데, 틀리면 매일 뜨거나(통째로 무시) 영영 안 뜨고
— 어느 쪽이든 **일주일이 지나야 알 수 있고 에러는 안 난다**. 반영 도구는 데이터 파일을 직접
덮어쓰는데 판단이 틀어져도 JSON 은 멀쩡히 파싱되고 화면도 정상으로 그려진다(F2·F3·F5·F6 이 전부
그 파일에서 나왔다). 같은 커밋의 `looksLikeChrome` 은 정탐·오탐 쌍으로 고정돼 있어 대비가 뚜렷하다.

### F-3. 문서가 한쪽만 고쳐졌다

| 항목 | 어디 | 무엇 |
|---|---|---|
| **F8** tools-ci 머리 ↔ 꼬리 | `tools-ci.yml:139-141` ↔ `:12-19`·`:118-121` | 위에서 읽으면 "렌더는 배포 게이트에 있다", 끝까지 읽으면 "게이트에 없고 사람이 손으로 부른다". `7b1babd` 가 **두 사본 중 하나만** 고쳤다. 꼬리를 믿고 `browser` 잡을 지우면 게이트 절반이 사라진다 |
| **F9** 회귀 기준값 | `DEPLOY.md:77-86` ↔ `dev-testing/SKILL.md:118-119` | 같은 질의에 **저장소가 두 개의 정답**. 2026-09-05 라이브 실측으로 스킬 표의 **두 줄 다** 어긋남을 확인했다 — 팔달구 라이브 `1,259` 대 표 `1,260`, 개포동 라이브 `145` 대 표 `144`. 팔달구 쪽은 원인이 **실제 데이터 이상**이다(주소는 수원 팔달구인데 `si` 가 안양시로 붙은 레코드 1건). 표를 고치기 전에 그것부터 |
| **F10** 적재 예시 | `DEPLOY.md:66` (↔ 고쳐진 `:56`) | `66,211건·3개 시도`로 낡음(부산 누락). **부산이 안 실린 반쪽 적재를 "문서와 같으니 정상"으로 판정하게 된다.** 같은 커밋이 열 줄 위를 고치면서 지나갔다 |
| **F11** `SURVEY_OUT_DIR` | `DEPLOY.md:279` | 결과를 축소했다 — "기본 위치로 흩어진다"가 아니라 **아무것도 안 남고, 주간 다이제스트가 영원히 안 뜨고**(발화 조건이 리포트 7개인데 파일 자체가 없다), 단계 E 의 어제 대비 비교가 판단 보류가 된다. 새 서버에서 이 줄을 미루면 C4 가 고치려던 상태로 조용히 되돌아간다 |

### F-3-1. 결정 4를 처리하며 새로 확인한 것

#### F18. 가맹점 한 건의 `si` 가 주소와 어긋난다 — 있을 수 없는 조합 `안양시 + 팔달구`
`확인: 직접 조회` · `크기: 작다` · `2026-09-06 신규`

```
name : 평촌왕소금구이
addr : 경기도 수원시 팔달구 경수대로 600 (우만동)
si   : 안양시      ← 주소와 어긋난다
gu   : 팔달구
```

경기는 `si` 를 공식 API 의 `addrCd` 에서, `gu` 를 주소 파싱에서 가져온다
(`build_region_full.py:417-419`). 2026-09-01 에 그렇게 정한 이유는 인천 자치구 개편이
API 목록에는 반영됐는데 **가맹점 주소에는 옛 이름이 남아 있어서**였다. 그 원칙 자체는
지금도 옳다. 다만 두 원천이 어긋나는 건이 생기면 아무도 모르게 **존재하지 않는 조합**이
만들어진다 — 안양시에는 팔달구가 없다.

이용자 영향은 작다. 안양시를 보는 사람의 구 목록에 팔달구가 끼고, 수원시 팔달구를
보는 사람은 이 한 곳을 못 본다. 30,193건 중 1건이다.

**고칠 방향은 두 갈래다.** 주소 쪽을 믿어 `si` 를 덮어쓰면 인천에서 겪은 문제가 돌아온다.
그러니 값을 고치기보다 **어긋난다는 사실을 수집기가 세어 보고하게** 하는 편이 원칙에 맞다
(`si`·`gu` 조합이 실재하는지 화이트리스트로 대조). 이번 회차의 착수 목록 밖이라 손대지 않았다.

#### F19. "확인했고 달라진 게 없었다"가 날짜에 반영되지 않는다 — **2026-09-06 수정 완료(닫음)**
`확인: 실측(첫 실전 반영)` · `크기: 작다` · `사용자 요청으로 같은 날 수정`

주간 다이제스트는 변화가 있는 몰만 싣는다(`weeklyDigest` 의 `filter(r => r.brands.length || r.cats.length)`).
그래서 **정상 채록됐지만 새로 보인 것이 없는 몰은 통째로 빠지고, 확인일도 오르지 않는다.**

인어교주해적단이 그 예다. 2026-08-27 과 09-03 에 정상 채록됐고(`ok=true`·`thin=false`)
두 회차 다 변화 0건이었다. 그래서 딥링크 오분류를 고쳐 갱신 경로를 열어 줬는데도
확인일이 **2026-08-21 에 그대로 있다** — 이번에는 다른 이유로 묶여 있다.

화면은 `7곳은 2026-08-21 확인분` 이라 말한다. 그중 6곳은 `section` 이라 사실이지만
인어교주해적단은 09-03 에 확인됐으므로 **실제보다 낡게 말하는 것**이다. 틀리는 방향이
안전한 쪽이라 급하지는 않다.

**고쳤다.** 다이제스트가 `confirmed` 목록을 함께 싣는다 — 읽는 사람에게는 여전히 `rows`(변화)만
보이고, 반영 도구는 그 목록으로 확인일만 올린다. 값은 하나도 건드리지 않는다. 보류 규칙
(`section`·`partial`·`survey_scope` 미기재)과 "날짜는 앞으로만" 은 그대로 적용된다.
옛 다이제스트에는 이 필드가 없으므로 없으면 없는 대로 돈다.

실물 확인: 인어교주해적단이 `2026-08-21 → 2026-09-03` 으로 올랐고, 그 결과 화면에서
`2026-08-21` 로 남은 곳이 **정확히 `section` 몰 6곳**이 됐다 — 이제 낡은 날짜에는 전부
근거가 있다(그 6곳은 이 도구가 갱신하지 않는 것이 설계다).

#### F20. `section` 몰 6곳은 자동 갱신되지 않는다 — **2026-09-06 결정: 그대로 둔다 (닫음)**
`확인: 실측` · `사용자 결정`

반영 도구는 `survey_scope: "section"` 인 몰을 건드리지 않는다. 남의 몰 안의 한 구획이라
채록하면 호스트 몰의 GNB·카테고리 메뉴가 통째로 섞여 들어오기 때문이다. 대상은 여섯 곳이다 —
11번가 온누리마켓 · 사이소 · 현대홈쇼핑 · 공영쇼핑 · 우체국쇼핑 · 롯데ON 온누리상생스토어.

**대가를 분명히 적어 둔다.** 그 여섯의 취급 품목 태그는 2026-08-21 에 사람이 확인한 것이
마지막이고, 이 경로로는 앞으로도 갱신되지 않는다. 화면은 `6곳은 2026-08-21 확인분` 이라
그 사실을 그대로 말하고, 그 숫자는 시간이 갈수록 낡아 보일 것이다. **낡아 보이는 것이
맞다** — 실제로 낡았기 때문이다.

받아들이는 이유는 대안이 더 나쁘기 때문이다. 자동 반영하면 호스트 몰 전체의 메뉴가 그
몰의 태그로 들어가, 이용자가 "여기서 이걸 판다"고 읽는 목록이 사실과 갈라진다.
2026-08-21 롯데ON 에서 실제로 겪은 일이다.

**갱신하려면 사람이 한다.** 그 몰의 구획 화면을 열어 취급 품목을 눈으로 확인하고
`data/online_catalog.json` 을 직접 고친 뒤 `surveyed_on` 과 `config.js` 의 `dataVersion` 을
함께 올린다. 자동화할 방법을 찾았다면 그때 `survey_scope` 를 바꾸는 것이 아니라
**왜 이제는 섞이지 않는지**를 먼저 적어야 한다.

### F-4. 작은 것

| 항목 | 무엇 |
|---|---|
| **F12** ✔ 닫힘(2026-09-06) — 브랜드 `삼천리 자전거` 의 공백 | **고치며 진단이 절반 틀렸음을 확인했다.** 브랜드 착지(`?brand=`)는 원래 멀쩡했다 — `applyBrand` 가 `std.replace(/\s+/g,"")` 로 공백을 이미 지우고 있었고, 변조 실험으로 두 표기 다 1곳이 나오는 것을 확인했다. 실제로 깨져 있던 곳은 **브랜드 검색 팝업**이고, 방향도 반대였다: 데이터가 `삼천리 자전거` 라 공백형은 찾히고 정식 표기 `삼천리자전거` 가 0항목이었다. 조치 셋 — 별칭 사전에 한 건(`삼천리 자전거 → 삼천리자전거`)과 사본 재생성, 카탈로그 값을 정식 표기로, 그리고 **팝업 검색도 착지와 같은 규칙으로**(양쪽 공백 제거). 앞의 둘만 하면 실패가 없어지는 게 아니라 반대편으로 옮겨 간다(실측: 정식 표기 1항목 · 공백형 0항목). 셋을 다 하니 두 표기 모두 1항목이다 |
| **F13** 절대경로 가드 사정권 | `test_frontend_static.js:352-359` 가 `dev_scripts` 만 훑는다. 같은 실수가 `backend/tools` 에서 나면 CI 가 아니라 **00:30 서버 배치**가 맞고, `require` 실패를 "playwright 미설치"로 적어 **원인과 전혀 다른 메시지**가 남는다 |
| **F14** `--out` 값 검증 없음 | 마지막 인자면 리포트도 다이제스트도 **조용히 안 남는데** 로그에는 회차 요약이 정상으로 찍힌다. 종료코드 3 이 이미 '입력 파일 문제'로 정의돼 있으니 요란하게 죽는 편이 이 파일의 자기 계약에 맞다 |
| **F15** 없는 id 침묵 | 카탈로그에 없는 id 가 델타에 있으면 **아무 말 없이 버려진다** — 다른 모든 보류 사유는 로그를 남긴다. 지금은 두 목록이 같아 발현하지 않지만, 이 도구의 용도가 **옛 회차를 나중에 반영하는 것**이라 그 사이 id 가 바뀌면 그 회차 관측이 통째로 증발한다 |
| **F16** UTC 날짜 | `apply_survey_delta.js:27` 이 `toISOString`. KST 00:00~09:00 에 부르면 리포트보다 하루 이전으로 찍힌다. 2026-08-23 에 고친 결함 유형인데 **틀리는 방향이 '더 낡게'** 라 이 저장소 기준으로는 안전한 쪽 |
| **F17** tools-ci 트리거 경로 주석 | 이 워크플로에 없는 테스트를 근거로 든다. 다른 경로 주석들이 근거를 정확히 적어 둔 파일이라, 누가 "정적 계약은 이제 여기 없으니 이 경로도 빼자"고 하면 **D-F1 트리거가 통째로 사라지는** 방향으로 오도한다 |

### F-5. 이전 목록에서 넘어온 것

| 항목 | 상태 |
|---|---|
| **지도 페이로드를 14곳 중 12곳에서 버린다** | C1 보고서 결함 ②. 트리거 겸직이라 단순 제거는 안 되고, C1 비용 순서 2~3단계가 이걸 다룬다 |
| **찬스의 브랜드 추출이 카테고리 메뉴를 물어 온다** | 118건 중 진짜 4건. C4 의 `looksLikeChrome` 으로 **델타에서는 갈라 놓지만** `extractListSegment` 종료 조건 자체는 그대로고 카탈로그의 옛 값도 그대로 |
| **단계 A 를 같은 날 두 번 돌리면 화면에 거짓 경고** | `DEPLOY.md` 에 **명문화만** 했다. 코드 가드는 없다 |
| **B3 시장을 방으로** ✔ 닫힘 | **2026-09-06 사용자 결정 — 그대로 둔다.** 그날 다시 쟀다 — `/shop/` **403** · `sitemap.xml`·`robots.txt` **404** · 홈 내부 링크 41개에 상품 경로 **0개** · 홈 42KB 인데 텍스트 2,081자. **배달 주소를 고르기 전에는 카탈로그가 존재하지 않는다.** 브라우저를 들여도 "어느 주소를 대표 카탈로그로 삼을 것인가"라는 데이터 정의 문제가 먼저다. 화면이 `1곳은 2026-08-10 확인분` 이라 밝히는 현 상태가 가장 정직하다. **다시 열려면** 대표 주소를 먼저 정해야 한다 — 그것이 이 몰의 태그를 통째로 좌우한다 |

### 이 절을 만들며 확인한 것

- 1차 훑기의 교훈은 "자동 훑기가 **적혀 있다**와 **지금도 안 돼 있다**를 구분 못 한다"였다.
  2차는 반대쪽이다 — **내가 "했다"고 적은 것이 절반만 된 경우**를 훑기가 잡았다.
- 후보 중 두 건은 같은 항목이 두 번 잡혔다(F1·F9). 중복을 걷어낸 뒤가 17건이다.
