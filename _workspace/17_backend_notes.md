# 17. 백엔드 노트 — API 계약·마이그레이션·검증 기록

backend-dev 산출물 로그. 계약 변경(필드·의미)·스키마 마이그레이션·로컬 검증 결과를 시간순으로 남긴다.

## 2026-08-12 — 온라인 플랫폼 API 신설 + 야간 배치 (ADR-14)

### 마이그레이션
- **V5__online_platform.sql** — `online_platform` 테이블 신설. `data/online_platforms.json`의 items와 의미상 1:1.
  - 컬럼: id(PK 슬러그), post_no(공식 postNo), ord(공식 ordNo), kind(shopping/delivery), name, summary, note, url, region_limited, source_url, collected_on, status(active/removed).
  - 인덱스: `idx_online_platform_ord`, `idx_online_platform_post_no`.
  - JPA 엔티티 없음(JdbcTemplate 직조회, report 패턴) → ddl-auto=validate 영향 없음.

### API 계약 (프론트 전달용 — online.html·terms.html JSON 폴백과 매핑)
- `GET /api/online/platforms`, `POST /api/online/platforms` — 동일 응답(필터 없음, POST는 본문 컨벤션 통일).
- 응답 shape:
  ```
  { "meta": { "collected_on": "YYYY-MM-DD"|null, "source": "…", "sourceUrl": "…" },
    "items": [ { "id","no","kind","name","summary","note","url",
                 "regionLimited","sourceUrl","collectedOn","status" } ] }
  ```
- **직렬화 키 최종본(프론트가 매핑할 정확한 이름)**:
  - meta: `collected_on`(snake — JSON 파일 meta와 동일), `source`, `sourceUrl`.
  - item: `id`, `no`(=post_no), `kind`, `name`, `summary`, `note`, `url`, `regionLimited`, `sourceUrl`, `collectedOn`, `status`. (item은 전부 camelCase.)
  - ⚠ meta만 `collected_on`(snake)이고 item은 `collectedOn`(camel) — 프론트 JSON 폴백(data/online_platforms.json)의 기존 키와 정확히 일치시킨 결과다. 바꾸지 말 것.
- 정렬: `ord ASC NULLS LAST, name`. **removed 포함 전체 반환** — 프론트가 `status`로 필터.
- `meta.collected_on` = active 항목의 `min(collected_on)`(확인 안 한 항목 날짜를 스탬프에 올리지 않는다). active 없으면 null.
- 계약 고정: `OnlineContractTest`(record 컴포넌트명 + 직렬화 키). 이름 변경 시 프론트와 한 변경 단위로.

### 적재기·배치
- **tools/load_online_platforms.py** — JSON → online_platform 초기 upsert(멱등, id 기준). ord=배열 순서, post_no=null(배치 첫 실행이 채움), 큐레이션 필드 포함 전체 적재.
- **tools/nightly_update.py** — 서버 cron(00:30) 배치. 단계 A(가맹점 stage-swap)·B(온라인 upsert)·C(RAG). 스킵 플래그 `--skip-merchants/--skip-online/--skip-rag`, 로컬 스왑 검증용 `--no-collect`(재수집 생략). 상세 설계는 ADR-14.

### 로컬 검증 결과 (2026-08-12, docker onnuri-db)
- V5 Flyway 적용 성공(앱 부팅 UP, JPA validate 통과).
- API GET/POST: 30건, meta.collected_on 정확, camelCase 키 계약 일치, ord 정렬 보존.
- 온라인 배치(실측 공식 API): 30건 수집→30 갱신(이름 매칭), note·region_limited·source_url·id 보존, post_no 채움(11번가 31·cyso 30 등), collected_on 갱신.
- 가맹점 stage-swap: `--no-collect` 연속 2회 멱등(인덱스명 `merchant_stage_*`→`merchant_*` 정규화 후 재실행 충돌 없음, 잔여 stage 테이블 없음). 회귀 유지: 서울 29,462/인천 7,236/경기 29,522/부산 12,514, 강남구 761, 개포동 145, 수원 팔달구 1,241.
- 가드 발동 실증: 부산 12,514→100(축소본) 주입 시 "가드 위반 +99.2%"로 스왑 중단, 기존 DB 무결.
- `./gradlew test` 전체 녹색.

## 2026-08-18 — 관리자 로그인 API 신설 (POST /api/admin/login)

### 배경
admin-report.html 진입에 48자리 `APP_ADMIN_KEY`를 직접 입력해야 했다. 기억 가능한 비밀번호로
로그인해 키를 받아오는 경로를 추가한다. 키 자체의 쓰임(`X-Admin-Key` 헤더 → `POST /api/reports/{id}/status`)은 불변.

### API 계약 (프론트 admin-report.html과 합의된 고정값)
- `POST /api/admin/login`, 요청 body: `{"password":"…"}` (Content-Type: application/json)
- **200**: `{"key":"<APP_ADMIN_KEY 값>"}` — 프론트가 읽는 키 이름은 `key` 하나뿐.
- **403**: 비번 불일치 / `app.admin.password` 미설정 / `app.admin.key` 미설정. body `{"message":"비밀번호가 올바르지 않습니다."}`
  — **실패 이유를 구분하지 않는다**(설정 상태가 응답으로 새지 않게).
- **429**: rate limit 초과. body `{"message":"시도가 너무 잦습니다…"}`
- 실패 응답에는 키를 절대 담지 않는다(계약 테스트로 고정).

### 구현
- `gift/onnuri/admin/AdminController.java` (신규) — 패키지-당-기능 관례(report·visit·meta·news) 유지.
  스키마 변경 없음(마이그레이션 없음), 엔티티 없음.
- rate limit: `ChatConfig`에 `adminLoginRateLimiter` 빈 추가 — **IP당 분 5회·일 30회**
  (`app.admin.rate-per-minute:5` / `rate-per-day:30`). RateLimiter 빈이 3개가 되었고
  주입은 기존과 동일하게 **파라미터명**으로 선택된다(reportRateLimiter 선례).
  한도 검사가 비번 검증보다 **먼저** — 비번 미설정 상태에서도 대입 시도가 카운트된다.
- 비번 대조 `AdminController.authenticate()`: `MessageDigest.isEqual`(UTF-8 바이트) — 길이가 달라도
  조기 반환하지 않는 상수시간 비교. 설정값이 null/blank면 무조건 거절(= 기능 비활성).
- 비밀번호는 로그에 남기지 않는다(로그 grep 0건 실증).
- `application.yml`: `app.admin.password: ${APP_ADMIN_PASSWORD:}` 추가(키와 동일 패턴 — 서버 .env에만 실제 값).
- CORS 무변경 — 기존 `CorsConfig`가 `/api/**` + `allowedHeaders("*")`로 이미 덮는다(프리플라이트 실측 확인).

### 테스트 (`src/test/java/gift/onnuri/admin/AdminLoginContractTest.java`, 7개)
스프링 컨텍스트 없이 컨트롤러를 직접 조립한다 — 이 저장소 테스트는 DB 없이 도는 것이 규약.
정상 비번 200+key / 오답 403(키 미노출) / 비번 미설정 403 / 키 미설정 403 /
분당 한도 초과 429(다른 IP는 영향 없음) / 본문 없음·빈 body 403(500 아님) / authenticate 경계값.

### 검증 결과 (2026-08-18)
- `./gradlew test` **44개 전부 녹색**(기존 37 + 신규 7).
- 로컬 부팅 실측(포트 8099, docker onnuri-db): 정답 200 `{"key":…}` / 오답 403 / 빈 본문 403 /
  6번째 요청 429(정답 비번이어도) / `APP_ADMIN_PASSWORD` 미설정 재기동 시 정답 비번도 403.
- CORS 프리플라이트: `Origin: https://onnuri.koscomlabor.cloud` → 200,
  `Allow-Methods: GET,POST`, `Allow-Headers: content-type`.
- 로그에 비밀번호·키 문자열 노출 0건.

## 2026-08-18 — F-6/F-7 수정: XFF 스푸핑에 의한 rate limit 우회 차단

### 결함 (dev-qa 18_devqa_report.md F-6)
`clientIp()`가 `X-Forwarded-For`의 **첫 값**을 IP로 썼다. 첫 값은 클라이언트가 그대로 실어 보낼 수
있어, 매 요청 다른 값을 넣으면 rate limit 버킷이 매번 갈려 한도가 통째로 무력화된다.
dev-qa 재현: 오답 20회를 매번 다른 XFF로 → **403 20회, 429 0회**.

### 수정
- **`gift/onnuri/web/ClientIp.java` 신규** — XFF의 **마지막 값**(공백·빈 요소는 건너뜀), XFF 없거나
  전부 공백이면 `remoteAddr`. 프록시는 받은 XFF *뒤에* 자기가 본 TCP 상대 주소를 덧붙이므로
  마지막 값만이 위조 불가다.
- **전제 확인(코드 주석에도 기록)**: 프로덕션은 Caddy 1홉 — `deploy/Caddyfile`이
  `reverse_proxy app:8080`뿐이라 XFF 재작성이 없고, `docker-compose.prod.yml`의 app은
  `expose`만 있어(`ports` 없음) Caddy 우회 직접 접근 경로가 없다.
  ⚠ **앞단에 CDN·LB를 추가하면 마지막 값이 그 중계자 IP가 되어 전 이용자가 한 버킷을 공유한다**
  (정상 이용자가 서로의 한도에 걸린다). 홉이 늘면 ClientIp와 Caddy trusted_proxies를 함께 재설계할 것.
- **호출부 3곳을 공용 헬퍼로 통일**: `AdminController`, `ReportController`,
  그리고 **`ChatController`** — 세 번째 지점은 리더 지시·F-6 지적 어디에도 없었으나 동일 결함이었고,
  이 한도는 "공개 사이트에 노출된 유일한 LLM 비용 통제 장치"(RateLimiterTest 주석)라 위험도가 가장 높았다.
  이제 `X-Forwarded-For` 문자열은 `ClientIp` 한 곳에만 존재한다(grep 확인).
- **F-7 로그**: 로그인 403·429에 한해 WARN 1줄(`관리자 로그인 실패/한도 초과 — ip=…`).
  시각은 로그 포맷이 붙인다. 비밀번호·시도값은 남기지 않는다.
- 부수: `ChatController`의 낡은 주석 "빈 2개(chat/report)" → "빈 3개(chat/report/adminLogin)".

### 테스트 (52개, +8)
- **`web/ClientIpTest`(신규 6)**: XFF 부재→remoteAddr / 단일 값 / 다중은 마지막 / 위조 앞값이 달라도
  같은 키 / 공백·빈 요소 건너뜀 / XFF 공백뿐이면 remoteAddr.
- **`AdminLoginContractTest`(+2, 총 9)**: XFF 첫 값을 매번 바꿔도 6회째 429(F-6 회귀) /
  프록시 뒤에서도 실제 클라이언트가 다르면 버킷 분리.

### 검증 결과 (2026-08-18, 로컬 8099 + docker onnuri-db)
- `./gradlew cleanTest test` **52개 녹색**(기존 44 + 신규 8), 실패·에러 0.
- **F-6 공격 재현이 차단됨**: XFF 첫 값을 1~8로 바꿔가며 오답 → **1~5회 403, 6~8회 429**
  (수정 전에는 429가 영원히 나지 않았다). 실제 IP가 다른 클라이언트(198.51.100.8)는 정상 200.
- 다른 두 한도도 동일 공격에 견딤: 제보 1·2회 200 → 3·4회 **429**(분 2회), 챗 10회 통과 → 11회 **제한**(분 10회).
- F-7 로그 실측: 실패 5줄·한도 초과 3줄, 각 줄에 시각과 ip만. 로그 전체에서
  비밀번호·시도값("brute")·"password"·관리자 키 문자열 **0건**.
- 검증 후 정리: 임시 제보 2행 삭제(report 0건), 앱 종료.

### 2026-08-18 마무리 (dev-qa 2차 F-9·F-10)
- **F-9**: DEPLOY.md 1홉 경고에 한 줄 추가 — `app` 서비스에 `ports:`를 붙여 직접 노출하면
  Caddy를 거치지 않은 요청이 XFF를 통째로 지어낼 수 있어 F-6이 재발한다. `expose: 8080` 유지.
- **F-10**: 로그인 **성공**에도 로그 1줄(INFO, `관리자 로그인 성공 — ip=…`). 실패만 남기면
  대입 시도가 끝내 뚫렸는지를 로그로 알 수 없다. 키·비밀번호는 남기지 않는다.
- 검증: `./gradlew cleanTest test` **52개 녹색**. 실측 로그 — 실패 WARN·성공 INFO 각 1줄에 시각과 ip만,
  비밀번호·관리자 키·"password" 문자열 0건. 앱 종료 완료.

---

## 2026-09-02 — 제외 사유 전수화 + 전일 색인 층 (ADR-18)

### 마이그레이션
- **V8__online_product_index.sql** — `online_product_index` 신설.
  - 컬럼: `platform_id VARCHAR(60) NOT NULL REFERENCES online_platform(id)`, `url VARCHAR(700) NOT NULL`,
    `name VARCHAR(300) NOT NULL`, `collected_on DATE NOT NULL`. PK `(platform_id, url)`.
  - 인덱스: `idx_online_product_index_platform`.
  - `platform_id` 타입은 V5 의 `online_platform.id`(VARCHAR(60))에 맞췄다 — ADR 초안의 64 가 아니다.
  - PK 가 URL 을 포함하는 이유: 같은 상품이 목록·카테고리 양쪽에서 걷혀도 한 행이 된다.
  - **소유권: 배치 단계 F 가 몰 단위로 교체 적재하고 앱은 읽기만 한다.** 앱이 쓰면 "반쯤 걷힌 회차"를
    앱이 만들어 낼 수 있고 배치의 50% 가드가 그것을 막지 못한다.
  - JPA 엔티티 없음(JdbcTemplate 직조회) → `ddl-auto=validate` 영향 없음.
  - 검증: 일회용 postgres:16 컨테이너에 V5→V7→V8 순차 적용 성공, FK·PK·인덱스 실물 확인 후 폐기.

### API 계약 변경 (프론트 통지 대상)
- `OnlineSearchResult` **맨 뒤**에 `IndexLayer index` 추가. 기존 12개 필드의 순서·의미는 불변.
  **null 이 아니다** — 색인이 없으면 `{asOf:null, platformCount:0, foundCount:0, notice:null, items:[]}`.
- `IndexLayer(asOf, platformCount, foundCount, notice, items)` /
  `IndexHit(platformId, name, matchCount, sampleTitles, samplePartial, searchUrl, collectedOn)`.
  - `name` 은 **몰 이름**(ProbeHit 과 같은 자리), 상품명은 `sampleTitles`.
  - `IndexHit` 에 `status` 가 **없다** — 실시간 상태 목록(none/likely/…)에 `indexed` 를 더하지 않는다.
    "어제 올라와 있었다"와 "지금 검색된다"는 다른 주장이라 섞으면 문구가 둘 중 하나에 대해 거짓이 된다.
  - `notice` 는 서버가 만든다(실시간 층과 같은 이유 — 프론트가 items 를 세어 헤드라인을 재계산하면
    계약이 바뀔 때 조용히 틀린 숫자가 나온다). 4분기: 색인 없음(null) / 전체 낱말 매치 / 일부 낱말만 / 무매치.
- 응답 예시:
  ```json
  "index": {
    "asOf": "2026-09-01", "platformCount": 1, "foundCount": 1,
    "notice": "전일 색인: 1곳에서 검색어 전체를 담은 상품명을 찾았습니다(2026-09-01 수집 기준). 어제 올라와 있던 이름이라, 지금 재고와 온누리 결제 가능 여부는 몰에서 확인하세요.",
    "items": [ { "platformId": "genius-mall", "name": "지니어스몰", "matchCount": 2,
                 "sampleTitles": ["다이슨 청소기 필터", "다이슨 무선 청소기 V15 컴플리트"],
                 "samplePartial": false, "searchUrl": "https://luxurysystem.co.kr",
                 "collectedOn": "2026-09-01" } ] }
  ```

### 판정 규칙 (IndexJudge — 순수 함수, DB·네트워크를 모른다)
- 낱말 분리는 `ProbeQuery.countTokens()` 를 **그대로 재사용**한다. 새 분리 규칙을 만들면 두 층이
  같은 검색어를 다르게 쪼개 서로 다른 답을 낸다. 비교는 대소문자만 접는다.
- 상품명이 **모든** 낱말을 담으면 full → `matchCount` 에 센다. 샘플은 full 중 최대 3건.
- full 이 0이고 1개 이상 낱말을 담은 이름이 있으면 `matchCount=0`·`samplePartial=true` 로 그 이름 3건.
  ("다이슨 청소기"에 'LG 청소기'를 근거로 내밀면 이용자는 그 몰에 다이슨이 있다고 읽는다 — 2026-08-31 공공몰과 같은 함정.)
- 아무 낱말도 안 걸린 몰은 items 에 넣지 않는다(다만 `platformCount` 에는 든다).
- **색인 대상 = 색인 행이 있는 몰 − `ProbeTargets.ids()`** — 한 몰이 두 층에서 다른 말을 하지 않게.
  플랫폼 목록(active·shopping)에 없는 id 도 뺀다(이름을 몰라 그릴 수 없다).
- `asOf` = 포함 몰의 **min(collectedOn)**. 최신 날짜를 쓰면 "어제 기준"이라며 사흘 전 데이터를 섞어 말하게 된다.
- `searchUrl` 은 실시간 층과 같은 `searchUrlFor` 규칙(데이터 `search_url_template` 우선, 없으면 홈).

### 실시간 층과의 독립성
- 색인 층은 킬스위치 OFF·레이트리밋·캐시 적중 어느 경우에도 계산한다(DB 읽기뿐 — 아웃바운드 0).
  실시간이 막힌 몰을 위해 만든 층이라 실시간이 죽을 때 함께 죽으면 뜻이 없다.
- **실시간 결과 캐시에 색인을 함께 가두지 않는다.** 캐시 적중 시 `withIndex()` 로 오늘 값을 갈아 끼운다 —
  실시간은 60분 캐시가 맞지만 색인은 매일 밤 갈려 수명이 다르다(자정을 넘긴 뒤 옛 색인을 "전일 기준"이라 말하는 것을 막는다).
- 색인 DB 예외는 빈 층으로 흡수한다. 보조 기능이 본 기능을 끌어내리면 안 된다.
- 저장소 SQL 은 **예선일 뿐 판정이 아니다** — `ILIKE` 로 낱말을 하나도 안 담은 행만 버리고, 무엇을 매치로
  셀지는 IndexJudge 가 정한다. 규칙을 SQL·Java 두 곳에 두면 갈라진다. 검색어의 `%`·`_` 는 이스케이프한다.

### 제외 사유 사전 전수화
- `EXCLUSION` 을 2곳 → **11곳 전수 명시**. 사유 3종: `robots-blocked`(8) /
  `scope-first`(2, 놀장·시장을 방으로) / `no-static-search`(1, 인어교주해적단).
- 한때 넣었던 `no-search-feature`(지니어스몰)는 **상수째 뺐다** — 그날 지니어스몰이 조회 대상이 되어
  붙는 몰이 없어졌다. 해당하는 곳이 없는 사유를 남기면 화면에 설명만 있고 실체가 없는 항목이 생긴다
  (2026-09-01 `rules-unverified` 제거와 같은 이유).
- 기본값(`getOrDefault`)은 남겼지만 **거기 기대지 않는다** — 그 폴백이 곧 오표기의 원인이었다.
  이전 화면은 12곳 중 10곳을 "화면에서만 만들어져 읽을 수 없음"이라 말했는데 12곳 중 어디에도 맞지 않는 문구였다.
- robots 8곳은 **허가 없이는 보류**(사용자 결정 2026-09-02). 정적 조회 성공까지 실측돼 있어 허가만 오면 규칙 몇 줄로 편입된다.
- 완전성을 테스트가 고정한다: `data/online_platforms.json` 의 `kind=shopping` 중 `ProbeTargets.ids()` 에
  없는 모든 id 가 사전에 **명시적으로** 있어야 한다. 사유별 곳 수(8/2/1)·합계 11·사전과 대상의 비중첩도 함께 고정.

### 조회 대상 11번째 — 지니어스몰 (2026-09-02, 팀 결정 변경)
- 앞서 "검색 기능 자체가 없다"고 본 것이 **틀렸다.** 플랫폼 기본 검색 URL 4종만 시험하고 접었는데,
  실제 폼은 `<form class="search_bbs" action="/product/product.html" method="GET">` + `name="search"` 였다.
  **없는 URL 을 지어내 시험하기 전에 그 몰의 폼을 먼저 읽어야 한다** — 2026-09-01 꾹AI 에 이어 두 번째다.
- robots.txt: `Allow : /`, 금지는 `/ko_mall/` 뿐 → `/product/` 허용.
- 실측(UA 동일, curl): 로봇청소기 200·50,936B·상품 12건·0.20초 / zzqqxyw12345 200·29,844B·상품 0건.
- 없음-문구: 원문은 `총 <i>0</i>개의 상품이 있습니다` 인데 **판정은 태그를 걷어낸 텍스트에 대고 한다** →
  사전에는 `총 0 개의 상품이 있습니다` 로 적었다. 질의 비의존형이라 등급 B.
  **있음 응답에는 이 문구가 없다**(건수가 12로 찍힌다 — 대조 확인, 테스트로 고정).
- `echoesQuery=false` 실측 — 없는 질의가 원문에 1회 있으나 검색창 `<input value>` 라 `stripEcho` 후 토큰 0이다.
  문구가 깨져도 토큰 0 판정이 '없다'를 받쳐 준다.
- `titlePattern` `<em>([^<]+)</em>` — 상품명에 class 가 없다. 실측: 있음 응답 `<em>` 12개 = 상품 12개로 정확히
  일치하고 없음 응답에는 `<em>` 이 하나도 없다(목록 밖에서 쓰이지 않는 태그).
- 카나리아 present 질의는 `로봇청소기` 다. 가전 전문몰이라 다른 몰이 쓰는 `김치` 는 0건이다.
  카나리아 요청량 20 → 22건/일(`ProbeTargets.ALL` 에서 자동 파생 — SelfTestService 무수정).
- 픽스처 2개 실측 응답 그대로 저장: `src/test/resources/probe/genius-mall-{hit,none}.html`.
- **판정 실측**
  | 질의 | status | confidence | hits | 근거 |
  |---|---|---|---|---|
  | zzqqxyw12345 (absent) | none | high | 0 | `총 0 개의 상품이 있습니다` |
  | 로봇청소기 (present) | likely | medium | 21 | 샘플 3건(에브리봇 AI 올인원로봇청소기 Q11 등) |
  | 다이슨 청소기 | likely | medium | 21 | `samplePartial=true` — 다이슨을 담은 이름이 없다 |
- 색인 층 대상은 `ProbeTargets.ids()` 제외 규칙에 따라 자동으로 놀장·인어교주해적단 2곳이 됐다.
  **테스트가 이 파급을 먼저 잡았다** — 색인 예시로 쓰던 지니어스몰이 실시간 대상이 되자
  "실시간 대상은 색인 층에서 뺀다" 테스트가 실패했다.

### 테스트 (144개, +33 — 이전 111)
- **`IndexJudgeTest`(신규 14)**: 전체 낱말 매치만 셈 / 일부 매치 표시 / 무매치 몰 제외 / 대소문자 무시 /
  샘플 정렬·3건 상한 / **실시간 대상 제외** / 목록에 없는 몰 제외 / `asOf`=min / 빈 색인 / 검색 링크 / notice 4분기 / 문장 종결.
- **`OnlineProductIndexRepositoryTest`(신규 3)**: 빈 대상·빈 낱말이면 질의하지 않음(`IN ()` 문법 오류 방지) / LIKE 메타문자 이스케이프.
- **`OnlineSearchServiceTest`(+5)**: 색인 층 항상 채움 / 킬스위치 OFF 에도 색인 계산 / 캐시 적중 시 색인 재계산 /
  색인 장애가 실시간을 죽이지 않음 / 사유 4갈래 실측.
- **`OnlineSearchContractTest`(+4)**: `index` 가 맨 뒤 / IndexLayer·IndexHit 컴포넌트 / 직렬화 키 / 빈 층은 null 아님 /
  `IndexHit` 에 status 부재(층 혼입 방지).
- **`ProbeTargetsTest`(+5)**: 비대상 전수 사유 명시 / 사유 4종 화이트리스트 / 사유별 곳 수 / 대상·사전 비중첩 / 사전의 유령 id 금지.
- 정리: `OnlineSearchServiceTest` 의 낡은 단언 교정 — 온누리5일장이 `EX_NO_FETCH` 라던 줄(이제 조회 대상),
  지니어스몰 사유가 `EX_NO_FETCH` 라던 줄(이제 `EX_NO_SEARCH`).
- **`ProbeJudgeTest`(+2)**: 지니어스몰이 건수 문구로 none 확정·있으면 상품명 샘플 / 없음 문구가 있음 응답에 없음(등급 B 전제).
  기존의 `ALL` 순회 테스트 5건이 지니어스몰까지 자동으로 덮는다(픽스처 2개가 그래서 필요하다).
- `./gradlew cleanTest test` **144개 녹색**. 계약 확장이 **테스트 컴파일 단계에서 막혀** 필드 순서 고정이 작동함을 재확인.

### 남긴 우려
- 색인 층은 **적재기가 아직 없다**(배치 단계 F 는 다른 담당). 그때까지 테이블이 비어 `index` 는 빈 층으로 나간다 —
  화면에는 블록이 아예 나타나지 않아야 하고, `platformCount=0`·`notice=null` 이 그 신호다.
- 한 요청이 읽는 행 수를 5,000 으로 상한했다. 몰당 하루 100~150건 규모에서는 닿지 않지만,
  색인이 그보다 커지면 `matchCount` 가 조용히 낮아진다 — 단계 F 가 적재량을 정한 뒤 이 값을 재검토할 것.
- 색인 조회는 요청마다 DB 를 두 번(요약·후보) 친다. 캐시하지 않은 것은 의도(수명이 다르다)이나,
  실시간 조회 요청량이 늘면 재평가 대상이다.

---

## 2026-09-03 — 조회 대상 11 → 14곳 (ADR-19)

2026-08-31 조사에서 **정적 조회가 되는데도 robots.txt 때문에 뺐던** 3곳을 편입했다. 사용자 결정이며,
규칙은 이번에 새로 실측했다. robots.txt 는 그대로다(2026-09-03 재확인) — 굿데이·인더마켓
`Disallow: /` + `Allow: /$`, 팔도시장 `disallow: /Goods/`.

### 판정 실측 (2026-09-03, 앱과 같은 UA)

| 몰 | 질의 | HTTP·크기·소요 | status | conf | hits | 근거 |
|---|---|---|---|---|---|---|
| 온누리굿데이 | zzqqxyw12345 | 200 · 193,231B · 1.71s | none | high | 0 | `입력하신 단어로 검색된 결과가 없습니다` |
| 온누리굿데이 | 로봇청소기 | 200 · 256,139B · 1.13s | likely | medium | 21 | 샘플 3건(슈미트 올클린 자동충전 로봇청소기 등) |
| 인더마켓온누리몰 | zzqqxyw12345 | 200 · **269,296B** · 0.86s | none | high | 0 | 같은 문구 |
| 인더마켓온누리몰 | 로봇청소기 | 200 · 199,404B · 0.80s | likely | medium | 22 | 샘플 3건 |
| 온누리팔도시장 | zzqqxyw12345 | 200 · 38,658B · 3.75s | none | high | 0 | `‘{q}’의 대한 검색결과 총 0 개의 상품이 있습니다`(등급 A) |
| 온누리팔도시장 | 로봇청소기 | 200 · 64,182B · 1.88s | likely | medium | 12 | 샘플 3건(보랄 Z10 AI 로봇청소기 등) |

### 규칙과 그 근거

- **굿데이·인더마켓은 같은 솔루션**이다 — 같은 파라미터(`?pn=product.search.list&search_word=`),
  같은 마크업(`<a class="item_name">`), 같은 없음 문구. 규칙을 한 벌로 쓴다.
  `echoesQuery=false` — 없는 질의가 원문에 22~23회 있으나 **전부 `<title>`·`<meta>`** 라
  `stripEcho` 후 토큰 0이다(2026-08-31 조사가 "제목 에코"로 관찰한 그 자리다). 토큰 0 판정이
  없음 문구의 예비 수단이 된다.
- **팔도시장은 질의를 되뿌린다**(`‘{q}’의 대한 검색결과 총 N 개…`) → `echoesQuery=true`,
  `noiseFloor=1`. 토큰 0 판정을 쓸 수 없으므로 등급 A 문구가 유일한 확정 수단이고,
  등급 B(`고객님이 검색하신 상품이 없어요`)를 보조로 함께 뒀다.
  상품명은 `<div class="item_n">`.

### 함정 3건 — 전부 회귀 테스트로 고정

1. **인더마켓은 없음 응답이 더 크다** — 있음 199,404B < 없음 **269,296B**. 없음 화면이
   추천상품을 40건 붙이기 때문이다. 응답 길이로 있음·없음을 가르는 규칙을 만들었다면
   정확히 거꾸로 판단했을 것이다.
2. **없음 화면에도 상품 카드가 20~40건 온다**(굿데이·인더마켓). 카드 수를 근거로 삼았다면
   없는 질의에도 늘 '있음'이 됐을 것이다. 판정은 문구가 하고, 샘플은 질의 토큰 필터가 거른다.
   인더마켓의 없음 회차 추천 40건 중 1건이 '김치'를 담고 있었다 — 그런 회차에는 등급 B 가
   none 대신 **unclear** 로 물러선다(설계대로. '있다'로 기울지는 않는다).
3. **팔도시장 자동완성 JS 에 `검색된 정보가 없습니다.` 가 모든 응답에 있다.** 원본 HTML 에
   문자열 매칭했다면 상품이 있어도 늘 '없음'이 됐다(온누리마켓 약관과 같은 함정).
   `ProbeJudge` 가 `<script>` 를 먼저 걷어내 텍스트에 남지 않는 것을 실측 확인했다.

또 하나 확인한 것: 굿데이·인더마켓의 상품코드 집합이 질의마다 **완전히 disjoint** 다.
2026-09-01 꾹AI 에서 겪은 "검색이 실행되지 않는데 결과처럼 보이는" 상태가 아님을 이 방식으로 확인했다.

### EXCLUSION 재편 — 8곳
- `EX_ROBOTS` 상수·항목 **삭제**. 굿데이·인더마켓·팔도가 대상이 되면서, 남은 곳들은 robots
  이전에 **정적으로 읽히지 않는다**는 사실이 먼저다.
- 새 `EX_BOT_BLOCKED = "bot-blocked"`(사이소 1곳) — 성격이 다르다. 화면 검색이 WAF 1.7KB 로
  **능동 차단**되어 돌아온다. "결과가 안 실린다"가 아니라 "우리를 막는다"이므로 사유를 따로 둔다.
- `scope-first` 2곳(놀장·시장을 방으로) / `no-static-search` 5곳(11번가·현대홈쇼핑·롯데ON·공영쇼핑·인어교주해적단).
- 완전성·사유별 곳 수(1/2/5)·합계 8을 테스트가 고정한다.

### 부담·운영
- 부담 억제 설정은 불변(버튼 트리거·캐시 60분·몰당 동시 1건·몰당 분6일600·IP당 분5일100·전역 동시 12).
- 카나리아는 `ProbeTargets.ALL` 자동 파생이라 코드 수정 없이 22 → **28건/일**이 된다.
  present 질의는 세 곳 모두 `김치`(가전 전문몰이 아니라 종합몰이다).
- 픽스처 6개 실응답 그대로 저장: `src/test/resources/probe/{onnuri-goodday,inthemarket-onnuri,onnuri-paldo-sijang}-{hit,none}.html`.

### 테스트 (155개, +11 — 직전 144)
- `ProbeJudgeTest`(+5): 인더마켓 길이 역전 / 없음 화면 추천상품 / 팔도 script 문구 / 팔도 등급 A 확정 /
  편입 3곳의 없음 문구가 있음 응답에 없음(등급 B 전제).
- 기존 `ALL` 순회 테스트 6건이 세 곳을 자동으로 덮는다(픽스처 6개가 그래서 필요하다).
- `ProbeTargetsTest`·`OnlineSearchServiceTest`: 대상 14곳·사유 3종·곳 수 1/2/5·사이소 예시로 교체.
- `./gradlew cleanTest test` **155개 녹색**(skipped 1 = 평소 skip 하는 라이브 카나리아).

### 남긴 우려
- **팔도시장의 robots 는 `/Goods/`(대문자)를 막는다.** 우리가 조회하는 검색 경로는 `/goods/`(소문자)라
  문자 그대로는 금지 밖이지만, **결과에 실려 오는 상품 상세 링크가 전부 `/Goods/Content.aspx`** 다.
  2026-09-01 조사 때 이 저장소는 "대소문자 우회는 하지 않는다"고 적었다 — 이번 편입은 그 판단을
  뒤집은 것이므로 근거가 robots 해석이 아니라 **사용자 결정**임을 여기 남긴다.
- 굿데이·인더마켓의 `Disallow: /` 는 지금도 유효하다. 야간 배치 단계 E 의 robots 감시가 이 세 곳을
  계속 보게 되는데, **감시가 "변화 없음"을 보고해도 그것이 허용을 뜻하지는 않는다.** 감시의 의미가
  바뀌었다는 점을 다음 사람이 알 수 있어야 한다.

---

## 2026-09-03 (2) — 현대홈쇼핑 편입(15곳) + 사유 `scope-mixed` 신설

### 판정 실측 — 현대홈쇼핑 온누리샵

| 질의 | HTTP·크기·소요 | 상품 | status | conf | hits |
|---|---|---|---|---|---|
| zzqqxyw12345 | 200 · 658B · 0.06s | 0 | none | high | 0 (`"itemList":[]`) |
| 세트 | 200 · 46,738B · 0.07s | 20(상한) | likely | medium | 26 |
| 쌀 | 200 · 22,915B · 0.05s | 10 | — | — | — |
| 청소기 | 200 · 7,306B · 0.06s | 3 | — | — | — |
| 로봇청소기 | 200 · 830B · 0.06s | **0** | — | — | — |

샘플: `호정가 찹쌀약과세트 30개입 / 달콤한 전통과자세트` 외 2건. 조회 대상 중 가장 빠르다.

### 판단이 필요했던 것 4가지

1. **캐시 경로 vs 직접 경로 — 캐시 경로를 쓴다.** 화면이 부르는 것이 그것이고, ADR-17 이
   "공식 화면이 보내는 것과 같은 요청이라 우회가 아니다"라고 세운 근거가 여기서도 그대로다.
   응답에 늘 `__cache_metadata.status=HIT_STALE` 이 붙어 신선도를 실측 대조했다 —
   **4질의 × 두 경로에서 상품명 집합이 완전히 동일**했다(청소기 3·쌀 10·로봇청소기 0·없는 말 0).
   신선도가 문제가 되면 `https://www.hmall.com/api/hf/dp/v1/sect-mng/plansale-ancr-paging?…`
   으로 바꾸면 된다(같은 파라미터·같은 응답 구조).
2. **`jsonApi` 신호 신설.** 이 몰은 **평범한 GET 인데 JSON 을 준다** — 기존 `isApi()` 의 두 신호
   (formBody·`{qq}`)로는 잡히지 않는다. 선언하지 않았다면 `searchUrlFor` 가 이 JSON URL 을
   이용자 링크로 내보냈을 것이다(2026-09-02 와 같은 사고). `ProbeTarget` 에 컴포넌트를 더하고
   보조 생성자를 하나 추가해 기존 14곳은 무수정으로 뒀다.
3. **`searchUrlFor` 완화 — `{q}` 없는 링크도 쓴다.** 전용관 화면이 URL 의 `searchTxt` 를
   무시해 검색어를 실은 링크를 만들 수 없다. 대신 그 화면에 자기 검색창이 있어 링크만으로도
   이용자가 검색할 수 있다. 데이터에 `{q}` 없는 템플릿을 가진 몰은 이 한 곳뿐이라 영향 범위가 좁다.
   계약 테스트는 "`{q}` 담은 링크 필수" → **"비어 있지 않은 링크 필수, `{q}` 는 선택"** 으로 바꿨다.
   **비어 있지 않음 검사는 유지했다** — 그 테스트가 막던 것은 링크가 없어 조회 URL 이 이용자에게
   나가던 사고이고 그 위험은 그대로다.
4. **카나리아 질의 `세트`(20건).** 김치는 1건이라 그 상품 하나가 빠지면 매일 거짓 실패가 난다.
   지시받은 `쌀`(10건)을 넣었다가 **테스트가 막았다** — 1자라 `ProbeQuery.MIN_LEN`(2)에 미달해
   카나리아가 매일 400 을 받는다. 2026-08-31 에 바로 이 낱말로 겪은 일이고, 그때 넣어 둔 가드가
   같은 실수를 두 번째로 잡았다. `세트` 는 페이지 상한(20)을 채워 상품 하나에 흔들리지 않는다.

### 픽스처 질의를 몰별로 나눴다
`ProbeJudgeTest` 의 `ALL` 순회 테스트가 `로봇청소기` 를 15곳 전부에 들이대고 있었는데,
현대홈쇼핑 온누리샵에는 **로봇청소기가 0건**이다(식품 중심 전용관). 픽스처 질의 표를 두고
이 몰만 `세트` 로 채록했다 — 그 몰의 카나리아 질의와 같은 말이라 픽스처와 라이브가 같은 것을 본다.

### 사유 재편 — 비대상 7곳
| 사유 | 곳 | 근거 |
|---|---|---|
| `bot-blocked` | 1 (사이소) | robots 가 `/api/` 금지 + 화면은 WAF 1.7KB |
| `scope-first` | 2 (놀장·시장을 방으로) | 시장·주소 선택이 선행 |
| **`scope-mixed`(신설)** | 3 (11번가·롯데ON·공영쇼핑) | 검색은 정적으로 되지만 결과가 몰 전체 |
| `no-static-search` | 1 (인어교주해적단) | 검색 API 401 + SPA |

`scope-mixed` 를 만든 이유는 **라벨이 사실과 달랐기 때문**이다. 셋 다 `no-static-search` 로
묶여 있었는데 2026-09-03 재조사에서 셋 다 검색이 정적으로 재현된다는 것이 확인됐다
(19_online_probe 6-9절). 화면은 그 라벨을 근거로 이용자에게 틀린 설명을 하고 있었다.
셋의 진짜 문제는 읽지 못하는 것이 아니라 **읽어도 대부분 온누리 밖 상품**이라는 것이고
(공영쇼핑은 김치 40건 중 온누리 기획전 상품이 4건), 그건 우리 사정이 아니라 이용자를 위한
선택이므로 화면이 그렇게 말할 수 있어야 한다.

`robots-blocked` 상수는 붙는 몰이 없어져 삭제했다 — 지금까지 같은 이유로 사라진 사유가 셋이다
(`rules-unverified`·`no-search-feature`·`robots-blocked`).

### 테스트 (158개, +3 — 직전 155)
- `ProbeJudgeTest`(+2): 전용관 범위가 응답으로 확인됨(`sectInfo.sectNm`) / GET 이라도 JSON 이면 API 선언.
- `OnlineSearchServiceTest`(+1): 검색어를 실을 수 없는 몰은 전용관 주소를 준다(JSON 이 링크로 안 나감).
- `ProbeTargetsTest`: 대상 15곳·사유 4종·곳 수 1/2/3/1·합계 7·API 링크 계약 완화.
- `./gradlew cleanTest test` **158개 녹색**(skipped 1 = 평소 skip 하는 라이브 카나리아).
- 카나리아 요청량 28 → **30건/일**.

### 남긴 우려
- 현대홈쇼핑 robots 는 `Disallow: /` 그대로다(2026-09-03 확인). ADR-19 의 결정이 근거이며
  robots 해석이 근거가 아니라는 점은 앞 절과 같다.
- 이 몰의 '없음' 응답은 658바이트다. `isApi()` 덕분에 하한 60자가 적용되지만, 선언이 빠지면
  HTML 기준 500자에 걸려 확실한 '없음'이 unknown 으로 뭉개진다(이지웰 316자와 같은 함정).
  `jsonApi` 를 지우면 조용히 그렇게 된다 — 테스트로 고정해 뒀다.

---

## 2026-09-03 (3) — 11번가·공영쇼핑 편입(17곳), 비대상 5곳 (ADR-19)

전체 검색의 **상세 필터**로 온누리 범위가 잡힌다는 재조사(19절 6-10-1)를 반영했다.
앞 절이 "전용관 안에 검색이 없다 → 범위 오염"으로 접었던 두 곳이다.
전용관(범위)과 전체 검색(도구)을 따로 본 것이 잘못이었다 — **전체 검색에 범위 필터가 있으면 둘이 만난다.**

### 판정 실측 (2026-09-03, 앱과 같은 UA)

| 몰 | 질의 | HTTP·크기·소요 | 상품 | status | conf | hits | 근거 |
|---|---|---|---|---|---|---|---|
| 11번가 | zzqqxyw12345 | 200 · 161,052B · 0.28s | 0 | none | high | 0 | `"groupName":"noSearchData"` |
| 11번가 | 로봇청소기 | 200 · 65,963B · 0.27s | 22 | likely | medium | 49 | 로보락 Q Revo L Pro 로봇청소기 외 2건 |
| 11번가 | 김치 | 200 · 841,317B · 0.62s | 938 | — | — | — | — |
| 공영쇼핑 | zzqqxyw12345 | 200 · 1,124B · 0.08s | 0 | none | high | 0 | `<rsltYn>N</rsltYn>` |
| 공영쇼핑 | 김치 | 200 · 10,688B · 0.10s | 184 | likely | medium | 56 | 팽현숙 최양락의 포기김치 외 2건 |
| 공영쇼핑 | 로봇청소기 | 200 · 1,136B · 0.11s | **0** | — | — | — | 필터 범위에 가전이 없다 |

### 판정 엔진을 고쳤다 — API 몰은 **원문에도** 대조한다

지시받은 없음-문구 `<rsltYn>N</rsltYn>` 이 **그대로는 절대 매치되지 않는다.** 판정은
`toText` 를 거친 텍스트에 하는데 공영쇼핑은 XML 이라 태그가 걷히면 ` N ` 만 남는다(실측).
`총 <i>0</i>개`(지니어스몰)·`총 <i>0</i>개`(팔도) 때와 같은 유형이지만, 그때는 텍스트 형태로
바꿔 적어 풀 수 있었고 이번에는 텍스트에 남는 것이 홑글자 `N` 이라 그 방법을 쓸 수 없다.

`ProbeJudge` 의 등급 B 검사에 **API 몰에 한해 원문 대조**를 더했다. 태그 제거는 HTML 산문에서
문구가 태그로 쪼개지는 것을 막으려고 넣은 규칙인데(`<span>등록된</span> 상품이 없습니다`),
구조화된 API 응답에서는 **그 구조 자체가 신호**라 같은 규칙이 정반대로 작용한다.
화면 HTML 몰은 이 경로를 타지 않는다 — 허용하면 옛 결함이 되살아난다. 테스트로 양쪽을 고정했다.
기존 API 몰 4곳(이지웰·5일장·현대홈쇼핑·꾹AI)의 판정이 바뀌지 않았음을 픽스처로 확인했다.

### 규칙과 그 근거
- **11번가** `filters=ONNURI` — 11번가가 스스로 붙인 **결제 가능 속성**이라 기획전(242건)을
  포함하는 상위집합이다. `jsonApi=true`(GET 이지만 JSON). 이용자 링크는 데이터의 화면 주소로,
  새로 열면 퀵필터가 checked 로 걸린다.
  - **`"totalCount":0` 은 넣지 않았다.** 지시는 "함께 둘 수 있다"였으나 면(facet)마다 카운트가
    실리는 응답이라 어느 한 면이 0이면 **있는 상품을 없다고 할 수 있다.** 가장 위험한 방향이고,
    `noSearchData` 가 늘 함께 나오므로 얻는 것도 없다.
  - **titlePattern 앵커 필수** — `"title"` 만 쓰면 SEO 제목 `로봇청소기 - 11번가 추천` 이
    첫 샘플로 나간다(실측·테스트 고정).
  - **noiseFloor 9** — 없는 질의 4종에서 전부 정확히 9회. 없으면 등급 B 문구가 있어도 unclear.
- **공영쇼핑** `benefit=trdit_mrkt_goods_yn` — 몰이 이 값을 **'온누리'로 부른다**(혜택 목록의
  `<nm>온누리</nm>`). 필터를 빼면 김치 40건 중 온누리 상품이 4건뿐이라 **지워서는 안 되는 값**이다.
  이용자 링크는 데이터의 기획전 홈(`url`)이다 — 몰 전체 검색 화면은 90%가 범위 밖이라 주지 않는다.
  - ⚠ **불확실성**: 이 필터 값이 결제 가능 속성인지를 **상품 화면에서 직접 확인하지는 못했다.**
    몰의 이름표와 기획전 표본 20/20 일치까지가 근거다. 뜻이 바뀌면 범위가 조용히 어긋난다.

### 롯데ON 은 보류 — 사유 두 가지를 주석에 남겼다
조회는 `u31=onnuri` 로 범위가 잡힌다(김치 1,149건, 반환 상품 전부가 온누리 표식). 그럼에도 뺐다.
①**이용자 링크가 필터를 못 싣는다** — 주소에 붙여 새로 열어도 칩이 꺼지고 조회에도 실리지 않는다.
조회는 온누리 범위인데 링크는 몰 전체가 되면 이용자는 "여기 있다더니 다른 게 나온다"를 겪는다.
②**'없다'를 확정할 수단이 없다** — 결과가 없으면 본문이 **0바이트**다. API 몰 하한 60자 아래는
unknown 으로 접으므로(빈 응답과 장애 응답을 구분할 수 없어서) 없는 상품에 늘 unknown 이 나간다.
쓰려면 "빈 본문 = 없음"을 타깃 단위로 선언해야 하고, 그건 일부러 세운 가드를 무르는 일이다.

### 계약 완화 두 번째
API 몰의 이용자 링크 검사를 "`search_url_template` 이 비어 있지 않을 것" → **"`search_url_template`
또는 `url` 중 하나가 비어 있지 않을 것"** 으로 넓혔다. `searchUrlFor` 가 그 순서로 고르고 API 몰은
조회 URL 로 폴백하지 않으므로, 둘 중 하나만 있으면 이용자가 JSON 을 마주할 일이 없다.
공영쇼핑은 홈 자체가 온누리 기획전이라 검색 화면 링크를 **일부러 비운** 경우다.

### 사유 분포 — 비대상 5곳
| 사유 | 곳 | 몰 |
|---|---|---|
| `bot-blocked` | 1 | 사이소 |
| `scope-first` | 2 | 온누리 놀장 · 시장을 방으로 |
| `scope-mixed` | 1 | 롯데ON |
| `no-static-search` | 1 | 인어교주해적단 |

### 픽스처 질의 표에 공영쇼핑 추가
`ALL` 순회 테스트의 기본 질의 `로봇청소기` 가 이 몰의 온누리 필터에서 **0건**이라(식품 중심)
`김치` 로 채록했다. 현대홈쇼핑(`세트`)에 이어 두 번째 예외다.

### 테스트 (162개, +4 — 직전 158)
- `ProbeJudgeTest`(+4): XML 없음문구를 원문에서 잡는다 / 화면 HTML 몰은 원문 매칭을 쓰지 않는다 /
  11번가 앵커를 빼면 SEO 제목이 섞인다 / 11번가 에코 9회를 상품 신호로 세지 않는다.
- `ProbeTargetsTest`·`OnlineSearchServiceTest`: 대상 17곳·사유 곳 수 1/2/1/1·합계 5·링크 계약 완화.
- `./gradlew cleanTest test` **162개 녹색**(skipped 1 = 평소 skip 하는 라이브 카나리아).
- 카나리아 요청량 30 → **34건/일**. 색인 층 대상은 자동으로 놀장·인어교주·롯데ON 3곳이 된다.

---

## 2026-09-03 (4) — 카나리아가 규칙과 **다른 것을 재고 있었다**

라이브 단계 E 가 굿데이·인더마켓에 대해 `echoesQuery 선언(false)과 실측(true)이 다르다` 를
보고했다. 지시는 "안전한 쪽으로 두 몰을 true 로 정정"이었으나, **선언을 뒤집지 않았다.**
실측해 보니 신고 쪽이 틀렸다.

### 무엇이 어긋났나
- `echoesQuery` 가 뜻하는 것은 "이 몰이 어딘가에 질의어를 되뿌리는가"가 아니라
  **"히트 0 판정을 쓸 수 없는가"** 이고, 그 히트는 `ProbeJudge` 가 **`stripEcho`**
  (제목·메타·검색창 등 에코 자리를 걷어낸 본문) 위에서 센다.
- 그런데 `SelfTestService` 는 `echoed` 를 **`toText`** 로 재고 있었다 — 태그만 걷고
  에코 자리는 그대로 둔 텍스트다. `<title>zzqqxyw12345 검색결과 - 온누리굿데이</title>` 가
  그대로 잡힌다.
- 두 몰의 에코는 **전부 `<title>`·`<meta>`** 다. 없는 질의 3종 × 2몰 = **6건 전부**
  `toText` 포함 true / `stripEcho` 포함 false 였다(원문 출현 22~23회).
  즉 판정에는 닿지 않는다 — 선언 `false` 가 맞다.

### 지시대로 뒤집었다면
그 몰들이 쓸 수 있는 **'없다' 확정 수단 하나(토큰 0 판정)를 근거 없이 버리는 것**이 된다.
등급 B 문구가 있어 `none` 자체는 성립하므로 겉으로는 아무 일도 없어 보인다 —
이 저장소가 반복해서 경계해 온 "에러 없이 조용히 나빠지는" 방향이다.
그리고 원인이 그대로 남아 **제목·메타에만 에코하는 몰이 새로 들어올 때마다 같은 거짓 경보**가 난다.

### 조치
- `SelfTestService.echoed` 를 `stripEcho` 기준으로 고쳤다(한 줄). 카나리아가 규칙과 같은 것을 잰다.
- 굿데이·인더마켓 픽스처 4개를 2026-09-03 실응답으로 갱신했다.
  갱신 후 판정 동일: 없음 `none/high/hits 0`, 있음 `likely/medium/hits 21·22/샘플 3`.
- 테스트 2건 신설 — 카나리아가 판정과 같은 기준을 쓰는지(소스 대조로 고정),
  제목·메타에만 남는 에코를 에코로 세지 않는지(실응답 픽스처로 고정).

### 이 수정이 드러낸 것 — 다음 회차부터 note 2건이 새로 뜬다
기준을 맞추자 **온누리핫딜·온누리찬스**가 `선언=true / 실측=false` 로 갈린다.
두 몰도 에코가 에코 블록 안에만 있다. 선언이 **실제보다 보수적**인 쪽이라 위험하지 않지만
(쓸 수 있는 판정 수단을 안 쓰는 것), 사실과 다른 선언인 것은 같다.
**건드리지 않았다** — 특히 온누리찬스는 등급 C 라 `false` 로 바꾸면 토큰 0 판정이 열려
"확정 수단이 없는 몰은 없다고 말하지 않는다"(ADR-17 1단계 결정·테스트)가 뒤집힌다.
그 결정을 다시 볼지는 판단이 필요하다.

### 테스트 (164개, +2 — 직전 162)
`./gradlew cleanTest test` **164개 녹색**(skipped 1 = 평소 skip 하는 라이브 카나리아).

---

## 2026-09-03 (5) — 카나리아 note 정리(핫딜 정정 · 찬스 대조 제외)

앞 절의 기준 정정으로 드러난 note 2건을 없앴다. **note 가 소음이 되면 사람이 note 를 통째로
무시하게 되고, 그 순간 카나리아는 죽는다** — 그래서 미루지 않았다.

### 온누리핫딜 — `echoesQuery` true → **false** (실측대로)
1단계에 `true` 로 둔 것은 이 몰이 질의를 되뿌린다고 본 것인데, 그 에코는 전부 **에코 블록 안**
(제목·검색창)이라 판정이 보는 본문(`stripEcho`)에는 남지 않는다.
등급 **A**(질의가 박힌 문구)라 지시 조건에 해당해 정정했다.

**판정은 바뀌지 않는다** — 등급 A 문구가 먼저 확정한다. 회귀 확인:
없음 `none/high/hits 0`(근거 `"zzqqxyw12345" 검색 결과 검색 결과가 없습니다`),
있음 `likely/medium/hits 19/샘플 3`. 달라지는 것은 **문구가 깨졌을 때뿐**이고,
그때 `unclear` 대신 `none(medium)` 이 나온다 — 토큰 0 판정이 문구의 예비 수단이 되는 것이라
안전한 방향이다.

### 온누리찬스 — 선언 유지, **대조에서 제외**
`echoesQuery=true` 는 사실 주장이 아니라 **정책 선언**이다. 등급 C 라 없음-문구가 없고,
여기에 토큰 0 판정까지 열면 근거 없이 '없다'를 말하게 된다(ADR-17 1단계가
"확정 수단이 없는 몰은 없다고 말하지 않는다"로 정한 그 자리). 실측이 `false` 로 나와도
선언을 바꿀 일이 아니므로 매일 뜨는 note 는 소음일 뿐이다.

**조건을 `canDecideAbsent(t)` 로 잡았다** — 지시는 "등급 C 면 비교하지 않는다"였으나
그대로 하면 **온누리마켓의 안전망이 함께 사라진다.** 이 몰도 등급 C 지만 `echoesQuery=false` 라
**그 선언이 곧 '없다'의 근거**이고, 사실이 아니게 되면 없는 것을 없다고 못 하거나
있는 것을 없다고 하게 된다. 대조가 반드시 필요하다.
`canDecideAbsent` = 없음-문구가 있거나 `!echoesQuery` — 정확히 찬스 한 곳만 빠진다.

### 결과
픽스처 기준으로 **선언과 실측이 어긋나는 몰 0곳**, 대조 제외 1곳(찬스, 사유 표기).
다음 회차부터 echo note 는 뜨지 않는다.

### 테스트 (167개, +3 — 직전 164)
- 확정 수단이 없는 몰은 에코를 대조하지 않는다(찬스).
- 등급 C 여도 선언이 '없다'의 근거인 몰은 계속 대조한다(마켓) — 등급만 보고 끄는 것을 막는 회귀.
- 픽스처가 있는 대상 전부에서 선언과 실측이 일치한다(어긋나면 매일 note 가 뜬다는 사실을 고정).
- `./gradlew cleanTest test` **167개 녹색**(skipped 1 = 평소 skip 하는 라이브 카나리아).

---

## 2026-09-03 (6) — 카나리아 질의 무작위화 (상대 몰 인기 검색어 오염 수습)

라이브에서 **우리 카나리아 질의가 온누리굿데이 인기 검색어 2위에 올라 있는 것**이 발견됐다.
고정 문자열 `zzqqxyw12345` 를 매일 보낸 결과다. 그 몰을 쓰는 사람들 화면에 우리가 만든 낱말이
보이는 것이라 **그 자체로 폐를 끼치는 일**이고, 부수적으로 판정도 흔든다 —
인기 검색어·자동완성 블록은 `stripEcho` 가 걷지 않는 본문이라 히트로 잡힌다.

### 실측 대조 (2026-09-03, 같은 회차)
| 몰 | 질의 | 인기검색어 포함 | 자동완성 포함 | stripEcho 히트 |
|---|---|---|---|---|
| 온누리굿데이 | `zzqqxyw12345`(매일 보낸 말) | **있음(2위)** | **있음** | **2** |
| 온누리굿데이 | `zqulenbpbi`(무작위) | 없음 | 없음 | **0** |
| 인더마켓 | 두 질의 모두 | 인기검색어 블록 자체가 없음 | 없음 | 0 |

**무작위 질의에서는 에코가 사라진다.** 원인이 상대 몰의 성질이 아니라 우리가 같은 말을 반복한 것이었다.

### 조치 — `absentQuery()` 생성기
회차마다 `zq` + 소문자 8자를 새로 만든다. 숫자를 넣지 않아 상품 코드·수량(`1kg`)과 우연히 겹칠 일이
없고, 10자라 `ProbeQuery` 길이 제한(2~40)을 넉넉히 지킨다. 리포트에는 그 회차에 실제로 쓴 말이
`SelfTestCase.query` 로 남는다. 픽스처 테스트는 픽스처 안의 질의(`zzqqxyw12345`)를 그대로 쓰므로 영향이 없다.

### `echoesQuery` 는 두 몰 다 **false 로 둔다**
지시는 굿데이를 `true` 로였으나, **무작위화하면 에코가 사라진다는 것이 위 실측이다.**
`true` 로 바꾸면 우리가 만든 부작용을 근거로 그 몰이 쓸 수 있는 '없다' 확정 수단 하나를 영구히
버리는 셈이 된다. 원인을 없애는 쪽이 맞다. 인더마켓은 인기 검색어 블록 자체가 없어 해당 없음.
- 실제 이용자 질의가 마침 인기 검색어와 겹치는 경우(예: `선풍기`)는 히트가 1~2 생기지만,
  그때는 토큰 0 판정이 애초에 발동하지 않고 등급 B 문구가 판정한다 — 결과가 달라지지 않는다.
- 잔여: 굿데이 인기 검색어에 `zzqqxyw12345` 가 아직 남아 있다. 더는 보내지 않으므로 순위에서 내려갈 것이다.

### 테스트 (169개, +2 — 직전 167)
- 없는 말 질의가 회차마다 달라진다(200회 생성에 190종 이상, 형태·조회 가능성 고정).
- 없는 말 질의에 숫자를 넣지 않는다.
- `./gradlew cleanTest test` **169개 녹색**(skipped 1 = 평소 skip 하는 라이브 카나리아).

---

## 2026-09-03 (7) — 롯데ON 편입(18곳), 등급 C·**never none**, `scope-mixed` 삭제

프로모션 필터 `u31=onnuri` 로 범위가 잡히는 것을 확인해 편입했다. 다만 **'없다'는 말하지 않는다** —
적대적 검증에서 REFUTED 된 두 건이 설계를 정했다.

### 판정 실측 (2026-09-03, 헤더 없이 UA 만)

| 질의 | HTTP·크기·소요 | 상품 | status | conf | hits |
|---|---|---|---|---|---|
| 로봇청소기 | 200 · 114,923B · 0.60s | 31(total 31) | likely | medium | 74 |
| 김치 | 200 · 220,870B · 0.71s | 60(total 1,166) | — | — | — |
| 골프채 | 200 · 88,419B · 0.67s | — | — | — | — |
| 샤넬 | 200 · **130B** · 0.47s | 0 | **unclear** | null | 0 |
| zzqqxyw12345 | 200 · **0B** (text/html) | — | (unknown) | — | — |

샘플: `삼성전자 비스포크 AI 스팀 플러스 새틴그레이지 로봇청소기 VR80F01ADG` 외 2건.
**범위 확인**: 반환 상품 31건 전부가 `"type":"onnuri"` · `emblemName:"온누리"` 를 갖는다(테스트로 고정).

### 이 몰은 **never none** 이다 — 등급 C 를 정책으로 택했다
- 온누리 0건이면 `{"itemList":[],"total":0}` 130바이트가 온다. 그런데 **존재하지 않는 필터값
  (`u31=onnuriZZ`)을 보내도 바이트·md5 가 완전히 같다.** 130바이트는 "온누리에 없다"와
  "필터가 깨졌다"를 구분하지 못한다 — 롯데ON 이 파라미터 이름을 바꾸는 날
  **모든 질의가 조용히 '없음'** 이 된다. ADR-17 이 가장 경계한 방향이다.
- 무의미어·`q` 누락·`q` 빈 값은 200 · text/html · **본문 0바이트**로 전부 같다.
  현실 다어절 질의 8건 중 2건이 실제로 0바이트였다.
- 그래서 없음-문구 사전을 비우고(등급 C) `echoesQuery=true` 를 **정책으로 선언**했다
  (온누리찬스 선례 — 토큰 0 판정을 쓰지 않겠다는 뜻이지 에코 실측값이 아니다).
  결과적으로 **likely / unclear 만 낸다.** 실측에서 130바이트 응답이 `unclear` 로 나오는 것을 확인했다.
- `canDecideAbsent` 가 false 가 되어 카나리아 absent 대조에서 자동으로 빠진다.
  그래서 **필터가 깨졌을 때 알아채는 유일한 수단이 present 기대치**(김치 → likely + 샘플 ≥1)다.
  이 사실을 주석과 테스트에 함께 남겼다.

### 이용자 링크 — 데이터는 계속 비워 둔다
`promo=onnuri` 는 모바일 UA 첫 화면에서만 먹고 PC UA 는 302 로 필터가 증발하며, 모바일도 첫
스크롤부터 풀린다(온누리 100%→29%). **세 실패가 전부 조용해서** 이용자는 온누리 링크인 줄 알고
결제 안 되는 상품을 본다. 링크는 `url`(상생스토어 단축주소)로 나간다 — 공영쇼핑과 같은 처리다.

### titlePattern 은 앵커가 필요 없었다
`"pdName"` 매치 수가 itemList 와 **정확히 같다**(로봇청소기 31 = total 31 · 김치 60 = 한 페이지).
11번가는 `"title"` 이 SEO 제목까지 물어 앵커가 필요했지만 여기는 새는 자리가 없다. 테스트로 고정했다.

### 픽스처
- hit = **로봇청소기**(114KB). 김치는 220KB 라 저장소가 무거워지고 회귀 검증에 더 주는 것이 없다.
- none = **샤넬**(130B). 무의미어는 본문이 0바이트로 와서 픽스처로 쓸 수 없다 — 그 한계를 주석에 남겼다.

### `scope-mixed` 상수 삭제 — 비대상 4곳
| 사유 | 곳 | 몰 |
|---|---|---|
| `bot-blocked` | 1 | 사이소 |
| `scope-first` | 2 | 온누리 놀장 · 시장을 방으로 |
| `no-static-search` | 1 | 인어교주해적단 |

붙는 몰이 없어져 사라진 사유가 **넷째**다 —
`rules-unverified`(09-01) · `no-search-feature`(09-02) · `robots-blocked`(09-03) · `scope-mixed`(09-03).

### robots 기록
ADR-19 로 대상 선정 기준에서는 빠졌으나 사실은 남긴다 — **조회 호스트가 몰 본체
(www.lotteon.com)이고 `Disallow: /` 다.** 11번가가 조회를 `apis.11st.co.kr` 로 갈라 보내는 것과 다르다.

### 테스트 (171개, +2 — 직전 169)
- 롯데ON 은 없다고 말하지 않는다(등급 C·정책 선언·카나리아 제외를 한 자리에서 고정).
- 롯데ON 상품명은 앵커 없이도 상품만 뽑는다(매치 수 = itemList · 온누리 표식 31/31).
- `확정_수단이_없는_몰은_정책으로_정한_두_곳뿐이다` 로 개정 — 찬스·롯데ON 만 예외이고
  **나머지는 반드시 확정 수단을 가져야 한다**(잃으면 그만큼 카나리아가 눈이 먼다).
- `./gradlew cleanTest test` **171개 녹색**(skipped 1 = 평소 skip 하는 라이브 카나리아).
- 카나리아 요청량 34 → **36건/일**. 색인 층 대상은 놀장·인어교주 2곳으로 줄어든다.

---

## 2026-09-03 (8) — robots 감시 정정: 조회 호스트·경로 기준 판정 (ADR-19 후속)

앞선 감시는 두 가지를 틀렸다. **관측점**이 이용자 링크 호스트였고(우리가 두드리는 곳이 아니다),
**판정**이 `Disallow: /` 한 줄만 봤다(경로별 Allow 를 못 읽는다). 둘 다 고쳤다.

### 설계 판단 — 판정을 **앱에** 뒀다
판정이 **경로**를 봐야 하는데 우리가 실제로 두드리는 경로를 아는 곳은 `ProbeTargets` 뿐이다.
배치에 파서를 두면 그 경로를 손으로 옮겨 적게 되고, 2026-08-31 에 감시 도메인을 손으로 적었다가
엉뚱한 사이트를 보고 있던 사고가 형태만 바꿔 되살아난다(단계 E 를 만들 때의 논리와 같다).
아웃바운드는 호스트 단위 중복 제거로 하루 18건 안쪽 — 카나리아 36건에 더해 54건이 된다.
킬스위치가 꺼져 있으면 robots 도 조회하지 않는다(아무도 두드리지 않는데 허락을 물을 이유가 없다).
다만 **엔드포인트 목록은 꺼져 있어도 채운다** — 상수 파생이라 네트워크가 필요 없고,
배치가 도메인을 손으로 적지 않게 하는 것이 그 목적이라 꺼졌다고 사라지면 뜻이 없다.

### 계약 (`SelfTestReport` 맨 뒤에 3필드 추가)
- `probeEndpoints[]` = `{platformId, host, path}` — **실제 조회 주소**. 이용자 링크가 아니다.
- `robotsUserAgent` = `onnuri-guide` (판정에 쓴 제품 토큰. 나중에 누가 봐도 알 수 있게 싣는다)
- `robots[]` = `{platformId, allowed, rule, group, error}` — `platformId` 로 위와 이어 본다.
못 읽은 호스트는 `allowed=false` + `error` 다. **모르는 것을 허용으로 적지 않는다.**

### 판정 규칙 (`RobotsRules`, 순수 함수)
표준대로 ①우리 토큰에 맞는 가장 구체적인 UA 그룹(없으면 `*`) ②그 안에서 최장 일치
③길이가 같으면 Allow 우선. 걸린 규칙이 없으면 허용(robots 는 금지를 적는 파일이다).
빈 `Disallow:` 는 규칙이 아니다 — 빈 패턴을 규칙으로 다루면 길이 0으로 모든 경로에 걸려 정반대가 된다.

### 구현 중 잡은 것 — **경로만 보면 두 곳이 뒤집힌다**
처음에 `path` 만 실었다가 굿데이·인더마켓이 **허용**으로 나왔다. 두 몰의 조회 주소는 경로가 `/` 뿐이고
검색 조건이 전부 쿼리에 있어서, 그 몰들의 `Allow: /$`(루트만 연다)에 정확히 걸린 것이다.
robots 매칭은 경로+쿼리를 보므로 쿼리를 붙였고, 붙이자 `Disallow: /` 로 뒤집혔다.
질의어 자리는 이미 고정 토큰 `Q` 라 이용자 검색어가 실리지 않는다. 이 뒤집힘을 테스트로 고정했다.

### 라이브 판정 실측 (2026-09-03, 18곳)
| 몰 | 조회 호스트 | 판정 | 근거 |
|---|---|---|---|
| 온누리굿데이 | www.onnurigood.com | **차단** | `Disallow: /` |
| 인더마켓온누리몰 | inthemarket.co.kr | **차단** | `Disallow: /` |
| 현대홈쇼핑 | www.hmall.com | **차단** | `Disallow: /` |
| 공영쇼핑 | www.gongyoungshop.kr | **차단** | `Disallow: /search/` |
| 롯데ON | www.lotteon.com | **차단** | `Disallow: /` |
| 나머지 13곳 | — | 허용 | `Allow: /` 또는 걸린 규칙 없음 |

**차단 5곳 / 18곳.** 이 숫자가 ADR-19 가 내건 균형점("정책이 강해지거나 항의가 오면 즉시 끈다")의
관측값이다. 판정 결과로 조회를 **자동 비활성화하지 않는다**(ADR-17 이 기각한 조용한 축소) —
리포트와 로그까지이고, 끄고 켜는 것은 사람이 한다. 다만 앱이 차단 건수를 WARN 으로 남긴다.

호스트가 실제로 갈리는 것도 확인됐다 — 11번가 조회는 `apis.11st.co.kr`(이용자 링크는
`search.11st.co.kr`), 온누리5일장은 `api.samaint.co.kr`(본몰이 아니다), 롯데ON 은 몰 본체다.
앞선 감시가 이용자 링크 호스트를 보고 있었으므로 이 셋은 **다른 사이트를 감시하고 있었다.**

### 테스트 (189개, +18 — 직전 171)
- **`RobotsRulesTest`(신규 13)**: 전면차단 아래의 경로 허용 / 최장일치 / 동률 Allow 우선 /
  빈 Disallow / 규칙 없음 / 우리 지목 그룹 우선 / 다른 봇 그룹 무시(이지웰·꾹AI 실측 원문) /
  더 구체적인 그룹 / 굿데이·팔도·공영쇼핑 실측 원문 / 주석·빈줄 / 와일드카드·끝앵커.
- **`SelfTestContractTest`(+5)**: 대상 전부가 엔드포인트 목록에 있음 / 질의어 미포함 /
  **조회 호스트가 이용자 링크 호스트와 다를 수 있음**(11번가·롯데ON·5일장을 값으로 고정) /
  robots 토큰 / 경로에 쿼리를 붙여야 판정이 맞음.
- 리포트 계약 테스트를 새 3필드까지 포함하도록 갱신(맨 뒤 추가 — 앞 순서가 바뀌면 배치가 어긋난다).
- `./gradlew cleanTest test` **189개 녹색**(skipped 1 = 평소 skip 하는 라이브 카나리아).

---

## 2026-09-03 (9) — dev-qa 6차 지적 2건 반영 (F-2 주석·I-2 410)

### F-2 — 주석이 판정을 **뒤집는 방향으로** 거짓이었다
`ProbeEndpoint` 의 클래스 주석이 `경로만 싣고 쿼리스트링은 버린다` 로 남아 있었다.
구현은 그 반대다(쿼리를 붙인다). 구현 중에 내가 직접 겪어 고친 함정인데 **주석만 이전 상태로 남았다.**

이게 보통 수준의 문제로 그치지 않는 이유는 방향이다 — 주석을 믿고 "쿼리 붙이는 건 실수네" 하며
정리하면 굿데이·인더마켓이 `Allow: /$` 에 걸려 **차단에서 허용으로** 뒤집힌다.
감시가 조용히 느슨해지는 쪽이고, 이 저장소가 가장 경계하는 방향이다.

주석을 실제 동작에 맞추고 **왜 필요한지**(그 두 몰의 경로가 `/` 뿐이라는 사실)와
**이 뒤집힘을 고정하는 테스트 이름**을 그 자리에 적었다. 다음 사람이 지우려 할 때 근거가 보인다.
질의어 자리는 고정 토큰 `Q` 라 개인정보 우려는 없다는 것도 함께 적었다.

### I-2 — robots 404 만 보던 것을 **404·410** 으로
배치와 DEPLOY.md 는 둘 다 "파일 없음"으로 보는데 앱만 404 였다. 앱을 맞췄다 —
같은 사실을 두 곳이 다르게 판단하는 것이 이번 라운드가 없애려던 병이다.

**RFC 9309 가 허용하는 만큼 넓히지는 않았다.** 표준은 4xx 전체를 "unavailable"로 보고 접근을
허용해도 된다(MAY)고 하지만, 401·403 은 파일이 없다는 뜻이 아니라 **우리를 막는다**는 신호에
가깝다. 그것을 "금지 없음"으로 적으면 모르는 것을 허용으로 바꿔 적는 셈이 된다.
그런 응답은 지금처럼 예외로 떨어져 `error` 로 남고 `allowed=false` 가 된다 — 모르면 모른다고 적는다.
판정을 `ProbeFetcher.robotsMissing(int)` 순수 함수로 빼서 테스트로 고정했다.

### 테스트 (191개, +2 — 직전 189)
- 파일이 없다는 응답은 금지가 없다는 뜻이다(404·410).
- 막는 응답을 금지 없음으로 바꿔 적지 않는다(401·403·429·451·500·503).
- `./gradlew cleanTest test` **191개 녹색**(skipped 1 = 평소 skip 하는 라이브 카나리아).

---

## 2026-09-03 (10) — 카나리아 무작위 질의: 고정 접두 제거 + 실패 시 1회 재시도

라이브에서 `onnuri-paldo-sijang [absent] 기대=none 실제=unclear` 실패 1건. **판정은 옳았고
질의 생성기가 문제**였다. 그 회차 질의 `zqhaerxqaq` 가 그 몰에서 실제로 7건을 물었다.

### 원인 — 접두 `zq` 자체가 그 몰에서 생산적이었다
| 질의 | 팔도시장 총 건수 |
|---|---|
| `zq` | **386** |
| `zqh` | 11 |
| `zqhaerxqaq`(그 회차) | **7** |
| `zqbcdfghjk` · `zqxvkwmqpl` | 0 |

그 몰 검색이 정확 일치가 아니라 뒤에 무엇이 붙느냐로 0이 되기도 7이 되기도 한다.
**접두가 고정이면 한 몰에서 걸린 날 매일 걸린다.** 숫자를 뺀 판단(상품코드·수량 회피)은
맞았지만 고정 접두를 둔 것이 새 충돌을 만들었다.

### ① 고정 접두 제거 — 열 자 전부 무작위
실측(2026-09-03, 무작위 10자 8건을 팔도시장에 직접 조회): **8건 전부 0건**.
같은 회차 대조로 `zq` 386 · `zqhaerxqaq` 7 을 재현했다.
테스트로 첫 글자 15종·앞 두 글자 100종 이상 흩어지는 것을 고정했다.

### ② 실패하면 새 말로 **한 번 더** 묻는다
무작위 낱말이 우연히 걸리는 일은 드물지 않다(위가 그 증거). 그런데 **서로 다른 두 낱말이
같은 몰에서 연달아 걸릴 확률은 훨씬 낮다.** 두 번 다 실패할 때만 FAIL 로 올린다 —
이렇게 하면 "우리 말이 우연히 걸렸다"와 "없음-문구가 깨졌다"가 갈린다.

- 요청량은 실패한 absent 건에서만 +1이라 **평시 증가가 없다**(테스트로 고정).
- 기대치가 없는 몰(온누리찬스·롯데ON)은 재시도하지 않는다 — 다시 물어도 판단이 달라지지 않는다.
- 리포트에 **두 질의를 모두** 남긴다. 재시도로 통과하면 note 에
  `첫 질의 '…' 가 걸려 새 말로 다시 물었다(그 몰 검색이 느슨하다)` —
  그 몰 검색이 느슨하다는 신호이고 나중에 규칙을 볼 때 재료가 된다.
- `SelfTestCase.withNote()` 는 컴포넌트를 바꾸지 않아 직렬화 계약이 그대로다.

### 테스트 (197개, +6 — 직전 191)
고정 접두 없음 / 우연히 걸리면 재시도해 통과 / 두 번 다 걸리면 실패 / 두 질의가 리포트에 남음 /
처음에 통과하면 다시 묻지 않음(평시 요청량 불변) / 기대치 없는 몰은 재시도 안 함.
`./gradlew cleanTest test` **197개 녹색**(skipped 1 = 평소 skip 하는 라이브 카나리아).
