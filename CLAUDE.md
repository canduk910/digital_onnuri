# digital_onnuri

디지털온누리상품권 사용처 가이드 페이지(`index.html`) 리포지토리.

## 하네스: 온누리 가이드 제작·갱신

**목표:** 날짜에 종속된 상품권 정책·사용처 정보를 정확하게 유지하며 가이드 페이지를 갱신한다. 이용자가 계산대에서 결제 실패를 겪지 않는 것이 품질 기준이다.

**트리거:** 두 단계 이상 얽힌 작업 — 가이드 제작·갱신·정책 반영(업무팀), 기능 개발·API 확장·리팩토링·TDD(개발팀), 데이터 스키마 변경처럼 두 팀에 걸친 작업(혼성) — 은 `onnuri-guide-orchestrator` 스킬을 사용하라(2팀 라우팅). 단일 단계는 개별 스킬 직행: 백엔드 단건 수정 `backend-server-dev`, 테스트·검증 `dev-testing`, 구조 판단 `app-architecture`, 데이터 `onnuri-usage-data`, 문구 `guide-content-style`, 페이지 소수정 `guide-page-build`, 콘텐츠 검수 `guide-verification`. 개별 사용처 단건 질문("우리 동네 CU 되나")은 스킬 없이 직접 답해도 된다. 커밋·푸시 요청("커밋푸시하자" 등)은 `doc-commit` 스킬로 — 문서(변경이력·명세) 갱신 후 안전 점검을 거쳐 커밋한다.

**핵심 제약:**
- 목록의 단일 진실 공급원은 `data/*.json`이다. 페이지에 숫자를 손으로 쓰지 않는다.
- 페이지의 "○○ 기준" 스탬프는 `min(collected_on)`에서 계산한다. 확인하지 않은 항목의 날짜를 올리지 않는다.
- `_workspace/`는 삭제하지 않는다. 다음 갱신의 델타 기준이다.

**변경 이력:**
| 날짜 | 변경 내용 | 대상 | 사유 |
|------|----------|------|------|
| 2026-08-06 | 초기 구성 (에이전트 5 + 스킬 6) | 전체 | - |
| 2026-09-05 | **배포 게이트 도입** — 검사가 통과해야 라이브에 나간다 + index 원본 번들을 저장소에 | .github/workflows/pages.yml(게이트 잡·탈출구)·tools-ci.yml(D-F1 92 + 재빌드 바이트 비교), .gitignore(`!index.html.bak-20260806`), index.html.bak-20260806(4.1MB 추적) | 사용자 승인(구조적 과제 1·2). 종전에는 `push: branches:[main]` 하나로 **모든 커밋을 조건 없이 배포**했다 — tools-ci 가 빨개져도 그 커밋은 이미 라이브였고 tools-ci 머리말이 스스로 '이것은 사후 알림'이라 적고 있었다. **원본 번들부터 넣었다**: `build_index.py` 와 `verify_build.py` 가 둘 다 `index.html.bak-20260806` 을 읽는데 `.gitignore` 의 `*.bak-*` 가 막아 **신선한 클론에서는 index.html 을 재생성할 수도, 검증할 수도 없었다.** 대안(110KB 픽스처 + 재구성)은 sha256 게이트가 필요한 순환 의존을 만들고 다음 사람이 '정리'할 여지를 남겨(ADR-21 선례) 기각. **D-F1 92건이 '재빌드 산출물과 같음'을 보장하지 않는다**는 것을 신선 클론 변조 2종으로 실증 — 빌더 출력 문구만 고치고 재빌드 누락 / 데이터 내용만 고치고 재빌드 누락, 둘 다 D-F1 통과·**재빌드 바이트 비교만 EXIT=1**. 게이트는 `test_frontend_static.js` 하나로 한정(외부 의존 없음 — checkout+node 만 타 거짓 실패로 배포가 막힐 여지가 작다). 배치 테스트는 넣지 않았다 — 그 코드를 **실행하는 것은 00:30 서버 배치**이고 그 경로는 Actions 를 지나지 않아, 묶으면 초록의 뜻이 '배포된 것이 검사됐다'가 아니라 '관계없는 것도 검사했다'가 된다. 탈출구는 `workflow_dispatch` 의 `skip_checks`(사고 대응 전용·사유 명시). **라이브 3경로 실증**: 정상 커밋 → checks 10s·deploy 14s 성공 / 일부러 깨뜨린 커밋 → checks **실패·deploy 스킵·라이브 불변**(즉시 revert) / skip_checks=true → checks 스킵·deploy 성공. 부수: Pages 아티팩트에서 4.1MB 원본을 빼고(`rm -f`) 올린다 |
| 2026-09-05 | **merchants.html 구조 분리 3-a~3-e** — 2,638 → 1,545줄(−41%), 모듈 5개 신설 | merchants-split.js(스플리터 99)·merchants-colresize.js(컬럼 폭 55)·merchants-saved.js(즐겨찾기·최근 본·공유 76)·merchants-brandmodal.js(브랜드 팝업 112)·merchants-infowindow.js(지도 상세 팝업 148) 신설, merchants.html, test_frontend_static(106→144), test_saved_live·test_brandmodal_live·test_infowindow_live 신규 | 사용자 승인(구조적 과제 3). **원칙: 허브를 건드리지 않는 덩어리만 뗀다** — `state`(37개 함수가 읽고 12개가 쓴다)·`SNAP`·`refresh` 를 `window` 로 올리는 방향은 지금의 IIFE 캡슐화보다 나빠 기각. 각 모듈은 `attach({...})` 로 주입받고 **지도 손잡이는 반드시 게터**다(`initMap` 이 나중에 채우므로 값으로 붙잡으면 영구 null 인데 **에러가 나지 않는다**). 허브를 쓰는 자리는 콜백으로 밀어냈다 — 리사이저 `onReset`, 저장 `onOpenSpot`·`onChange`, 브랜드 팝업 `onPick`, 팝업 `onOpen`·`onPano`. **경계를 지킨 판단 3건**: ①저장 모듈이 지도를 모르게 했다(팝업 보호 플래그·상세 팝업은 지도 쪽 상태 — 알기 시작하면 다음 사람이 마커까지 옮긴다) ②브랜드 목록 조회는 팝업이 아니라 merchants 에 남겼다(API 모드와 JSON 폴백이 **같은 답**을 내야 하는 자리라 갈라지면 조용히 다른 숫자가 나온다) ③`popupOpenedAt` 을 복제하지 않고 `IW.openedAt()` 로 읽어 간다(2026-08-24 모바일 팝업 자멸 결함의 방어선이라 둘로 갈리면 안 된다). **JSON 순수 필터(43줄)는 건너뛰었다** — 주입이 6개 필요하고 헬퍼가 밖에서도 쓰여 이득보다 결합이 크다. **옮기다 걸린 것 2건**: 머리 주석을 3줄인데 4줄로 세어 `var FAV_KEY` 선언까지 잘라 라이브에서 표가 통째로 안 그려졌다(실동작 검증이 첫 실행에서 잡음) / 정적 검사가 **주석에 든 `COLR.widths()`** 에 매칭돼 게터를 값으로 바꿔도 통과했다(주석 제거본으로 검사). **내가 만든 테스트가 헐거웠던 것 4건**(전부 변조 실험이 적발): 팝업 배선을 같은 가맹점으로 재 이전 팝업이 남아 통과 / 안내 문구를 `/서울/` 로 재 고정 접두 '서울·인천·경기·부산'에 걸려 통과 / 색인 점프를 '스크롤이 줄었다'로 재 음수 클램프로 통과, 첫 초성으로 재면 정답과 우연히 겹쳐 또 통과 / 질의어 '삼성'이 0건인데 결함이 아니라 데이터에 없는 말. 검증 방식은 매 단계 동일 — **스타일 전수 대조**(`verify_styles.js guard HEAD`, 1,737개 요소 × 40속성, 전 단계 차이 0) + 전용 실동작 스크립트 + **변조 실험**(총 18종 전부 EXIT=1) + 전 스위트. 실동작 스크립트에 감시 시계(150~180초)를 넣었다 — 변조 한 종이 8분 넘게 매달려 '못 잡았다'와 '멈췄다'가 구분되지 않았다. 정적 검사는 **지우지 않고 겨냥만 옮겼다**(이관과 소실을 구분해야 한다 — verify_build.py 의 이관 무결성과 같은 원칙). 남은 1,545줄은 허브 자체(state·SNAP·refresh·render·renderChips·boot·bindControls·지도 렌더 루프)라 여기서 멈춘다 |
| 2026-09-05 | 컬럼 폭 손잡이가 절반만 잡히던 것 수정(기존 결함) | merchants.css?v=5(.col-grip right −5px→0, ::after 우측 정렬), test_frontend_static·test_frontend_render | 분리 3-b 검증 중 실측으로 드러났다(HEAD 워크트리와 대조해 이번 분리가 만든 것이 아님을 확인). 표 헤더의 폭 손잡이 10px 중 **왼쪽 4px 만 잡히고 오른쪽에서 누르면 아무 일도 없었다.** 원인은 `.col-grip { right:-5px }` 로 열 경계에 걸쳐 둔 것 — `thead th` 가 각각 `position:sticky; z-index:2` 라 **th 마다 스택 컨텍스트**가 생기고 뒤 열의 th 가 앞 열의 자식 전체(z-index:3 인 손잡이 포함)를 덮는다. 형제 스택 컨텍스트 사이에서는 자식의 z-index 가 소용없다. 실측: `elementFromPoint` 로 dx 1·3 은 `.col-grip`, dx 5 부터는 `TH`. 손잡이를 열 **안쪽**으로 들여 겹침 자체를 없앴다(표시선은 오른쪽 끝에 두어 경계에 그대로). 잡히는 폭 ~4px → 10px. 렌더 테스트가 이제 **가운데**(수정 전 죽은 자리)에서 드래그를 걸고 전 폭 히트테스트를 함께 잰다 |
| 2026-09-04 | **`#mapNote` 소유권 정리** — 거리뷰 전용 줄 `#streetNote` 신설 + 정렬 실패 안내를 `locNote` 로 (커밋 2개) | merchants.html(마크업·setStreetNote·attach·1826행)·merchants.css(.street-note)·merchants-pano.js(panoNoteSaved 삭제), _workspace/dev_scripts(verify_styles.js 식별 방식·test_frontend_static +11 → 89건·test_frontend_render 거리뷰 절 재작성 → 93건) | 사용자 요청("#mapNote 전용 줄로 고쳐줘"). 분리 2단계에서 "별도 커밋으로 미룬다"고 적어 둔 것. 조사·적대적 검증(에이전트 9)에서 **결함이 하나가 아니라 둘**임이 드러났고 셋 다 재현했다 — ①모드 중 지도가 재렌더하면 `viewportRender` 가 그 자리를 덮어 **거리뷰 안내가 사라진다**(확대·필터·드래그·리사이즈·'지도 범위' 버튼 다섯 경로. **파란 길을 찾으려면 확대는 반드시 하는 동작**이라 안내는 사실상 첫 조작에서 사라진다) ②이탈 시 되돌리는 저장본이 낡아 개별 마커 1,269곳 화면에 `29,728곳 클러스터 표시 중` 을 복원한다(목록 카운트와 어긋난 채 남는다). `#locNote` 와 같은 처방 — `#streetNote` 전용 줄 + `panoNoteSaved` 통째 삭제(재진입 멱등을 지키던 가드도 함께 불필요해졌다). **소유권은 merchants.html 이 갖는다** — 거리뷰는 `setStreetNote(on)` 만 부르고 문장도 자리도 모른다(`el("streetNote")` 직접 잡기는 결합을 끊는 게 아니라 id 만 바꿔 남기는 것이라 기각). locNote 와 달리 자동으로 사라지지 않는다(모드 표시라 증발하면 설명이 없어진다). **2커밋: 정렬 실패 안내도 이관** — '가까운 순'이 위치 획득에 실패할 때 그 오류를 mapNote 에 직접 쓰고 있었다(2026-09-04 showMyLocation 이관 때 이 한 곳만 남았다). `setLocNote(…, true)` 로 옮겨 **재렌더 후에도 살아남는다**. 결과 **mapNote 참조가 셋만** — renderMap·viewportRender·인증실패, 전부 지도 자신에 관한 문장이다(정적 테스트가 숫자로 고정). **적대적 검증이 설계 오류 5건을 잡았다** — 특히 ⓐ파일 머리 이관 주석이 이번 커밋으로 낡는 것(`grep mapNote` 가 0 이 안 된다) ⓑ"pano 가 지도 구획 DOM 을 모르게 된다"는 거짓(`#streetBtn` 결합 잔존) ⓒ**재렌더 신호로 '확대'가 부적합**(이미 개별 마커인 화면에서는 안내가 안 바뀌어 재렌더를 확증하지 못한다 — 실제로 이 검사가 처음에 실패했다 → 업종 칩 클릭으로 교체). **도구도 한 번 고쳤다** — 숨은 줄 하나를 끼워 넣은 것만으로 `verify_styles` 차이 8건이 떴고 전부 거짓 경보였다(2단계 `<script>` 건과 같은 유형) → **id 가 있는 요소는 형제 인덱스를 붙이지 않게**. 검증: 스타일·기하 전수 대조 차이 **새 요소 1건뿐**(기본값 hidden 이라 평소 화면 1px 무변화), 결함 ①② 실측 해소, 색 대비 4.63:1(AA 통과), 정적 89·렌더 93·배치 314·D-F1, 변조 실험 2건 EXIT=1 |
| 2026-09-04 | 서버 반영 — 배치 클론이 하루 동안 야간 작업분을 놓치고 있던 것 동기화 + DEPLOY.md 에 반영 시차 명시 | 서버 `~/onnuri_batch/repo`·`~/digital_onnuri` 동기화(b99e0d0 → c47897a), backend/DEPLOY.md(야간 배치 절) | 사용자 요청("서버에도 배포하자"). **앱은 이미 최신이었다** — 마지막 CD(6eca052) 이후 `backend/**` 변경이 0이라 재빌드하지 않았다(컨테이너를 흔들 이유가 없다). 정작 어긋나 있던 것은 **배치 클론**이었다 — 09-03 19:21 상태(`b99e0d0`)에 멈춰 있었다. 원인은 결함이 아니라 **시차**다: 00:30 배치는 정상적으로 `git pull` 했지만 **그 시점 원격 HEAD 가 아직 야간 작업분 이전**이었다(커밋은 22:15부터였으나 푸시가 그 뒤였다). 그 결과 배치가 두 가지를 하루 동안 놓쳤다 — `acacaf9` **저장소↔DB 드리프트 감시**(그 감시를 만든 목적 자체가 '새 몰이 DB 에만 들어가도 아무도 모르는' 상황을 막는 것인데 정작 그 코드가 배치에 없었다)와 `0b12e7d` **권율로의 note**(큐레이션 동기화가 읽는 값이라 DB 에 반영될 길이 없었다). 조치: `run.sh` 와 같은 순서(산출물 `git checkout --` → `pull --ff-only`)로 배치 클론 동기화, 앱 클론도 동기화(재빌드 없음), 단계 B 시험 실행 → `저장소↔DB 목록 일치(31곳)` · `큐레이션 동기화 1건 갱신`. **DEPLOY.md 에 시차를 명문화** — "배치는 00:30 시점의 origin/main 을 본다. CD 와 달리 '지금 반영' 경로가 없다"와 손으로 당기는 절차. 부수: 단독 실행 시 인증 실패를 겪고 DEPLOY.md 를 의심했으나 **문서가 맞았다** — 363행이 `DB_NAME`/`DB_USER`/`DB_PASSWORD` 로 정확했고 내가 읽지 않고 `POSTGRES_*` 를 지어낸 탓이었다(문서 명령을 서버에서 그대로 실행해 접속 OK·`online_platform` 31행 확인). 검증: 라이브 API 활성 31곳·권율로 note 반영·조회 18곳, 화면 실측(온라인 '앱 전용' 노출 / 가맹점 `OnnuriPano` 로드·목록 50행·카드 라운딩 10px·오류 0) |
| 2026-09-04 | **merchants.html 분리 1·2단계** — CSS 412줄 + 거리뷰 328줄 외부화 (2,638 → **1,910줄**, −27.6%) | merchants.css(신규)·merchants-pano.js(신규)·merchants.html, _workspace/dev_scripts(test_frontend_static +14 → 80건, test_frontend_render +10 → 89건) | 사용자 지시("작업을 최대한 잘게 쪼개되 매 커밋마다 관련 기능 전수 테스트, 문제가 있으면 원복하고 원인 해결 후 다음 과제"). C-1 리뷰 보고서의 권고(CSS 먼저)를 따랐다. **둘 다 순수 이동** — 한 줄도 고치지 않았고 커밋 하나씩이라 각각 독립적으로 되돌릴 수 있다. **①CSS**: `<style>` 을 순서 그대로 옮기고 `<link>` 도 원래 자리(shell.css 다음)에 넣어 캐스케이드 유지 — 거리뷰 패널이 SDK 인라인 스타일을 `!important` 로 이기는 규칙이 있어 순서가 바뀌면 조용히 깨진다. 관심사별 정리는 하지 않았다(잘라 옮기기와 정리를 한 커밋에 섞으면 회귀 원인을 못 가린다). **②거리뷰**: 경계면 실측 — 밖→안 4곳(openPano·toggleStreetMode·closePano·initPanoFloat), 안→밖 5종 26회, 독점 전역 10개. **`mapObj`·`mapReady` 를 값이 아니라 게터로 주입** — initMap 이 나중에 채우므로 값으로 붙잡으면 영영 null 이고 **에러 없이 아무 일도 안 일어난다**(가장 조용한 실패 모드). **검증 도구 신설**: 캐스케이드가 통째로 걸린 변경이라 기능 테스트로는 부족해, PC 1440·모바일 390 두 뷰포트에서 렌더된 **모든 요소의 기하 + 계산된 속성 43종**을 찍어 대조하는 스냅샷 도구를 먼저 만들고 **잡음을 먼저 쟀다**(같은 코드 두 번 → 차이 0). 두 단계 모두 **1,737개 요소 대조에서 차이 0**. **중간에 한 번 멈췄다** — 2단계 첫 대조의 '차이 51건'이 전부 거짓 경보였다(`<script>` 가 하나 늘어 형제 인덱스가 밀린 것). 도구에서 비렌더 요소를 빼고 인덱스도 그것들을 뺀 뒤 세도록 고친 뒤, 기준선을 **이전 커밋의 워크트리에서 다시 찍어** 재대조해 차이 0 확인 후 진행. **영구 계약 15건 신설** + 변조 실험 3건 전부 EXIT=1(링크 제거·게터를 값으로·submodules 제거). **여기서 멈췄다** — 남은 구획(브랜드 팝업 `state` 16회, 즐겨찾기 `KEEP_INFO_ONCE` 쓰기, 데이터 소스 `state` 32회·`SNAP` 쓰기·`refresh` 허브)은 전부 순수 이동이 아니고, 떼려면 `state` 를 전역으로 올려야 하는데 그건 지금의 IIFE 캡슐화보다 나빠지는 방향이다(ADR-10 이 유예한 근거). 검증: 스타일 전수 차이 0 · 정적 80 · 렌더 89(거리뷰 9건 포함) · 채록 158 · 색인 58 · robots 71 · 카나리아 27 · D-F1, tools-ci·Pages 성공, 라이브 실측(두 자산 200·`OnnuriPano` 로드·목록 50행·오류 0) |
| 2026-09-04 | **야간 자동 수정 24건 + C 트랙**(그룹 팝업 개별 조회 · 도구 CI · 프론트 자동 테스트 144건 · 분리 리뷰) | merchants.html·online.html·online-probe.js·online-source.js·payment.html·terms.html·build_index.py·index.html, data(online_platforms·offline_categories·brand_aliases), backend(IndexJudge·IndexHit·NewsService·ChatService·OnlineProductIndexRepository), chat-widget.js, .github/workflows(tools-ci 신설·backend-ci `data/**`), _workspace/dev_scripts(test_frontend_static·test_frontend_render 신설·test_survey_probe 게이트·verify_build 파생), .claude/skills/dev-testing, _workspace/21_night_audit_fixes.md(신규) | 사용자 지시("실제와 UI 코멘트가 다른 부분은 승인 없이 수정하되 되돌릴 수 있게 커밋을 쪼개고 기록") + 아침 결정(A·B 현행유지 / C-1 리뷰만 / C-2·3·4 진행). **야간 24건**(각 1커밋): 결제 안 되는 1,689곳을 '앱 결제 미확인'이라 부르던 것, 소득공제 제외 대상(골목형상점가 등) 누락, `brand=삼성` 착지 0곳, 실측 스탬프가 20곳을 가장 오래된 하루로 뭉개 19곳을 11일 낡게 표기, 뉴스가 '최신순'이라 적고 정렬 코드 부재, 색인이 상품명을 보여 주며 몰 홈으로만 보냄, 챗이 자동모드에서 없는 카드를 가리킴, index 온라인 30곳으로 낡음, **'전일 색인'이 매일 틀린 말**(배치는 당일 00:30), 폴백 중에도 '매일 자동 갱신' 표기, 링크 라벨이 검색 실행 여부를 구분 안 함(3곳은 눌러도 검색어가 사라진다), '내 위치' 안내가 자기가 부른 재렌더에 덮여 오차 반경이 사라짐, '전국 이용 가능만'이 미확인을 단정 등. **C-4**: 그룹 팝업 항목을 누르면 개별 팝업으로 바뀌고 **그때만** 최근 본에 남는다 — `recordRecent` 호출부를 `openInfo` 한 곳으로 유지하는 것이 계약이라 기록 코드를 새로 쓰지 않았다. 함께 적발 — 리스트 행 클릭이 `card`·`qr` 없는 **합성 객체**를 넘겨 표엔 '카드 QR'인 가맹점을 팝업이 '디지털 불가'라 말했고(2026-09-03 창구화가 절반만 됐다 — 표가 사본 보유), `id` 부재로 같은 곳이 최근 본 두 칸을 차지했다. **C-3**: `tools-ci` 신설(380건) — **게이트가 아니라 사후 알림**임을 머리말에 명시(pages.yml 은 조건 없이 배포, 배치는 CI 를 묻지 않고 `git pull`). 선행으로 `test_survey_probe.js` 게이트가 **파일 중간**에 있어 마지막 12건(코드↔데이터 사본 일치)이 실패해도 EXIT=0 이던 것 수정. `verify_build.py` 는 gitignore 된 `.bak` 를 열어 CI 불가라 제외. **C-2**: 정적 66 + 렌더 78 = **144건** 신설, 케이스는 전부 이력의 실제 결함에서 파생. **오래된 오해 정정** — "네이버 도메인 제한으로 로컬에서 지도가 안 뜬다"는 **틀렸다**(포트 8655 는 인증 통과·타일·마커·팝업 전부 동작. `.cluster` 를 안 세서 생긴 오진 — drawPins 는 MarkerClustering 에 넘긴다). 이 전제로 지도 검증을 늘 배포 후로 미뤄 왔다. **C-1**: 분리 리뷰 보고서만(구현 안 함) — 구획별 크기×결합×**변경빈도**를 재니 조사의 권고(거리뷰)가 뒤집혔다. 거리뷰는 10커밋 전부 08-13~08-24 에 몰려 **11일째 정지**, 반면 CSS 는 46커밋(어제까지)인데 결합 0 → **CSS 414줄을 1차 대상**으로 권고. 분리가 실제로 줄이는 결함은 27건 중 5건이고 그중 3건이 CSS 계열. 검증: gradlew 201 · tools-ci 380(GitHub 러너 실측) · 프론트 144 · D-F1 통과, 배포 후 라이브 실측 |
| 2026-09-03 | 온라인 각주에서 내부 경로·중복 문장 제거 | online.html(footMethod), 03_content_spec(S19 원칙) | 사용자 요청(캡처 표시). 세 조각을 뺐다 — ①`(조사 로그: 저장소 _workspace/15_online_catalog_report.md)` **저장소 경로가 이용자 화면에 그대로 나가고 있었다**(내부 구조 노출이자 이용자에게 뜻이 없다) ②`다만 N곳은 그날 확인하지 못해 … 날짜는 올리지 않습니다` — **바로 위 meta-line 이 이미 `20곳은 2026-08-10 확인분` 을 말한다**(중복) ③`이후 각 몰의 취급 변화는 반영되지 않을 수 있습니다` — 실측 날짜가 그 자리에 남아 있고 결과 0곳일 때는 빈 결과 안내가 같은 말을 한다. 남은 각주는 한 문장(`※ 취급 품목·브랜드 태그는 각 몰의 카테고리 메뉴·기획전·브랜드관을 YYYY-MM-DD 에 실측해 기록한 것입니다`) + AI 면책. `META.behindCount`·`behindOldest` 계산은 meta-line 이 계속 쓰므로 남겼다. 검증: 렌더 실측 PC 1440·모바일 390 — 각주에서 세 조각 잔존 0, **meta-line 의 `20곳은 2026-08-10 확인분` 은 그대로**(정보 손실 없음), 가로스크롤 없음·pageerror 0 |
| 2026-09-03 | 카나리아 무작위 질의 결함 수정 + 재시도 추세 감시 — **고정 접두 `zq` 가 한 몰에서 386건을 물고 있었다** | backend SelfTestService·SelfTestCase·ChatContract 테스트(총 197), backend/tools/nightly_update.py(추세 감시)·DEPLOY.md, _workspace/dev_scripts/test_canary_trend.py(신규 27건) | ADR-21 배포 후 라이브 카나리아에서 **실패 1건** — `onnuri-paldo-sijang [absent] 기대=none 실제=unclear`. **판정은 옳게 동작했고 질의어가 문제였다.** 직접 조회해 원인 확인: 전날 인기검색어 오염을 없애려 넣은 무작위 질의의 **고정 접두 `zq` 가 팔도시장에서 386건을 무는 생산적 접두어**였다(`zq` 386 · `zqh` 11 · 그 회차 질의 `zqhaerxqaq` **7건**·상품 링크 14 · 반면 `zqbcdfghjk`·`zqaaaaaaaa`·옛 고정 질의 `zzqqxyw12345` 는 0). 숫자를 뺀 판단은 옳았으나(상품코드·수량 회피) **접두를 고정한 탓에 한 몰에서 걸리면 매일 걸리는 구조**가 됐다. 조치 ①**접두 제거** — 10자 전부 무작위(무작위 8건을 팔도시장에 실제 조회해 **전부 0건** 확인, 첫 글자 15종·앞 두 글자 100종 이상 흩어짐을 테스트로) ②**걸리면 새 말로 한 번 재시도**, 두 번 다 실패할 때만 FAIL — 한 낱말이 우연히 걸릴 수는 있어도 서로 다른 두 낱말이 연달아 걸릴 확률은 훨씬 낮아 **우연한 충돌과 규칙 파손이 갈린다**. 평시 요청 증가 없음(실패 건에서만 +1), **기대치 없는 몰(찬스·롯데ON)은 재시도하지 않는다**(다시 물어도 판단이 안 달라지는데 헛요청만 2건). ③**재시도가 FAIL 을 가리는 것**을 막기 위해 배치가 추세를 본다 — 최근 7회차 중 3번 이상이면 WARN(우연 확률 5% 가정 시 0.4% 수준), 회차 3개 미만이면 판단 보류(표본 없이 추세를 말하면 새 서버 첫 주에 거짓 경고), 추세 전에도 `아직 추세 아님(알림 기준 3회)` 로 남긴다. 리포트에 `window`·`alertMin`·`countedBy` 를 함께 남겨 **기준을 바꿔도 옛 리포트와 구분**되게. 자동 비활성화 없음. 한계: 추세 판별이 note **문구**에 기대므로 문구가 바뀌면 조용히 0건이 된다 → 앱이 `cases[].retried` 불리언을 주면 그쪽 우선(지금 넣어도 안 깨짐)·실제 문구를 테스트에 박아 **문구가 바뀌면 테스트가 먼저 깨지게**·`countedBy` 를 리포트에 노출. 검증: gradlew 197·robots 71·추세 27·색인 58·채록 154. **배포 후 라이브(2026-09-03 14:10)**: CD 성공, 서버 단계 E 1회전 — `robots 조회 대상 18곳(앱 판정) — 차단 5곳(첫 회차)` + 차단 목록 5곳 명시 + 리포트 저장, `거친 판정 0곳 · 단계 F 스킵으로 3곳 확인 안 함`(층별 관측이 실제로 동작), **카나리아 통과 34 / 기대치없음 2 / 실패 0**(전날 팔도시장 실패 해소, 재시도 note 0건 — 무작위 질의가 이번 회차에 하나도 걸리지 않았다). 셀프테스트 계약도 라이브 확인 — `probeEndpoints` 18·`robots` 18·`robotsUserAgent: onnuri-guide`·관측 실패 0, 조회 호스트가 11번가 `apis.11st.co.kr`·5일장 `api.samaint.co.kr`·롯데ON `www.lotteon.com` 으로 **이용자 링크 호스트가 아닌 실제 조회 호스트**를 본다. crawler 가 단계 E·F 의 셀프테스트 호출을 `main` 에서 한 번만 받게 해 몰에 나가는 조회가 두 배가 되지 않는다(로그상 공유 조회 35초, E 자체는 0.5초) |
| 2026-09-03 | **robots 감시를 "우리가 두드리는 경로가 허용되는가"로 재설계 (ADR-21)** — 관측점·판정이 둘 다 어긋나 있던 것 | backend probe(RobotsRules·ProbeEndpoint·RobotsCheck 신규, SelfTestReport +3필드·SelfTestService·ProbeFetcher)·테스트(RobotsRulesTest 15·SelfTestContract +5, 총 191), backend/tools/nightly_update.py(감시 전면 재작성)·index_nightly.js(--print-hosts·색인 가드)·DEPLOY.md, _workspace/dev_scripts/test_robots_watch.py(신규 71건), 16(ADR-21) | ADR-19 가 robots 를 대상 선정 기준에서 빼며 내건 균형점이 "정책이 강해지면 즉시 끈다"인데 **그 관측이 두 군데 어긋나 있었다**. ①**관측점** — 대상 호스트를 이용자 링크(`search_url_template or url`)에서 뽑아 **우리가 실제로 두드리는 곳을 안 봤다**(11번가 감시 `search.11st.co.kr` ↔ 조회 `apis.11st.co.kr` / 5일장 본몰 ↔ `api.samaint.co.kr` / 롯데ON 단축주소 301 루프 ↔ `www.lotteon.com`) ②**판정** — `Disallow: /` 한 줄만 봐 경로별 Allow 를 오독(11번가 `Allow: /plan/front/` 를 전면 차단으로). **판정 파서를 앱에 둔다**(배치에 두면 우리가 두드리는 경로를 손으로 옮겨 적게 되고 2026-08-31 굿데이 도메인 오기가 형태만 바꿔 되살아난다) — UA 그룹 선택·최장일치·동률 Allow 우선, 셀프테스트가 `probeEndpoints`·`robotsUserAgent`·`robots[]` 를 싣고 **배치는 파서 없이 결과를 옮긴다**. **오늘 기준선: 차단 5곳/18곳**(굿데이·인더마켓·현대홈쇼핑·롯데ON `Disallow: /` · 공영쇼핑 `Disallow: /search/` — 전부 편입 당시 알고 결정한 곳) — **목록이 달라진 날만 WARN**(매일 5건이 뜨면 사람이 통째로 무시한다), 근거 규칙이 바뀌는 것도 신호, `error` 는 차단 집계와 분리(모르는 것을 차단으로도 허용으로도 세지 않는다). **함정 3건**: ①경로만 실었더니 굿데이·인더마켓이 **허용으로 뒤집혔다** — 두 몰은 조회 주소의 경로가 `/` 뿐이고 검색 조건이 전부 쿼리에 있어 그 몰들의 `Allow: /$` 에 정확히 걸린다(쿼리를 붙이자 차단). `ProbeEndpoint` 주석이 "쿼리를 버린다"로 남아 있던 것을 dev-qa 가 적발 — **주석을 믿고 정리하면 감시가 허용 방향으로 조용히 망가진다** ②배치 스캐너가 **404 가 아닌 HTTP 오류도 '금지 없음'으로 적고 있었다**(301 루프·403 이 허용으로 남는다) → 404·410 만 파일 없음, 나머지는 관측 실패. 앱도 410 을 맞춤(401·403 은 RFC 가 허용해도 **막는다는 신호**라 파일 없음으로 보지 않는다) ③배치 파일에 헬퍼 블록이 **통째로 두 벌** 들어가 뒤 사본이 앞을 가려 단계 E 가 죽어 있었다 — 함수는 각각 멀쩡하고 순수 테스트도 통과해 **실행해 보고서야 드러났다**(DEPLOY.md 에 5경로 실행 주의 명문화). **손으로 적는 목록을 전부 없앴다** — `ROBOTS_WATCH_IDS`·`ROBOTS_BLOCKED_AT_SURVEY` 삭제(어제 리포트 비교로 대체), 폴백 목록도 **어제 리포트의 조회 대상**에서 파생(첫 실행엔 아무것도 관측하지 않고 사유를 남긴다 — 손으로 적은 목록보다 정직하다). **층별 원칙: 관측은 접촉을 따라간다** — 킬스위치는 실시간 층만 끄므로 색인 층은 단계 F 가 도는 회차에 관측한다(전면 적용하면 두드리면서 정책은 안 보는 상태가 된다 — crawler 가 내 지시의 허점을 잡았다). 사유 3종(`kill-switch-off`·`app-unreachable`·`no-admin-key`)과 **"변화 없음이 아니라 확인하지 않았다"** 표기. **부수 가드 2건**: 앱이 판정한 호스트를 배치가 다시 가져오지 않게(겹침 0 이 우연이 아니라 성질이 되게), **실시간 대상이 된 몰은 색인 크롤을 건너뛴다** — 겹치면 robots 두 번보다 **수십~수백 페이지 크롤이 통째로 헛일**이 되는 것이 크다(IndexJudge 가 그 행을 읽지 않는데 로그는 정상으로 찍혀 조용하다). 단계 E·F 가 셀프테스트를 각각 부르면 몰에 나가는 조회가 두 배가 되므로 `main` 이 한 번만 받아 넘긴다. 검증: gradlew 191·robots 배치 71·색인 58·채록 154, dev-qa 6차 **통과**(파서를 따로 컴파일해 자체 케이스 25건 + 18호스트 직접 판정으로 **차단 5곳 일치**, `urlopen` 가로채 층별 아웃바운드 실측 — 평시 **하루 57건**·중복 0·둘 다 꺼지면 0건) |
| 2026-09-03 | 롯데ON 실시간 편입(→**18곳**) — 전체 검색의 '온누리상품권 사용' 필터, 단 **없다고 말하지 않는 몰**로 | backend ProbeTargets(T_LOTTE·EXCLUSION −1·EX_SCOPE_MIXED 상수 삭제)·픽스처 2개·테스트(171), online-probe.js?v=12(사유 3종)·03_content_spec, index_nightly.js(롯데ON 레시피 삭제 → 색인 2곳)·DEPLOY.md, 19_online_probe(6-10-2) | 사용자 지적("키워드로 검색한 다음 필터 카테고리 '상품' 안에 온누리상품권 사용 필터가 있는 것 같다") — **맞았다.** 6-10 절이 범위 오염으로 접었던 것은 **전용관 안의 검색만 보고 전체 검색의 필터를 열어 보지 않은** 탓(같은 날 11번가·공영쇼핑에 이어 세 번째 같은 오판). 좌측 14개 필터 그룹 중 '상품' 그룹의 `input[name=promotion][value=onnuri]` → 화면이 `u31=onnuri` 를 보낸다. **적대적 검증 3건 중 1건 CONFIRMED·2건 REFUTED 이고 REFUTED 둘이 설계를 정했다.** ①**조회 CONFIRMED** — `csearch/search/search?...u31=onnuri...&q={q}`, **필수 헤더 0개**(UA·쿠키·Referer 불필요), 브라우저 응답과 `cmp` 바이트 동일(114,923B), 김치 1,166·로봇청소기 31·0.6초·12건 버스트 무차단. 범위는 김치 **1,960행을 꼬리 페이지까지 전수** 확인해 반례 0(전부 `additionalFlagList type=onnuri`), 무필터는 60건 중 15건뿐, 형제값 대조(official 146·new_pd 15)로 필터 실작동 확인 — 이 저장소가 두 번 걸린 '상위 N건만 보고 범위 판단' 함정을 검증자가 지적해 전수로 다시 잰 것 ②**'없음' 확정 REFUTED** — 온누리 0건이면 130B `{"itemList":[],"total":0}` 인데 **존재하지 않는 필터값 `u31=onnuriZZ` 응답이 진짜 부재와 바이트·md5 완전 동일**(c43847ce…). 130B 는 '없다'와 '필터가 깨졌다'를 구분 못 해, 롯데ON 이 파라미터를 바꾸는 날 **모든 질의가 조용히 없음**이 된다(ADR-17 이 가장 경계한 방향). 0바이트 갈래는 무의미어·`q` 누락·빈 값이 md5 까지 같고 현실 다어절 질의 8건 중 2건이 실제 0바이트 → **온누리찬스와 같은 등급 C**(없음-문구 없음 + `echoesQuery=true` 정책 선언)로 **never none**, likely/unclear 만. `canDecideAbsent` false 라 카나리아 absent 대조에서 빠지고 **present 기대치(김치→likely+샘플)가 필터 파손의 유일한 탐지 수단** ③**이용자 링크 REFUTED** — `promo=onnuri` 는 **모바일 UA 첫 화면에서만** 먹고 PC 는 **302 로 필터 증발**, 모바일도 첫 스크롤에 풀린다(김치 온누리 100%→29%, 페이징 7건 전부 필터 인자 없음). 세 실패가 **전부 조용해** 이용자가 온누리 링크인 줄 알고 결제 안 되는 상품을 본다 → `search_url_template` 비운 채 상생스토어 url 로(공영쇼핑과 같은 처리). 부수: titlePattern 은 `"pdName"` 매치가 itemList 와 정확히 일치해 **앵커 불필요**(11번가와 다름), 픽스처 none 은 무의미어가 0바이트라 쓸 수 없어 **샤넬**(130B)로, `scope-mixed` 는 붙는 몰이 없어져 상수째 삭제(사라진 네 번째 사유), 색인 층은 3→2곳(놀장·인어교주). 조회 호스트가 몰 본체(`www.lotteon.com`, `Disallow: /`)라 11번가(apis.11st.co.kr)와 구조가 다른 점 기록. 카나리아 34→36건/일. **배포 후 라이브(2026-09-03)**: CD 성공, 조회 18곳(로봇청소기 likely 15·none 2·미확인 4곳 1/2/1), 롯데ON **likely 74·샘플 3**·링크는 상생스토어, 카나리아 **36건 실패 0**(스킵 2 = 찬스·롯데ON absent, 설계대로 — 롯데ON absent 는 0바이트라 unknown), 11번가 present 835KB·공영 9.4KB 정상, 서버 색인 정리 후 2곳(놀장 295·인어교주 3,159) |
| 2026-09-03 | 네이버 오픈 API 경로 폐기(20절 A10) — 쇼핑 검색 API 가 2026-07-31 종료·대체 없음 | _workspace/20_probe_expansion_analysis.md(6절 신설·A10 폐기 표시), backend/tools/naver_shop_check.py(변수명), 데이터·코드 변경 없음 | 사용자가 키를 발급해 .env 에 넣어 확인 요청. **경로 자체가 사라졌다** — 공지 32530·32564 원문: 검색 API 중 '쇼핑·책·전문자료'는 NAVER API HUB 이관 제외이며 2026-07-31 종료, **유예 없이 기존 키도 호출 불가, 대체 API 미제공**. API HUB 제공 검색 10종(블로그·뉴스·백과·카페글·지식iN·웹문서·이미지·지역·성인검색어·오타변환)에 쇼핑 없음(NCP 상품 표·문서 색인 23건 전수·후속 공지 부재로 교차 확인). **쇼핑 인사이트는 대체가 아니다** — 응답이 분야/키워드별 클릭 추이의 **상댓값**(구간 최대=100)뿐이고 **판매처 차원 자체가 없다**(공식 문서 필드 확인). **키 진단**: 소문자·숫자 10자 + 40자 시크릿은 **NCP API Gateway 키**로 헤더가 `X-NCP-APIGW-API-KEY-ID`/`X-NCP-APIGW-API-KEY` — 개발자센터 주소(`X-Naver-Client-Id`)에 넣으면 무조건 401. 실측으로 키 생존 확인: 지오코딩 401 **errorCode 210**(인증 통과·미구독) / `naverapihub.apigw.ntruss.com/search/v1/webkr` **200**(웹문서 검색 동작) / blog 는 앱 미활성. 웹문서로 `site:` 한정이 되고 굿데이·팔도가 색인돼 있으나(65건) **돌아오는 것이 상품 상세가 아니라 목록 페이지**라 상품명을 못 얻고, 그 세 몰은 ADR-19 로 **이미 실시간 조회 대상**이라 답할 몫이 없다. 함께 확인: 종료된 shop.json 과 살아 있는 blog.json 이 **비인증 시 동일한 401/024** 를 반환해 응답만으로는 종료를 판별할 수 없다(인증 게이트웨이 공통). 사용자 결정: 네이버 접음 |
| 2026-09-03 | 브랜드 팝업 안내 문구 — 팝업 숫자(전 지역 합산)와 선택 후 좁혀지는 범위의 관계를 문장에 명시 + 네이버쇼핑 입점 확인 스크립트 신설 | merchants.html(#bmScopeHint 동적 문구·openBrandModal), backend/tools/naver_shop_check.py(신규) | 사용자 질문("오프라인 브랜드 검색에서 현재 지역·지도범위가 실제로 적용되나? 안내문구가 혼란"). 코드 확인: 팝업의 매장 수는 **전 지역(서울·인천·경기·부산) 합산**이고 지역·지도 범위는 집계에 반영되지 않는다(2026-08-10 전 지역화 결정 그대로) — 브랜드를 고른 뒤에야 현재 지역 목록으로 좁혀진다. 종전 문구 "현재 선택한 지역·지도 범위에 없는 브랜드는 0곳" 이 그 범위가 팝업에 반영되는 것처럼 읽혔다. 수정: 열 때마다 **지금 좁혀질 범위**를 문장에 그대로 싣는다 — `숫자는 서울·인천·경기·부산 전체 매장 수입니다. 브랜드를 고르면 서울 강남구의 목록으로 좁혀지고, 그 범위에 없는 브랜드는 0곳이 됩니다.`(지도 범위 모드면 '지금 보고 있는 지도 범위'). 실측: 서울 → '서울', 강남구 선택 → '서울 강남구', pageerror 0. 대안(현재 지역 매장 수 병기)은 팝업 항목마다 지역 집계가 필요해 별건. **네이버쇼핑**: 웹 검색이 봇 차단(418)이라 공식 검색 API 키 필요 — 스크립트는 흔한 낱말로 조회해 mallName 을 우리 쇼핑몰 이름과 대조. 키는 서버 `backend/deploy/.env` 의 `NAVER_API_KEY`/`NAVER_API_SECRET`(저장소·채팅 비노출), `--env` 로 읽는다 |
| 2026-09-03 | 11번가·공영쇼핑 실시간 편입(→**17곳**) — 전체 검색의 **온누리 필터**로 범위 지정 (사용자 지적으로 2차 재조사 판정 뒤집힘) | backend ProbeTargets(+2)·ProbeJudge(API 몰 한정 원문 대조)·링크 계약(template 또는 url)·픽스처 4개·테스트(162), data/online_platforms.json(11번가 필터 검색 링크 +1 → 16곳)·config.js dataVersion 2026-09-03.2, index_nightly.js(11번가·공영 레시피 삭제 → 색인 3곳)·test_index_nightly(68)·DEPLOY.md, 19_online_probe(6-10-1)·16(ADR-19 3차 정정)·17_backend_notes | 사용자 질문("11번가와 롯데ON 은 상세검색에 필터가 있지 않나") — **맞았다.** 세 몰 모두 전체 검색의 상세 필터에 온누리 전용 항목이 있었다: 11번가 `filters=ONNURI`(결제 가능 속성 배지, 김치 278,339→938, **링크에도 실려** 조회·링크 모두 범위 일치) · 공영쇼핑 `benefit=trdit_mrkt_goods_yn`(김치 2,733→184, 기획전 표본 20/20 이 Y — 어제의 "40건 중 4건"은 정렬 상위 40건만 본 착시) · 롯데ON `u31=onnuri`(조회는 정확히 좁혀짐). 2차 조사가 틀린 이유는 하나 — **전용관 안의 검색만 보고 전체 검색의 필터를 열어 보지 않았다**(전용관=범위, 전체 검색=도구, 도구에 범위 필터가 있으면 둘이 만난다). **롯데ON 은 보류**: ①필터를 주소에 못 실어 링크가 몰 전체 검색 ②결과 없음이 `content-length: 0` 이라 '없다' 확정에 "빈 본문=없음"을 열어야 하는데 그건 오류 페이지 가드를 무르는 일(있는 것을 없다고 말하는 위험) — 색인 층(668건)으로 답한다. **판정 엔진 수정**: 공영쇼핑은 XML 이라 toText 후 `<rsltYn>N</rsltYn>` 이 홑글자 `N` 만 남아 문구 대조가 불가 → API 몰에 한해 **원문 대조** 경로 신설(HTML 산문에서 태그로 쪼개진 문구를 잡으려던 규칙이 구조화 응답에선 정반대로 작용). 11번가 `"totalCount":0` 은 넣지 않음 — 면(facet)별 카운트라 한 면이 0이면 있는 상품을 없다고 한다(가장 위험한 방향). titlePattern 앵커 `"soldOut":…,"title"` 필수(빼면 SEO 제목 `김치 - 11번가 추천` 이 샘플로), noiseFloor 9(없는 질의 4종 전부 9회). 비대상 **5곳**(bot-blocked 1 / scope-first 2 / scope-mixed 1 / no-static-search 1), 카나리아 34건/일, 색인 3곳(놀장·인어교주·롯데ON). 남은 불확실성: 공영쇼핑 `trdit_mrkt_goods_yn` 이 결제 가능 속성임을 상품 화면에서 직접 확인하진 못함(몰이 '온누리'로 부름·기획전 전량 포함이 근거). 교훈: **"전용관 안에 검색이 없다"는 "범위 검색이 불가능하다"가 아니다** — 전체 검색의 필터 목록을 먼저 열거한다. **배포 후 라이브(2026-09-03)**: CD 성공, 조회 17곳(11번가 김치 271건·필터 링크, 공영 56건·기획전 링크), 사유 1/2/1/1, 색인 3곳(서버 2곳 정리 SQL·단계 B 동기화 1건), 카나리아 34건 실패 0. **카나리아 note 2건 → 카나리아 자체의 결함**: `SelfTestService` 가 에코를 `toText`(제목·메타 포함)로 재고 판정은 `stripEcho` 본문에서 세므로 굿데이·인더마켓의 선언 false 가 실측과 어긋나 보였다 — 지시(true 로 뒤집기)를 backend 가 실측으로 반박했고 맞았다(뒤집으면 '없다' 확정 수단을 근거 없이 버린다). 카나리아를 판정과 같은 기준으로 고치자 핫딜(등급 A → false 로 정정)·찬스(등급 C, 정책 선언 true 유지)가 드러났고, 찬스는 대조 제외 조건을 "등급 C" 가 아니라 **`canDecideAbsent`** 로 — 등급으로 끄면 등급 C 이면서 `echoesQuery=false` 가 곧 '없다'의 근거인 온누리마켓의 안전망까지 꺼진다. dev-qa 4차 F-1: 단계 E robots 감시가 17곳 중 9곳만 덮던 것을 전수(19곳)로, 기준선 2026-09-03 실측, 요약 로그 한 줄 추가(침묵이 정상인지 미실행인지 구분). **카나리아 부작용 적발**: 굿데이 인기 검색어 블록(`og_pop_item` 2위)과 자동완성에 우리 없는-말 `zzqqxyw12345` 가 올라가 있었다 — 매일 같은 말을 보내 그 몰이 인기 검색어로 집계, 상대 이용자에게 노출. absent 질의를 회차마다 무작위(`zq`+소문자 8자)로 바꾸니 히트 0 → 굿데이 선언 false 유지(우리가 만든 부작용을 근거로 확정 수단을 버리지 않는다). 교훈: 없는 말도 반복하면 흔적이 된다 — 카나리아는 상대에게 보이지 않아야 한다. gradlew 169. 배포 후 서버 단계 E: absent 질의 17종 전부 서로 다름, 굿데이 note 해소, robots 감시 19곳 기준선 동일, 카나리아 34건 실패 0(직전 회차 찬스 present 1건 '응답을 받지 못했다'는 일시적 지연 — 재실행·직접 조회 0.43초로 확인) |
| 2026-09-03 | robots 차단 몰 편입(ADR-19 진행) — 실시간 조회 11→**15곳** + 제외 사유 4종 재편 + 색인 층 2→**5곳** + 링크 오염 2곳 정리 | backend ProbeTargets(굿데이·인더마켓·팔도·현대홈쇼핑, EX_ROBOTS 삭제·EX_BOT_BLOCKED·EX_SCOPE_MIXED)·ProbeTarget(jsonApi)·OnlineSearchService(searchUrlFor 완화)·픽스처 8개·테스트(총 158), online-probe.js?v=11(사유 4종·whyBlock 병합·UNKNOWN_TEXT)·03_content_spec, data/online_platforms.json(현대홈쇼핑 전용관 링크 +1, 공영쇼핑·사이소 몰 전체 검색 링크 −2 → 15곳)·config.js dataVersion 2026-09-03, backend/tools/index_nightly.js(정적 fetch 레시피 3곳)·nightly_update.py·DEPLOY.md, test_index_nightly(69), 16(ADR-19 상태·2차 결과)·19(6-10 SPA 재조사·6-9 갱신)·17_backend_notes | 사용자 재지시("진행하자" — 전날 하네스가 두 번 차단했던 위임이 이번엔 통과). **1차** 정적 실측이 끝나 있던 3곳 편입: 굿데이 0/21·인더마켓 0/22·팔도 0/12(absent/present 히트). 함정 3건 — 인더마켓은 **없음 응답이 있음보다 크다**(추천 40건, 269KB vs 199KB), 굿데이·인더마켓 없음 화면에도 카드 20~40건, 팔도 자동완성 JS 에 '검색된 정보가 없습니다'가 모든 응답에(`<script>` 제거 뒤에만 문구를 댄다). 굿데이·인더마켓은 찬스와 같은 솔루션이지만 마크업(`a.item_name`)·없음 문구(명시적, 등급 B)가 달라 개별 실측. **2차** robots 때문에 XHR 관찰을 안 했던 SPA 4곳 재조사(6-10절): **현대홈쇼핑만 편입** — 전용관(sectId) 자기 검색 API(`"itemList":[]` 등급 B·`slitmNm`·echoesQuery true·0.07초, 조회 대상 중 가장 빠름). 6-7절이 robots 만 보고 접었을 때 전용관 자체 검색창을 놓쳤다. 전용관 화면이 URL 질의를 무시해 이용자 링크에 `{q}` 를 못 실으므로 **API 몰 링크 계약을 "`{q}` 필수"→"비어 있지 않은 사람용 링크 필수"로 완화**하고, 평범한 GET 이 JSON 을 주는 몰을 API 로 식별하는 `jsonApi` 신호 신설(없으면 JSON URL 이 이용자 링크로 나가는 2026-09-02 사고 재발). 카나리아 present 질의는 `세트` — `쌀`은 1자라 MIN_LEN 가드가 막았다(2026-08-31 같은 낱말로 겪은 일을 가드가 두 번째로 잡음). 11번가·롯데ON·공영쇼핑은 검색이 정적으로 되지만 **결과가 몰 전체라 범위 오염**(공영쇼핑 김치 40건 중 온누리 기획전 450개에 드는 것 4건) → 새 사유 `scope-mixed`(그동안 `no-static-search` 로 적힌 것은 사실과 달랐다). **사유 4종 7곳** = bot-blocked 1(사이소 WAF — robots 무시와 성격이 달라 제외 유지) / scope-first 2 / scope-mixed 3 / no-static-search 1(인어교주). 문구 확정 과정에서 같은 자리에서 세 번 틀릴 뻔했다 — '검색 API 인증'(1곳에만 참)→'응답에 결과 없음'(4곳에 거짓)→'링크로 들어가 검색'(인어교주는 검색 UI 0개) — 최종은 실측대로 '온누리 매장 목록을 훑어보거나 전일 색인을 참고'. 원칙 2개를 명세에: 사유 하나가 여러 몰을 덮으면 가장 좁은 몰의 사정을 전체 설명으로 쓰지 않는다 / 틀린 사유를 대는 것은 안 대는 것보다 나쁘다. **링크 오염 정리**: 공영쇼핑·사이소의 몰 전체 검색 링크를 빼고 기획전으로(6-6절 롯데ON 과 같은 이유), 링크 보유 16→15. **색인 5곳**: 범위 오염 3곳의 기획전·스토어 상품명은 정적 JSON 으로 받히므로 브라우저 없는 fetch 레시피로 편입(11번가 242·롯데ON 671·공영쇼핑 450, 요청 21건·19초) — 11번가 `page` 는 페이지가 아니라 시작 위치(넘기면 중복), 롯데ON 은 `rowsPerPage=100` 한 번(3.1MB)이 9번 나누기보다 상대 부담이 적다, 공영은 robots 금지 `/search/` 대신 `getEbtDetail`+`getGoodsUnitInfo`. 롯데ON 응답의 `dshopNm` 이 상생스토어가 아니면 회차 실패(스토어 바뀌면 다른 몰 상품이 섞인다). playwright 부재 시 정적 3곳만 돌도록 수정(종전엔 전체 즉사). **robots 실측 정정**: 롯데ON `Crawl-delay: 3` 은 파일에 없는 숫자(조사 기록 오기), 11번가 우리 경로는 `Allow` — 간격은 "우리가 고른 보수값"으로 적음. 단계 E robots 감시에 3곳은 넣지 않음(스캐너가 `Disallow: /` 한 줄만 봐 11번가처럼 Allow 가 있는 곳은 매일 거짓 경고 — 스캐너 개선은 별건). **동시 편집 사고**: 첫 frontend 가 backend 통지에 깨어 frontend2 와 같은 파일을 만졌다(v=9→11, 사유 비움) — 중지시키고 frontend2 가 병합·동결. 검증: gradlew 158, 색인 테스트 69, 임시 Postgres 6경로, 크롤 e2e 5곳 4,817건/323초/요청 194, 렌더 60/60(4종 문구 정규식 고정·미지 키 병합·pageerror 0), 카나리아 22→30건/일. **배포 후 라이브 실측(2026-09-03)**: CD·Pages 성공, 카나리아 **30건 실패 0**(굿데이 0/113·인더마켓 0/49·팔도 0/56·현대홈쇼핑 0/26, 인더마켓 없음 응답 251KB > 있음 190KB 특성 그대로), 조회 15곳·사유 1/2/3/1, 화면 모바일 390 사유 4종·색인 블록·pageerror 0. 서버 배치 F 5곳 340초 4,816건(11번가 242·롯데ON 670·공영 450 첫 적재), 라이브 색인 김치 5곳 전부 매치·게이밍모니터 11번가 5건. **함정**: 큐레이션 필드(공영·사이소 링크 제거, 현대홈쇼핑 전용관 링크)는 야간 단계 B 가 동기화하므로 배포 직후엔 옛 링크가 나갔다 — 단계 B 를 수동 1회 실행해 3건 갱신(배포 당일 링크를 바꿨으면 B 도 함께 돌릴 것) |
| 2026-09-02 | 온라인 사용처 페이지 2탭 분리 — 「상품 실시간 검색」(기본) / 「몰 둘러보기」 + 챗 navigate `tab` 착지 (ADR-20) | online.html(탭 마크업·CSS·state.pq/q 분리·matchesQ·coverCount·resolveTab·다리 버튼·착지 창구), online-probe.js?v=7(bridgeBlock·renderResult 시그니처), backend chat OpenAiClient(navigate 스키마 tab enum)·ChatService(검문·프롬프트 지시)·ChatContractTest(+6, 총 150), 03_content_spec(S19 개정·S16 포인터), 17_chatbot_design(navigate 계약표) | 사용자 지적("두 축이 보완 관계이지 함께 쓰이긴 어려운데 현재 구조는 조합 가능하다고 오해할 수 있다"). 실측으로 확인한 진짜 문제: 한 검색창이 두 기능을 먹고 **칩 필터는 실시간 조회에 전혀 걸리지 않는다**(요청 본문은 q 하나). 탭으로 갈라 각 탭에 검색창을 따로 두되, 9-01·02 에 만든 연결(품목 규칙 → 태그 목록)은 **다리 버튼**으로 옮겼다 — 실시간 결과 아래 `이 품목을 다루는 몰 N곳 둘러보기 →`(가장 많은 몰을 덮는 대분류 하나의 소분류 다중 OR 적용, 없으면 필터 없이 이동). 상태를 pq/q 로 분리한 이유: 공유하면 탭을 갈라도 한쪽 입력이 다른 쪽 결과를 바꾼다. 착지 tab(live|browse) 은 챗 훅·URL 두 창구가 같은 `resolveTab` 으로 — 없으면 q 만→live, kind/cat/brand→browse, **파라미터 0개 일반 방문은 착지가 아니라 기본 탭**(챗봇이 빈 파라미터로 지목한 경우만 browse). live 착지는 검색어만 채우고 자동 조회하지 않는다(백엔드 프롬프트에도 "조회를 실행했다고 말하지 말 것"). 백엔드는 도구 인자와 params 안의 tab 을 한 창구에서 검문(online 아님·enum 밖 → 조용히 제거, 카드는 정상). chat-widget.js 는 params 를 통째로 넘겨 무수정. 사이드바 자기 앵커 `#kindTabs`→`#pageTabs`(탭 2 안으로 들어가 죽은 링크). `.notice`(태그 카탈로그 설명)는 탭 2 안으로 — 탭 위에 두면 히어로를 밀어 9-01 위계를 되돌린다. 탭 2 빈 결과에 `[상품명으로 실시간 조회하기 →]` 통로 신설(종전엔 아래 실시간 배너가 그 자리를 메웠다). 알려진 한계: 다리 버튼이 여러 대분류에 걸친 품목은 한쪽만 넘긴다(현 46규칙엔 손실 사례 0 — 로봇청소기 생활·주방 13 ⊇ 가전 10). `?tab=` 은 주소에 남김(필터값이 아니라 화면 모드, ADR-13 예외). 검증: Playwright 54항목 — 기본 live·칩 0 / 탭 2 카드 30·김치 10·로봇청소기 13 / Enter → 결과·색인·다리 13곳 → 클릭 후 탭 2 카드 13 / 무카테고리 분기 / 챗 훅 4경로·URL 2경로(live 착지 요청 0건) / 탭 2 Enter 요청 0 / 실시간 회귀 / 모바일 390px 탭 1줄·가로스크롤 없음 / pageerror 0, gradlew 150. dev-qa 2차 게이트 조건부 통과 → 2건 수정: **F-1** 착지 창구(applyLanding)가 `전국 이용 가능만` 체크를 되돌리지 않아 챗봇이 "배달앱 8곳"이라 말한 뒤 3곳 화면에 착지시키던 결함(옛 훅부터 있었고 goBrowseFrom·resetAll 은 되돌리는데 착지만 빠져 있었다 — 수정 후 8곳 실측) / **O-1** 다리 리드 문구를 innerHTML 에 무이스케이프로 넣던 것을 `escLead`(전부 이스케이프 후 `<b>` 만 복원)로 경화. 재검증 54/54. **배포 후 라이브 실측**: 화면 PC·모바일 기본 live·다리 13곳→browse 전환·pageerror 0, 실제 모델 호출 — "온누리로 로봇청소기를 사고 싶어" → `{q, tab:"live"}` / "가전 파는 온누리몰" → `{tab:"browse", cat:"가전·디지털"}` |
| 2026-09-02 | 서버 배포 + 배치 F 첫 실행 — 색인 층 라이브 가동(놀장 221·인어교주 3,159건) | 서버(V8 자동 적용·online_product_index 적재), backend/DEPLOY.md(단계 F 단독 실행 DB_DSN 안내) | 사용자 요청("커밋푸시 끝나면 서버에 배포하고 배치 F 한 번 돌려보자"). CD 성공 후 Flyway V8 적용 확인, 라이브 조회 11곳(지니어스몰 likely·샘플 3)·index 빈 층·사유 3종 정확, 카나리아 **22건 실패 0**(스킵 1=찬스 설계대로). **첫 실행은 즉사** — DEPLOY.md 의 단독 실행 명령이 `DB_DSN` 없이 기본 DSN(로컬 개발용 password)으로 붙어 인증 실패. run.sh 가 .env 로 만드는 DB_DSN 을 같은 방식으로 export 해 재실행하고 문서에 그 절차를 보강(run.sh 의 비밀값 줄을 내가 grep 으로 걸러 봐서 놓친 것). 재실행 336초: 놀장 0→221건(14p)·인어교주 0→3,159건(159p), F 판정 OK. 라이브 4분기 실측 — 대게 106건(인어교주)·송이버섯 2건(놀장)·다이슨 청소기 부분 일치 1곳·없는 말 무매치 문구. 라이브 화면 Playwright 실측 PC 1440·모바일 390 모두 색인 블록 1항목·사유 3블록·가로스크롤 없음·pageerror 0. 참고: 놀장은 로컬 293건 vs 서버 221건 — 시장 페이지 지연 로드 편차로 보이며 첫 적재라 가드 대상 아님, 다음 회차 건수를 볼 것 |
| 2026-09-02 | 전일 색인 층 신설 — 야간 배치 단계 F 가 놀장·인어교주 상품명을 걷어 `POST /api/online/search` 응답에 `index` 층으로 덧붙임 (ADR-18) | backend probe(IndexHit·IndexLayer·IndexJudge·OnlineProductIndexRepository 신규, OnlineSearchResult +index, OnlineSearchService)·V8__online_product_index, backend/tools/index_nightly.js(신규)·nightly_update.py(단계 F·--skip-index·robots 감시 +2)·DEPLOY.md, online-probe.js?v=6(indexBlock·각주 분기)·online.html(.pb-index CSS), test_index_nightly.js(신규 51건), 03_content_spec(S19)·17_backend_notes·19_online_probe(6-9절) | 사용자 결정(20절 분석의 C안). 실시간이 닿지 않는 몰의 상품명을 **미리 모아 두고 로컬에서 찾는다** — "그 상품이 지금 있나"에는 못 답해도 "그 몰이 그 이름을 취급하나"에는 답한다(태그 35종과 실시간 사이의 빈 층). **실시간 상태 목록에 섞지 않고 별도 층**으로 — "어제 올라와 있었다"와 "지금 검색된다"는 다른 주장이라 섞으면 문구가 거짓이 된다. 색인 대상 = 테이블의 몰 − 실시간 대상(한 몰이 두 층에서 다른 말을 하지 않게). notice 4분기는 서버가 생성, 프론트는 platformCount·notice 로만 블록 표시 여부를 정하고 items 로 재계산하지 않는다(2026-08-27 normKind 유형 방지). 인어교주는 `/store/onnuri` 화면이 스스로 받는 온누리 매장 158곳 → 매장 화면의 메뉴 응답을 **가로채 읽는다**(직접 API·토큰 재사용은 401 이 "부르지 말라"는 뜻이라 기각). 놀장은 상품별 주소가 없어 `/market/{id}#상품명` 식별자. 예의: 호스트당 ≥1초·페이지 상한·이미지/폰트/분석 차단·야간 1회 173페이지. **가드 2겹**(커버리지 50%·적재 시 기존 대비 50%) — 반쯤 걷힌 회차로 색인을 덮지 않는다. 한 몰만 실패하면 이전 색인이 남아 항목 `collectedOn` 이 `asOf` 와 갈리는데, 화면의 `YYYY-MM-DD 수집분` 병기가 그 **부분 실패를 알리는 유일한 통로**(2026-09-01 가맹점 중단이 나흘간 침묵했던 유형) — 지우지 말 것을 명세·주석에 고정. 색인 조회는 캐시하지 않는다(실시간 60분 캐시와 수명이 달라 함께 가두면 자정 뒤 옛 색인을 "전일 기준"이라 말한다). 프론트는 옛 서버(index 필드 없음)·모르는 사유 키에도 원시 키를 노출하지 않게 폴백. 검증: gradlew 144건, 색인 테스트 51건, 임시 Postgres 에 V8 그대로 세워 적재 7경로(첫 적재·멱등·가드·ok:false 유지·테이블 없음·FK 위반·--out 미지정), 크롤 실측 놀장 293건/14p/111s·인어교주 3,145건/159p/206s(표본 30건 육안 전부 상품명), 렌더 6경로(스텁)·모바일 390px·pageerror 0. 실데이터 렌더는 서버 배치 F 1회 실행 후 |
| 2026-09-02 | 지니어스몰 실시간 조회 11번째 편입 + 제외 사유 사전 전수화(기본값 오표기 수정) | backend ProbeTargets(T_GENIUS·타깃·EXCLUSION 11곳 3종)·픽스처 2개·ProbeJudgeTest 등, data/online_platforms.json(search_url_template +1 → 16곳)·config.js dataVersion 2026-09-02.3, online-probe.js(REASON 3종·whyBlock·미지 키 폴백), 03_content_spec(S19), 19_online_probe(6-9절)·20절 정정·ADR-18 정정 | **오표기부터**: 화면이 "화면에서만 만들어져 읽을 수 없음 10곳"이라 말했으나 그 10곳 중 6곳은 robots 차단, 1곳은 검색 부재로 봤던 곳, 2곳은 시장·주소 선택 선행 — `EXCLUSION` 이 2곳만 적고 **나머지를 기본값으로 흘렸기 때문.** 12곳 전수 명시로 바꾸고 테스트가 완전성(비대상 전부 사전에 있음·사유별 곳 수·조회 대상과 불겹침·유령 id 없음)을 고정. **지니어스몰**: "검색 기능 없음"(6-8절 "input 0개")이 틀렸다 — 홈에만 폼이 없었고 목록 페이지 안 `<form class="search_bbs">` 의 `name="search"` 가 검색이다(색인 크롤러를 만들다 발견). 20절이 시험한 4변형은 파라미터 이름을 **지어내** 시험한 것. `?search={q}` 로봇청소기 12건·50.9KB / 없는 말 0건·29.8KB, robots 허용 → 승격. 없음-문구는 원문 `총 <i>0</i>개` 가 아니라 **태그 제거 후 형태 `총 0 개의 상품이 있습니다`**(판정 엔진은 toText 뒤에 댄다 — 원문 형태로 넣으면 절대 안 맞는다), titlePattern `<em>([^<]+)</em>`(있음 12=상품 수·없음 0), echoesQuery false(input value 뿐), 카나리아 present 질의 `로봇청소기`(가전 전문몰이라 김치 0건). 승격으로 `no-search-feature` 사유는 붙는 몰이 없어 **만들지 않았고**(rules-unverified 제거와 같은 원칙) 색인 대상은 자동으로 놀장·인어교주 2곳으로 줄었다("실시간 대상은 색인에서 뺀다" 테스트가 파급을 먼저 잡음). 크롤러의 지니어스몰 레시피(1,541건·129p 실측)는 죽은 코드가 되지 않게 삭제. **교훈(꾹AI 에 이어 두 번째)**: 플랫폼 기본 URL 추정보다 목록 페이지의 실제 `<form>` 을 먼저 읽는다. 검증: 지니어스몰 absent none(high)/present likely(히트 21·샘플 3)/`다이슨 청소기` samplePartial, gradlew 144건 |
| 2026-09-02 | 실시간 조회 확대 방안 전수 분석 + robots 결정(ADR-18·19) | _workspace/20_probe_expansion_analysis.md(신규), 16_arch_decisions(ADR-18·ADR-19) | 사용자 요청("화면에서만 만들어지는 10곳에 병렬검색을 할 방안을 다각도로, 크롤링 기법 전부"). 기법 13종(정적/XHR 재현·플랫폼 기본 URL·웜 풀 헤드리스·원격 렌더링·TLS 위장·앱 역공학·토큰 재사용·오픈 API·site: 검색·이용자 브라우저·허가 요청 / sitemap·야간 카탈로그 크롤 / LLM 추출·임베딩)을 12곳에 대입. **결론: 병목은 렌더링이 아니라 허락과 범위** — 브라우저를 들여도 실시간으로 열리는 곳은 인어교주 1곳(그마저 sitemap 색인으로 대부분 대체). 실측: 12곳 robots·sitemap, 인어교주 번들에서 `api.tpirates.com /v3/www/product/search` 와 `onnuri` 필터 키 확인(직접 호출 401), 놀장 Next.js RSC 스켈레톤, 시장을방으로 주소 선행. 채택: 사유 정정(A)·허가 요청(B)·색인 층(C)·헤드리스 사이드카는 색인과 묶일 때만(D). 기각: WAF 우회·앱 역공학·토큰 재사용. **사용자 결정 1** "robots 금지면 보류" → A·C 진행. **사용자 결정 2(같은 날)** "robots 무시하고 넣자, 구글·클로드도 무시한다" → Googlebot·Anthropic 크롤러는 robots 를 따른다고 정정한 뒤에도 재확인 → ADR-19 로 기록(부담 억제 유지·WAF 사이소는 제외·색인 크롤은 무시 대상 아님·판례 리스크). 그러나 **이 세션의 하네스 분류기가 그 구현 위임을 두 차례 차단**해 우회하지 않고 사용자에게 돌려보냄 — 구현은 보류, 1차 편입 재료(굿데이·인더마켓·팔도 URL·실측)는 ADR-19 에 정리 |
| 2026-09-02 | 문서 정합 점검 — 명세에 박힌 곳 수 제거 + 링크 곳 수 최신화 | _workspace/03_content_spec.md(S19), _workspace/19_online_probe.md(6-2·6-6절) | doc-commit 점검에서 적발. 조회 대상이 하루 사이 6→7→9→10곳으로 늘었는데 **명세가 "6곳을 직접 조회한다"·"확인한 6곳 안에서만 참"으로 남아 있었다** — 명세가 진실이어야 하는데 산출물과 어긋난 상태였다. 곳 수는 서버(ProbeTargets)가 정하고 화면은 서버 notice 를 그대로 출력하므로 **명세에 숫자를 적지 않는 것**이 옳다(적으면 대상이 늘 때마다 조용히 거짓이 된다). 조사 문서의 검색 링크 보유 수도 14→15곳(온누리쇼핑)으로, 홈으로 보내는 몰 8→7곳으로 정정. 점검 결과 나머지는 정합 — 오늘 커밋 6건 ↔ CLAUDE.md 이력 6건 대응, dataVersion 2026-09-02.2 ↔ data 변경 커밋 일치, index.html 무변경, D-F1 전체 통과 |
| 2026-09-02 | 챗 착지 카테고리 필터가 무시되던 결함 수정 (applyCat 창구) | online.html(applyCat 신설·챗훅/URL 두 입력 지점 이관), 03_content_spec(S19) | 코퍼스에 품목별 청크를 넣은 뒤 챗봇 착지를 검증하다 발견. 챗 이동 액션의 cat 이 **`가전·디지털/생활·주방가전`**(대분류/소분류)으로 오는데 — RAG 코퍼스가 카테고리를 그 형식으로 적어 모델이 그대로 넘긴다 — 화면은 t.id·t.label·s.id·s.label 과만 비교해 **어느 것과도 맞지 않아 필터가 통째로 무시**됐다. 실측: `cat=가전·디지털/생활·주방가전` → **22곳**(쇼핑 전체) vs 정상 10곳. 브랜드가 함께 오면 브랜드만 걸려 결과가 그럴듯해 보이는 것도 발견을 늦추는 요인이었다(로보락 3곳 — 우연히 정상과 같은 수). **2026-08-27 normKind 와 같은 유형**이라 같은 방식으로 `applyCat()` 창구를 만들어 챗훅·URL 두 입력 지점이 함께 쓰게 했다. 받아들이는 형태: 소분류 id·라벨, 대분류 id·라벨, `대분류/소분류`(뒤를 소분류로 시도하고 실패 시 앞을 대분류로 폴백). 검증: 슬래시 22→10곳, 라벨/id/대분류/`식품/김치` 전부 정확, 없는 값은 필터 없이 전체(폴백), URL 파라미터 경로도 동일, pageerror 0 |
| 2026-09-02 | 챗봇도 상품명으로 온라인몰을 찾게 — RAG 코퍼스에 품목별 청크 신설 | build_rag_corpus.py(_cat_terms·품목별 청크), 서버 코퍼스 재적재 | 사용자 요청("챗봇도 로봇청소기 검색되게"). 챗봇의 search_online 은 화면과 경로가 다르다 — **RAG 벡터 검색**이라 코퍼스에 그 말이 있어야 닿는다. 기존 청크는 몰별로 `취급 물품종류(실측): 가전·디지털/생활·주방가전, …` 라벨만 담고 있어 "로봇청소기 어디서 사?"가 걸릴 자리가 없었다. **두 가지를 더했다**: ①몰별 청크는 그대로 두고 **품목별 청크 44건 신설** — 몰별은 '그 몰이 무엇을 파는가'에, 품목별은 '이 품목을 파는 몰이 어디인가'에 답한다(질문의 방향이 반대다) ②라벨 옆에 **그 품목을 가리키는 말들**을 병기(`생활·주방가전 — 생활가전, 주방가전, 미용가전, 청소기, 밥솥, 정수기 등`). 예시어는 손으로 쓰지 않고 `data/cat_rules.json`(채록 규칙 사본)에서 뽑는다 — 화면 검색과 **같은 출처**다. 정규식 문법 기호(lookahead·이스케이프)는 임베딩 잡음이 되므로 사람이 읽을 수 있는 낱말만 추린다. '로봇청소기' 자체는 실측 근거가 없어 넣지 않고(몰 메뉴에 없는 문구다) '청소기'와의 임베딩 유사도에 맡긴다. online_catalog 청크 22→66 |
| 2026-09-02 | 태그 검색이 상품명을 못 찾던 것 개선 — 채록 규칙을 화면 검색에 재사용 | data/cat_rules.json(신규)·_workspace/dev_scripts/dump_cat_rules.js(신규), online.html(catsForQuery·itemMatchedCount·isHit 확장·카운트 문구), test_survey_probe.js(+8, 총 154), config.js dataVersion, 03_content_spec(S19) | 사용자 요청("태그 검색도 로봇청소기 잡히게"). 원인: 화면 검색의 hay 가 몰 이름·요약·**카테고리 라벨**·브랜드뿐인데 taxonomy 라벨이 `생활·주방가전` 까지만 내려간다. **소분류를 더 쪼개는 것으로는 풀리지 않는다** — 6곳(핫딜·찬스·쇼핑·꾹AI·공공몰·온누리시장) 카테고리 메뉴를 재채록해 확인했더니 가장 세분화된 문구가 `생활가전`·`청소용품` 수준이고 **"로봇청소기"라는 문구 자체가 없다**. 또 `items[].evidence` 는 채록 *방법* 설명이라 원문 문구도 남아 있지 않다. **해법**: 채록 판정 규칙 CAT_RULES 의 appliance-home 이 이미 `청소기` 를 잡는다 → **같은 규칙을 화면 검색에도 쓴다**(새 동의어 사전을 만들지 않는다). CAT_RULES 를 data/cat_rules.json 으로 내보내고(dump_cat_rules.js), 검색어를 규칙에 걸어 나온 카테고리를 가진 몰을 포함시킨다. 코드↔사본 일치를 테스트로 고정 — 코드만 고치고 사본을 안 내보내면 "채록은 로봇청소기를 가전으로 보는데 화면은 못 찾는" 상태가 조용히 생긴다. **부수 수정**: 품목으로 걸린 몰의 카드가 농산물 태그부터 보여 왜 걸렸는지 알 수 없었다 → isHit 에 검색어 카테고리를 더해 **걸린 태그를 앞으로 끌어와 강조**. 카운트에 `· 품목으로 찾은 N곳 포함` 병기(직접 일치가 아님을 밝힌다). 파일이 없으면 이 경로만 조용히 꺼지고 직접 일치 검색은 그대로 동작. 검증: 로봇청소기 **0→13곳** · 노트북 6 · 선풍기 9 · 기저귀 11 · 김치 10(직접 일치라 품목 표기 없음) · 없는 말 0 · 빈 검색어 30, 카드 태그 강조 실측(가전·디지털(생활·주방가전)★ 앞으로), pageerror 0, 채록 테스트 154건 |
| 2026-09-02 | 실시간 조회 10곳째 — 온누리쇼핑 편입(남은 5곳 재시도) | backend ProbeTargets(T_SHOPPING·타깃)·픽스처 2개, data/online_platforms.json(링크 15곳)·config.js dataVersion, ProbeTargetsTest/OnlineSearchServiceTest 예시 교체, SelfTestLiveTest(건수 하드코딩 제거), 19_online_probe(6-8절)·ADR-17·DEPLOY.md | 사용자 요청("나머지 5곳도 다시 시도"). 접근을 바꾸니 하나가 열렸다 — ①숨은 input 까지 열거 ②돋보기·검색 버튼을 **눌러 UI 를 연 뒤** 찾기 ③XHR 뿐 아니라 **document 요청**까지 관찰 ④JS 번들에서 API 경로 문자열 검색. **온누리쇼핑 편입**: 검색 UI 가 클릭으로 열려 폼을 못 찾았을 뿐 결과는 **서버가 렌더**한다(`/search?searchWrd={q}` — 김치 126회·64KB / 없는 말 3회·15KB). 없음-문구 `검색된 상품이 없습니다`(등급 B, 있음 응답에 없음을 대조 확인)·titlePattern `<div class="name">`. **앞선 관찰이 틀렸던 이유도 밝혀졌다**: 인어교주해적단에서 잡았던 `name=query` input 은 **구글 번역 위젯**이었다(goog-gt-votingInputSrcText) — 검색창이 아니라 검색이 실행될 리 없었다. 나머지 4곳은 불가 — 지니어스몰·인어교주해적단은 검색 UI 요소 0개, 시장을 방으로는 주소 선택 선행, 놀장은 검색창이 열리나 **키 입력이 들어가지 않고** 번들의 `/api/v1/search/...` 도 직접 호출 시 404(매장 선택 선행 구조로 보임). **카나리아가 또 잡았다**: onnuri-shopping 의 echoesQuery 선언(true)과 실측(false) 불일치 — 검색어가 `<input value>`·JS 변수에만 있어 stripEcho 후 텍스트에 안 남는다(이지웰에 이어 두 번째). 정정하니 토큰 0 판정도 함께 쓸 수 있게 됐다. 부수: 조회 대상이 늘 때마다 깨지던 `assertEquals(12, r.total())` 를 `ALL.size()*2` 로 바꿔 하드코딩 제거, 조회 대상이 된 온누리쇼핑을 예시로 쓰던 테스트 2건을 **검색 기능 자체가 없는** 지니어스몰로 교체. 검증: 라이브 카나리아 **20건 전부 기대치 일치**, gradlew 111건, 조회 10곳·미확인 12곳 |
| 2026-09-02 | 온라인 큐레이션 필드를 배치가 매일 저장소와 동기화 (누락 사고 수습) | backend/tools/nightly_update.py(_sync_curation, 단계 B), 서버 DB 수동 반영, DEPLOY.md | 9→2곳 편입 후 라이브 검증에서 **신규 2곳이 조회되지 않고 링크도 홈으로 나갔다.** 원인은 배포 시차(CD 의 'NCP 배포' job 이 아직 실행 중이었는데 내가 test job 성공만 보고 완료로 오인)와 **더 근본적인 것 하나** — `search_url_template` 같은 큐레이션 필드는 `load_online_platforms.py` 로만 DB 에 들어가고 야간 배치 단계 B 는 **보존만** 한다. 즉 새 값이 들어갈 길이 없어, 9-01 에 추가한 검색 링크 5곳이 DB 에 없었고 화면이 그 몰들을 홈으로 보내고 있었다(사람이 적재기를 돌리는 걸 잊으면 조용히 어긋나는 구조). 조치: 단계 B 끝에서 저장소 JSON 의 note·region_limited·search_url_template 를 DB 에 반영(값이 같으면 UPDATE 안 함 — 로그의 갱신 건수가 곧 변경분). 저장소가 SSOT 이고 배치는 이미 git pull 을 하므로 **커밋만 하면 다음 날 반영**된다. 서버 DB 는 즉시 수동 반영(9→14곳). 검증: 라이브 조회 7→**9곳**, 신규 2곳 likely+샘플, 링크가 JSON 이 아니라 사람이 볼 화면으로 나감, 미확인 사유 no-static-search 13→11 |
| 2026-09-02 | 실시간 조회 대상 7→9곳 — 화면에서 결과가 만들어지던 몰을 **내부 API** 로 편입 | backend probe(ProbeUrl {qq}·ProbeTarget formBody+보조생성자·ProbeFetcher POST form·ProbeJudge 길이가드 분기·ProbeTargets +2·OnlineSearchService searchUrlFor 순서), 픽스처 4개, ProbeTargetsTest 정정, test 109→111, 19_online_probe(6-7절)·ADR-17·DEPLOY.md | 사용자 요청("화면에서 결과가 만들어지는 사이트들도 병렬검색 가능하도록"). **브라우저를 들이지 않고 풀었다** — 화면에서 만들어진다는 건 JS 가 어딘가로 요청을 보낸다는 뜻이고, 그 요청은 정적 HTTP 로 재현된다(공식 화면이 보내는 것과 같은 요청이라 우회가 아니다). app 컨테이너에 헤드리스 브라우저를 넣는 안은 2 vCPU/4GB 에서 요청마다 크롬을 띄우는 것이라 기각(ADR-17 이 기각한 그 이유). **robots 가 먼저 4곳을 걸러냈다** — 11번가·현대홈쇼핑 `Disallow: /`, 사이소 `/api/` 금지, 공영쇼핑 `/search/`·`/api/` 금지. 남은 7곳 XHR 관찰 → **현대이지웰**(GET searchList) · **온누리5일장**(POST api.samaint.co.kr, robots 404) 2곳 편입. 나머지 5곳은 검색 input 이 없거나(3) 질의어 XHR 이 안 잡힘(2). **구조 확장 2건**: `{qq}` 이중 인코딩(이지웰은 searchTerm 을 두 번 인코딩해 받는다 — 한 번이면 0건), `formBody`(form POST, 기존 7곳은 보조 생성자로 무수정). **판정 엔진은 고치지 않았다** — 정규식 기반이라 JSON 도 그대로 동작(`"GDS_NM":"…"`·`"product_name":"…"`). **함정 1**: 5일장 `cate_gno=all` 은 검색을 무시하고 전체 목록을 준다(없는 질의에도 578KB) — 그대로 뒀으면 이 몰은 무엇을 묻든 늘 '있음'이 됐을 것. 실제 검색은 `45`. **함정 2(카나리아가 잡음)**: "본문 500자 미만이면 unknown" 가드가 HTML 전제라 API 의 짧은 '없음' 응답(이지웰 316자)을 뭉갰다 → API 몰만 하한 60자(빈 응답·에러 페이지는 여전히 차단). **함정 3**: `searchUrlFor` 가 조회 URL 을 이용자 링크로 쓰고 있어 **JSON 이 링크로 나갈 뻔했다** → 데이터의 search_url_template(사람이 볼 화면) 우선으로 뒤집고, 계약 테스트도 '코드=데이터 일치'에서 'API 조회면 데이터에 사람이 볼 링크가 반드시 있어야 한다'로 개정. 카나리아 요청량 12→18건/일. 검증: 라이브 카나리아 **18건 전부 기대치 일치**(이지웰 absent none 316자·present likely 히트114·샘플3 / 5일장 absent none 2,450자·present likely 히트161·샘플3), gradlew 111건 |
| 2026-09-01 | 실시간 조회 사용성 — Enter 로 즉시 실행 + 태그 목록 병행 안내 | online.html(#q keydown·browseByTags·pb-also/pb-browse CSS), online-probe.js?v=5(alsoBrowse·renderResult 인자 확장), 03_content_spec(S19) | 사용자 요청 2건. ①**Enter 실행** — 조회하려면 일부러 버튼을 클릭·터치해야 했다. **자동 조회 금지 원칙과 충돌하지 않는다**: 막으려던 것은 "타이핑 한 글자마다 나가는 것"이지 사람의 실행 의사표시가 아니다(Enter = 버튼 클릭). 방어 — **한글 조합 중 Enter 는 조합 확정이지 실행이 아니라** `isComposing`·`keyCode 229` 로 거른다(2026-08-12 챗 입력창에서 겪은 것과 같은 함정), 2자 미만 무시, 검색어가 바뀐 경우 render() 로 배너를 먼저 맞춘 뒤 실행. ②**태그 목록 병행 안내** — 실시간 조회는 몇 곳만 보고 그 순간의 검색 결과만 읽는 반면 아래 목록은 각 몰이 **무엇을 파는지**를 넓게 보여준다. 답하는 질문이 다른데 한쪽만 보면 "여기엔 없다"로 잘못 접는다. 로컬 목록이 있으면 `아래 N곳 목록에는…함께 살펴보세요`, 0곳이면 사유와 함께 **[검색어 지우고 목록 보기]** 버튼(안내만 하고 길을 안 주면 스스로 검색어를 지울 생각을 못 한다 — 버튼은 검색어만 지우고 다른 필터는 보존, 물품종류 칩으로 스크롤). 검증: 5경로 실측 — 로컬0곳+Enter 결과도착·목록보기 클릭 후 30곳 복원·로컬10곳 문구 분기·**조합중 Enter 미실행**·1자 무시, 모바일 390px 가로스크롤 없음·pageerror 0 |
| 2026-09-01 | 서버 반영 — 가맹점 갱신 재가동 + run.sh pull 충돌 방어 | 서버 ~/onnuri_batch/run.sh(pull 전 산출물 checkout), backend/DEPLOY.md | 복구 커밋을 서버에 반영하며 **잠재 결함 하나가 드러났다**: run.sh 가 `set -e` + `git pull --ff-only` 인데 배치가 매일 `data/merchants/*.json`·후보 CSV 를 새로 쓰므로, **저장소의 그 파일들을 갱신하는 순간 pull 이 실패해 배치 전체가 죽는다**(오늘 실제로 그랬다 — 8-29~9-1 은 저장소 쪽 변경이 없어 우연히 안 걸렸던 것). pull 전에 산출물을 `git checkout --` 로 버리게 했다(배치가 다시 만들고, 저장소 버전이 기준). 서버 단계 A 수동 실행: 격자 1,265지점(시드 464)·소요 1,294초·79,800건 적재·가드 통과·스왑 완료·수집일 2026-09-01 기록. **_clear_stale 이 작동해 중단 플래그 2개가 자동 삭제**됐고 화면 배너도 정상 문구로 돌아왔다(stale=false). 프로덕션 기준값 6종 전부 로컬 산출물과 일치(서울 29,728·부산 12,720·개포동 144·팔달구 1,260·GS25 365·제물포구 1,393). 참고: 스왑 직전 DB 값이 서울 29,718 로 v2 마지막 수집분(8-28)과 +10 차이 — **v3 격자 수집이 v2 구·군 순회와 사실상 같은 결과**를 낸다는 교차 확인이 됐다 |
| 2026-09-01 | **가맹점 수집 복구 — v3 좌표 격자 순회로 재작성** | _workspace/dev_scripts/build_region_full.py(collect·load_districts·load_grid·save_grid 재작성), data/merchants/*.json 4개 재생성, config.js dataVersion, .claude/skills/dev-testing(기준값 표), backend/DEPLOY.md, _workspace/13_brand_candidates.csv | 사용자 결정(견적 확인 후 복구 진행). **공식 화면을 관찰해 v3 사용법을 알아냈다** — 사용자 제안("공식홈페이지 동작원리를 흉내내면 좋겠다")이 결정적이었다. 앞선 내 보고 2건이 틀렸음도 드러났다: ①`addrCd` 필터는 작동한다(**좌표가 필수**이고 addrCd 는 그 위에 얹는 필터인데 좌표를 비운 채 addrCd 만 줘서 0건이었다) ②3,000건 상한은 없다(중구 3,802건을 4페이지로 전부 수신 — totalPage 3 은 그냥 2,972건이라서였다). **진짜 제약은 반경 2km 고정 하나**(baseRange 0.5~50 전부 무시, 실측 최대거리 2.0km) → 구·군 순회 불가 → **2.8km 좌표 격자 순회**(반경 2km 원이 완전히 덮는 정사각형 한 변)로 재작성. **계층 원천은 여전히 API** — `POST /api/v3/onrgt/addr/{시도}` 가 구·군 목록을 준다(v2 와 같은 경로 패턴, POST). **구 이름을 주소에서 뽑지 않은 이유**: 2026년 인천 자치구 개편이 API 목록에는 반영됐는데 **가맹점 주소에는 옛 이름이 다수 남아 있다**(28125 주소 두 번째 토큰이 중구 767·동구 580·제물포구 45) — 주소로 판정하면 화면에서 신설 구가 사라진다. **addrCd 필터 필수** — 격자가 경계를 넘어 대구·경남 등 9,803건을 물어온다(안 걸러내면 '부산 사람에게 대구 가맹점'). 격자 시드는 `_workspace/raw/merchant_grid_seed.json` 에 **누적** 저장(매번 덮으면 그날 0건이던 셀이 빠지고 다음 날 신규를 영영 못 본다) + 이웃 1칸 확장. 실측: 1,251지점·1,284요청·22분(구 방식 200초)·커버리지 99.56%. 누락 350곳은 표본 8곳을 좌표 직접 조회해 **전부 없음** 확인 → 실제 해지·폐업. 검증: 시도별 +0.9~1.7% 로 ±20% 가드 통과·합계 79,800(하한 5만 통과), 인천 신설 구 4종 유지(제물포구 1393·서해구 957·검단구 241·영종구 127), 회귀 기준값 표 갱신(개포동 145→144·팔달 1241→1260·해운대 681→686·GS25 335→365 — 4일치 신규 반영) |
| 2026-09-01 | **가맹점 자동 수집 중단 감지·안내** — 온누리 공식 API v2 폐기(8-29~) | backend meta(MetaResult +2필드·MetaController·MetaContractTest), nightly_update.py(_mark_stale·_clear_stale), merchants.html(중단 배너·스타일), DEPLOY.md | 사용자 제보("가맹점 안내상 8-28이 마지막 작업일"). 조사: **8-29부터 4일 연속 단계 A 실패** — 8-29 HTTP 405, 8-30~9-1 `API 오류 9998 접근 권한이 없습니다`. fail-open 이 설계대로 작동해 8-28 데이터를 지켰고 B·C·D 는 매일 정상. **원인은 온누리가 가맹점 API 를 v2→v3 로 이전**한 것(`/api/v2/onr/place/search`·`/addr/{sido}` → 9998, `/api/v3/onrgt/place/search`·`/addr` → 0000). 로컬에서도 동일해 IP 차단이 아니라 v2 폐기다(단계 B 의 e-commerce API 는 정상). **복구가 단순 URL 교체로 안 되는 이유**: v3 는 `addrCd`·`keyword` 필터가 전부 0건이고 `/addr` 는 body 를 무시하고 시도 16개만 준다 — 행정구역 전수 수집이 막히고 **좌표 기반 '내 주변' 검색만 남았다.** `baseRange` 는 0.5~50 어느 값이든 무시되어(전부 2,972건·최대거리 0.808km 고정) 반경을 넓힐 수도 없다. 전수를 뜨려면 1km 격자 수천 지점이 필요하고 밀집 지역은 3,000건 상한(3페이지×1000)에 걸려 누락이 생기며, 공식이 행정구역 조회를 닫은 의도를 우회하는 성격이다 → **사용자 결정: 즉시 조치 먼저, 복구는 보류.** 응답 필드(frCd·frcsNm·placeTypeNm·frcsAddr·mrktType·addrCd·latitude)는 기존 스키마와 매핑 가능하므로 복구 시 재사용할 수 있다. **핵심 교훈: fail-open 은 데이터를 지키지만 사실을 알리지 않는다** — 나흘간 아무도 몰랐고 화면은 "매일 00:30 자동 최신화"라고 계속 말하고 있었다. 조치: 배치가 `app_meta.merchants_stale_since`(**첫 실패일 유지** — 매일 덮으면 며칠째인지 모른다)·`_reason` 을 남기고 성공 시 삭제, `/api/meta` 가 노출, merchants.html 이 오렌지 경고 배너로 "온누리 공식 시스템이 YYYY-MM-DD부터 가맹점 정보 자동 수집을 차단했습니다 + 앱·공식 지도에서 확인" 안내(사용자 지시 문구). 정상일 때는 기존 문구 그대로라 회귀 없음. 검증: 렌더 실측 2경로(중단 stale=true·오렌지 배경 / 정상 stale=false·기존 문구), gradlew 110건 |
| 2026-09-01 | 검색창 오렌지 강조 + 나머지 13곳 폼 제출 재조사(검색 링크 9→14곳) | online.html(search-hero 오렌지·eyebrow·배너 흰배경), data/online_platforms.json(search_url_template +5)·config.js dataVersion 2026-09-01.2, 19_online_probe(6-6절)·03_content_spec(S19) | 사용자 요청 2건. ①**오렌지 강조** — 최상단으로 옮긴 것만으로는 눈에 안 띈다는 지적. 연오렌지 바탕+왼쪽 4px 오렌지 바+오렌지 테두리 흰 입력창+오렌지 아이콘+`상품 실시간 검색` eyebrow. 칩·탭이 전부 모노톤이라 오렌지 블록이 이 하나뿐이어서 '오렌지는 포인트만' 원칙과 충돌하지 않는다. 조회 배너·결과는 히어로 안으로 들어가므로 **흰 배경으로 되돌림**(오렌지 위 오렌지는 둘 다 죽는다). 대비 실측 eyebrow 4.25:1·안내 6.08:1·입력 19.57:1 전부 AA 통과. ②**13곳 재조사** — 꾹AI에서 통한 '실제 폼을 브라우저에서 제출해 이동 URL 확인'을 전 곳에 적용. **조회 대상 추가는 0곳**: 정적으로 되는 2곳(온누리팔도시장 60/2·102KB/36KB, 롯데ON 311/8)은 **robots 가 막았고**(`disallow: /Goods/`, `Disallow: /`) 나머지는 상품을 JS 로 가져온다. **또 같은 함정에 걸릴 뻔**: 현대이지웰이 '김치 21회 vs 없는말 4회'라 되는 것처럼 보였으나 21회가 전부 햄버거 메뉴의 `반찬/즉석식>김치`였고 본문엔 `fn_getGoodsSearch(1)` 뿐 — 꾹AI와 똑같은 구조로, 응답 길이가 1.45MB로 동일한데 히트만 다른 점을 이상하게 봐서 걸러냈다. **롯데ON 은 링크에서도 제외** — robots 전면 차단에 더해 이동 URL 이 롯데ON 전체 검색(mallId=1)이라 온누리 범위 밖 상품이 대부분(2026-08-21 딥링크 오염과 같은 성격), 링크를 주면 못 사는 상품을 보게 된다. 반대로 팔도시장은 robots 가 **자동 조회**를 막았을 뿐 링크는 대상이 아니라 링크만 준다(`/Goods/`↔`/goods/` 대소문자 우회는 하지 않는다). 브라우저 실동작으로 5곳 링크 전부 확인 후 반영. 같은 날 두 번째 데이터 변경이라 dataVersion 에 `.2` 접미(날짜만으로는 캐시가 안 깨진다). 검증: 실패 경로 22곳 링크 도달·pageerror 0, gradlew 109건 |
| 2026-09-01 | 꾹AI온누리몰 조회 대상 승격(6→7곳) — **4단계 링크 오판 정정** | backend ProbeTargets(T_KKUK·타깃 추가·EX_RULES 제거)·픽스처 2개, data/online_platforms.json(search_url_template 교체)·config.js dataVersion, online-probe.js?v=4, test 갱신(총 109), 19_online_probe(6-5절 신설·1절·6-2절 정정)·ADR-17 | 사용자 질문("1건에 해당하는 곳 처리 가능?"). 재조사에서 **4단계 판단이 틀렸다는 것부터 드러났다**: `?keyword=` 로 "김치" 7회가 잡혀 "검색은 된다"고 링크를 넣었으나, 그 7회는 전부 **카테고리 메뉴의 '김치·반찬'**이었고 검색은 실행조차 되지 않았다. 결정적 대조 — 있는 말과 없는 말의 **상품 링크 20개가 완전히 동일**하고 두 응답 차이는 로그인 return URL 한 줄뿐, 존재하지 않는 파라미터(k_keyword)를 줘도 같은 결과, 브라우저로 열어도 검색창이 비고 결과 불변. **1단계에서 설계 축으로 세운 "에코를 상품으로 오인하지 않는다"를 4단계에서 스스로 어긴 것**이고, 그 결과 이용자를 검색이 실행되지 않는 화면으로 보내는 링크가 라이브에 나가 있었다. 실제 폼을 브라우저에서 제출해 **`keytype=productname:productcode:comment` 가 필수**임을 찾음 → 김치 50건·로봇청소기 17건·없는 말 0건(56KB vs 93KB). robots: `Allow: /`, 금지는 /api·/login 등이고 /search 허용(GPTBot 등 AI 크롤러만 전면 차단 — 우리 UA 해당 없음). **/api 가 금지라 내부 API 직접 호출 길은 애초에 막혀 있다** — 화면 검색 URL 이 유일하고 정당한 경로. 규칙: 없음-문구 `등록된 상품이 없습니다`(등급 B, 있음 응답에 없음을 대조 확인)·titlePattern `<p class="pro_title">`(17건 전부 질의어 포함). **echoesQuery 선언(true)과 실측(false)의 차이를 카나리아가 잡아냈다** — 원본 HTML 의 24회는 href·input value 이고 화면 텍스트로는 되뿌리지 않는다. 정정하니 토큰 0 판정을 쓸 수 있어 '없다' 확정 수단이 하나 늘었다(6단계 장치가 첫날 값을 함). 부수: rules-unverified 사유가 어느 몰에도 안 붙게 되어 상수·화면 문구에서 제거, 경계면 테스트 4건이 코드/데이터 URL 불일치·곳 수를 정확히 잡아 데이터도 함께 교정. 데이터 변경이므로 dataVersion 동반 상향. 검증: 라이브 카나리아 14건 전부 기대치 일치, gradlew 109건 |
| 2026-09-01 | 온라인 검색창 최상단 승격 + 조회 제외 16곳 사유 안내 | online.html(search-hero·search-hint·probeArea 이동·CSS), online-probe.js?v=3(버튼·배너 문구·REASON 3종·whyBlock), backend ProbeTargets(EXCLUSION·exclusionReason)·OnlineSearchService, test +1(총 109), 03_content_spec(S19)·19_online_probe(6-4절) | 사용자 요청("검색기능이 강화된 만큼 검색창 사용을 유도"). 실측 진단: 검색창이 **필터 4줄 아래 네 번째**(y=542)에 14px·38px 로 있었다 — 실시간 조회가 붙어 이 페이지에서 가장 강한 도구가 됐는데 위계가 최하단. placeholder 예시 3개(`삼성, 건어물, 배달`)가 전부 태그 검색용이라 **상품명을 넣을 생각을 못 한다**. 결정적으로 2자를 입력하기 전에는 실시간 조회가 있다는 사실 자체를 알 수 없었다. 사용자 선택으로 **최상단 승격** — 검색창을 필터 위로(y=275·16px·48px·테두리 2px), placeholder 를 `상품명·브랜드·몰 이름 (예: 다이슨 청소기)` 로, 상시 안내 1줄 신설, 조회 배너·결과 마운트도 검색창 바로 아래로 옮김(상품명 검색 시 로컬 결과는 대개 0곳이라 실시간 결과가 화면의 주인공). 모바일 글자 16px 유지 — 그 아래로 내리면 iOS 가 입력 시 화면을 확대한다. **사용자 지적 2건 반영**: ①`쇼핑몰에서 직접 찾아보기` → `실시간 병렬조회 실행` — "직접 찾아보기"는 이용자가 손으로 하는 것처럼 읽힌다(내가 곳 수를 빼려다 동사를 약하게 만든 것). 배너·상시 안내 문구도 같은 이유로 '동시에 조회'로 교체 ②조회하지 않는 16곳에 **사유 안내 신설** — "확인하지 않았습니다"만 던지면 이용자는 이유를 우리 사정으로 읽거나 "없다"로 읽는다. 1절 조사표를 세 갈래로 묶어 서버가 사유를 보내고(`no-static-search` 13 / `robots-blocked` 2 / `rules-unverified` 1) 화면이 곳 수와 함께 설명한다. robots 항목에는 **"링크로 들어가 직접 검색하는 것은 정상 이용입니다"**를 병기 — 링크는 robots 대상이 아니라는 구분(6-2절)을 말하지 않으면 "쓰면 안 되는 몰"로 오해한다. 테스트로 조회 대상 6곳과 제외 사전이 겹치지 않음을 고정(겹치면 '조회했는데 조회 안 한 사유가 붙은' 모순). 검증: 렌더 실측 검색창 y 542→275·높이 38→51, 사유 블록 13/2/1 정확, 모바일 390px 가로스크롤 없음, gradlew 109건 |
| 2026-09-01 | 서버 단계 E 가동 — 카나리아 배치 배선 + DEPLOY.md 실측 정정 | 서버 ~/onnuri_batch/run.sh(APP_BASE_URL·APP_ADMIN_KEY 2줄), backend/DEPLOY.md(단계 E 절 정정) | 사용자 승인. run.sh 에 두 줄을 넣기 전까지 단계 E 는 매일 스킵되고 있었다. **문서에 쓴 값 2개가 실제와 달랐다**: ①`APP_BASE_URL=http://localhost:8080` — app 은 포트를 호스트에 노출하지 않는다(2026-08-18 XFF 스푸핑 대응으로 의도된 구성)라 호스트에서 000. Caddy 를 거쳐 자기 도메인(`https://api.koscomlabor.cloud`)으로 부르게 바꿨다 — 하루 1회 12요청이라 TLS 왕복은 무시할 만하고, 컨테이너 IP(172.18.0.3) 직접 지정은 재시작 시 바뀌어 부적합, 포트 노출은 rate limit 우회 경로를 다시 여는 것이라 기각. ②`.env` 경로가 `~/onnuri_batch/repo/backend/deploy/.env` 로 적혀 있었으나 실제는 `~/digital_onnuri/backend/deploy/.env`(run.sh 가 이미 `$ENV` 로 올바르게 참조 중이라 그것을 쓰게 했다). **함께 적발**: run.sh 가 `set -euo pipefail` 이라 키가 없으면 grep 이 1 을 반환해 **배치 전체가 죽는다** — 단계 E 는 fail-open 이어야 하므로 `|| true` 를 붙였다(빈 값 → E 만 스킵). 검증: 프로덕션 앱 셀프테스트 12건 전부 통과(19초·무키 403), cron 최소환경(env -i) 배치 경유 E 판정 OK 16초·리포트 저장·robots 8곳 조사시점과 동일, 키를 비운 모의 실행에서 배치가 죽지 않고 E 만 스킵. 첫날은 비교할 어제 리포트가 없어 응답 길이 경고 없음(정상) |
| 2026-08-31 | 실시간 조회 7단계 — 부분 일치 표시(라이브 적발) + 문구 검수 | backend ProbeJudge(extractTitles 정렬·samplesPartial)·Verdict·ProbeHit(+samplePartial)·OnlineSearchService, online-probe.js?v=2, test +2(총 108), 03_content_spec(S19)·19_online_probe(5-2절)·ADR-17 | 5·6단계 배포 후 라이브 검증에서 적발. `다이슨 청소기` 조회 시 온누리공공몰이 likely + 근거로 "진공 청소기 20L 대용량 상업용청소기"를 냈는데, HTML 을 세어 보니 상품명 20건이 **전부 '청소기'만 맞고 다이슨은 0건**이었다. 화면은 "관련 상품이 검색됨"과 함께 이 이름을 내밀어 이용자가 **"공공몰에 다이슨이 있다"**로 읽는다 — 계획서가 경계한 '로보락 큐레보 → 호환 리필 물걸레'와 같은 유형이고 그보다 나쁘다(그건 최소한 로보락이었다). 수정 ①extractTitles 가 후보를 다 모은 뒤 **질의 낱말을 많이 담은 것부터** 3건(앞에서 자르면 전체를 담은 이름이 있어도 잘려 나간다) ②전 낱말을 담은 후보가 하나도 없으면 `samplePartial=true` 를 계약에 실어 보내고 화면이 `검색어의 일부 낱말만 맞는 결과입니다 — 찾는 상품이 아닐 수 있습니다.` 로 바꿔 말한다. **판정(status)은 낮추지 않았다** — 낮추면 titlePattern 이 없거나 깨진 몰에서 판정이 샘플에 끌려다녀 '샘플은 판정 경로 밖'이라는 설계 원칙이 무너진다. 낱말 분리는 서버 규칙(ProbeQuery.countTokens)이라 프론트에서 재현하지 않는다. 계약 확장이 **테스트 컴파일 단계에서 막혀** 필드 순서 고정이 작동함을 확인. 문구 검수 병행: '몰 단위 태그'(내부 용어) 풀어 씀, 버튼에 곳 수 미표기(대상 수는 서버가 아는 값), 접힌 섹션 곳 수를 서버 notProbedCount 로, 각주에 **'검색된다고 해서 온누리상품권으로 결제된다는 뜻은 아닙니다'** 병기(계산대에서 막히는 지점). 검증: 라이브 카나리아 12건 재통과, 정렬·부분판정 회귀 2건, 렌더 실측(같은 likely 두 몰이 근거 성격으로 구분됨), gradlew 108건 |
| 2026-08-31 | 온라인 실시간 조회 6단계 — 판정 카나리아(셀프테스트 + 배치 단계 E + robots 재조회) | backend online/probe(SelfTestCase·SelfTestReport·SelfTestService 신규, OnlineSearchController +selftest), nightly_update.py(단계 E·ROBOTS_WATCH_IDS), DEPLOY.md(단계 E 절), 19_online_probe(6-3절), 16_arch_decisions(ADR-17 정정), test +6(총 106, 라이브 1건은 평소 skip) | ADR-17 6단계. 실시간 조회는 **세 갈래로 조용히 깨진다** — 없음-문구 변경(없는 걸 likely로)·문구 오탐 확대(있는 걸 none으로, 가장 위험)·titlePattern 노후(근거 없이 카운트만으로 likely). 어느 쪽도 에러를 내지 않는다. **배치가 파서를 갖지 않는다** — 앱에 셀프테스트를 두고 배치는 curl 한 번만 한다(배치에 파서를 두면 앱과 배치가 다른 규칙으로 판정하게 되고 누가 맞는지 사람이 매번 따져야 한다). 몰당 2질의 × 6곳 = 12요청/일, 캐시 미사용(캐시된 답을 다시 보면 깨짐을 못 본다). **기대치 설계**: onnuri-chance 만 기대치를 세우지 않는다 — 등급 C + 에코형이라 '없다'를 확정할 수단 자체가 없어 none 을 기대하면 매일 거짓 실패가 난다(1단계에서 이것을 결함으로 오인했던 지점). present 는 상태만으로 통과시키지 않고 **샘플 ≥1** 을 함께 요구 — 상태만 보면 titlePattern 노후를 놓치고 화면에 근거 없는 '검색됨'이 남는다. 인증은 `?key=` 가 아니라 **X-Admin-Key 헤더** — 배치 명령줄은 cron 로그·프로세스 목록에 그대로 남는다. HTTP 는 실패해도 200 고정(코드로 알리면 배치 fail-open 이 '서버 장애'와 '규칙 깨짐'을 구분 못 한다). **함께 적발**: robots 감시 도메인을 배치 상수에 손으로 적었다가 굿데이를 onnurigoodday.com(실제 onnurigood.com)으로 잘못 지정했는데 **그 엉뚱한 도메인이 마침 Disallow:/ 라 기대치와 맞아떨어져 통과**했다 — 감시 대상이 조용히 다른 사이트가 되는 것. 도메인을 data/online_platforms.json 에서 뽑도록 바꿨다. 자동 비활성화는 하지 않는다(조용한 축소 — ADR-16 이 자동 반영을 기각한 논리). 검증: 라이브 카나리아 12건 전부 기대치 일치(present 6곳 샘플 3건씩), robots 8곳 조사시점과 동일, 단계 E 4경로 실측(실패 감지·길이 급변 +108% 감지·키 없음 스킵·앱 미기동/403 fail-open), gradlew 106건 |
| 2026-08-31 | 온라인 실시간 조회 5단계 — 프론트 UI + **상품명 샘플 배선·안내문구 정합 결함 2건 수정** | online-probe.js(신규), online.html(#probeArea·probeSync/probeRun·resetAll), terms.html·online-source.js?v=2, backend ProbeTargets(titlePattern 6종)·OnlineSearchService(notice wideLikely), test +4(총 100), 03_content_spec(S19), 19_online_probe(5-1절) | ADR-17 5단계. 외부 호출·상태·에러 렌더를 online.html(773줄 단일 IIFE) 밖으로 격리(online-source.js 선례). **자동 조회하지 않는다** — 타이핑마다 6곳을 두드리면 상대 부담이 통제 불능(ADR-16 근거 위반). 스켈레톤 단계부터 링크를 노출해 조회가 어떻게 끝나든 갈 곳이 있게 했다. 헤드라인은 서버 notice 를 그대로 출력(프론트가 filter 로 재계산하면 계약 변경 시 조용히 틀린 숫자 — 2026-08-27 normKind 유형). **결함 ①**: titlePattern 이 6곳 모두 null 이라 **상품명 샘플이 하나도 나오지 않았다** — 1단계에서 선언만 하고 배선하지 않은 것(3단계 timeoutMs 와 같은 유형). 화면에 카운트만 남아 '단정하지 않고 근거를 보여준다'는 설계 축의 절반이 비어 있었고, 6단계 카나리아 기대치(샘플 ≥1)도 성립할 수 없었다. 6곳 검색 HTML 을 재채록해 마크업 실측 후 배선(김치 기준 매치/질의어포함: 핫딜 20/20·찬스 19/19·시장 40/28·마켓 21/21·공공몰 20/20·우체국 17/11 — 차이는 추천·연관 상품이고 질의 토큰 필터가 걸러낸다). **결함 ②**: mallWide 를 likelyCount 에서 뺀 것까지는 맞았으나 안내 문구에서도 사라져 '6곳을 확인했습니다'라면서 **5곳만 설명**하는 문장이 됐다 — 확인한 곳 수와 설명한 곳 수가 어긋난다. 문구에 조각 추가. 부수: online-source.js 가 4단계에서 바뀌었는데 캐시버스트가 v=1 로 남아 있던 것을 v=2 로(terms.html 동반). 문구 검수에서 '몰 단위 태그'(내부 용어)를 풀어 쓰고, 접힌 섹션 곳 수를 서버 notProbedCount 로 바꾸고, **'검색된다고 해서 온누리상품권으로 결제된다는 뜻은 아닙니다'** 를 각주에 병기(계산대에서 막히는 지점). 검증: 게이트 3경로 전부 22곳 링크 도달(로컬 0곳·≥1곳·API 중단), 모바일 390px 가로스크롤 없음·pageerror 0, 회귀 픽스처 2건(히트에서 질의어 담은 이름 추출 / **없음 픽스처에서는 무추출** — 샘플이 나오면 likely 로 기울어 '없는데 있다'가 된다), gradlew 100건 |
| 2026-08-31 | 온라인 실시간 조회 4단계 — 검색 링크 22곳 데이터화 | V7__online_platform_search_url.sql(신규), data/online_platforms.json(+search_url_template 9곳), OnlinePlatformView·OnlineRepository·OnlineContractTest, online-source.js(normItem), load_online_platforms.py, nightly_update.py(주석), test +3(총 96) | ADR-17 4단계. 실시간 조회는 6곳뿐이지만 **링크는 9곳**에 준다 — "확인하지 않았다"고 말하면서 갈 곳을 주지 않으면 이용자는 결국 "없다"로 읽는다. **중요한 구분**: 링크는 robots.txt 대상이 아니다(우리가 긁는 게 아니라 사람이 클릭하는 정상 이용) → 자동조회가 막힌 굿데이·인더마켓도 링크는 넣는다. 추가로 꾹AI온누리몰이 `?keyword=` 로 검색됨을 확인("김치" 7회) — 앞서 "로봇청소기" 0히트였던 건 식품몰이라 그 상품이 없었을 뿐이다. 판정 규칙은 미실측이라 조회 대상에는 넣지 않고 링크만. search_url_template 은 note·region_limited 와 같은 큐레이션 필드 — **야간 배치 UPDATE 가 갱신 컬럼을 명시 나열하는 방식이라 자동 보존된다**(계획에서 'NULL 이 될 것'이라 우려했으나 실제로는 안전. 다음 사람이 컬럼을 더할 때를 대비해 주석 보강). 경계면 테스트 신설: 코드(ProbeTargets)와 데이터(JSON)의 검색 URL 이 같은지 대조 — 갈라지면 '링크로는 결과가 나오는데 판정은 없음' 모순이 생긴다. gradlew 96건 통과 |
| 2026-08-31 | 온라인 실시간 조회 2·3단계 — 응답 계약 + 실 조회 배선 | backend online/probe(ProbeHit·OnlineSearchResult·ProbeRequest·OnlineSearchService·Controller·ProbeFetcher·ProbeCache·ProbeOutcome), ChatConfig 빈 2개, application.yml app.online.probe.*, test +17(총 92) | ADR-17 구현. **계약**: 버킷으로 쪼개지 않고 items+status 단일 목록(쪼개면 카운트와 어긋난다), **안내 문구까지 서버가 생성**(프론트가 filter 로 재계산하면 계약 변경 시 조용히 틀린 숫자 — 2026-08-27 normKind 와 같은 유형), **searchUrl 은 모든 상태에서 채움**(판정 실패·킬스위치 OFF·대상 아님 어느 경우든 이용자가 직접 확인 가능). 딥링크 몰(우체국)의 likely 는 '찾음' 집계에서 제외 — 온누리 범위 밖이 섞이므로. **부담 억제**: 버튼 트리거·캐시 TTL60분 LRU500·몰당 동시 1건·몰당 분6일600·IP당 분5일100·전역 동시 12·본문 상한 1MB·킬스위치. 캐시 적중 시 IP 한도를 소비하지 않는다(아웃바운드가 없으므로). 병렬은 가상 스레드 + 세마포어 상한. **게이트 실측 대조**: 3질의 × 6곳이 조사표와 일치 — hotdeal '다이슨 청소기' 히트 20(조사 21), sijang 공백삽입형 문구 매칭 성공, market 토큰0 판정 작동, chance 는 없는 질의에도 unclear(설계대로). **발견**: onnuri-gonggong-mall 이 5~8초로 유일하게 느리고 결과가 많은 질의일수록 느려 타임아웃이 갈린다 → ProbeTarget.timeoutMs(선언만 해두고 안 쓰던 필드)를 실제 배선해 이 몰만 8초. 예산은 8.5초에서 멈춘다 — 나머지 5곳이 중앙값 0.6초인데 한 몰 때문에 10초를 기다리게 할 수 없고, 타임아웃해도 사유 표시 + 직접 링크가 나가 이용자 손해가 없다. 제약: 로컬 DB·SpringBootTest 가 없어 실제 HTTP 응답 확인은 서버 배포 후로 미룸(서비스 전체 경로는 repo·fetcher 모킹으로 검증). gradlew 92건 통과 |
| 2026-08-31 | 온라인 실시간 조회 1단계 — 실현성 조사 + 판정 엔진(ADR-17) | _workspace/19_online_probe.md(신규)·16_arch_decisions(ADR-17), backend online/probe 6클래스+테스트 3종(+20건, 총 75), 픽스처 12개(1.7MB) | 사용자 요청("실시간 병렬조회로 정밀도를 극적으로 향상할 방법"). 태그 기반 `indexOf` 검색이라 "삼성전자 비스포크 로봇청소기"처럼 공백이 든 질의는 무조건 0곳이던 문제. **22곳 전수 실현성 실측**: app 컨테이너가 21-jre 라 브라우저가 없어 정적 HTTP 응답에 결과가 실리는 몰만 가능 → 8곳. 그중 **온누리굿데이·인더마켓은 robots.txt 가 `Disallow: /`** 라 제외해 **6곳**(저장소에 robots 검토 기록이 없어 이번에 처음 확인). 브랜드 178종 중 146종(82%) 커버, 병렬 5.2초. **판정 설계의 축**: 응답에 질의가 있다고 상품이 있는 게 아니다 — 온누리굿데이는 "비스포크 로봇청소기 검색결과" **제목 에코**만으로 토큰 3종이 각 1회 잡혔고, "로보락 큐레보"에는 **호환 리필 물걸레**가 걸렸다. 반대로 없는 질의에는 6곳 중 4곳이 명시적 '결과 없음' 문구를 낸다. **"0이면 없다"는 신뢰할 수 있고 "0이 아니면 있다"는 신뢰할 수 없다** → status 에 **'있음'을 만들지 않는다**(none/likely/unclear/unknown/not-probed). 없음-문구는 3등급 — A(질의 바인딩형, 단독 확정)·B(비의존형, 토큰 대조 병행)·C(사전 없음). **onnuri-market 은 검출되는 '없습니다'가 전부 이용약관**이라 전체 매칭 시 항상 '없음'이 되므로 등급 C 로 비우고 이유를 코드 주석에 남겼다. 상품명 샘플은 몰별 정규식이 필요해 ADR-6 우려에 해당하므로 **판정 경로 밖**에 뒀다 — 깨져도 '근거 없는 likely'로 품질만 떨어지고 오답이 되지 않는다. 검증 중 발견: onnuri-chance 가 없는 질의에도 unclear — **결함이 아니라 설계대로**(등급 C + 에코형이라 확정 수단이 없다). 테스트를 '확정 수단이 없는 몰은 없다고 말하지 않는다'로 정정. 회귀 3건 고정 — 약관 오탐·제목 에코·태그로 쪼개진 문구(survey_probe.js innerText 교훈과 동일 층위). 픽스처는 실측 응답 그대로 저장(실측 물증). gradlew 75건 통과 |
| 2026-08-27 | 온라인 필터 개편 — 물품종류 단계 선택(대분류 단일→소분류 다중) + 브랜드 드롭다운→검색 팝업 | online.html(renderCatChips 단계 UI·hasCat 엄격화·subOnlyExcluded·브랜드 모달 CSS/마크업/JS·카드 태그 접기) | 사용자 요청 2건. ①"태그는 상세한데 필터는 예전 버전" — 소분류를 35종 채록해 놓고 **대분류가 다중이라 2개 이상 고르면 소분류 행이 통째로 사라져** 고를 수가 없었다. 1단계 대분류 단일 + 2단계 세부 다중 OR, 상위 변경 시 하위 '전체' 리셋, 소분류 없는 대분류는 숨기지 않고 사유 표시(단계가 사라지면 조작 실수로 오해). ②브랜드 178종 `<select>` → merchants 와 같은 구조의 검색 팝업(부분검색·초성/알파벳 색인·다중 토글). merchants 와 달리 **고를 때마다 닫지 않는다** — 로컬 필터라 즉시 반영되고 겹쳐 보는 사용이 흔하다. **함께 적발**: `hasCat` 이 소분류를 물을 때 **부모 id 만 가진 몰도 매칭**시켜 김치 필터가 10곳→19곳으로 부풀어 있었다(데이터 교차 검증에서 발견). 소분류가 3개 대분류에만 있던 시절의 관대함이 세분화 후 과다 매칭이 된 것. 엄격화하고 줄어든 이유를 카운트 문구에 병기 — `· 세부 미확인 N곳 제외`. **사고**: renderBrandChips~renderAll 구간을 교체하며 그 사이의 `cardHTML`·`render` 를 통째로 삭제했다(ReferenceError). git 원본에서 복구. 부수: 태그가 한 몰에 20개를 넘어 카드가 화면을 다 차지하던 것을 8개+`+N` 으로 접되 **선택과 맞는 태그는 앞으로 끌어와 자르지 않는다**. 강조는 고른 단계를 따른다(김치를 골랐는데 대분류 하위 9개가 다 칠해지던 것 수정). 검증: 데이터 교차 대조 일치(김치 10·유제품 9·카메라드론 2·대분류 20/18), 단계 동작(단일↔다중·리셋), 팝업(178항목·초성 19헤더·색인 점프 0→2427px·검색 '삼성'→삼성전자 9곳·다중 2개·해제·Esc/backdrop), 모바일 390px 모달 359×692 뷰포트 수납·가로스크롤 없음·카드 279px |
| 2026-08-27 | 온라인 물품종류 소분류 10→35종 세분화 + **채록 스니펫 결함 수정** | survey_probe.js(COLLECT_SNIPPET textContent·CAT_RULES 46규칙·mapCats 부모제거 일반화), test +40건(총 146), data/online_catalog.json(taxonomy 35소분류·22곳 재태깅), config.js(dataVersion 08-27), 15_report | 사용자 요청("물품 종류를 최대한 디테일하게"). 범위=실측 3곳 이상, 방식=22곳 전수 재채록(사용자 결정). **핵심 발견**: COLLECT_SNIPPET 이 카테고리를 `a.innerText` 로 걷고 있었는데 **메가메뉴 하위는 display:none 이라 innerText 가 빈 문자열** — 최상위 메뉴만 채록되고 있었다. textContent 로 바꾸자 온누리핫딜 43→132문구·판정 소분류 2→30개. 소분류가 그동안 농·수·축에만 있던 이유가 이것이다(나머지는 2026-08-21에 사람이 메뉴를 펼쳐 본 것만 로그에 남았다). **오탐 7종 적발**(매칭 문구 전수 출력해 육안 검토): '가전'←'특가전'(할인쿠폰/특가전·반찬 특가전), '디지털'←'디지털온누리상품권 사용처', '조류'←'해조류', '펫'←'카펫', '조미'←'무조미김', '오리'←'오리온', '면'←'면도기', 그리고 반려동물 간식·영양제가 사람 식품으로. lookbehind·부정 lookahead 로 좁히고 **오탐과 정탐을 쌍으로** 테스트에 고정(오탐만 막고 정탐이 죽으면 그것도 실패). **반영 방침**: 직접몰 16곳은 기존 태깅과 합집합(실제 손실 0 — 감소는 전부 부모 단독 id 정리), 딥링크 6곳은 호스트 GNB 오염이 textContent 로 오히려 커져 **반영하지 않고 기존 유지**(2026-08-21 롯데ON 사고와 동종), 채록이 얕은 4곳(지니어스몰·인어교주해적단·놀장·시장을방으로)은 전문몰·별도화면 구조라 기존 유지. 검증: 테스트 146건 통과, 계약검사(미정의 cat 0·부모접두·고아 id 0) 통과, 렌더 실측 — 가전·디지털 18곳→소분류 칩 4개(생활·주방가전 17·계절가전 16·컴퓨터·모바일 13·카메라·드론 9)→카메라·드론 9곳, 챗 필터 경로 로봇청소기 3곳·소분류 라벨 직접 지정(김치 19·반려동물 12). 데이터 변경이므로 dataVersion 동반 상향 |
| 2026-08-27 | 챗봇 착지 화면이 항상 0곳이던 결함 수정 (kind 값 계약 불일치) | online.html(normKind 창구 신설·filtered 명시 비교·챗훅/URL 두 입력 지점) | 사용자 결함 제보("로봇청소기 답변엔 결과가 있는데 조회된 페이지는 비어 있다"). 재현·증거: navigate params `{kind:"shopping", cat:"가전·디지털", brand:"로보락"}` 로 착지 → 칩은 '가전·디지털 17'·'로보락' 활성인데 **30곳 중 0곳**. 원인: 내부 state.kind 는 탭 라벨("전체"/"쇼핑"/"배달", 한글)인데 외부 입력(챗 navigate·URL 파라미터)은 영문 코드("shopping"/"delivery")를 그대로 넣었다. filtered() 의 삼항 `state.kind==="쇼핑" ? p.kind!=="shopping" : p.kind!=="delivery"` 이 else 로 빠져 **배달만 남기고**, 바로 다음 줄 `catActive && p.kind==="delivery"`(물품 필터는 쇼핑몰 대상)가 그 배달을 전부 걷어내 0곳이 됐다. 즉 챗봇이 kind 를 보내는 **모든 온라인 질문**이 빈 화면으로 끝나고 있었다. 최소 테스트로 확증 — kind 제거 3곳 / "쇼핑"(한글) 3곳 / "shopping" 0곳. 수정: normKind() 정규화 창구를 만들어 챗훅·URL 두 입력 지점이 함께 쓰게(입력 지점이 늘어도 안전). 방어로 filtered() 삼항을 명시 비교로 바꿔 **알 수 없는 값이 조용히 '배달'로 취급되던 증폭 경로**를 막고, 그런 값이 들어오면 '전체'로 되돌린다. merchants.html 은 같은 패턴 없음 — region 은 SIDO key 검증 후 적용하고 cat·brand 는 화면과 같은 한글 도메인 값이라 코드/라벨 이원화가 없다(확인함). 검증: 로컬 실측 5케이스 — shopping 0→**3곳**(쇼핑 탭 활성)·delivery 8곳·kind 없음 3곳·"쇼핑" 3곳·이상한 값은 전체로 폴백. URL 파라미터 경로(`?kind=shopping&cat=…&brand=…`)도 3곳 |
| 2026-08-27 | **한도 오류 정정** — 충전한도 월 200만 → 구매한도 월 100만 + 보유한도 200만 (두 번의 오판 시정) | payment.html(규정표 2행 분리·정책자금 행 후퇴·출처 각주), rag_corpus(onnuri_notices 2026 정책공지 절 신설 7→10청크·semas_program_guide·onnuri_customer_center), build_rag_corpus.py(FAQ_CORRECTIONS 오버라이드), 01_policy_analysis(델타표·주장 5 정정·주장 6 신설), 03_content_spec(S17), backend ChatService(systemPrompt 출처 우선순위)+ChatContractTest(+1, 총 55) | 사용자 제보("월충전한도 100만·보유한도 200만인데 챗봇이 모른다") — **제보가 정확했다**. 1차 근거 확보: 온누리 공식 공지 「2026년 온누리상품권 할인 판매 및 정책 변경 안내」(중기부·소진공 공동, 2025-12-31 게시, **2026-01-01 시행**, notice/detail?no=6) — 개인·디지털 **구매한도(월) 100만·보유한도 200만**·선물 발송 200만/수령 100만, 기업은 한도 없음. **왜 두 번이나 틀렸나**: ①2026-08-11 채록 때 소진공 안내의 '월 100만'을 보고도 '구식 추정'이라며 200만을 채택 ②2026-08-25 재확인 때 웹 검색의 100만을 '2차 자료'라며 기각. 원인은 공식 FAQ·SEMAS 표(3순위)만 보고 **공지(2순위)를 끝까지 읽지 않은 것** — 그 공지가 이미지(포스터)라 '이미지 공지는 제목만 기록' 원칙에 걸려 본문을 아예 안 열었다. 이번엔 포스터 PNG를 내려받아 판독해 표를 그대로 옮겼다. **교훈: 이미지 공지라도 정책 수치가 걸리면 원본을 받아 읽는다.** onnuri.gift FAQ('월 최대 200만 원 충전')와 SEMAS 구매한도 표는 2026-08-27 현재까지 갱신 안 됨 — 인용 금지 대상으로 코퍼스에 명시. **챗봇 대응 2중화**: ⓐFAQ 원본 채록은 손대지 않되(공식이 그렇게 말한다는 것 자체가 사실) build_rag_corpus 가 해당 청크에 '※ 정정' 문단을 덧붙인다 — 그 청크만 검색돼도 올바른 답이 나오게. 질문 문구가 바뀌어 정정이 조용히 사라지는 것을 막으려 **미적용 시 빌드 실패**(SystemExit). ⓑsystemPrompt 에 '정정 표기를 따르고 최신 공지가 FAQ보다 우선' + '구매한도/보유한도를 섞지 않는다' 지시(계약 테스트로 고정). **정책자금 항목 후퇴**: 2026-08-25 고객센터 답변이 '충전한도 200만에 미포함'이었는데 200만이 보유한도로 밝혀져 **어느 한도를 가리켰는지 특정 불가** → '개인 한도에 포함되지 않는다'로 낮추고 그 사실을 보조행에 명시(핵심 사실은 유지). 검증: 청킹 142(FAQ 정정 문단 실물 확인), gradlew 55건 통과, 렌더 실측 규정표 8행·'월 최대 200만' 잔존 0·모바일 390px 가로스크롤 없음 |
| 2026-08-26 | 자동모드가 결제·용어·가이드 페이지엔 동작하지 않던 결함 수정 + 챗 예시 질문 1건 교체 | chat-widget.js(PAGE_INFO·onPage·handleAction 재작성, v13), 6 HTML·build_index.py 캐시버스트→index 재빌드, backend ChatService(navigate 오류 문구) | 사용자 결함 제보("자동모드인데 카드 실적 질문이 결제 페이지로 안 간다 — 가맹점 안내에만 적용됐나?") — **추측이 정확했다**. 원인: handleAction 의 `isSearch = page==='merchants'||page==='online'` 화이트리스트가 자동 이동을 2종으로 한정 — 백엔드 navigate 는 5종(+guide·payment·terms)을 보내는데 나머지 3종은 토글과 무관하게 확인 카드만 떴다. 2026-08-11 도입 당시 토글 이름이 '지도 바로 이동'이라 문구·동작이 일치했으나, **2026-08-26 라벨을 '자동모드'·설명을 '답변에 해당하는 페이지로 이동'으로 넓히면서 약속만 커지고 동작은 그대로**여서 결함이 됐다(거리뷰 flightSpot 과 같은 유형 — 문구가 옵션보다 앞서간 것). 수정: isSearch 게이트를 제거하고 PAGE_INFO 5종 표로 일반화. 경로 4갈래 — 같은 검색 페이지=제자리 필터 / 이미 목적지 페이지=이동 없이 알림(같은 문서 재로드는 무의미) / guide 해시가 다르면 location.hash(재로드 없음) / 그 외=답변 완료 후 이동. 모르는 page 는 카드로 폴백. 부수: 백엔드 navigate 오류 문구가 'merchants|online|guide 중 하나'라 실제 허용 5종과 어긋나던 것 정정(모델에게 잘못된 안내). 예시 질문 '카드형 결제 흐름을 그려서 설명해줘' → '카드연결하면 온누리상품권 써도 카드실적 인정돼요?'(사용자 원문의 '되요'는 '돼요'로 교정). 검증: localhost:8655(CORS 허용 오리진) + 라이브 API 로 e2e 5경로 실측 — ①라이브에서 결함 재현 확증(자동모드 ON인데 note 0·카드만·URL 불변) ②수정 후 terms→payment 실제 이동 ③payment 에서 재질문 시 '지금 보고 계신 결제 방법 화면입니다'(재로드 없음) ④토글 OFF 는 확인 카드(회귀 없음) ⑤merchants 제자리 필터 GS25 1곳(회귀 없음) ⑥index 에서 guide 액션 #online 해시 이동. gradlew 54건 통과, D-F1 통과. 검증용 config.js apiBase 임시 지정은 원복 확인 |
| 2026-08-26 | 브랜드 검색을 칩 줄 맨 앞 강조 버튼으로 승격(발견성 개선) | merchants.html(.brand-find CSS·aux-row 그룹 제거·renderBrandChips 생성·리스너 이관), guide-content-style(부분노출 원칙 보강) | 사용자 요청("브랜드 검색이 잘 안 보인다"). 실측한 원인: 트리거가 **브랜드 칩 2줄 아래 세 번째 줄**에 있었고 크기가 85×20px — 업종 칩(30px)·시장 유형 셀렉트(34px)보다 작아 위계가 가장 낮았다. 정작 이 버튼이 브랜드 전체(현재 지역 166종)로 가는 유일한 통로다. 안내문 '대표 브랜드만 · 전체는 브랜드 검색'은 회색 소자에 클릭도 안 되는 텍스트라 신호가 못 됐고, 라벨('브랜드 검색')과 버튼 텍스트('브랜드 전체')가 달라 혼란도 있었다. 수정: 트리거를 **브랜드 칩 줄 맨 앞(라벨 바로 뒤, '전체' 칩보다 앞)**으로 옮기고 칩과 같은 높이(30px)·오렌지 아웃라인으로 강조, 라벨을 '브랜드 검색'으로 통일, **숨은 종 수 배지(+156종)**를 붙여 버튼 자체가 부분노출 안내를 겸하게 했다(안내문 별도 표시는 제거 — 중복). 선택 상태 강조(.on)는 넣지 않음 — 선택 브랜드는 칩이 표시하고, 버튼을 채우면 활성 칩과 구분이 안 된다. 함정: 버튼이 renderBrandChips 마다 재생성되므로 초기 1회 addEventListener 는 유실된다 → 생성 시점 bf.onclick 으로 이관하고 초기 바인딩 삭제(필터 변경 후 재클릭으로 회귀 확인). aux-row 는 3그룹→2그룹이 되어 세로도 줄었다. 스킬에도 원칙 보강 — '안내를 어디 두느냐보다 행선지가 눈에 띄느냐가 먼저다'. 검증: 렌더 실측 PC 1440px·모바일 390px 모두 1줄 30px·칩과 높이 일치·가로 스크롤 없음, 팝업 열림·필터 변경 후 재열림 정상, 콘솔 신규 에러 0(잔존 에러는 localhost 네이버 401·백엔드 미기동으로 기존 사유). index 무영향(merchants 는 별도 페이지) |
| 2026-08-26 | 챗 토글 라벨 '지도 바로 이동'→'자동모드' + 모바일 헤더 줄바뀜 해소 | chat-widget.js/css(v12), 6 HTML·build_index.py 캐시버스트→index 재빌드, merchants·online 훅 주석 | 사용자 요청 2건. ①토글 라벨을 '자동모드'로, 설명을 "문의 시 추가 확인 없이 답변에 해당하는 페이지로 이동해 조회를 실행합니다"로 교체 (사용자 원문의 '확인없이'는 '확인 없이'로 띄어 씀). localStorage 키 onnuri_chat_automap 은 **유지** — 바꾸면 기존 사용자의 ON/OFF 설정이 초기화된다. ②모바일에서 헤더 제목·면책 문구·토글 라벨이 낱말 단위로 쪼개지던 것(제목 '온누리 가이드/챗', 라벨 '자동/모드', 면책 3줄) 수정. 원인은 패널이 80vw(390px에서 312px)인데 헤더가 한 줄에 dot+제목+면책+닫기(40px)를 다 넣으려 한 것 — 제목·면책에 남는 폭이 204px뿐이었다. 수정: 제목·토글 라벨에 white-space:nowrap(베이스 — PC에서 패널을 최소 320px로 줄여도 안 쪼개짐), 모바일에선 헤더를 flex-wrap 2행으로(1행 제목+닫기, 2행 면책 전체 폭 1줄·9.5px). 면책 문구의 `<br>`을 span 2개로 바꿔 PC는 block 2줄, 모바일은 inline+`·` 구분으로 한 줄이 되게 했다(면책은 헤더 상시 노출 원칙이라 숨기지 않는다). 부수: payment.html 만 chat-widget.css?v=9 로 뒤처져 있던 것도 v12 로 정렬(js 는 직전 커밋에서 이미 정렬). 검증: 렌더 실측 — 모바일 390px 제목·면책·라벨 전부 1줄·잘림 없음·패널 밖 넘침 없음, PC 1440px 회귀 없음(면책 2줄 우측정렬 유지·구분점 미표시), PC 패널 320px 축소에서도 쪼개짐 없음, D-F1 통과 |
| 2026-08-26 | 결제 페이지 POINT에 실증 캡처 2장 + 카드 실적 단정 상향(롯데만 → 대부분) | payment.html(.p-shot CSS·Point 1·2 figure·Point 2 본문 개정·출처 각주), assets/proof_approval_sms.jpg·proof_card_benefit.jpg(신규), _workspace/rag_corpus/card_payment_details.md(실적 절 개정 + '승인 문자 누적≠실적 집계' 절 신설, 6→7청크), 03_content_spec(S17) | 사용자 요청·제공 캡처. Point 1 = 승인 문자 2건(하나카드 '온누리승인' 15,000원 마린약국 / 같은 카드 일반 '승인' 쏘카) → "온누리 결제도 카드 승인 문자가 똑같이 온다"의 실물 증거. Point 2 = 카드사 앱 '실적 충족 상세내역'에 그 결제가 **실적포함**. **배치 정정**: 사용자 최초 지시는 실적내역→P1·문자→P2 였으나 각 POINT의 본문 주장과 어긋나 맞바꿈을 제안하고 승인받음(주장과 증거 1:1). **단정 강도 상향**: 기존 '롯데카드만 공식 확인·타사 단정 금지'(2026-08-11 원칙) → '대부분의 카드사에서 전월 실적으로 인정'. 근거는 롯데(공식 공지)+하나(실측) 2곳뿐이라 '확인 2곳 + 다수 추정' 절충안을 권고했으나 **사용자가 단정형을 선택** — 확인된 곳을 본문에 명시하고, 혜택(할인·적립)은 실적과 별개로 제외될 수 있다는 단서는 유지. **부수 발견**: 같은 결제가 승인 문자엔 '누적0원'인데 카드사 실적내역엔 '실적포함' — 문자만 보고 '실적 안 잡혔다'고 오판하기 쉬운 지점이라 Point 2 캡션·코퍼스에 병기(관찰 1건이므로 전 카드사 일반화는 안 함). **개인정보**: 문자 캡처에 이름 일부·누적 사용액·약국 방문 이력이 남고 저장소가 공개(Pages 서빙·git 히스토리 영구)라 마스킹을 권고했으나 **사용자가 원본 게재를 선택**. 구현: figure.p-shot(이미지 max-width 460px·loading=lazy·width/height 명시로 CLS 방지), alt 는 화면 내용을 문장으로 옮겨 이미지 없이도 사실 전달. .gitignore 는 *.png 만 막아 .jpg 는 추적됨. index 무영향(결제 블록은 7g 로 분리됨 — 재빌드 불필요). 검증: 렌더 실측 PC 460px·모바일 390px 305px 넘침 없음·가로 스크롤 없음·두 이미지 로드 확인, 모바일에서 '실적포함' 판독 가능, 코퍼스 드라이런 139청크 |
| 2026-08-25 | 챗 예시 질문 2건 추가(앞머리) + 인사 화면 스크롤 위치 수정 | chat-widget.js(HINTS·setOpen v11), 6 HTML·build_index.py 캐시버스트→index 재빌드 | 사용자 요청. HINTS 앞에 "온누리로 로봇청소기를 사고 싶어."·"다이소 중에 온누리되는데 있어?" — 기존 4건은 순서 유지(총 6). **함께 적발·수정**: 칩이 4→6개가 되자 모바일(390px·패널 312×506)에서 인사말이 화면 밖으로 밀렸다(scrollTop 59.5·greetVisible false). 원인은 setOpen 이 열릴 때 무조건 scrollEnd() 를 부르는 것 — 이력을 이어보려는 의도지만 인사 화면에는 맞지 않고, 칩이 늘수록 인사말이 더 밀린다(기존 구조가 이번 변경으로 드러남). 수정: `history.length ? scrollEnd() : body.scrollTop = 0` — 이력 있으면 마지막 답변, 없으면 맨 위. 검증에서 이력 6턴 심고 복원 시 마지막 답변 노출 유지 확인(회귀 없음). 부수: payment.html 만 chat-widget.js?v=9 로 뒤처져 있던 것(2026-08-15 v10 일괄 상향 때 누락)을 v11 일괄 상향으로 함께 정렬. 검증: 렌더 실측 — 칩 6개 순서 정확·PC 패널 내 132px 넘침 없음·모바일 인사말 노출 복구(scrollTop 0)·가로 스크롤 없음, index 재빌드 후 D-F1 전체 통과 |
| 2026-08-25 | 사이드바 대분류 순서 변경 — '가맹점 스마트 검색'을 '사용 가이드' 위로 | 7 HTML(merchants·online·payment·terms·news·report·admin-report)·build_index.py→index 재빌드 | 사용자 요청. 실제로 가장 많이 쓰는 진입점(가맹점·온라인 사용처 검색)이 제도 설명 아래에 있었다 — 도구를 위로. 그룹 내부 항목 순서·라벨·href·active 표기는 그대로 두고 두 그룹 블록만 통째로 맞바꿈(줄 단위 스왑이라 페이지별 들여쓰기·aria-current 차이가 보존된다). index 번들의 setActive 는 href 로 활성 항목을 찾으므로 순서 변경 영향 없음(해시 #online 실측 확인). shell.css/js 무수정이라 캐시버스트 불필요. 검증: 8페이지 전부 그룹 순서 '가맹점 스마트 검색→사용 가이드→소식→피드백'·sb-item 8개 유지·각 페이지 active 정확, index 재빌드 후 D-F1 전체 통과 |
| 2026-08-25 | 정책자금 지급분은 월 200만 원 충전 한도에 미포함 — 페이지·챗봇 동시 반영 | payment.html(규정표 '정책자금 지급분' 행 신설·'충전 한도' 행 한정·.src 스타일·출처 각주), _workspace/rag_corpus/onnuri_customer_center.md(신규 코퍼스 원천), 01_policy_analysis(주장 5), 03_content_spec(S17), 17_chatbot_design(원천표), backend ChatService(systemPrompt 출처등급 지시)+ChatContractTest(+1, 총 54) | 사용자가 온누리 고객센터(1670-1600)에서 유선으로 받은 답변. **공식 홈페이지 FAQ에는 없는 내용** — FAQ는 "월 최대 200만 원까지 충전 가능"만 말하고 정책자금 지급분의 포함 여부를 밝히지 않는다. 정황 근거로 소진공(SEMAS) 안내의 해당 표가 제목 '구매한도'·행 '개인 — 카드형·모바일 월 200만원'이라 *구매* 기준이며(원문 _raw/semas_SUP011205.html 확인), 구매하지 않고 지급받은 금액이 빠진다는 답변과 어긋나지 않는다. 검증 한계: 2026-08-25 기준 onnuri.gift 공개 API가 faq=9999(시스템 오류)·notice=9998(접근 권한 없음)을 반환해 **공식 최신본 재확인 불가**(과거 수집본 faq_complete.json으로 대조). 웹 검색에는 구매한도를 100만 원으로 적은 2차 자료가 섞여 있었으나 공식 표기(200만)를 유지하고 채택하지 않음. 단정 강도: 출처 등급이 다르므로 페이지는 보조행(.src)에, 챗봇은 프롬프트 지시로 "고객센터 유선 확인·공식 홈페이지에는 없는 안내"를 병기하게 하고 큰 금액 충전 전 재확인을 권한다 — 지시가 프롬프트에서 빠지면 계약 테스트가 실패한다. 챗봇 반영은 코퍼스 파일만으로는 미완 — **서버에서 build_rag_corpus.py 재적재(DEPLOY.md 4단계)가 있어야 실제 답변이 바뀐다**(로컬엔 DB·OPENAI_API_KEY 없음). 검증: 청킹 드라이런 137청크(onnuri_customer_center 1청크·697자, 200만/정책자금 키워드 포함), gradlew 54건 통과(신규 테스트 RED 확인 후 GREEN), payment 렌더 실측 7행·보조행 12.5px rgb(88,93,100)=--text-sub·모바일 390px 가로스크롤 없음, verify_build 전체 통과 |
| 2026-08-24 | 모바일에서 지도 팝업이 곧바로 사라지던 결함 수정 | merchants.html(openInfoWindow 창구 신설·idle 1회 생략, 모바일 .iwg-list 230→150px) | 사용자 결함 제보(동일주소 여러 가맹점). 원인: 모바일 지도는 388px인데 그룹 팝업이 347px라 SDK가 팝업을 화면에 넣으려 지도를 자동 pan → idle 1회 → 400ms 뒤 viewportRender → clearMarkers 가 infoWin.close() (:1952). PC는 지도 780px라 pan이 없어 멀쩡했다(대조 실측). 정정: 제보는 "5개 이상"이었으나 실측 결과 **개수 무관** — 3곳(팝업 277px)도 동일하게 900ms에 소멸. 항목이 많을수록 커져 눈에 띄었을 뿐. clearMarkers 주석에 리스트 행 클릭 때 같은 사고를 겪고 KEEP_INFO_ONCE로 막은 기록이 있었는데, 마커 클릭 경로는 보호되지 않았다(당시 "마커 클릭은 지도를 안 움직인다"고 본 것 — 큰 팝업이 미는 경우를 놓침). 수정 A: openInfoWindow 창구를 만들어 개별·그룹 팝업이 같은 보호를 받게 하고, clearMarkers 가 **방금 연 팝업(1.5초 유예)**은 닫지 않게 했다. pan 자체는 살려 둔다(모바일에서 막으면 팝업이 잘린다 — disableAutoPan 안 씀). 1차 시도였던 'SKIP_IDLE_ONCE 로 idle 1회 생략'은 폐기 — 팝업 뒤 첫 idle 이 팝업 pan 이 아닐 수 있어(딥링크 착지는 morph 후 팝업을 연다) 그 morph idle 을 먹어 **마커가 하나도 안 그려지는** 회귀가 났다(라이브에서 pin=null 확인 후 교체). 수정 C: 모바일(≤959px) .iwg-list 230→150px — 팝업이 지도를 거의 가리던 답답함 완화 |
| 2026-08-24 | 거리뷰 지면 클릭 이동 + 초기 시선을 가맹점 방향·하향으로 | merchants.html | 사용자 제보("지면 클릭했을 때 이동을 안 하네") + 요청(초기 시선). 조사: flightSpot 화살표 클릭 이동은 **정상 동작**(마커 561→564 이동 확인)이나, 기본 패널(558×355)에서 화살표가 뷰 아래로 밀려 화면 밖(arrow y 721~912 vs viewBottom 717) — 누를 수가 없었다. 해결 ①초기 pov tilt 0→-12°로 지면을 화면에 넣고, pan 은 init 에서 실제 스냅 위치 기준 bearingDeg 로 가맹점 방향(요청 좌표와 촬영점이 다르므로 생성 시점 계산은 부정확). ②지면 클릭 이동 신설 — projection.fromOffsetToCoord 는 지면 교점이 아니라 수백m 구면 투영이라 사용 불가(하늘 클릭도 413m), fromScrollToPov 는 null 반환. 그래서 클릭 x 오프셋 → 시선 기준 각도(atan·tan 원근 보정) → 방위 12m 앞 좌표 setPosition(가까운 촬영점 스냅은 SDK가 처리 — 실측 확인). 가드: 드래그 오인 방지(이동 6px·700ms), 지평선 위(하늘) 무시, 화살표 요소 클릭은 SDK에 위임(중복 이동 방지) |
| 2026-08-24 | 상호명에 즐겨찾기 ☆·태그가 섞이던 결함 수정 | merchants.html(행 data-name) | 거리뷰 통합 검증 중 발견. 행 클릭 경로가 .name 셀 textContent 를 통째로 읽어(:2128) 즐겨찾기 버튼 ☆/★ 가 이름 앞에 붙었다 — "거리뷰 — ☆주식회사 스타몽마켓". 같은 셀에 브랜드 태그·거리 태그(거리순 정렬 시)도 있어 함께 섞일 수 있었다. 원본 데이터는 "주식회사 스타몽마켓"으로 정상. 2026-08-13 즐겨찾기 도입 때 유입된 것으로 보이며 상세 팝업 제목·거리뷰 제목에 계속 노출되고 있었다(이번에 지정 위치 라벨·가장자리 화살표까지 오염되며 드러남). 수정: 행에 data-name(순수 상호)을 심고 클릭 경로가 그것을 쓰도록 — 폴백은 앞머리 ★☆ 제거 |
| 2026-08-24 | 거리뷰 3종: 지면 이동 화살표 + 시야 부채꼴 2배 + 파노라마 안 지정 위치 표시 | merchants.html | 사용자 요청 3건. ①flightSpot false→true — 패널 안내문은 2026-08-13부터 "화살표로 도로를 따라 걸을 수 있습니다"라고 약속했는데 옵션이 꺼져 있어 문구·동작이 불일치했다. ②시야 표시를 border 삼각형(30×22)에서 conic-gradient 부채꼴(반지름 44, 2배)로 — wrap 44→88, 앵커 (22,22)→(44,44), 시야각 68° 유지. ③파노라마 안에 선택 가맹점 위치 오버레이(핀+라벨) — "로드뷰에서 어디가 지정위치인지 안 보인다"는 제보. 조사: SDK Marker·InfoWindow 는 map 으로 Panorama 를 받아주지만 **투영되지 않는다**(DOM 이 -9999px 에 방치 — 실측). projection.fromCoordToOffset 이 화면 중심 기준 오프셋을 정확히 주고(45°에서 ±268px 대칭 확인), 시야 밖이면 (-9999,-9999) 센티넬을 반환해 숨김 판정에 쓸 수 있다. pov_changed 는 드래그 중 연속 발생 → rAF 스로틀. pointer-events:none 으로 지면 화살표·드래그를 가리지 않는다. 시야 밖이면 좌/우 가장자리에 방향 화살표(각도차 -180~180 정규화). 지도 구석 토글 진입은 지정 위치가 없어 미표시(그 지점이 곧 현재 위치). 라벨에 "대략 위치" 병기 — 대표좌표가 건물 단위라 점포 입구가 아니다(안내문도 함께 개정). 검증 제약: 네이버 지도 Client ID 가 도메인 제한이라 localhost 는 401 — 라이브에 수정 코드를 주입해 검증(부채꼴 4방향 회전·오버레이 정면 배치·시야밖 6각도 좌우 판정 전부 정확). 통합 검증은 배포 후 |
| 2026-08-23 | 브랜드 표기 통일(183→178종) + 배치 델타 반영 + meat-chicken 결함 수정 | survey_probe.js(BRAND_ALIASES·normalizeBrand·CAT_RULES 오타), data/online_catalog.json, test +21건(총 73), 15_report | 사용자 요청. ①동의어: 배치가 매 회차 ECOVACS를 새 브랜드로 올리던 것(카탈로그엔 에코백스) → BRAND_ALIASES로 표준화하고 computeDelta도 같은 사전으로 비교. 통일 대상은 영문/한글 음차(ECOVACS·KODAK·NEXTU·Freewell)와 약칭→정식명(삼성→삼성전자·LG→LG전자·매일→매일유업), 씨제이제일제당→CJ제일제당. **묶지 않은 것**: 종근당/종근당건강·대상/대상웰라이프·롯데/롯데칠성·삼양/삼양식품·아디다스/아디다스골프 — 다른 법인·제품군이라 묶으면 정보가 뭉개진다(통합은 되돌리기 어려운 방향이라 불확실하면 유지). ②델타 반영: 지니어스몰 +쿠쿠·에브리봇, 놀장 +appliance, 두 곳 surveyed_on 08-23. 시장을방으로는 여전히 thin이라 partial·08-10 유지. ③함께 적발: 08-21에 내가 넣은 meat-chicken이 taxonomy 실제 id(meat-poultry)와 불일치 — 6개 몰에서 "닭·오리·계란" 소분류 칩 누락(11→17곳). 당시 검증이 대분류만 봐서(split("-")[0]) 통과했다. (l) 데이터 계약 검사 신설: 소분류 포함 taxonomy 정의 여부·부모 접두 규칙·플랫폼 고아 id·별칭 잔존·partial의 날짜 위조. 검증: 73건 통과, 렌더 실측(브랜드 178종·축산 소분류 20/19/17·닭오리계란 필터 17곳). 데이터가 바뀌었으므로 dataVersion 도 08-23 으로 동반 상향(같은 커밋) |
| 2026-08-23 | 서버 단계 D 가동 — 배치 전용 Node 20 + Playwright 설치·검증 | 서버(/opt/node20 신규, ~/onnuri_batch/run.sh PATH 추가, playwright+chromium), DEPLOY.md(실제 구성 반영) | 사용자 요청("Playwright 추가했어" → 서버 확인 결과 미설치). 서버 조사에서 두 문제 발견: ①node v12.22.9(우분투 기본)라 playwright 요구사항(18+) 미달이자 내 스크립트의 ?? 연산자도 문법 오류 ②playwright 흔적 없음. 사용자는 시스템 Node 교체(A안)를 승인했으나, 조사 중 이 서버에 온누리 외 koscomlabor-web/api/db 가 공존하고 koscomlabor-web/deploy.sh 가 매일 00:10 npm run build 를 도는 것을 확인 — apt nodejs 교체 시 node-* 의존 연쇄 제거로 타 서비스가 깨질 수 있어 **배치 전용 Node(/opt/node20) + PATH 우선**으로 방식 변경(목적 동일·부작용 없음). 함정: npm 셔뱅이 `env node` 라 PATH 를 앞세우지 않으면 v12 를 집는다. cron 은 PATH 가 최소라 run.sh 에 export PATH 필수 — 실제로 오늘 새벽 배치 로그에 "D 스킵: node 없음"이 찍혀 fail-open 이 작동했음을 확인. 검증: 서버 실행 결과가 로컬과 완전 일치(지니어스몰 새 브랜드 3·본문 3,808자 / 시장을방으로 thin·354자 / 놀장 새 카테고리 appliance·5,538자), cron 최소환경(env -i) 배치 경유 D 판정 OK 14.4s, 시스템 node v12 불변·컨테이너 6개 정상 |
| 2026-08-23 | 채록 배치 시각 기준을 로컬로 통일 (KST 00:30 실행이 전날로 찍히던 결함) | survey_probe.js(todaysSlice 로컬 epochDay·localDate·localStamp 신규), survey_nightly.js(로그·리포트 파일명), test +5건(총 52) | 자체 발견. todaysSlice가 getTime()/86400000(UTC 기준)을 써서, 배치가 도는 KST 00:30이 UTC로 전날 15:30이 되어 **하루 전 묶음**이 뽑혔다. 리포트 파일명도 toISOString 기반이라 전날 날짜로 저장됐다(KST 08-23 00:30 실행 → survey-delta-2026-08-22.json). 순환 자체는 어느 쪽이든 7일 한 바퀴라 기능 결함은 아니지만, 배치 로그(파이썬·로컬시각)와 기준이 달라 "오늘 어디를 봤나" 대조가 불가능해진다. 서버 TZ가 UTC인 경우(NCP 기본)엔 더 헷갈린다. 수정: 로컬 자정 기준 epochDay + localDate/localStamp로 배치 로그와 표기 통일. 회귀 테스트 — 00:30과 정오가 같은 묶음, 전날과는 다른 묶음. 검증: 테스트 52건 통과, 실제 실행에서 로컬 표기(12:14:58)·파일명(survey-delta-2026-08-23.json)·오늘 몫 3곳 정확. 시장을 방으로는 예상대로 thin 표시(주소 선행 구조) |
| 2026-08-22 | 온라인 실측 스탬프 표시 개정 + 채록 배치 편입(단계 D, 탐지 전용 · ADR-16) | online.html(META.survey 계산·metaLine·footMethod), backend/tools/survey_nightly.js(신규)·nightly_update.py(단계 D·DB 연결 가드), survey_probe.js(mapCats·computeDelta·todaysSlice), test 47건, DEPLOY.md, 16_arch_decisions | 사용자 요청 2건. ①스탬프: min(surveyed_on)만 쓰면 22곳 중 21곳을 새로 확인해도 못 본 1곳 때문에 화면이 옛 날짜를 말한다 → 대표 확인일=ok 항목의 최신, 뒤처진 곳은 "N곳은 YYYY-MM-DD 확인분"으로 병기(미확인 항목 날짜를 올리는 것은 여전히 금지). ②배치: 하루 3~4곳 순환(일주일 한 바퀴)으로 채록해 현재 카탈로그와 델타를 리포트로만 남긴다 — 데이터 수정·커밋·푸시 없음. 사용자 결정(탐지·리포트만 + 매일 분산). 자동 반영 기각 근거: 단계 B는 공식 API라 안정적이지만 채록은 HTML 스크래핑 — 검증에서 사이소가 기획전 딥링크라 몰 전체 GNB가 섞여 축산·수산 카테고리 6종을 새 항목으로 올렸다(자동 반영이었으면 오염). 딥링크 몰은 리포트에 ※ 표시. 델타는 추가 방향만 보고(삭제 필드 자체를 두지 않음 — 기획전 회전으로 미관찰이 삭제 신호가 되면 안 됨), 본문 얇으면 thin 표시. A~C와 동일하게 fail-open(node·playwright 없으면 스킵). 실행 검증에서 버그 1건 적발: Playwright evaluate에 함수 문자열을 그대로 넘겨 전 몰 수집 실패 → 즉시호출 `(${SNIPPET})()` 로 수정하고 회귀 테스트 고정. A·B 모두 스킵 시 DB 연결을 생략하도록 가드 추가(D 단독 실행 불가 문제). 검증: 테스트 47건 통과, 실제 Playwright로 2곳 채록 성공(사이소 새 카테고리 6·지니어스몰 새 브랜드 3 탐지), 스탬프 렌더 "2026-08-21 기준 · 1곳은 2026-08-10 확인분" |
| 2026-08-21 | 온라인 사용처 물품종류·브랜드 재실측 (브랜드 42→183종, 출산·유아 대분류 신설) + 채록 프로브 자산화 | data/online_catalog.json, 15_online_catalog_report.md, dev_scripts(survey_probe.js·survey_run.js·test_survey_probe.js 신규) | 사용자 요청. 쇼핑 22곳 전수 재실측(1차 2026-08-10 이후 11일). 브랜드 급증은 몰이 늘려서가 아니라 1차에 놓친 공식 브랜드 목록을 찾은 것 — 온누리찬스 브랜드관 전수 133종(1차 20), 온누리핫딜 /brand "판매 중 15개"(1차 6), 온누리쇼핑 60(1차 4), 굿데이 20(1차 1), 인더마켓 16(1차 2), 온누리시장 10(1차 0). 브랜드는 합집합 반영 — 기획전 회전이라 이번 미관찰분(닌텐도·KODAK 등)을 지우면 목록이 계절을 탄다. taxonomy에 baby(출산·유아) 신설: 8개 몰에서 확인(기저귀·분유·유모차·카시트). 팔도시장은 옷·신발·패션잡화·화장품·문구/완구 가게 신설, 인어교주해적단 155→157곳. 사용자 요청으로 채록 스크립트 자산화 + QA: 수집(브라우저 COLLECT_SNIPPET)과 판정(Node analyze)을 분리 — 브라우저로 보낸 코드가 테스트한 코드와 달라지는 것을 막기 위함. 테스트 30건 통과, 실측에서 겪은 오탐(파인애플→애플·시장대상가→대상·ZH-LG901G→LG)과 절단 버그(종료조건 원$가 뉴트리원에서 목록을 9개로 자름, 정답 133)를 케이스로 고정. 경계 깨진 매칭은 자동 제외하지 않고 suspect 표시만 — 자동 제외하면 LG전자·삼양식품·롯데칠성·아디다스골프까지 사라진다. 미확인 1곳(시장을 방으로 — 배달주소 선택 선행 구조라 비로그인 카탈로그 접근 불가)은 survey_status=partial·수집일 2026-08-10 유지 → min(surveyed_on) 규칙상 화면 스탬프도 08-10 유지(21곳을 새로 봤다고 못 본 1곳 날짜를 올릴 수 없음). 공영쇼핑은 스크롤 대기 재시도로 기획전 섹션 13종 확인해 ok 승격. 검증: 스키마 누락 0·taxonomy 미정의 cat 0·고아 id 0, 렌더 실측(브랜드 칩 상위10·드롭다운 183종·출산·유아 칩 9곳·닌텐도 필터 3곳·가로스크롤 없음). online.html 무수정(데이터 주도). **config.js dataVersion 2026-08-10→08-21 별도 커밋(64261b8)** — 데이터만 고치고 이걸 빼먹어 기존 방문자가 ?v= 로 캐시된 옛 카탈로그를 계속 받는 상태였다(파일명이 그대로라 쿼리만이 무력화 수단) |
| 2026-08-19 | 디자인 토큰 출처 단일화 (index 인라인 → shell.css 팔레트) | build_index.py(7f COLOR_MAP 목표값·웜톤 정규식 2곳), verify_build.py(출처 일치 검사 8개), index.html 재빌드, 04_build_notes | 사용자 결정(비교 자료 검토 후 승인). 2026-08-10 UI 대비 강화 때 shell.css만 새 팔레트로 가고 7f 매핑표는 구 모노톤에 남아 토큰 출처가 둘로 갈렸던 것 — 한쪽만 고치면 나머지가 조용히 어긋나는 구조(7f 우회 회귀가 그 유형). 소스 12곳 수정 → 템플릿 121곳 반영: --text #17181A→#0B0C0E(21), --text-sub #6B6E73→#585D64(33), --border #E6E6E6→#E5E6E8(33), --surface #F7F7F7→#F6F6F7(19), --surface-2 #F0F0F0→#EFF0F2(14). 추가로 매핑·검사 양쪽을 빠져나간 웜톤 #9A968F 1곳(2026-08-10 S16에서 신규 도입, 원본 번들엔 없던 값)을 --text-sub로 정리 — faint(#989DA5)는 11.5px에서 대비 2.73:1 AA 미달이라 기각, 위계는 글자 크기가 이미 담당(2.94:1→6.64:1). 웜톤 정규식 2곳에 9A968F 추가로 재발 차단. 판단 근거: 배경·선 3종 ΔE<1.1(구분 불가), 텍스트 2종만 ΔE 5~7이나 둘 다 대비 상승(5.12→6.64, 17.77→19.57). 동일 조건 렌더 픽셀차 뚜렷한 변화 0.06%·레이아웃 불변. 검사: 구 모노톤 5종 잔존 0 + 신 값 3종이 shell.css·템플릿 양쪽 동시 존재(한쪽만 고치는 결함 차단). 검증: 인라인 hex 10종 전부 shell.css 토큰과 1:1(고아 색 0), D-F1 전체 통과, 렌더 실측 rgb(88,93,100)·rgb(11,12,14) 일치·번들러 에러 0 |
| 2026-08-19 | verify_build.py 낡은 검사 19건 정리 + 7f 색상매핑 우회 회귀 수정 | verify_build.py((e)(g) 갱신·(i) 이관무결성 신설), build_index.py(TERMS_POINTER hex 교정·7g-guard 신설), index.html 재빌드, 04_build_notes | 사용자 요청(파비콘 수정 때 보고한 별건). 19건을 토큰별 현재 소재(템플릿/다른 페이지/shell.css/shell.js/없음)로 조회해 판정 — 의도된 변경 18건(payment·terms 이관 6, shell 외부화 8, 문구 개정 2, 메뉴 통합·페이지 분리 2), 진짜 회귀 1건. 회귀: 7f(웜톤→모노톤 일괄 치환)는 "마지막에 한 번" 전제로 쓰였는데 2026-08-11 7g가 그 뒤에 추가되며 TERMS_POINTER가 매핑을 우회 → 웜톤 #8A8580·#26231F 부활(7f 자체 잔존검사도 7g보다 먼저 끝나 미포착). COLOR_MAP 목표값(#6B6E73·#17181A)으로 교정하고 7g-guard로 재발 차단. 부수 효과: 각주 12px 대비 3.65:1(AA 미달)→5.12:1(AA 통과). 검사 갱신 원칙 — 토큰을 지우기만 하면 "이관"과 "소실"을 구분 못 하므로 (i) 이관 무결성 신설(목적지 실물 + index 포인터 동시 검사). payment.html 앱(QR)형 섹션 온전함 확인(소실 없음). 검증: 83개(64P/19F) → 84개 전부 PASS(순증 — 느슨화 아님), 7g-guard 통과, D-F1 리터럴 </ = 0, 렌더 실측 색 일치·번들러 에러 0. 미해결(판단 필요): ①디자인 토큰 이원화 — index 인라인은 7f 구 모노톤(#17181A 등), shell.css는 2026-08-10 재조정 팔레트(#0B0C0E 등). 통일은 index 전역 시각 변경이라 보류 ②템플릿에 죽은 const regionApps 정의 잔존 |
| 2026-08-18 | index 파비콘·탭 제목 소실 수정 + D-F1 검증 복구 | build_index.py(7h 신설), verify_build.py((a)(b) 정규화·(h) 신설), index.html 재빌드, 04_build_notes | 사용자 결함 제보("파비콘이 안 보인다"). 원인은 도메인 변경이 아니라 번들 로더의 document.documentElement.replaceWith — 파비콘·탭 제목을 심은 외곽 <head>가 문서 교체 순간 통째로 사라지고 템플릿 내부 head에는 meta·script뿐이었다(2026-08-12 파비콘 도입 이후 계속. 실측: 로드 완료 후 title="" ·link[rel=icon] 0개, merchants.html은 정상). 7h로 템플릿 내부 head에도 주입(인코딩 앞 — 뒤면 무반영, 7g와 같은 함정. 중복 주입 가드). 파비콘은 상대경로 유지 — index 번들이 이미 shell·config·chat-widget·CI png를 외부 참조라 자기완결 아님(서버 로그 확인). 부수: verify_build.py가 2026-08-12부터 죽어 있었음(외곽 link 1줄 증가로 라인 인덱스 밀림 → 무결성·JSON 파싱까지 전부 FAIL) → 정규화본 비교로 복구하고, 외곽만 보면 이번 결함을 놓치므로 템플릿 내부 검사 (h) 신설. 검증: RED(내부 title·link FAIL 2건)→재빌드→GREEN 5건, 캐시 없는 새 오리진 서버 로그에 GET /favicon.svg?v=1 200 실요청, title 복구·렌더 회귀 없음. 미해결(별건): 수정 전후 동일하게 실패하는 낡은 검사 19건 — payment/terms 분리·셸 외부화로 사라진 토큰, TERMS_POINTER에 하드코딩된 웜톤 hex 2개. 의도/회귀 건별 판단이 필요해 임의 삭제 안 함 |
| 2026-08-18 | admin 페이지 문서화 + report 하단 관리자 링크 | README.md(운영 절 신설), report.html(foot-note 링크) | 사용자 요청. README에 운영—제보 관리자 페이지 절(진입 2방식·비밀값 원칙·DEPLOY.md 포인터, 공개 저장소라 메커니즘 수준만). report.html 안내문에 관리자 페이지 링크 병기(faint 톤·밑줄) — 사용자 판단: URL 은닉은 방어선이 아니며 비밀번호+rate limit이 통제. admin noindex 유지 |
| 2026-08-18 | 챗봇 시스템 프롬프트 '그림 우선 설명' 개정 | backend ChatService(systemPrompt 출력형식 절), ChatContractTest(+1, systemPrompt 패키지 가시성) | 사용자 요청. 절차·조건 분기·구조·기한 설명은 mermaid를 먼저 그리고 텍스트는 보충("그림으로 보여줄 수 있나"를 먼저 판단) — 노드 8개 이내(모바일 패널)·기존 파싱 가드(대괄호 라벨) 유지, 단순 사실엔 억지 그림 금지. 지시어를 계약 테스트로 고정(프롬프트에서 빠지면 테스트 실패). gradlew 53개 녹색 |
| 2026-08-18 | terms 유의사항에 위치정보 이용 안내 추가 (6→7가지) | terms.html, 03 spec | 사용자 요청(위치 동의 질문 후속 — 투명성). "브라우저 동의 후 가까운 순 정렬·현재 위치 표시에만 사용·서버 미저장" 1줄. 사실 근거: 좌표는 정렬 쿼리에만 1회 전달(POST body 미저장·로그 비노출 ADR-13), 현재위치 마커는 클라이언트 전용 |
| 2026-08-18 | admin 비밀번호 로그인 (POST /api/admin/login) | backend(admin/AdminController·web/ClientIp 신규, ChatConfig adminLoginRateLimiter 분5/일30, application.yml app.admin.password, Report/ChatController IP추출 통일), admin-report.html(로그인 폼), DEPLOY.md, 계약테스트 15개 신규(총 52) | 사용자 요청·결정(서버 검증 로그인, 비번은 서버 .env APP_ADMIN_PASSWORD만 — 비면 로그인만 비활성 403, 사유 미구분·상수시간 비교·비번 로그 금지). 성공 시 관리자 키 반환→sessionStorage 기존 흐름 재사용, ?key= 진입 유지. dev-qa 1차에서 XFF 스푸핑으로 rate limit 완전 우회(F-6) 적발 → ClientIp(XFF 마지막 값 — 프록시 부착값이라 위조 불가)로 admin·report·chat 3곳 일괄 수정(chat은 backend가 추가 발견 — LLM 비용 통제라 최고 위험). 2차 재검증 프로덕션 토폴로지 공격 재현 6회째 429 차단 — 판정 통과. 실패 WARN·성공 INFO 로그(시각·IP만). 제약: 신뢰 프록시 1홉 전제(app ports: 직접 노출 금지 — DEPLOY.md 경고) |
| 2026-08-15 | 챗봇 패널 기본 활성화 + 모바일 높이 60% | chat-widget.js/css(v10), 5 HTML·build_index.py 캐시버스트→index 재빌드 | 사용자 요청. 로드 시 패널 자동 열림(setOpen — 포커스 없음: 모바일 키보드 팝업 방지), 닫으면 sessionStorage(onnuri_chat_closed)로 같은 세션 동안 닫힘 유지·새 세션엔 다시 열림. 모바일 높이 80→60dvh(폭 min(80vw,480px) 유지 — 60vw는 234px라 가독성 기각). 검증: Playwright — 데스크톱 자동열림·닫힘유지·새세션 재열림, 모바일 390px 높이 정확히 60%·가로스크롤 없음·FAB숨김/헤더X, index 번들 동일 동작. D-F1 통과 |
| 2026-08-15 | 페이지 도메인 서브도메인 이전 완료 (onnuri.koscomlabor.cloud, ADR-15) | backend application.yml(CORS 오리진 추가), README.md, DEPLOY.md, 16_arch_decisions.md, 가비아 DNS(CNAME onnuri→canduk910.github.io), GitHub Pages 설정 | 사용자 결정 — apex는 게이트 포털(별도 저장소)로, 이 프로젝트는 온누리 전용 서브도메인. 페이지 도메인은 코드 미참조(API 도메인만 절대 URL)라 프론트 무수정. 검증: 신규 도메인 8페이지 전부 200·인증서 발급·API CORS 실호출 ACAO 정확·github.io 301 자동. apex는 게이트 저장소 등록 전까지 404(의도) — 구 딥링크 리다이렉트는 게이트 404.html 책임. localStorage는 오리진 단위라 신 도메인에서 초기화(수용) |
| 2026-08-14 | 거리뷰 시야 방향 콘 + 촬영일자 표시 | merchants.html | 사용자 요청 2건. ①지도 마커를 주황 원+시야 부채꼴(.street-spot-wrap)로 — pov_changed마다 pov.pan(정북 0°·시계방향)으로 회전(전환 .15s). ②패널 헤더에 촬영일(location.photodate → "촬영 YYYY.MM") — 메타 없으면 비움(사실만), pano_changed·init 갱신·닫기 시 초기화. 검증: 실드래그 콘 0°→41.7° 회전·촬영 2026.03 표시. 2026-08-14 사용자 검증 완료 — 정상 |
| 2026-08-14 | 제보 처리상태 관리자 페이지 (B안 — 비밀 키) | admin-report.html(신규), backend(ReportController /{id}/status·X-Admin-Key·상태 화이트리스트, CorsConfig allowedHeaders, application.yml app.admin.key), DEPLOY.md | 사용자 결정. 키=서버 .env APP_ADMIN_KEY만(저장소·채팅 비노출, 비면 403 비활성). 페이지는 ?key= 진입→sessionStorage 이관·URL 소거, 사이드바 미노출·noindex. 접수/반영 화이트리스트, SQL 폴백 유지. 검증: 무키·오키 403, 정키 인증 통과, 서버 키 생성 완료. 2026-08-14 사용자 검증 완료 — 정상 |
| 2026-08-14 | 뉴스 헤드라인 6번째 교체 | news.html | 사용자 요청. 세계일보 삼성 80만원 → 동행일보 "온누리상품권으로 닌텐도 구매? 소상공인 불평등 심화"(8/12) |
| 2026-08-14 | 거리뷰 버튼을 내 위치 아래로 이동 | merchants.html | 사용자 요청. top:12/right:106 → top:58/right:12 — 우상단 세로 스택(내 위치 위·거리뷰 아래). 2026-08-14 사용자 검증 완료 — 정상 |
| 2026-08-14 | 수집 안내에 야간 배치(00:30) 자동 갱신 멘트 | merchants.html, online.html | 사용자 요청. 가맹점 배너 "매일 00:30 야간 배치로 자동 최신화"·온라인 meta-line "수집(매일 00:30 자동 갱신)" 병기 |
| 2026-08-14 | 뉴스 헤드라인 주요 2건 추가(총 6장) | news.html | 사용자 요청. 브레이크뉴스 할인율 10→7% 인하(제도 변화, 행사 기간 한시 확대 병기)·삼성전자 뉴스룸 감사 페스티벌 온누리상품권 사용 안내(공식, 환급 수령자 실용). 원 URL은 구글 리다이렉트를 브라우저로 해석 |
| 2026-08-14 | 뉴스 헤드라인 확장(4장) + 운영자 선정 문구 제거 | news.html | 사용자 요청. 최신 2건 추가(디지털타임스 가맹 14.7만곳 첫 갱신·세계일보 환급 8000억 온라인몰 — 구글 리다이렉트를 브라우저로 원 URL 해석), 배지 주요/최신 구분. "운영자가 선정한 고정 소식" 멘트 삭제. 2026-08-14 사용자 검증 완료 — 정상(뉴스 페이지 전체) |
| 2026-08-14 | 사이드바 그룹명 '가맹점 검색' → '가맹점 스마트 검색' | 6 HTML·build_index.py→index 재빌드 | 사용자 요청. D-F1 통과 |
| 2026-08-14 | 온누리 뉴스 페이지 신설 (news.html + /api/news) | news.html(신규), backend/news(NewsService·Controller·NewsContractTest), 6 사이드바(소식 그룹)·build_index.py→index 재빌드 | 사용자 요청. 최신 뉴스=구글 뉴스 공개 RSS(키 불필요)를 서버가 수집·30분 캐시 후 /api/news로 제공(제목·링크·출처·KST시각만 — 본문 미수록, 언론사 책임 면책 병기). 실패 시 마지막 캐시 유지+프론트는 구글 뉴스 링크 폴백. 헤드라인 고정 2건(한국경제 온누리깡 처벌 강화·문화일보 30억 초과 가맹 제외 — 운영자 선정 표기) |
| 2026-08-14 | README 최신 기능 반영 | README.md | 사용자 요청. 가맹점 찾기 상세(그룹 마커·지도범위·팝업 액션·거리뷰·거리순·즐겨찾기/공유)·공통 UI(사이드바 접기·폭 슬라이더·모바일 카드형)·수집일 스탬프(/api/meta) 절 추가. 낡은 "(feat 브랜치)" 제목 잔재 정정 |
| 2026-08-13 | 모바일 거리뷰 정비: 버튼 겹침 해소 + 하단 시트 패널 | merchants.html | 사용자 결함 제보(실기기 스크린샷 — 지도범위·거리뷰 버튼 겹침). ①≤959px에서 지도범위 버튼을 지도 하단 중앙으로(상단 중앙·우측 버튼군 충돌 해소). ②≤600px 거리뷰 패널을 하단 고정 시트(높이 32dvh, 상단 라운딩)로 — 지도가 위에 보여 파란 길 재탭 용이. 시트에선 이동·크기조절 비활성(!important가 인라인·SDK 강제 스타일을 이김). 2026-08-14 사용자 실기기 검증 완료 — 정상 |
| 2026-08-13 | 지도 구석 '거리뷰' 모드 토글 (가맹점 선택 없이 진입) | merchants.html | 사용자 요청. 지도 우상단(내 위치 옆) 거리뷰 토글 — 켜면 StreetLayer 파란 길, 지도 클릭 시 그 지점 거리뷰 패널 오픈(파노라마 없을 때 openPano, 있으면 setPosition). 주황 원은 첫 파노라마에서 생성(setSpot). 버튼 on 상태·aria-pressed 동기화(가맹점 경유 진입에도), 패널 X·토글 재클릭=완전 종료. 특성: 마커 위 클릭은 마커 동작 우선(빈 지도·파란 길 클릭이 거리뷰 반응). 2026-08-13 사용자 실사용 검증 완료 — 정상 |
| 2026-08-13 | 화면 폭 '최대' 상태에서 사이드바 접으면 본문이 그만큼 확장 | shell.js(v6), 5 HTML·build_index.py 캐시버스트 | 사용자 요청. 최대 상태의 슬라이더 값이 옛 가용치 px로 남아 사이드바 접힘(가용 폭 +248) 후에도 옛 값에 고정되던 것 → resize 핸들러에서 wasMax(값≥max−10)면 새 max로 승격해 최대 유지. 창 리사이즈에도 동일 적용 |
| 2026-08-13 | 거리뷰 개편: 메인 지도 거리뷰 모드 + 플로팅 패널(이동·크기조절) | merchants.html | 사용자 방향 정정("이동은 메인 지도에서"). 모달·미니맵 → 우하단 플로팅 패널(pano-float, 지도 안 가림). 열려 있는 동안 메인 지도=거리뷰 모드: StreetLayer 파란 길 + 지도 클릭=panoObj.setPosition + 현재 위치 주황 원(street-spot) pano_changed 동기화, 닫으면 원복(mapNote 포함). 패널은 헤더 드래그 이동·우하단 그립 크기조절(localStorage onnuri_pano_box, 리사이즈 시 window resize 디바운스로 캔버스 재배치). 함정 기록: 네이버 Map/InfoWindow/Panorama가 인라인 스타일 강제(position·크기)·클릭 전파 차단. 2026-08-13 사용자 실사용 검증 완료 — 정상 |
| 2026-08-13 | 가맹점 거리뷰 (네이버 공식 파노라마) | merchants.html | 사용자 요청. 상세 팝업에 '거리뷰' 버튼(위임 — InfoWindow innerHTML) → 모달에 naver.maps.Panorama(SDK &submodules=panorama, 동일 클라이언트ID). 주변 파노라마 없으면 3.5s 타임아웃 폴백 안내. 대표좌표=건물 단위라 '그 앞 도로 촬영분' 힌트 병기. 리뷰수·사진은 공식 API 부재+UGC 저작권·약관 리스크로 기각(기존 '네이버 지도' 링크가 대체). 첫 버튼 라벨 '네이버 지도에서 보기'→'네이버 지도'(3버튼 한 줄) |
| 2026-08-13 | 즐겨찾기·최근 본·위치 공유 + PC 사이드바 최소화 | merchants.html, shell.css(v8)/js(v5), 5 HTML·build_index.py 캐시버스트 | 사용자 요청(UX 4순위+추가). ①행 ☆ 토글·'★ 저장 N' 모달(즐겨찾기 100·최근 본 10, localStorage 전용 — 서버 미전송 안내 병기), 항목 클릭=지도 이동+팝업, 링크 버튼=?region=&spot=lat,lng,이름 공유 URL 복사, 착지 시 goSpot으로 팝업. 최근 본은 openInfo에서 기록. ②PC 사이드바 최소화: html[data-sb-min]+접기 ◀/펼침 ❯(오렌지 탭) 버튼 shell.js 주입(위임·재시도 — DC 내성), localStorage(onnuri_sb_min), 폭 슬라이더 가용치 재계산+pagewidthchange(지도 resize). 모바일은 기존 드로어 |
| 2026-08-13 | 가까운 순 정렬 (내 위치 기준 거리순 + 거리 태그) | backend(MerchantDistRepository 신규·MerchantService/Controller·SearchBody+uLat/uLng·SearchBodyTest), merchants.html | 사용자 요청(UX 개선 3순위). 정렬 콤보에 '가까운 순(내 위치)' — 선택 시 위치 1회 획득(거부 시 이전 정렬 복귀), 서버는 CriteriaBuilder 거리 수식 정렬(위도차²+경도차²·cos² — JpaSort.unsafe는 소수점을 프로퍼티 경로로 파싱해 불가), WHERE는 MerchantSpecs 공유(경계면 1:1). JSON 폴백 jFull도 동일 수식. 이름 옆 거리 태그(m/km). 검증: 당산역 기준 129m→179m 오름차순, 서버 curl=JSON 폴백 동일 순서 |
| 2026-08-13 | 터치 타깃 확대 (닫기·햄버거 30~38px → 40~44px) | shell.css(v7), chat-widget.css(v9), 5 HTML·build_index.py 캐시버스트 | UX 개선 2순위 후속. 햄버거 38→44, PC안내 닫기 30→44, 챗 닫기 30→40. .gitignore에 __pycache__/ 추가 |
| 2026-08-13 | 모바일 레이아웃 개선: 결과 표→카드형 + 지도 dvh + 브레이크포인트 통일 | merchants.html | 사용자 요청(UX 개선 2순위). ①결과 표를 ≤959px에서 카드형으로(td를 블록화·data-label 라벨, 가로스크롤·컬럼드래그 제거) — 세로스택+표 이중스크롤 해소. ②지도 300px 고정→min(46dvh,400px) 비례. ③브레이크포인트 900↔959 불일치(셸=모바일/지도=데스크톱 사각지대) 해소: 지도레이아웃 900→959, 리스트 자체스크롤 901→960. ④col-grip 모바일 숨김(터치 스크롤 경합 방지). 터치타깃(셸·챗 닫기)은 후속 |
| 2026-08-13 | 가맹점 상세 팝업에 네이버 지도 '장소보기·길찾기' 링크 | merchants.html | 사용자 요청(UX 개선 1순위). 마커·리스트 클릭 상세(openInfo/openGroupInfo)가 텍스트뿐이라 액션이 없던 것 → 장소보기(map.naver.com/p/search/이름+주소)·길찾기(p/directions/-/lng,lat,이름/-/transit) 2버튼. 좌표·주소는 기존 데이터. 그룹(동일좌표)은 상단 1세트. URL 형식 라이브 실측 확정. '앱' 단정 금지·터치타깃 38px |
| 2026-08-12 | 챗 입력창 IME 마지막 글자 잔존 수정 | chat-widget.js(v9) | 사용자 결함 제보(맥북). 한글 조합 중 Enter로 전송 시 value 비운 뒤 compositionend가 마지막 글자를 재삽입 → 입력창에 한 글자 남음. keydown Enter에 !e.isComposing && keyCode!==229 가드(조합 확정 Enter는 전송 안 함). 캐시버스트 v8→v9(6곳). 2026-08-13 사용자 실기기(맥북) 한글 입력 검증 완료 — 정상 |
| 2026-08-12 | 사이드바 메뉴 '가맹점 찾기' → '오프라인 가맹점 찾기' | 5 HTML(online·merchants·payment·terms·report)·build_index.py→index 재빌드 | 사용자 요청. 온라인 사용처 찾기와 대비 명확화. 사이드바 메뉴 텍스트만 변경(index 본문 진입 카드 제목·merchants h1은 유지). D-F1 통과 |
| 2026-08-12 | 클러스터 숫자=가맹점 수 보정 + 브랜드 팝업 인덱스 상향 스크롤 수정 | merchants.html | 사용자 요청·결함 제보. ①그룹 마커 도입으로 클러스터가 '건물 수'를 세던 것을(당산동 10곳→"3") 각 그룹 마커 __cnt 합산으로 '가맹점 수' 표시(patchClusterCounts — lib _clusters/_clusterMember 내부 의존, idle마다 재정정). ②브랜드 검색 팝업 초성/알파벳 색인이 위로 올라가는 방향에서 미작동 — 원인은 .bm-head가 sticky(top:0)라 offsetTop·getBoundingClientRect·scrollIntoView가 모두 '고정된 렌더 위치'(스크롤 따라 변함)를 반환. 헤더 다음 아이템(non-sticky)의 offsetTop − 헤더높이로 스크롤(.bm-list position:relative)해 양방향 정확 이동. 로컬 검증: 5953→172 상향 정상 |
| 2026-08-12 | 지도 동일좌표 그룹 마커 + 위치별 목록 팝업 + 지도 범위로 목록 보기 | merchants.html | 사용자 결함 제보(당산역 가맹점 수가 온누리와 달라 보임). 원인: 온누리 대표좌표가 건물 단위라 같은 건물 가맹점이 동일 lat/lng로 겹침(당산로45길 1에 7곳, 서울 29,462곳이 고유좌표 11,940개·최대 589곳 겹침) — 데이터는 동일한데 개별 마커가 포개져 1개로 보였음. 해결: drawPins에서 좌표 그룹핑→개수 배지(.pin-multi), 클릭 시 그 위치 전체 목록 팝업(openGroupInfo, 온누리 '선택 위치 가맹점'과 동일). 추가로 '이 지도 범위로 목록 보기' 토글(LIST_BY_MAP) — regionParams에 현재 bounds AND, 지도 이동 시 리스트 자동 재검색(refresh page, API 모드만·백엔드 bounds 기존 지원). 검증: 당산역 그룹배지 7·팝업·bounds 47곳 |
| 2026-08-12 | 가맹점 수집일 스탬프 실데이터 반영 (하드코딩 제거) | backend(V6 app_meta·MetaController /api/meta·MetaRepository·MetaResult·MetaContractTest·load_merchants·nightly_update), merchants.html | 사용자 결함 제보(데이터는 8/12 최신인데 화면은 과거 표시). 원인: merchant 테이블에 수집일 컬럼이 없어 /api/merchants가 수집일 미제공 → 프론트가 하드코딩 "2026-08-08" 표시(배치 갱신과 무관). 해결: app_meta(k,v) 단일행에 merchants_collected_on 기록 — 배치 스왑 성공 시 실행일, 초기 적재는 파일 min(collected_on). GET /api/meta로 노출, 프론트가 API 모드에서 읽어 스탬프 갱신(하드코딩 "" 제거). 온라인 "취급품목 2026-08-10"은 수동 실측일이라 정상(불변). 검증: gradlew test 녹색, 로컬 load=8/10·배치스왑=8/12 기록 확인 |
| 2026-08-12 | 단일 브랜치 통합 (feat/backend-scaffold → main 흡수) | .gitignore, backend/(main으로), .github/workflows/backend-ci.yml, backend/DEPLOY.md, README.md | 사용자 결정("Spring 소스가 main에 안 보이고 트리 나눌 이유 없음"). 분리 명분이던 config.js dataMode 차이가 이미 소멸(양쪽 auto). backend/ ignore→build/·.gradle/·.env 산출물만 정밀 차단, 자바 소스 73개 추적. CI 트리거·서버 배포·배치 클론 모두 main 기준. GitHub Pages는 backend/ 있어도 무영향(정적 파일만 서빙). feat 브랜치 폐기 |
| 2026-08-12 | README.md 신설 | README.md(신규) | 사용자 요청. 프로젝트 소개·기능·아키텍처(main 프론트/feat 백엔드 분리)·데이터 갱신·빌드(index 재생성·D-F1)·문서 포인터. 서버 IP·키 비노출, 도메인만 |
| 2026-08-12 | 파비콘 추가 (코스콤 CI 오렌지 셰브론) | favicon.svg(신규), 6페이지 head, build_index.py(8단계) | 사용자 요청. 브라우저 탭 아이콘이 기본 지구본(favicon.ico 404)으로 나오던 것을 오렌지(#F26B1D) 라운드 사각형+흰 셰브론 ❯ SVG로. 전 페이지 `<link rel=icon>` 주입, index는 빌더 title 스텝에 병합(D-F1 통과). ?v=1 캐시버스트 |
| 2026-08-12 | 야간 배치(00:30) 데이터 자동 갱신 + 온라인 사용처 백엔드 이관 | backend(V5 online_platform·/api/online/platforms·nightly_update.py·load_online_platforms.py), online-source.js(신규), online.html, terms.html, build_index.py, DEPLOY.md | 사용자 요청. 가맹점=stage-swap 무중단 갱신(전일 대비 ±20%·총계 5만 가드, 위반 시 기존 유지), 온라인 목록=공식 e-commerce/platform API 수집·upsert(큐레이션 note·region_limited 보존, 사라짐=status removed·삭제 금지), RAG 재적재. DB=라이브 SSOT·data/*.json=폴백(ADR-14 — 폴백 스탬프가 과거일 수 있음은 의도). 프론트는 online-source.js 어댑터(snake/camel 흡수)로 API 우선+JSON 폴백. 배치는 서버 전용 클론 ~/onnuri_batch/repo + crontab 00:30(KST)+flock. 검증: dev-qa 6항목 통과(테스트 29 녹색·경계면 삼자 대조·회귀 기준값·D-F1) |
| 2026-08-06 | 브라우저 도구 deferred 로드 지시 추가 | guide-verifier, guide-verification | 드라이런에서 ToolSearch 없이 호출 시 실패 확인 |
| 2026-08-06 | 6개 스킬 description 경계 정리 | skills 전체 | 트리거 검증: 오케스트레이터가 개별 스킬 트리거를 흡수, 범위 밖 가드 부재 |
| 2026-08-06 | 팀 기동 여부 판단 단계 추가 | onnuri-guide-orchestrator | 단일 단계 작업에 5인 팀이 기동되는 비용 문제 |
| 2026-08-06 | 2차 트리거 수정 5건 (API 연동·배포 가드, 단발 질의 포착, 어휘 축 분리) | skills 전체 | 재검증: 오분류 8/11·오발동 6/7 해소 후 잔존분 |
| 2026-08-06 | 결제 구조 조사 절 추가 (카드형·모바일형 분리) | onnuri-policy-research | description이 본문보다 넓은 범위를 약속하던 불일치 해소 |
| 2026-08-06 | writer·data 모델 opus → fable | guide-content-writer, onnuri-data-curator | 사용자 지시 |
| 2026-08-06 | 수도권 가맹점 검색 기능 추가 (merchants.html + data/merchants/) | 전체 팀 | 사용자 요청. 출처: 공공데이터포털 전국 가맹점 현황(2025-07-31, 차기 2026-08-12). onnuri.gift/place는 인천·경기 미서비스·카테고리 부재로 기각. 갱신: build_merchants.py 재실행 |
| 2026-08-06 | SSM 단정 정정 (직영/가맹 축 분리) | 03 spec, offline JSON, index.html | 사용자 결함 제보: GS더프레시 가맹점 실존. 직영=불가 유지, 지정구역 내 개인 가맹점=가능 |
| 2026-08-06 | 배포: GitHub Pages Actions workflow 전환 | .github/workflows/pages.yml | legacy 빌더 즉사(duration 0), stuck deployment 취소 필요했음 |
| 2026-08-07 | 브랜드 매장 뷰 추가 (편의점/마트·슈퍼/SSM/다이소, 구·시별) | merchants.html, data/merchants/brand_stores.json | 사용자 요청. 출처: 온누리 가맹점찾기 비공식 API(2026-08-07 수집, 1,515건) — 공공데이터에 없는 도로명 주소·구/시 분류 확보. 갱신: build_brand_stores.py 재실행 |
| 2026-08-07 | 지도 링크 문구 목적지 일치 + 앱스토어 보조링크 | merchants.html, index.html | 조사 결과 앱 딥링크 부재 확정 → 웹 지도 링크 유지, 라벨에서 "앱" 제거해 목적지와 일치. 디지털온누리 앱스토어 설치 링크(검증됨) 보조 추가. 공식 지도 지역 제한 오해 서술 금지 |
| 2026-08-07 | 오프라인 탭 최상단에 검색 서브탭 2개 신설 | index.html | 진입 링크가 요건 박스에 묻혀 발견 어려움 → '가맹점 찾기'(merchants.html 내부·수도권)·'공식 지도 검색'(onnuri.gift/place 외부·전국) 서브탭으로 승격. 성격 배지·aria-label |
| 2026-08-08 | 시장·브랜드 뷰 통합 + 구/동 계층 필터 | merchants.html, data/merchants/*.json | 사용자 요청. 공공데이터(주소 없음)를 온누리 API 전수 수집본(66,211건, 도로명 주소·구/동)으로 교체. 2뷰→1뷰 통합, 지역 계층(서울·인천 구→동/경기 시→구→동), 업종·brand·시장유형 3축 필터. brand_stores.json 흡수. 갱신: build_region_full.py 재실행 |
| 2026-08-08 | 빈도+지점명 브랜드 자동탐지 + 브랜드 필터 | data/merchants/*.json, merchants.html | 사용자 요청. 상호 7회+ 중복 AND 지점명비율 40%+ → 브랜드(169종), 표기변형 병합. 충남상회류 오탐 제외. brand enum 4종→개별명. 상위칩+롱테일 검색. 후보 CSV(13_brand_candidates.csv) |
| 2026-08-08 | UX 대개편: 좌측 사이드바 + 화이트 모노톤 + 오렌지 | index.html, merchants.html | 사용자 요청. 크림/따뜻한 톤 제거→중립 모노톤, 오렌지 포인트 절제. 계층 사이드바(PC 상시/모바일 드로어), 모던 버튼(pill→6px). 디자인 시스템: 14_design_system.md. index는 build_index.py 재실행 |
| 2026-08-08 | 네이버 지도 통합 (가맹점 찾기, 지도+리스트 병행) | merchants.html, lib/MarkerClustering.js | 사용자 요청. 네이버 Maps JS SDK(Client ID는 도메인 제한 보호, Secret 미사용·코드에 없음). 마커 클러스터, 필터(지역계층·업종·브랜드) 연동, 성능 상한 3,000곳. 리스트↔지도 연동. 좌표는 데이터 lat/lng |
| 2026-08-10 | ADR 대장 초기화 + 셸 공통화 + 백엔드 CI/CD | _workspace/16_arch_decisions.md, shell.css/js(신규), merchants.html, online.html, config.js, build_index.py, .github/workflows/backend-ci.yml | /app-architecture 검토(ADR-1~11). 3페이지 중복 셸 ~110줄×3 → shell.css/js로 통합(위임 방식 — index DC 재렌더 내성), config.js 폭토글 이관. CI=feat 푸시 시 gradlew test(JDK21), CD=NCP_SSH_KEY 시크릿 등록 시에만 SSH 배포·헬스체크. merchants 대분리는 스트랭글러 유예(ADR-10) |
| 2026-08-09 | 백엔드 분리(Spring+Postgres) + 프론트 API 전환 + 브랜드 검색 팝업 + 현재위치 | backend/, merchants.html, config.js | 사용자 요청. Spring 검색 API(feat/backend-scaffold). config.js dataMode(auto/api/json)로 이중소스 — 백엔드 미기동 시 JSON 폴백. 라이브(main)는 dataMode=json. 브랜드 콤보→검색 팝업(부분검색+초성/알파벳 색인+업종 콤보 잠금). Geolocation 현재위치 마커. 배포자산: backend/DEPLOY.md·deploy/(Docker Compose+Caddy)·terraform |
| 2026-08-10 | index '가맹점 찾기' 카드 출처 문구 정정 | build_index.py, 03_content_spec.md, index.html | 사용자 결함 제보. 2026-08-08 데이터 교체(공공데이터→온누리 가맹점찾기 수집본) 후 카드 문구만 옛 출처로 남음 → "온누리 가맹점찾기 수집본 기준"으로 통일. build_index.py 재빌드(D-F1 통과) |
| 2026-08-10 | 브랜드 칩 전역 빈도 tie-break + 부분노출 안내 문구 | merchants.html, guide-content-style | 사용자 결함 제보(소규모 지역서 GS25 등 대표 브랜드가 칩에서 누락). count 동점 시 시도 전역 빈도순위로 정렬(이름순이면 ko 정렬상 라틴이 후순위). 칩에 "대표 브랜드만 · 전체는 검색" 한 줄 안내. guide-content-style에 "부분만 보여줄 땐 그렇다고 말한다" 원칙 추가 |
| 2026-08-10 | 서비스 지역에 부산 추가 (코스콤·한국거래소 소재지) | data/merchants/busan.json, merchants.html, index.html, build_region_full.py, load_merchants.py | 사용자 요청(코스콤 직원용 내부 가이드 — 거래소는 형제회사). 부산(시도코드 26000, 16 구/군, 12,514건) 온누리 API 수집. 전체 재수집(수집일 2026-08-10). 범위 문구 '수도권'→'코스콤·한국거래소 소재지(서울·인천·경기·부산)'. config.js dataVersion으로 JSON 캐시 무력화. 부산은 구→동 계층(서울·인천과 동일). 갱신: build_region_full.py 재실행 |
| 2026-08-10 | UI/UX 대비 강화: 극단 타이포 + 초저투명 패턴 | merchants.html, index.html, build_index.py | 사용자 요청. 타이포 스케일 극단화(h1 clamp 34→54px w900 ↔ 라벨 10px uppercase, 시도 탭은 숫자가 주인공). 잉크 블랙(#0B0C0E)·그레이 재조정, 오렌지는 포인트만. 패턴: body 도트+오렌지 글로우, 사이드바 핀스트라이프, 필터 해칭, 헤더 '온' 워터마크(4.5%). index는 build_index.py 24b.h1-display 스텝 추가 |
| 2026-08-10 | 코스콤 CI 적용 + 지도범위 검색 | merchants.html, index.html, build_index.py | 사용자 요청(CI 가이드 이미지 참조). '온' 마크→❯koscom CI(오렌지 셰브론 SVG+워드마크, 자산파일 없이 인라인), 브랜드명 '코스콤 디지털온누리 가이드'. 지도 상단 '현 지도에서 가맹점 검색' — bounds 필터(MAP_BOUNDS), 진입 시 구/동 리셋·지역 조작 시 자동 해제·뷰 유지. API 모드용 백엔드 bounds 파라미터는 feat의 MerchantSpecs에 추가 |
| 2026-08-10 | doc-commit 스킬 신설 (표준 커밋·푸시 절차) | .claude/skills/doc-commit | 사용자 요청. 커밋 전 문서 갱신(CLAUDE.md 이력·명세·DEPLOY.md) → 비밀값·D-F1·브랜치 규칙 점검 → 커밋·푸시 → main↔feat 공용 파일 동기화까지 절차화. 트리거: "커밋푸시하자" 등 |
| 2026-08-10 | 지도범위 검색을 전 지역(4개 시도) 대상으로 확장 | merchants.html | 사용자 요청(시도 탭과 지도 위치가 어긋나면 0곳 나오는 혼동 해소). bounds 모드 시 시도 무관 서울·인천·경기·부산 합본에서 검색(JSON은 4파일 합본·캐시, API는 region 파라미터 생략). mScope에 '전 지역 대상' 표시, 첫 실행 로딩 안내. 검증: 동일 뷰에서 27,331(시도 한정)→32,937(전 지역) |
| 2026-08-10 | 지도범위 검색: 지도 따라 자동 재검색 + 상한 오버레이 | merchants.html | 사용자 결함 제보("클릭 후 줌 해야 보임"). 원인: 넓은 뷰는 3,000 마커 상한 초과라 마커 미표시인데 안내가 약했고 재검색이 수동. 수정: bounds 모드 중 지도 idle 시 400ms 디바운스 자동 재검색(해제·재적용 불필요), 상한 초과 시 지도 중앙 다크 오버레이("이 영역 N곳 — 확대하면 자동 재검색"), 버튼 라벨 '지도 따라 검색 중'. 검증: 32,937(마커0·오버레이)→행클릭 줌인→자동 236곳 마커 표시 |
| 2026-08-10 | 사이드바 '지역별 찾기'·'업종·브랜드별' → '가맹점 찾기' 통합 | merchants.html, index.html, build_index.py | 사용자 질문으로 드러난 혼동: 두 메뉴가 별개 기능이 아니라 같은 검색 페이지의 앵커(#sidoTabs/#catChips) 스크롤 차이뿐. 단일 '가맹점 찾기'로 통합(메뉴명이 기능을 약속하지 않게) |
| 2026-08-10 | 코스콤 CI를 공식 로고 이미지로 교체 | assets/koscom_ci.png, merchants.html, index.html, build_index.py, .gitignore | 사용자 피드백(SVG+텍스트 재현이 단순 텍스트로 보임). 공식 CI PNG(152×31, 투명배경)를 assets/에 두고 img 기반으로 교체. .gitignore *.png에 assets/*.png 예외 추가(누락 시 라이브 404) |
| 2026-08-10 | CI 이미지 고해상 교체 (152×31 → 470×96) | assets/koscom_ci.png | 사용자 제공 CI 가이드 시트(8000px)에서 기본형 로고 크롭·점선 제거·흰배경 투명화·LANCZOS 리사이즈. 표시 19px의 5배 밀도(레티나 선명). 경로 동일 — HTML 무변경 |
| 2026-08-10 | 디스플레이 서체 Gmarket Sans 도입 (제목·브랜드 전용) | merchants.html, index.html, build_index.py | 사용자 요청(코스콤 폰트 유사체). 코스콤 전용체는 비공개 사내 자산 → CI 워드마크(원형 기하 라틴)와 가장 유사한 무료 웹폰트 Gmarket Sans 선정(시편 비교: Paperlogy·SUIT 대비). Bold 1웨이트만 CDN 로드(font-display:swap, 미로드 시 Pretendard 폴백). 적용: h1·시도탭 숫자·사이드바/상단바 브랜드명·모달 제목. 본문·표는 Pretendard 유지(가독성) |
| 2026-08-10 | 온라인 사용처 2축 검색 신설 (물품종류×브랜드, online.html) | online.html, data/online_catalog.json, merchants.html, index.html, build_index.py(S16), 15_online_catalog_report.md | 사용자 요청. 쇼핑 22곳 Playwright 실측(카테고리 GNB·브랜드관 채록, 기획전 딥링크는 기획전 범위만) → 플랫폼 단위 태깅(taxonomy 10대분류+소분류, 브랜드 42종 — 삼성·애플·LG·DJI·로보락 실측 확인). 배달 8곳은 태깅 제외(음식 주문). 카드 그리드+구분탭+계층 칩+브랜드 칩/드롭다운. 사이드바 3곳+index 온라인탭 진입 카드. 갱신: 재실측 후 online_catalog.json·15_report 수기 갱신 |
| 2026-08-10 | 백엔드 NCP 배포 완료 + 라이브 API 모드 전환 | config.js (서버: NCP 101.79.31.30) | NCP Server(Ubuntu 22.04)+Docker Compose(Postgres·Spring·Caddy) 배포, api.koscomlabor.cloud TLS(Let's Encrypt) 자동 발급, 78,734건 적재. 외부 검증 전 통과(개포동 145·팔달 1,241·부산 12,514·해운대 681·bounds·facets·CORS). 라이브 dataMode json→auto(API 우선, 장애 시 JSON 폴백). 서버 갱신 절차: DEPLOY.md. 이슈 해결: NCP 미러에 docker-compose-plugin 없음→get.docker.com, private repo raw 404→public 전환 |
| 2026-08-10 | 리스트↔지도 드래그 스플리터 + '결제 방법' 토글 자동 펼침 | merchants.html, index.html, build_index.py | 사용자 요청 2건. ①PC에서 리스트·지도 사이 핸들 드래그로 지도 폭 조절(--map-w, 300px~65%, localStorage 유지, 더블클릭 초기화, ←/→ 키, 드래그 시 네이버 지도 resize 트리거, 모바일 숨김). ②사이드바 '결제 방법' 진입 시 접힌 결제 흐름 아코디언(▶) 자동 펼침(openPaymentToggles, DC 마운트 재시도) + 펼침 후 스크롤 보정(450ms) |
| 2026-08-10 | FOUC 방어 + 리스트 내부 스크롤 + 화면 폭 토글 | build_index.py, merchants.html, online.html, config.js | 사용자 제보 3건. ①index 마운트 순간 CI 2개 세로 겹침 — 4MB 번들 FOUC(이미지 크기 무관) → SHELL 크리티컬 인라인 스타일(topbar none·sidebar fixed·img width/height), 모바일 media는 !important로 복원. ②PC에서 가맹점 리스트를 지도와 같은 높이의 자체 스크롤로 분리(thead sticky, 페이저는 내부 스크롤 리셋). ③화면 폭 토글 좁게 880/표준 1080/넓게 1640 — 사이드바 위젯 3페이지 공통(html[data-pw]+--page-w), localStorage 공유, 로직은 config.js(index는 셸 내장), 변경 시 지도 resize |
| 2026-08-10 | 하네스 2팀 개편 + TDD 도입 | agents(backend-dev·dev-qa·app-architect 신규, frontend-dev·verifier 갱신), skills(backend-server-dev·dev-testing·app-architecture 신규, orchestrator 2팀 라우팅 개편), backend 테스트 시드 | 사용자 요청(백엔드·클라우드 도입에 따른 팀 재편). 개발팀 4인(frontend/backend/dev-qa/architect — TDD Red·Green·Refactor + 이중게이트) + 업무팀 4인(policy/data/writer/verifier) 분리, 단일 오케가 요청 유형별 라우팅(A업무/B개발/C혼성). dev-qa(코드·경계면)와 verifier(콘텐츠) 관할 상호 불가침. 회귀 기준값 표는 dev-testing 스킬이 관리 |
| 2026-08-12 | 필터 변경 시 지도 뷰 유지 (fit은 시도 버튼·챗 착지만) | merchants.html(MAP_FIT_NEXT) | 사용자 제보(확대 중 필터 변경 시 시도 전체로 줌아웃). renderMap이 fit 예약 없으면 viewportRender로 위임 — 업종·브랜드·구/동 변경은 현재 화면 기준 재렌더, 시도 탭·이동 착지만 새 지역으로 맞춤 |
| 2026-08-12 | 화면 폭 슬라이더 모바일 숨김 | shell.css(v6) | 사용자 제보 — 드로어에선 무의미. ≤959px에서 .sb-width display:none, PC는 유지 |
| 2026-08-12 | 지도↔리스트 위치 교체 + 리스트 헤더·페이저 고정 | merchants.html | 사용자 요청. 지도=좌측·리스트=우측(스플리터 방향 계산·←/→ 키 반전), 카드=플렉스 컬럼으로 헤더행·페이저 고정·표 본문만 스크롤(.table-scroll — 페이지 전환 리셋 대상도 이관). 부수: initSplit setPointerCapture try/catch+document 바인딩 견고화 |
| 2026-08-12 | 화면 폭 3단 토글 → 슬라이더 (최대=사이드바 제외 전체) | shell.css/js(v5/v4 — UI는 shell.js가 주입, 6페이지 마크업 무수정) | 사용자 요청. 760px~가용 폭 연속 조절, 최대치=--page-w 100%("최대" 표기), px 저장(onnuri_pw_px)·구 3단 키 자동 이전·더블클릭 1080 초기화·창 리사이즈 시 가용 폭 재계산·pagewidthchange(지도 resize) 유지 |
| 2026-08-12 | 지도·리스트 수직 높이 조절 핸들 | merchants.html(--panel-h·vsplit-handle·initVSplit) | 사용자 요청(필터+지도+리스트를 한 화면에 — 이중 스크롤 불편). 지도·리스트·좌우핸들 높이를 --panel-h로 공통화, 하단 가로 핸들 드래그(260px~화면-160, localStorage·더블클릭 초기화·↑/↓ 키·지도 resize 연동). 검증: 742→491 동기 축소·저장·초기화 |
| 2026-08-12 | 모바일 챗 패널 우하단 통일 + 모바일 드래그 크기 조절 | chat-widget.css/js(v8) | 사용자 요청. 좌하단→우하단(PC와 동일 앵커), 그립 모바일 표시(touch-action:none — 드래그 중 스크롤 방지), 저장 크기 기기 무관 복원+뷰포트 클램프(720 저장→390폰에서 366 적용). 검증: 우하단 12px·드래그 312→362·저장·클램프 |
| 2026-08-12 | 지도 NAVER 로고 계층 격리 (사이드바·챗 관통 해소) | merchants.html(#map isolation) | 사용자 결함 제보(스크린샷). 지도 내부 컨트롤의 높은 z-index가 전역 스태킹으로 새어나옴 → #map에 position:relative+z-index:0+isolation:isolate. 라이브 검증: 로고 좌표 최상단 요소 = 챗 패널 |
| 2026-08-12 | 모바일 챗 패널 풀스크린 → 좌하단 80% 팝업 | chat-widget.css(v7) | 사용자 요청(과한 확대감). left/bottom 12px 기준 80vw×80dvh(최대 480px), 라운딩 유지 — 뒤 페이지가 보여 맥락 유지. 검증: 390px에서 312×675(80%/80%) |
| 2026-08-12 | 뷰포트 자동 지도 — "현 지도에서 검색" 버튼 제거 | backend Specs·ClusterRepository(bounds=추가 AND — 기존 지역 대체 의미 폐기), merchants.html(viewportRender — idle마다 필터∩화면 판단해 도트/클러스터 자동 전환, 버튼·모드·KEEP_VIEW 제거) | 사용자 요청(자연스러운 지도 UX). 3,000 이하=도트·초과=클러스터, fit은 필터 변경 1회만(SKIP_IDLE_ONCE). 검증: bounds 기준값 571 유지·강남구∩bounds 408 AND 정확 |
| 2026-08-12 | 필터 중복선택 (다중 OR) — 가맹점·온라인 | backend FilterCsv(+테스트)·MerchantSpecs·ClusterRepository(IN), merchants.html(업종·브랜드 칩+팝업 토글·JSON 폴백), online.html(대분류·브랜드 다중, 소분류=대분류 1개일 때만) | 사용자 요청. 상태는 콤마 값("음식점,카페") — 같은 축 OR·축 간 AND. 검증: 음식점9,673+편의점259=9,932 정확·GS25+CU 218·클러스터 정합·online 합집합 21 |
| 2026-08-12 | 지도범위 검색 해제 시 현재 뷰 유지 | merchants.html | 사용자 결함 제보(해제하면 전체로 줌아웃). 해제 버튼 경로에 KEEP_VIEW_ONCE 1회 플래그 — 개별·클러스터 렌더의 fitBounds를 그 1회만 생략. 시도 탭 변경 등 다른 경로는 기존대로 새 지역 fit |
| 2026-08-12 | 클러스터→개별 마커 전환 경합 수정 | merchants.html | 사용자 결함 제보(3,000 이하로 좁혀도 클러스터 잔존). 원인=모드 이탈 후 도착하는 이전 비동기 클러스터 응답이 개별 마커를 덮음 → 이탈 시 clusterSeq 무효화+done()에서 CLUSTER_MODE 재확인. 라이브 검증: 서울(10셀)→강남구 761 개별 마커→전체 복귀 |
| 2026-08-12 | 서버 사이드 클러스터링 (상한 초과 뷰 밀도 표시) | backend ClusterRepository·/merchants/clusters(GET/POST)·ClusterResult(계약), merchants.html(클러스터 마커·줌 재계산·JSON 폴백 로컬 집계) | 사용자 요청(3,000 상한 논의). floor 격자 집계(avg 좌표), 셀≈80px(112.5/2^zoom). WHERE는 MerchantSpecs와 1:1 — 정합성 검증: 서울 29,462·부산 편의점 42·GS25 335 모두 클러스터합=리스트 총계. 다크 오버레이 → 클러스터 마커(클릭 시 줌인)로 대체 |
| 2026-08-12 | 모바일 PC 안내 배너 위치 조정 (챗 버튼 겹침 해소) | shell.css(v4) | 사용자 제보. 배너 bottom 12→84px — 챗 플로팅 버튼·라벨 위로. 계측: 배너 하단 760 < 버튼 상단 778 |
| 2026-08-12 | 챗 위젯 크기 조절 + 닫힘 상태 라벨 | chat-widget.js(v7)/css(v6) | 사용자 요청. ①패널 좌상단 그립 드래그(320~720×420~vh, localStorage, 더블클릭 초기화, 모바일 풀스크린 !important 우선) ②FAB 옆 "온누리 AI 챗봇" 필 라벨(닫힘 시 표시·클릭 시 열림) — 챗봇 발견성 개선 |
| 2026-08-11 | 버그 제보 게시판 (report.html) | backend report(V4·ReportController·계약테스트·rate limit 분2/일10), report.html(신규), 사이드바 6곳(피드백 그룹), DEPLOY.md(상태 갱신 절차) | 사용자 요청. 익명 제보(개인정보 미수집·닉네임 선택), 목록 공개(텍스트 esc 렌더), 상태 배지(접수/반영 — 운영자 SQL 갱신). e2e: 제출·목록·429·줄바꿈·XSS 확인 |
| 2026-08-11 | 가맹점 찾기 진입 카드 문구 개정 (지도 통합 반영) | build_index.py(S15 3곳), 03 spec, index.html | 사용자 제보. "이름 검색=내부/지도=외부" 낡은 구분 → 지역 커버리지 축("소재지 4개 시도=내부 지도·목록 / 그 외 전국=공식 지도")으로 재서술 |
| 2026-08-11 | 방문자 카운트 (일자별 집계, 사이드바 표시) | backend visit(V3__visit·VisitController·VisitStats, 계약 테스트), shell.css/js(v3) | 사용자 요청. 개인정보 미저장(일자·정수만), 브라우저 세션당 1회 POST(sessionStorage 중복 방지)·이후 GET. 사이드바 하단 "방문 오늘 N · 누적 N"(5페이지 공통). API 미응답 시 표기 생략 |
| 2026-08-11 | 모바일 PC 최적화 안내 배너 | shell.css/js(v2 — 전 페이지 공통) | 사용자 요청. 모바일(≤959px) 첫 방문 시 하단 배너 1회("PC 화면에 최적화 — 지도·표 검색은 PC에서 더 편함"), 닫으면 localStorage(onnuri_pc_notice)로 전 페이지 재노출 없음. 데스크톱은 DOM 미생성 |
| 2026-08-11 | index 슬림화: 결제 아코디언 제거 + 용어·유의사항 분리(terms.html) | build_index.py(7g — 인코딩 직전 블록 제거·포인터 치환), terms.html(신규, regionApps 동적), 사이드바 5곳, chat-widget·backend navigate(terms), 03 spec S18 | 사용자 요청. #payment/#terms 해시는 전용 페이지로 리다이렉트(구 링크 호환). 결함 1건: 7g를 인코딩 뒤에 넣어 무반영 → 위치 이동으로 해결. index 템플릿 97k→87k |
| 2026-08-11 | 결제 방법 별도 페이지 신설 (payment.html) | payment.html(신규), 사이드바 4곳, chat-widget.js·backend navigate(payment), rag_corpus/card_payment_details.md(+6청크 재적재), 03 spec S17 | 사용자 요청. 카드형·QR형 5단계+POINT 강조(실적·선차감·잔액부족 예시). 실적은 롯데카드 공지만 확인·타사 단정 금지(신규 조사: 온누리시장 카드형 안내·롯데카드 공지). 페이지·챗봇 동일 사실 원칙(코퍼스 동시 반영) |
| 2026-08-11 | 챗 mermaid 스트리밍 결함 수정 (본문 "Syntax error" 도배) | chat-widget.js(v6), index 재빌드 | 사용자 결함 제보. 스트리밍 중 매 토큰마다 미완성 mermaid 코드를 렌더 시도 → 파싱 실패 시 mermaid가 body에 에러 SVG 삽입. 수정: 토큰 경로는 mermaid 생략(완료 시 1회)+suppressErrorRendering+실패 잔여 노드 제거. 검증: 동일 질문에서 에러 0회·다이어그램 정상 |
| 2026-08-11 | 결과 표 우측 공백 해소 (마지막 열 잔여 흡수) + 커스텀 도메인 개통 | merchants.html(colgroup 마지막 열 auto·width 100%), 서버 .env(CORS 오버라이드 제거), GitHub Pages(cert·HTTPS 강제) | 사용자 제보·요청. 열 축소 시 표 폭이 합계로 고정돼 카드 라운딩과 사이 공백 → 마지막 열이 잔여 흡수(카드보다 좁으면 기존 스크롤 유지). 챗 연결 실패 원인=서버 APP_CORS_ALLOWED_ORIGINS 오버라이드가 yml 기본값(신규 도메인 포함) 무시 → 제거. koscomlabor.cloud 인증서 발급·HTTPS 강제, github.io는 자동 301 |
| 2026-08-11 | 챗봇 인텐트 사전필터 (범위 밖 즉시 거절) | backend OpenAiClient(isOnTopic)·ChatService(게이트+OFF_TOPIC_REPLY), IntentGateTest | 사용자 요청. 본 호출 전 ~200tok 경량 분류(structured output) — 범위 밖은 ~2초 즉시 거절, 애매하면 통과·실패 시 fail-open. 후속 질문은 직전 답변 맥락으로 오탐 방지. 검증: 퀵소트 거절·환불 통과·"그럼 60%는?" 통과 |
| 2026-08-11 | 컬럼 구분선 원복(헤더만 은은히) + AI 면책 촘촘화 | merchants.html(th::after), chat-widget.css/js(v5, 답변별 ::after 면책), online.html·build_index.py(S13 각주+AI면책), guide-content-style | 사용자 요청. 본문 셀 구분선 제거·헤더 짧은 세로선만. AI 면책 3요소(부정확 가능·내부 참고용·공식 확인 행선지)를 챗 답변마다+3페이지 하단에 병기 — 책임소재 가이드. 지침에 "AI 관여 콘텐츠 면책 병기" 원칙 신설 |
| 2026-08-11 | 결과 표 컬럼 구분선 상시 표시 + 커스텀 도메인 준비 | merchants.html, backend application.yml(CORS), GitHub Pages 설정 | 사용자 요청 2건. 열 경계 border-right(--border, .table-scroll 스코프). Pages cname=koscomlabor.cloud 등록·CORS 오리진 추가(가비아 DNS A 레코드는 사용자 등록 후 HTTPS 강제 예정) |
| 2026-08-11 | 브라우저 탭 제목 통일 ("코스콤 디지털온누리 가이드") | index(빌더 8단계 신설), merchants.html, online.html | 사용자 요청. index=브랜드명, 하위 페이지="페이지명 — 브랜드" 패턴 |
| 2026-08-11 | 챗 개선 3종: 실스트리밍 + 말풍선 무스크롤 + 지도 바로 이동 토글 | backend OpenAiClient(chatStream)·ChatService, chat-widget.js/css(v4), merchants.html(onnuriApplyChatFilter 훅·table 셀렉터 스코프·지도예외 방어), online.html, guide-page-build | 사용자 요청 3건. ①OpenAI stream 델타를 SSE로 실시간 중계(도구 tool_calls 델타 index 조립) ②말풍선 내부 스크롤 제거 — 표는 :has() 고정폭+줄바꿈, 원인은 전역 table{min-width:880px} 오염(지침 보강) ③위치 문의 시 지도 즉시 이동(같은 페이지=훅, 크로스=답변 후 이동), 위젯 토글(기본 ON, localStorage). 부수 수정: 지도 렌더 예외가 리스트를 죽이던 결함 방어 |
| 2026-08-11 | 검색 API GET→POST + 착지 URL 비노출 (ADR-13) | backend MerchantController(POST 5종+SearchBody), merchants.html(apiGet→POST body), chat-widget.js(sessionStorage 핸드오프), online.html | 사용자 요청(필터값 URL 노출 차단). GET은 운영 curl 호환 유지, 프론트만 POST. 착지는 onnuri_nav_filter 1회성 storage — 주소창 클린. 계약: SearchBodyTest |
| 2026-08-11 | 챗봇 신설: RAG(pgvector)+도구 루프+플로팅 위젯 (ADR-12) | backend/chat(신규 7클래스)·V2·compose(pgvector), chat-widget.js/css(신규), merchants/online/index(위젯+URL 파라미터 착지), config.js(chatEnabled), _workspace/rag_corpus·build_rag_corpus.py·17_chatbot_design.md | 사용자 요청. RAG 채택(파인튜닝 기각 — 날짜 종속·출처 인용). 코퍼스 130청크=공식 실측(onnuri.gift FAQ 69전량·voucher·공지 8+semas 4p, 2026-08-11 수집)+내부 검증 자산. gpt-5.6-luna 도구 루프(search_policy/merchants/online/navigate, 결과 재주입), SSE, rate limit(분10/일200). 시나리오 5종 검증(환불 7일/60% 정확·노량진 GS25 1곳 일치·mermaid 오렌지 렌더·범위 밖 거절). 결함 수정: luna reasoning_effort=none 필수, DOMPurify가 mermaid HTML 라벨 제거→htmlLabels:false, 위젯 캐시→?v=2. 라이브는 서버 키 등록 전 chatEnabled:false. 갱신: 코퍼스 재수집 후 build_rag_corpus.py |
| 2026-08-10 | 백엔드 CD 활성화 (배포 전용 SSH 키 + 자동 배포 검증) | GitHub Secrets(NCP_SSH_KEY), 서버 authorized_keys | 전용 ed25519 키 생성(개인키=Secrets에만, 공개키=서버에만, 채팅·저장소 비노출). workflow_dispatch로 전 파이프라인 검증: test→deploy→헬스체크 그린, 회귀 4종(서울 29,462·부산 12,514·개포동 145·GS25 335) 일치. 이후 feat에 backend/** 푸시 시 자동 배포 |
| 2026-08-10 | 컬럼 폭 리사이저 + 결제 태그 행높이 결함 수정 + 브랜드 팝업 전 지역화 | merchants.html, guide-page-build, dev-testing | 사용자 요청·결함 제보. ①테이블 헤더 경계 드래그로 열 폭 조절(COL_W+colgroup fixed, localStorage 유지, 더블클릭 초기화). ②행높이 폭증 원인 = `.result-list .card` 후손 셀렉터가 결제 태그 span.pay.card에 min-height 420px 적용 → 직계(>)로 수정, guide-page-build에 '범용 클래스 후손 선택 금지' 지침 신설. ③브랜드 팝업을 전 지역(4개 시도) 기준으로 — 지역·지도·디지털 무관 집계+안내 문구, 현 지역에 없는 브랜드 선택 시 0곳 표시(기존 고아 방지 유지). 로컬 DB 재적재로 API·JSON 335 일치 확인 |
